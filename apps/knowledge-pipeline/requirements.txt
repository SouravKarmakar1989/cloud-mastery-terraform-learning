# Browser automation — used only for first-time login and cookie extraction
playwright>=1.43.0

# HTTP client with Chrome TLS fingerprint impersonation (bypasses Cloudflare)
curl-cffi>=0.7.0

# LLM client for blog post generation (supports OpenAI, Azure, and compatible APIs)
openai>=1.30.0

# Loads API keys from .env file
python-dotenv>=1.0.0

# DOCX generation for cloud account onboarding guides
python-docx>=1.1.2

# YouTube transcript extraction
yt-dlp>=2024.1.0
youtube-transcript-api>=0.6.2
