Presenter: In this video, I'm gonna walk you through the solutions for the Helm Installation Lab. For the first question, it's asking us to "Identify the name of the Operating system "installed on this machine." We can get that information by running this command. And the output shows that this is in fact an Ubuntu machine, so I'll select Ubuntu. The next question is telling us to "Install the helm package." And to do that, we're gonna take a look at the Helm documentation. So if we go to the installation page or the installing Helm section, and then scroll down to our specific operating system. So we're gonna look for Ubuntu, and we can see that here. And so what I'm gonna do is I'm actually just going to copy all of these commands, and we could just paste it directly in here. And at this point, Helm should be installed. So I'll run the validation. The next question, it says to "Use the help page of the helm command "to identify the command used "to retrieve helm client environment information." So I'll run a helm dash -- help, and if we scroll to the available commands, you can see that there's this command, env, which is going to give us the helm client environment information. So that's what we want. Make sure you select the lowercase one. "Identify the version of helm installed on the cluster." To do this, just run helm version, and we can see we're running version 3.9.2. "What is the command line flag "used to enable verbose output?" Once again, I'll do helm -- help. And we can see here for enabling verbose output, you pass in the --debug flag. And that's going to wrap up the Helm Installation Lab.