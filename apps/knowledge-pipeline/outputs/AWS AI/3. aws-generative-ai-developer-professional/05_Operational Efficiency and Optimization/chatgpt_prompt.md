<!-- Section 5 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 5 OF 20: Operational Efficiency and Optimization
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: Section Intro

Welcome to the Section on Operational Efficiency and Optimization, your guide to tuning generative AI systems for speed, stability, scalability and cost effectiveness. In this part of the course, we move beyond simply building gen AI applications and focus on how to make them perform exceptionally well in real world production environments. We'll begin with the core principles of token efficiency and context management, two of the biggest contributors to cost and latency in the foundation model workloads. You'll learn how to trim unnecessary context structure prompts efficiently and choose the right context window for the task at hand. From there, we'll look at a cost effective model selection, how to match the right foundation model to the right use case, and how to maximize resource utilization and throughput. We'll explore intelligent caching strategies, including semantic and result caching that can help reduce repeated calls to expensive models. Next, we'll dive into building responsive and resilient AI systems. You'll learn how to optimize latency, design for graceful degradation, and handle overload conditions. We'll cover techniques like exponential backoff, connection pooling, and using Bedrock's cross-region inference capabilities for stability and availability. You'll get hands on practice building a pipeline using Lambda and EventBridge to automatically ingest new documents into a vector database, a key component of efficient retrieval workflows. We'll also go deep into retrieval performance, throughput optimization, and tuning your architectures for specific use cases. You'll learn how to design resource allocation systems and optimize end to end FM performance to handle production traffic smoothly. By the end of this section, you'll know exactly how to squeeze the maximum performance, reliability, and cost efficiency out of your generative AI solutions. Skills that are essential both for the exam and for real world gen AI engineering. Let's get started.

---

### Lecture: Token Efficiency

So generally speaking, models will charge you by the token. So if you're looking to save money and control your costs, trying to manage the number of tokens that are going in and out of your foundation model is a good place to start, right? So before we can try to optimize our token usage, we need to measure it somehow. And fortunately bedrock makes that easy. So there's a count tokens API in bedrock that lets you do that. And the beauty of it is that we'll return the token count for a given request for a given prompt without actually running it, and it's free. So if you just want to experiment with different prompts and see how many tokens they actually use, you can do that for free using the Count Tokens API. So you can use this in a bunch of different ways, so you can try testing out different models and see which ones tokenize more efficiently. You can also do things like optimizing your prompts to fit within a given limit of tokens, so you can have token limits in some situations. Maybe it's a budget that you're trying to impose on yourself, and maybe you're just trying to use a really smaller model that has a more fixed number of tokens available to it. So if you're trying to fit your prompts into a given token limit or token window, the Count Tokens API is a good way of doing it. Another tool in your tool chest is CloudWatch for this purpose, and that can also track token usage on the output side so it logs both input token count and output token count for going in and going out of your foundation model. And again, measuring it is the first step to optimizing it, right. So you can experiment with different types of applications, different kinds of prompts, different kinds of approaches to what you're doing and see what yields the lowest token count and therefore the lowest cost. Now there are other stuff that CloudWatch monitors as well that you might find helpful for optimizing your applications. And that includes the count of your requests and the time that they take. Of course, the obvious stuff. Also, it measures the time to first token. So when you're streaming responses back from a foundation model, that time that it takes to start seeing that streaming response come back to you is very important from a responsibility standpoint, right? So your application won't seem responsive if that stream doesn't start pretty soon. And that lets you measure that. So that's very important from a user standpoint. It would also log any throttles that happen, any server errors, any client errors that happen upon invoking your models. And at the model level it will measure the latency. The invocation count and any throttling happening at the model as well. And of course input and output token counts as well. Very important for managing your costs. Now there are some specific techniques for managing token efficiency that we can talk about here. One is called context window optimization or context pruning. These are related. So a lot of times the bulk of your tokens isn't coming from the prompt itself, but by the context that you're adding into that prompt. Right. So if you're using knowledge bases or retrieval augmented generation techniques, those are going to be going off and retrieving a bunch of ancillary data from some vector store or something. Right. And including that in the prompt. And that's where a lot of those tokens are going to come from. So this is why it's important to have a right balance between the size of your chunks coming back from that vector store and the relevancy that you need. Also, you can limit the number of chunks you retrieve to control that cost as well. Do you really need to be getting the top ten chunks? Well, the top two do well, the top five do. That's a really big lever for managing your token efficiency. You can also filter those chunks based on metadata. So we talked earlier about hybrid search techniques there where we're not just looking at the vector distances for your embeddings, but also keyword search as well. And if your keywords reveal that some of those chunks are irrelevant to the to the prompt, you can toss them and save that token count as part of your context as well. Now, outside of rag and knowledge bases, another big source of token efficiency or inefficiency as it is, is the past conversation, history or things that have been actually injected into the conversation itself, like other documents and stuff. So if you're having a long conversation, we have to keep including that past conversation history as part of the short term memory that's passed in along with your prompt, that also eats up tokens. So what you could do is as your chat gets longer and longer and longer, you can just start to summarize the older parts of that chat, you know, so you can start to maybe safely forget about the details of it and just have a summary that you generate that will save on tokens going forward in that conversation. Or maybe you just cut it off. So that's the idea of a context window. You know, we just say, okay, beyond this size, we're just going to forget whatever was said beyond that point. So that might be an appropriate trade off for you if cost is a concern. Also response size controls and response limiting. So we talked about optimizing stuff going in. How do I optimize stuff going out. Well you can use a max tokens parameter on most of these models to say please try and get the response within this many tokens, and it will do its best to honor that. You can also just bake that into the prompt itself if you want to, and say respond in 50 words or less. Of course, the irony is you're using more input tokens in that case to try to reduce your output tokens, but you're probably going to win in the end there. Usually that works too. You can also provide some examples in your prompt to illustrate the desired verbosity that you want. So if it's not as simple as saying fit it in this number of words or tokens, maybe you want to give some examples of the level of detail that you want to get back as part of the prompt, and you can use that to control the verbosity that you get back. Also, even better, use structured data on the output side. So use JSON output and that will force it to use a given format and length in its response. Also, prompt compression is another technique. This one's a little bit iffy. So the idea here is that if you do have a large chat history or documents embedded in your prompt before you send that to a big, expensive model, maybe you can use a smaller model first to summarize it instead. And this is kind of a balance, right? Because obviously there's going to be costs involved in running that smaller model. And the cost of that small model needs to be less than, you know, the cost that you would have had by just processing it in the first place in the large model. So there's a balance to be struck there for sure, but sometimes it makes sense. Obviously that's going to increase your latency as well. So now you need to wait for that small model to do its summarization thing before it gets sent off to the bigger model. That's going to increase your latency and make your application less responsive, which might be a bad thing, but it can save on tokens. So if that's more important to you, go for it. Also, um, if you can use knowledge bases instead of just embedding entire documents in your prompt, that's a win, right? So the whole idea of rag and knowledge bases is that we're just retrieving these little chunks of data from a larger document, not the entire document. So it may be tempting just to stuff an entire document in with the prompt. Uh, it's better to just include that in an external data store where we can chunk it up and just get back the relevant bits of it, and only put those relevant bits in the prompt.

---

### Lecture: Cost-Effective Model Selection

So minimizing your token counts is one lever you have. The other big lever is what model you're using. So different models cost different amount of money per token. The latest and greatest big huge models are going to cost more than the older ones that are more simple. Right. So this is also an opportunity to save money. So you need to make an informed trade off between cost and capability. Ask yourself do I really need this big expensive model or does my application not really require that sort of smarts? And a lot of times in these applications that you're building, the smarts are going to be in the data that it's retrieving from your system or your knowledge bases, or maybe within the tools that it's using under the hood. Right. So if that's the case, maybe you can get away with a smaller model. Maybe you don't need to do PhD level reasoning or whatever it is within your application, and you can get away with a cheaper model. So you kind of have to ask yourself, you know, what do I really need there? Also, if you're just using a foundation model for pre-processing, odds are you don't need an expensive model for that. Things like summarization and compression and classification and chunking. These are things that foundation models have been good at for a very long time, so you probably don't need the latest and greatest, most expensive model for that. That's also an opportunity to save a lot of money. All right. So the other thing you can do is dynamic routing. That's a very important concept. So sometimes you have a system where you don't really know upfront how complicated of a model you need, and you need to take a look at the input prompt that's coming in, and then route it to a model that has the appropriate level of complexity for the prompt that it's looking at, right. So this is a key way to how OpenAI manages cost. For example, if you use ChatGPT, whatever it is, it's going to look at your prompt and under the hood model that to the least expensive model that can do the job that it thinks it can do. And they save a lot of money that way, and so can you. So there's actually a feature baked into bedrock these days called intelligent prompt Routing that will do this for you. You can just go turn it on and it will say, okay, I will analyze your prompt, try to estimate its complexity, and then automatically route that to an appropriate foundation model under the hood that matches that level of complexity. So that way I'm not using the most expensive model all the time. When some of my prompts can make do with something that's less expensive. Now, if you're not using intelligent prompt routing, there are other ways to do it as well. You could obviously implement this in Amazon flows by having a conditional statement. So we saw that earlier. You know we had a prompt classifier agent that then had a conditional statement that said, well, if it's this kind of a prompt, I'm going to route it to this model. Otherwise route it to this other model, so on and so forth. So that's an example of dynamic routing. You could conceivably do this within a lambda function as well. That intercepts your prompt and executes some sort of logic. Lambda functions can do pretty much anything and decides to route it to an underlying model from that point. Agent squad, of course, is built for this sort of a thing as well. It's all about routing based on prompts, and you could also implement this using the Strands Agents SDK as well. And this is a really great application of agents by the way. So you know, if as long as it has some sort of a tool to score the complexity of that prompt, we can use that tool to route that prompt to the appropriate model to another agent downstream. So dynamic routing of prompts to appropriate models. Very important lever in managing your costs. Now as you're managing your costs and trying to choose the right models, you need to measure the price to performance ratio that you're trying to achieve, right? So it's all a trade off. You need to balance the performance of your system against the cost of this system as a whole. So you're making all these trade offs and tuning, but they'll probably have an impact on the performance of your system. So you need to measure it right. You need to know what you're doing and make informed choices. So Amazon Bedrock Evaluations is a tool we'll look at in a little bit more depth later on. But fundamentally it allows you to measure the performance of your foundation models. Levels, and doing that is not as easy as it sounds, you know, because often the quality of the results you're getting from a large language model is kind of subjective, right? How do you measure that? Well, it has features for actually injecting humans in the loop here. So I can use a human set of judges to evaluate by hand which responses are better to a human being. I can also use LM judges. So you know, a foundation model can judge the other foundation model or your system as a whole and evaluate the quality of the responses, just using a separate model to judge that response. And there are a few things you can measure just with predefined metrics as well. And we can track that too. What's important, though, is that Amazon Bedrock Evaluations has a compare feature, so we can actually use that to visualize the performance of different models with your prompts and see which ones do better. So when you have that performance metric, you can pair that with token counting to estimate the cost of each prompt in each system. Right. So using these together you can say okay, this prompt costs this many tokens. And I know that many tokens costs with this model. So that's what the estimated cost of this thing. That's the price. And then I can balance that with the performance that I'm measuring with Amazon bedrock evaluations or some other means. All right. So that's the key to measuring your price to performance ratio and allowing you to make informed trade offs between the two.

---

### Lecture: Maximizing Resource Utilization and Throughput

Yet another lever you have at your disposal for optimizing the efficiency of your system is making sure you're maximizing your resource utilization. So are you paying for computing capacity that's just sitting idle and not doing anything? Well, there's an opportunity. If so, one way to make sure you're eating up all that capacity efficiently in all at once is by using batching strategies. So instead of sending individual prompts and getting back individual responses, maybe I can batch those up in some situations. A good example is when I'm computing embedding vectors for my vector stores for my knowledge bases. So do I really need to do that one at a time? No, that would be crazy. So if I have a huge document with like a huge number of chunks, I want to submit those all together in these big batches, and that's going to save me a lot of money and be more efficient. So batch up those embedding requests for vector stores don't do them one at a time. It'll be a lot faster and a lot cheaper. Also on the inference side, there is bedrock batch inference as well. So if you don't need immediate real time responses, maybe you can get away with this and also use this to optimize the efficiency of your system so you can submit many prompts together, stick them in an S3 bucket somewhere and say hey, bedrock batch inference. Go two on these and give me back all the responses together in S3 as well. So that's going to be a much more efficient way of doing a lot of prompts at once. Again, if you don't need a real time interaction there. Now, how do I plan for capacity with bedrock? So how do I know if I have enough resources to begin with, or if I'm being throttled, or I'm hitting some quota limits? Well, basically, you want to start by figuring out your desired tokens per minute and request per minute that you expect your system to be able to handle. And from there, you can figure out what kind of capacity you actually need. Now, the AWS Service Quotas tool has within it a bedrock tool, which you can see here on the right that can help you estimate the amount of capacity that you actually need on bedrock. And if you actually need to request a quota increase because you've exceeded the actual built in approvals for the amount of capacity you have for bedrock. If you do, there's a form you can fill out to ask for that. Also, if you need additional capacity, you can also buy provisioned throughput. We'll talk about that in a moment as well. CloudFormation is also an important tool for capacity planning, so it does have templates that can help you with this too. Another thing is tensor parallelism. And this is something that generally happens under the hood. I just want you to know the term. This is the idea of sharding a large model's weights across many GPUs, which any large model has to do because they won't fit in a single GPU. Right. But the end result is that we have better memory utilization per container. So I can really say. Okay, as I'm sharding this large model across many containers, I'm really optimizing the use of that memory on each container, right? So that's what tensor parallelism is all about. Again, something you usually don't have to think about as a application developer. But the term comes up. So I want to make sure you're familiar with it a little more depth on maximizing resource utilization with bedrock provisioned throughput. So with bedrock you are allowed to provision by tokens or by model units, which is a somewhat opaque unit, but it corresponds roughly to token throughput. So if you do need dedicated capacity for deterministic performance, you can do that through provisioned throughput with bedrock. And you can see an example of that over here to the right. Now if you're using a customized model you must provision throughput for that. That is a requirement, but it's generally useful for any requirement that you have for consistent performance under high volume. So anytime you see a scenario where you have really high performance requirements for bedrock model and it's not keeping up provision, throughput might be the answer. And one little fine point of this that you should know that you are provisioning for a given model, not for bedrock overall. And when you're actually going to use that provision throughput, you need to pass through the Arn of that specific provisioned model that you provisioned resources for. So when you're making an actual API call for inference against that model, you need to be sure to provide the Arn for the provisioned model and not just a generic model ID for the model as a whole. That's a very important point. Also, for utilization monitoring, CloudWatch is your friend for keeping track of how you're using your resources. It has built in dashboards as well that can be helpful for monitoring utilization monitoring. So there are dashboards you can build with CloudWatch. You don't necessarily have to use QuickSight. Also, AWS Cost Explorer is a valuable tool for attributing model cost to business functions. So if you need to go back and figure out who's driving up my AWS bill, AWS Cost Explorer can help you narrow that down to a specific application, a specific business function, or department that might be behind it. Also, auto scaling is an important tool in maximizing your resource utilization. Be sure to make use of serverless solutions like Lambda, bedrock, OpenSearch, serverless, and Agent Core to take advantage of those serverless auto scaling scenarios so you don't have to worry about resource utilization so much in the first place. So there are some tips and tricks and tools for maximizing resource utilization with your generative AI system.

---

### Lecture: Intelligent Caching Systems for GenAI

All right. For any system, caching is a big important thing for optimizing performance, right? So if you're getting the same repeated requests over and over again, you don't want to go under the hood and do a bunch of complicated stuff to just get back the same result over and over again. You want to save that stored result in a cache somewhere, and retrieve that more quickly and more immediately if you see the same requests coming in repeatedly. The problem with generative AI, though, is that you don't always get things being asked for the same way, right? So someone could ask for something in 10 million different ways just by wording it differently. And how do I know that those are actually mapping to the same thing under the hood? So this is where semantic caching comes into play. The idea here is that instead of caching the actual text of the prompt itself, we're caching it's meaning instead. And how do we do that? We create an embedding vector. So the idea here is that as a new prompt comes in, we create an embedding vector for it that kind of captures its meaning. And we cache the response for that against that embedding vector. So as new prompts come in, if we find a matching embedding vector or an embedding vector that's pretty close to it. We can say, okay, let's use this cached response instead of actually hitting our underlying foundation model. Where do I store this. So some options would be ElastiCache for Valky or Memorydb for actually storing this cache of prompt embeddings to responses. You could use OpenSearch for that as well. Okay. So the idea basically is that instead of caching the prompt itself, we're going to cache the meaning of the prompt using embedding vectors. And some tools for that could be ElastiCache or Memorydb or OpenSearch. The reference architecture here on the right kind of illustrates how AWS recommends doing this in more detail, but you can see at the heart of it there's elastic cache for the actual semantic cache on the right there, and it doesn't actually hit the LM unless there's a cache miss on that cache. And also we can see the embedding model being used by a lambda function. That kind of orchestrates the whole thing there. So you know we have API gateway receiving the prompt. Lambda goes off and creates an embedding for it checks the cache for it. If it doesn't exist, it's going to go and cash that if it does exist and it has a hit, then we can go ahead and use that cash response instead of actually hitting the underlying foundation model for the response. Now, you do have to make sure you're tuning your similarity threshold carefully so embeddings aren't perfect, right? So if somebody isn't really asking for the same thing and getting an irrelevant response, that's a bad user experience. So this can be a little bit dodgy if you're not getting it right. So you do need to make sure you're testing this thoroughly and tuning that similarity threshold where you consider two prompts to be asking for the same thing. Also, you need to make sure that the overhead of all this does not outweigh the benefits. So if you're not getting a lot of cache hits, why are you paying for all this extra infrastructure? So we've got all this extra stuff for, you know, actually paying for the cache itself. We have to pay for those embedding vectors. This can add up, and the cost of that very well may exceed the benefits that you're getting from actually not doing those foundation model hits every time. So you need to measure that and make sure that you're actually getting a benefit out of this complexity. Complexity is bad if you don't need it. However, if it does work out for you, this can dramatically improve the latency for some applications. So if you do have an application where people tend to be asking for the same thing over and over and over again, semantic caching can be a good way of improving latency by not actually hitting the foundation model every time for the response. Another form of caching is prompt caching, so sometimes people are actually asking for the same thing, using the same words. And well, the people aren't the ones doing the asking here. It's usually other stuff. Now this is built into bedrock for some supported models, so most of the new ones. The idea though is that you're caching a prompt prefix. So to make this make sense, a lot of times you're going to have like instructions that are baked into the prompt or a system prompt, or like few shot examples, things that you're actually appending to what the user typed in themselves. Right. So a lot of prompts have these elaborate instructions attached to them, or examples or maybe even documents are being attached to the prompt. In those cases, we can definitely cache that part of the prompt, right. So if we put that first, that becomes a prompt prefix and that prefix can be cached. The dynamic content stuff people are asking for would be at the end. And in between the two we have what's called a cache checkpoint. Okay, so with prompt caching, all those instructions that are static, all those documents that are static, those are cached, the dynamic stuff at the end is not so. Pretty simple really. So by caching that common prompt prefix, you don't need to tokenize it over and over again and that saves you money. You might also again cache uploaded documents that are queried repeatedly. So instead of embedding an entire PDF document or a huge spreadsheet or whatever, you can catch that bit, put it in the the prompt prefix. Now, the good thing is that if you're using cached content, that is going to be discounted per token. So it's not entirely free usually, but it's usually cheaper than non cached uh prompt prefixes. However writing them is going to be more expensive in a lot of cases. So there's again a trade off there if you're not actually getting enough benefit back from the cached prefixes, it might not actually work out for you. So again, it's something you want to measure. I've definitely seen systems where prompt caching costs more than it saves, because writes are more expensive, even though reads are less expensive. If you're not getting those hits often enough, it might not pay for itself. And you can monitor this, of course, in CloudWatch, which will record cache reads and writes. And that can be a useful tool for measuring whether or not you're getting a good bang for the buck there. Also, this is an option in Bedrock Prompt Management, a feature we looked at earlier. So you can enable prompt caching on your prompts that are stored in prompt management. Two another form of caching is edge caching. So we can actually use CloudFront on the edge. And if you're not familiar with CloudFront, usually that sits in front of S3 content or content for your website, right to accelerate the retrieval of that content all around the world. So they have these edge locations where they cache that content all over the world. So if you're in India, you can get your data from a server that's in India, instead of going all the way back to some other country to get it. You can use this for your generative AI applications as well, to reduce latency and to reduce the number of back end requests that filter through that cache. So if my edge cache can give me an answer right away, that's great. It's going to be super fast. It's not going to cost me a hit to my model on the back end. But again, we have the problem where a lot of the prompts that we're getting are dynamic, right? How do we cache this? Well, it's a similar thing to semantic caching. Like we need to somehow fingerprint these prompts and identify what they are so we can try to catch them up front. And if you do have a situation where some prompts are in fact identical, maybe that is just a hash code, right? Um, it could be that simple in this case. So maybe you have an application that has sort of a menu or a list of prompts that it chooses from, that it sends through to a back end system. In that case, you might have a hash or a fingerprint associated with each prompt. And if you do expect to get a similar response from that prompt every time, um, you can just cache that on the edge. So to make it more specific, the Get request that actually goes to your application could include that fingerprint of the request itself. And that way CloudFront could cache the actual result of that request on the edge. Now this gets a little bit dodgy if you have like personalized output or anything like that. Uh, so if that's the case this might not be a good fit. Also, if your data is changing a lot and your responses change a lot in response to that, you want to make sure you have an appropriate time to live on that edge cache as well, to make sure that changes are being picked up in a reasonable amount of time. You can also fingerprint the results too. So if you want to encode some information as to when it's appropriate for a result to be used for a given set of model configuration values, you could also embed that into the result fingerprint as well. And of course, there's more to an application than just the large language model piece. As with any web application or application in general, if there's images or static content that can be cached on the edge, you should do so.

---

### Lecture: Building Responsive AI Systems

Another important thing to optimize for is the responsiveness of your AI system. So how does it feel to the end user? Are they sitting around all day waiting for an answer? If so, they're not going to like your application very much. They want to get instant gratification from what they're doing with you, right? So what are some ways for speeding up the response of your system? Well, some of them have gone through before. So one is using parallel requests for complex workflows. So if you have a bunch of heavy lifting that can be done in parallel, do them in parallel. So these multi agent workflows can be useful for that. We looked at the orchestrator synthesizer pattern earlier where an orchestrator agent analyzes your prompt and breaks it up into things that can be done in parallel, then farms those out to specialized worker agents to do those tasks all together at the same time, and then collates the responses back together. So if those workers are doing some time intensive heavy lifting, that's going to be a lot faster than doing those operations in sequence. So pretty much any SDK can pull that off these days. But step functions can do it as well if you want to do it the harder way. Also, like we talked about in the previous lecture. Caching can help your responsiveness a lot. So if you do have predictable queries or people asking the same query over and over again, semantic caching or prompt caching can help with responsiveness of that system as well. By saying hey, I've seen this question before, I cache the answer to it. Here you go. No model required. That can be just come back right away. Also, you want to use streaming whenever you can for responsiveness. So instead of firing off a prompt and waiting for the complete response to come back from the foundation model, we can start streaming the response back. And if you use ChatGPT, you see this all the time, right? So instead of sitting around for three minutes waiting for a long response to come back, you'll see that streaming back word by word and actually see that being presented to you as an end user. That feels a lot better because you can start reading before it's actually done. So huge, huge thing for responsiveness. You streaming when you can also, um, there's a feature of bedrock called latency optimized inference for foundation models. I don't know why you wouldn't use this, but to use it you just specify performance config equals latency optimized as part of your API call. So when you're calling the converse API or whatever it is, stick that as part of the request and it will optimize for you automatically the time to first token the output tokens per second and the end to end latency, which takes into account more than just the model you know, from end to end. What is the user actually seeing for latency? That's what's really important at the end of the day. All right. Some other tips. Do use intelligent prompt routing when you can. So instead of going to a big, fancy, time consuming and expensive reasoning model, a lot of prompts don't need that level of complexity. So if you can identify a simple prompt and route that to a simpler, smaller model, those tend to be more responsive. They're going to come back faster. So intelligent prompt routing isn't just about saving money, it's also about responsiveness. Also, be sure to keep your prompts concise so the less information you give the model to chew on, the faster it will give you back a response. Just like a human being. You know, if you're sending somebody an email asking them to do something. There are more, more likely to do it and get it right. If you give them a really concise and precise set of instructions, instead of a whole bunch of word salad that goes on for pages and pages. So that will both improve the quality of your results and also make them come back faster. And if you are in a situation where you think your prompts might get truncated, if you're really forcing a limit on that token length on the input side, make sure you're putting the important stuff first in case it does get truncated. And also it just helps it kind of like keep track of what's important within the model itself. Context pruning we talked about earlier as well. So um, don't give it a bunch of information to chew on that it doesn't need. That will also slow down your system. So if you're getting back results from your retrieval augmented generation system, from your knowledge base coming back from your vector store, if they're not valuable, you know, and you've identified that through hybrid search, maybe. Or they're redundant. You know, you have a bunch of chunks that are similar to each other and not really helpful on their own. Uh, ditch them, throw them out. The less information I send, the faster my response will be. Also, I can explicitly limit the response sizes that will also make things faster. So if I need to generate 300 tokens, that's going to come back a lot faster than 3000 tokens, right? And sometimes, you know, a concise result is a good thing. So limiting your response sizes. And we talked about ways of doing that earlier can also improve the speed of your system its responsiveness. I can set a max tokens explicitly. I can bake it into the prompt like give me a response within this many words, or I can force a JSON response to really force what kind of a format I get back. If you have a complex task, break it up if you can. Instead of waiting for the entire complex task to come back and then presenting that to the user, you can instead present to the user what each step of that task came back with in sequence. So they're going to see a result sooner. That way, if you can break up those tasks into more simple subtasks, that can be displayed back to the user as they come back in. All right. So those are some tips and tricks for optimizing the responsiveness of your system. How zippy it feels to the end user.

---

### Lecture: Optimizing Retrieval Performance

All right. Let's talk about optimizing retrieval. Performance retrieval in the sense of retrieval augmented generation. So if I'm looking up external context from a vector store or a knowledge base or whatever you want to call it, how do I optimize the retrieval of that information to make sure that it's relevant to what I'm trying to get back and getting back, you know, all the information that I have available that might be helpful. So a lot of the details of optimizing the actual system performance, you know, at the index level we talked about earlier. So you might want to go back and review OpenSearch vector stores in the various knobs and dials you have for optimizing that performance and optimizing your indices as well. So there are several ways to actually optimize the performance of the underlying data store itself just by how you organize that data. So at a low level that's a place to start. But there's also higher level things we can do. Right. So we talked about hybrid search a few times. That's searching not just for the embedding vector distances but also for keyword search at the same time. So that can help improve your relevancy. If I'm searching for both keywords and embedding vectors, you know, semantic search and keyword search at the same time, that can help my relevancy as well. Something I haven't mentioned before, though, is that it's possible to have your own custom hybrid scoring function. So how do I combine that semantic score to the actual Keyword based relevance score. The way that I do that might improve the performance of my overall system. Another thing we can do is pre-process the query coming in. So again, thinking back to how retrieval augmented generation works, a prompt comes in, I go and query my knowledge base against that prompt and hopefully get back chunks of data that's relevant to what that person's been asking about. But that doesn't always work. That doesn't always go according to plan. So one thing that can help is if the query itself that you're sending to that search is in the same style as the information that you're searching against. So imagine that you have a knowledge base full of scientific papers, right? If I'm asking for information in the same language, you know, the same style of scientific papers, I'm much more likely to get relevant hits than if I'm, you know, asking in more of an informal tone or, or something like that. So if there's a way to normalize the style of the query to the style of the content I'm retrieving, that can help with the relevance of the results that I get back. Also, you need to watch out for people asking for two things at once. So somebody says, I want to learn about this and this. Uh, you're not going to find a hit most often that actually covers both of those things. So splitting that up into two separate queries on the retrieval side can help. So if you do see somebody asking for two or more things as part of the same prompt, you want to split that up and search for relevant documents or chunks of data that are relevant to each part of that individually. Okay. And you might need a separate model to do that. Also, if they're asking for stuff that's irrelevant to what your system does. Toss that out. That will help. Right. So if they're if you have a system that lets you query scientific papers again, and somebody's asking for, um, I don't know, yesterday's news or something, uh, if you can identify that and toss it out the window, you know, that's not going to be in your knowledge base to begin with. So that can help too, if you can reduce the ambiguity of the prompts coming in before you send that off to retrieval for looking for that relevant documentation. Uh, that also helps. What I mean by that is like, if somebody says, I want to learn more about this thing I talked about earlier, uh, your knowledge base does not know what this thing I talked about earlier means. You need to spell that out and try to resolve that ambiguity before you actually try to do a search against your knowledge base. And also, if you are doing hybrid search, if you're able to extract keywords from the prompt up front, that can help too. So if I can say, hey, this prompt has some keywords that I might have indexed, let's just pull those out and search for those directly. That will make your hybrid search even more efficient.

---

### Lecture: Optimizing for Specific Use Cases

Another topic might be how do I optimize my foundation model for specific use cases? How do I tailor my model's performance to what I'm trying to do? And different models will have different parameters that you might be able to tune for that purpose. Now, to measure the effect of those parameter changes, you can use a B testing of course, and you don't have to roll your own system. Bedrock evaluations has a B testing capabilities, as does CloudWatch. Evidently that's a feature in CloudWatch that lets you do that. SageMaker also has some features along those lines too. What are some examples of parameters you might want to tune? Well, you know, there's the usual stuff like max tokens and things like that where you can control the verbosity of the response for a given use case. Some other ones you might run into a lot and you know, these come up a lot are temperature. So temperature is basically how much randomness is in the response. So, you know, a value of zero would mean that I get a consistent response every time. Well, usually there are things that change in the model itself that might make that not true as well, whereas a value of one makes it more creative. So the higher the temperature, the more randomness there is in the response. And by randomness, I don't mean totally random and off the wall. You know, it's basically choosing between a list of candidate tokens or next words to put in their response. And those candidate tokens themselves have been vetted to be, you know, reasonable next choices. How do I control how reasonable those are? Well, that's where top comes in top. P is sometimes called nucleus sampling, and this is the probability threshold used for a token candidate to be generated in the first place. So how good does that token candidate. And again a token is more or less the next word that I'm predicting in this sentence. Um, how good is that prediction need to be before I'll consider it as a candidate for the next token? So top P would control how good those token candidates need to be. Temperature would control how much randomness do I have in choosing among those candidates. Now typically you will either specify temperature or top p. You don't usually specify those together. Top K is just how many of those candidates do I have? So how many token options do I have to sample from. So you can see that top p and top k can be a little bit mutually exclusive as well. Top K is saying I have this fixed number of candidates that I want where I just have the best results up top. Top P is saying I'm going to have this specific probability threshold before it becomes a candidate, and that might be limited by top k. Okay. So this all kind of works together. But that's what those common parameters mean. And this is the sort of thing that people like to quiz you on. So understand the result temperature, the amount of randomness in choosing the next token top p, the how good those tokens need to be before they become a candidate, and top K how many candidates do I choose from?

---

### Lecture: Optimizing Foundation Model System Performance

So there's a bunch of other stuff you can do to optimize your foundation model systems performance, and I'm just kind of collecting them all in this little lecture here. So they're not all entirely related, but just some quick hits here. So first of all, if you are trying to optimize your performance, you need to figure out what's working right. So we talked about the impact of caching and batching and improving your retrieval augmented generation. But before you know if that will actually help, you need to understand how your system is being called. So API call profiling is an important tool for identifying patterns in your requests and figuring out are people asking for the same thing over and over again? If so, caching might help. Um, are people asking for things that can be batched together? Are they asking for things in a way that matches up with the context that I'm trying to retrieve? You know, if if not, then maybe there's room for optimization there as well. All right. Also a tip is to use structured input and output whenever possible. This has come up a few times already in this course. But again if you can require JSON or XML templates or something on the input and or output side, that will improve both the efficiency and the accuracy that you're getting back from these models. Chain of thought instruction patterns. So if you are trying to do something more complicated, make sure you're using some sort of a reasoning framework to force the foundation model to process that complicated query in a step by step manner. Now, a lot of models just kind of do this themselves under the hood. So maybe this is more of a dynamic routing issue where you're making sure that if you do have complex reasoning tasks coming in, those are being routed to a model that supports chain of thought instruction patterns. And this comes up a lot too, for some reason. Feedback loops. AWS is really big on this, so you do want to have the ability to capture user feedback. Are they liking the results they're getting back from your system? If not, you got to go back and fix something. Or if something's working well, maybe you can use that feedback loop to identify prompts that work especially well with your system, and encourage the use of those prompts more often. Some SageMaker specific stuff for optimizing your systems. So one thing you need to keep in mind is that these models can get really big. So SageMaker AI can deploy a model up to 500GB in size. And as you might imagine, it takes a while to download that and spin it up and run a health check on it. So you can imagine a scenario where you're deploying this massive model to SageMaker AI and it never starts. Maybe that's all it is. You just need to adjust the timeout for allowing that to download and get the health check run. Also, if you're deploying your own models and you need to worry about model parallelization, which typically you wouldn't if you're just using bedrock or something. But third party parallelization frameworks are supported. Those include Triton, Faster Transformer and Deep Speed. So if you do need to split up a model across many GPUs, these are tools you can use to do so. And there is some specific instance type guidance that I ran across in the various documentation. So if you are deploying a large model to SageMaker AI from scratch, you might want to choose a very large GPU instance, obviously. So MLP, 4D 24 Xlarge is an example of such an instance type for really small models, though maybe you're just doing something simple like named entity recognition. In some cases, maybe you can get away with just a CPU instance. Maybe you don't need a GPU at all. Kind of hard for me to imagine, but the docs say for NER you could get away with an MLP. Five nine xlarge I mean, for any of these models, they're fundamentally deep learning models and they're going to benefit from a GPU. But if cost is a primary concern, there are some situations where maybe you can get away with a CPU instance. Also, at the lower level there's something called an ultra server. So if you do have a model that you're deploying yourself across your own EC2 instances and it's being distributed across those instances, the speed in which those instances can communicate with each other is very important. And that's where ultra servers come in. So a couple examples of ultra server instance types are TR2 that's trainium two or P60. GB 200 is another type. So what this offers the these ultra servers is really low latency and high bandwidth interconnects between those instance types. So that's important if you're going to be distributing a model across many, many EC2 instances. And one last bit of tidbit of information a Lambda endpoint lifecycle management is an important tool. So if you are going to be spinning up your own endpoints for your inference, you can actually use a lambda endpoint to actually manage the download of any model artifacts you might need from S3 or whatever. This all happens on demand when you're processing is triggered. So lambda endpoint lifecycle management can make sure that all this stuff that you need to run that model gets pulled in on demand.

---

### Lecture: Exponential Backoff and Connection Pooling

A couple of things that got called out in an oddly specific way in the AWS Skill Builder. Materials for this exam are exponential backoff and connection pooling. And these are by no means things that are specific to bedrock or generative AI. These are just general ways of managing larger systems and how they call each other. So exponential backoff if you're not familiar with it, it's basically how you deal with a failed API call in a controlled manner. So you don't want to get yourself into a situation where you have a back end service that goes down, and now all of a sudden everybody's trying to hit it all over the time, you know, repeatedly, really quickly, hoping that it comes back up. That's just going to make matters worse, right. So exponential backoff is a way of saying, okay, I'm going to I'm going to try hitting it a little bit less frequently over time. And that way I can ease the load on this thing and maybe give it a better chance of coming back up. You don't want to flood the poor broken service. So it's fundamentally a way to retry failed API calls in a controlled manner, right? And doesn't flood the poor thing. Give it a chance to recover. And there are ways to configure this. So when you're calling the AWS SDK, you know some client through Boto3 or whatever it is, you can set up your own custom retry policies and some oddly specific numbers that were in the AWS material. Maybe you want to start at 100 milliseconds for your retry frequency, with a backoff factor of two, with a max retry count of 3 to 5 with a jitter of plus or -100 milliseconds. I'm not sure I'd bother memorizing that, but it does seem weird that they have those very specific numbers in the exam prep materials. The jitter, by the way, is just to make sure that all of the clients aren't trying to hit it at the same time, right. So that would definitely make matters worse. If you have a a back end system that's under stress right now and every, you know, one second, 1000 clients are hitting it all at once. So, you know, again, it's all about giving that failed service some room to recover. And yeah, I'm not sure why they got into those specific numbers, but they do. Uh, also it talks about connection pooling. Again, just a general best practice for when you're dealing with external services that are over HTTP, over some sort of a RESTful interface. So it can be very expensive to establish a new HTTP request, right? So instead of doing that for every single request, connection pooling maintains a pool of HTTP connections to your back end service that are always active. And you just reuse those connections for different requests as they come in. So that spares you all the overhead of doing all that handshaking and establishing that connection every time. So always a good idea. Uh, oddly specific guidance again. Uh, they are recommending between 10 and 20 connections per instance. So if you have a, uh, instance that's hitting some sort of a back end foundation model, maybe 10 to 20 connections kept open to that foundation model is a good number to start from. And maybe the time to live, uh, would be 60 to 300 seconds. So how long do these shared connections stay active? You know, maybe you don't want to really keep them open forever. Uh, maybe you only want to keep them open for a minute to five minutes at a time. And the idea here is to strike a balance between resource utilization and the reuse of your connections. So, you know, that's why we need to kind of think about the number of connections per instance there. Again oddly specific guidance in skill builder.

---

### Lecture: Amazon Bedrock Cross-Region Inference

As you go through the documentation for AWS gen AI capabilities and bedrock, you see bedrock Cross-region inference come up a lot. A lot of features require it, and a lot of them just recommend it really strongly. But it's a very important tool for maximizing the efficiency of your system. So we got to talk about it. What it does is it distributes your workloads across AWS regions. So if there's a service interruption in one region, it can automatically switch that inference to another region for you without you having to think about it. Also, if there is restrictions from quotas in a given region or peak usage in specific regions, it can automatically reroute your inference to a different region to keep that throughput going for you. So it just is a way to spread out that load across multiple regions if it needs to because of capacity concerns or because of some sort of a service interruption. So it's definitely a good thing to have to make sure your system keeps on running with bedrock. Now, there are things that can thwart your plans here. One of them is Amazon Organizations service Control policies, or SCPs for short. So if you have your SCP set up to block a region, then bedrock Cross-region inference can't work with it. And this is something you need to think about. Now. Normally when you're dealing with AWS, you're explicitly using a given region, right? Like you say, I'm in US East one. That's where all my stuff is. But with bedrock cross region inference, that's not necessarily the case. And if in Amazon organizations you've only allowed certain regions, that's going to be a problem. So this is something to worry about. So you need to make sure if you're using cross region inference that SCP is allowing all the regions that you might be enabling there, or just leave the region specified as unspecified. Sounds a little bit backwards, specified as unspecified, but that's actually what you do. You have to say unspecified in the actual specification. That's how that works. So just be aware of that potential conflict between SCP and cross region inference. There are a couple of different inference profiles you can choose from with cross region inference. You can choose to restrict it within a specific geography. So you could say, you know, I want to be in a region in India, but I don't really care which one, but it has to be within that specific geography. And we'll talk about why you might want to do that. Or you can select global and say, okay, send me wherever you need to go. You know, if I need to spread myself out halfway across the world to keep the system running, then that's what we're going to do. And that will just select the optimal commercial AWS region that's currently available. So it will be smart about it. You know, it's not going to send you to the other side of the planet if it doesn't have to. but it's nice to have that option just in case, right? Now, one thing to keep in mind is that Cross-region inference does not work with provisioned throughput, as you might imagine. So if you are provisioning explicit capacity in a given region, well, that's what you're stuck with. Okay, so those two things don't play nice with each other. So what are these different models. So like I said geographic cross-region inference will restrict you to a given geography. And this is useful if you have data residency requirements. So the only real reason you'd want to do this that I can think of would be if you're in the EU or something, and you are legally obligated to make sure that any data collected from EU citizens stays physically within the EU. So this makes sure that your data within that geography is not being sent outside of that geography for inference. So that would be geographic cross-region inference. Or you could have global cross-region inference, which lets you use the whole globe, the entire AWS world, and that will maximize your throughput. But and it also comes at a cost savings. So like not only is it better, it's also cheaper. So you actually get a 10% cost savings by enabling global cross-region inferences if you can. And you can see how that would benefit Amazon as to let them spread out the load a little bit more efficiently from bedrock inference, which can be expensive on their side. Pricing, however, is always going to be based on where you called it from. So even if under the hood, bedrock is going halfway across the world, I'm not going to be charged based on the rates for that region halfway across the world. I'm going to be billed based on the cost of where I call that from. Okay. So you don't have to worry about your prices ballooning as a result of this because you're going to some obscure region that's more expensive. That's not how it works. And also, you do not have to explicitly enable these regions in your account either. So even if it does send you halfway across the world, it doesn't matter if you've actually explicitly enabled that region at your account level. It does matter with SCP with Amazon organizations though. Okay. But beyond that, it's kind of magic. So generally speaking, you want it on, but you do want to think about any legal obligations you might have about geographic restrictions of where your data goes. And one more point. So your data, by the way, will be encrypted in transit, of course, as it's being sent around to different regions. And it will also be logged in CloudTrail. If you need to go back and figure out where it went after the fact. So that's bedrock Cross-region inference, very important tool for optimizing the efficiency and the throughput of your bedrock applications.
---

*Section 5 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*