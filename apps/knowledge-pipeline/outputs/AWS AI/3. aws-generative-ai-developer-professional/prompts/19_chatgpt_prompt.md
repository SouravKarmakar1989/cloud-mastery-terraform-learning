<!-- Section 19 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 19 OF 20: Notes on GenAI Architectural Tradeoffs
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: A Complete GenAI User-Facing System

All right. So the real exam is not just going to be trivia about all these services we've talked about. It's going to be about how do I put these services together to make larger applications. So let's fill in a few gaps that we haven't really touched on enough. I think in the course previously, we've certainly covered a lot of architectural discussions coming up to this, but this section is just about kind of filling in some gaps that we left before and reinforcing a few things. So let's take a look at an example system here. Now it's important to realize that there's more to generative AI applications than just bedrock. I mean, bedrock is going to be taking care of the actual AI part of it, but that's not all you need for an application. So there's more to a production system than just bedrock. And I stumbled across kind of a really cool resource here called the generative AI Application Builder on AWS. You can go search for that. Or if you have the copy of the slides, there's a link for that here as well. And what it is is just a collection of CloudFormation templates for some common generative AI application patterns. So here's an example of one here that we can walk through. And just kind of use that as a base to start off from. So over on the left here we have basically users talking through some client application. Maybe it's a web app, maybe it's a mobile app. I don't know, it's something and all it's doing is doing some sort of a LM based, um, agentic AI based thing using Amazon Bedrock agent under the hood. So way over there at number seven, we have bedrock sitting there kind of tucked away. Right. But there's all this other stuff around it that's important too. So for any application, the first thing you're going to need is the actual UI for it. And that's probably going to be largely static content. So that might just be stored in Amazon S3, the actual HTML or whatever resources it is, the images and whatnot. And you would probably have CloudFront sitting in front of that to make it more scalable. Right. So first thing we're going to do is hit CloudFront for the actual bones of the application itself. Once we have that downloaded and all the resources and code and whatnot that we need on the client side, we can then maybe go to authenticate the user. So the idea here is that we have an application that's probably dealing with some sort of a personal information or information that's at least tied to an individual somehow. So to do that, we need to authenticate. And we don't want to be hitting Cognito directly from the outside world. We're going to go through API gateway instead. Right. So step three here is to actually sign in. We're going to go to API gateway. Hit a Lambda function that then in turn calls Cognito. And maybe then that same lambda function says okay, I know who you are. Now let's go off to Amazon DynamoDB and retrieve which IAM policies are associated with that identity. And then we can pass those IAM policies around those IAM roles and whatnot, to make sure that you actually have the appropriate permissions to view the data that we're going to be showing you later on. All right. So now that we know who you are, we can actually then do gen AI stuff. Right. So again we're going to have lambda sitting between the actual API calls and bedrock agent itself. And in this particular example they've also stuck an SQS queue in between API Gateway and Lambda where it's actually doing the orchestration for our AI stuff. And again, that's just sort of a buffer in case Lambda gets backed up or you have a big surge in traffic and you need to sort of queue things up to recover, right. Now let's talk about the security side of things here too. So you can see that everything's going through this big virtual private cloud over here on the right, a VPC. And within that VPC, we in turn have things split up between two availability zones and an even higher level. All this stuff is just within a single region. So you might duplicate this all across multiple regions as well. But you can see that we have a VPC to VPC Nat connection here that's aligned to Lambda to talk into this virtual private cloud to keep things nice and safe. And then we have VPC gateway endpoints and interface endpoints coming out of it as well to talk to the back end services that we need. So imagine that our Lambda function is calling Lang chain, which is just a Python library for orchestrating AI stuff. Right. And this could be any other orchestration method you might want to, you know, I mean it could be flows, could be strands, agents, whatever. But it's going to talk to that VPC in that there to actually get into our VPC. And then in turn talk to bedrock agent. And maybe it's pulling in some information from DynamoDB along the way as well. Could be other stuff too. You know, maybe I need to retrieve some contextual information from Kendra or from a bedrock knowledge base along the way to that same lambda function might manage that, too. All right. So we've actually done some AI stuff here at this point. That's the bones of the application. This particular example here also has a feedback loop in it as well. So we have a mechanism for users to submit their feedback on the quality of the results. And that's what's going up on the top loop here. So in that case we have a web application firewall WAF sitting in front of the API gateway to make sure that nobody's going to be, uh, dosing our feedback API. That in turn talks to Lambda, which again goes through our VPC and out the other end, it might be storing that information in S3 where it might be analyzed later on. Finally, off to the right here we have CloudWatch sitting on top of everything. And it has its own interface endpoint there to kind of keep track of everything that's going on and logging all the activity that's happening for auditability purposes later on, and debugging whatever it might be. All right. So this is just one example of a AI system built using the generative AI Application Builder. Again, I'd encourage you to go through that site and see what else it has to offer. It might be a good review and spark some ideas on how to put some of these pieces together, because again, that's what the exam is all about.

---

### Lecture: Choosing a Vector Store

Something else you're likely to need to do in the exam is choose between different types of vector stores for a given sets of requirements in your generative AI applications, and we've built up quite a menu of places to store our contextual information. Now this is kind of a big wall of text. I'm not going to go through it and read it all to you. You know, you can hit pause and take a look at it or take a look at it in the slides. But I'll kind of call out some of the highlights here of what the pros and cons are for different vector stores and when you might want to use them. Something that kind of surprised me a little bit is how much Kendra comes up. Now, Kendra is not technically a vector store. It's not something where you're actually going to be storing actual vector embeddings and actually integrating that into a bedrock knowledge base, but it can play the same role. So you could have, for example, a lambda function that calls Kendra to do document search across your enterprise documents and then bake that into your prompt as additional grounding information as you send that actually into the LLM. Right. So the thing with Kendra, I'm going to skip to that one first. If you need document level access control list enforcement, or if you need to integrate with SharePoint or confluence, you might want to be using Kendra for that. Okay. So again, it's not really a vector store per se, but it can serve a very similar role in retrieving contextual information that you're using to ground your prompts in. And it has strong enterprise security, and you don't really have to think about it too much though. So a lot of times you'll see questions that are like, what's the lowest operations burden? Right? Kendra. Serverless. Really easy to use, very low operational burden on that. So consider Kendra. Also consider S3 vectors. This is a newer addition to the list here. The newest of this particular menu. Here. Again the ops burden on that is very low. So if somebody wants you to create a low maintenance vector store S3 vectors is a candidate. But remember the downside is that it can be slow. So it has potentially very high latency. If that's okay. Then S3 vectors can be both very simple and also very, very cheap. Okay. So you can see there's only a single dollar sign there. S3 vectors are dirt cheap. The trade off though is latency. That's the main one there. We talked about OpenSearch a lot. That's kind of the default go to version, serverless being the go to version for knowledge bases. And you know it is low ops burden because it is serverless. However, you know, it's um, it's going to give you less control. So if you do need to have more control over the actual tuning of the performance or if you need consistent capacity or provisioned capacity, serverless won't cut it. But if you do need to deal with spiky and variable query traffic and you need low operations, that's a good choice. If you do want that additional control and additional control over scaling and having provisioned capacity, you might want the managed version of OpenSearch instead. Then we also have Neptune Analytics. Um, it's basically used for when you have relationships, when you have those graph relationships, like with the social network or supply chain relationships or some other examples there. So if you're dealing with networks, you know, graph relationships, Neptune Analytics is probably going to be part of the answer there. And finally we have Aurora as an example as well. If you do have a relational database already in Aurora, you can use the pkg vector extension to turn that into a vector store as well. Now that is going to be a medium operational burden. Um, and it has some scalability concerns of course, but for many applications that might be a choice too. Let's kind of break it down a little bit more explicitly. So if you have a question that's mentioning a requirement for SharePoint integration, confluence integration, uh, really strong ACLs. Kendra is definitely something you want to be thinking about as a potential part of that solution. If you have graph relationships, you know, social networks, fraud rings, dependency graphs of some sort, Neptune Analytics with its vector store capability is definitely something to think about. If you have data that's already on Postgres, or if you need a big joins or fancy SQL operations, and you also need vector search on top of that, aurora with the pkg vector extension might be the answer. And if you have a huge amount of data and you want to keep it cheap and you don't really care too much about latency, as three vectors might be the answer there. And finally, if you want a full search capabilities and lots of tuning control, the managed version of OpenSearch is your friend. If you have unpredictable traffic and you don't want to deal with a lot of operations, OpenSearch serverless is probably the default solution for most applications. Finally, putting it one more way if your cost is your main constraint, if it says you want to do this cheap, what is the most inexpensive way of doing this? S3 vectors might be the answer if you're mostly concerned about permissions and ACLs and SharePoint integration. Kendra is your friend. Relationship data Neptune is your friend. If you need sequel, Aurora with PG vector might be your friend. And if you care about search primarily OpenSearch, either managed or serverless might be the droid that you're looking for. As we say. A little bit more depth on choosing between OpenSearch managed versus OpenSearch serverless. There are various dimensions you might want to consider there as well. If you care about the billing dimension of things, make sure that you understand that serverless is going to be billed per request, in addition to the storage that you're using under the hood. Open search. You can do provisioned capacity and have that capacity always on and being billed at a steady rate as well. If you need more tuning, if you need full control and want to be twiddling with shards and whatnot, use managed. If you don't care, use serverless. If you want to think about all that for you. If you want predictable latency, this is an important one. OpenSearch managed is going to be a way of doing that through provisioned capacity. With serverless, your latency could be variable, so that might be an important requirement for you. Scaling is automatic. Of course with serverless that's what serverless means. If you do want control over that though, OpenSearch manage is the way to do that, and it does have automatic ways of scaling up and down as well that we talked about earlier. Finally, if you have a question that talks about unpredictable traffic, serverless might be the way to go. If you have a requirement for fine grained control over OpenSearch, the managed version might be the one you want.

---

### Lecture: Orchestration  When to Use Step Functions

Let's quickly talk about orchestration and when to use step functions. Something that surprised me, frankly, when I was preparing for this exam is how much step functions come up. I never really thought of that in the terms of AI applications before. And the reason is because a lot of these modern agentic AI frameworks do orchestration for you. That's what they do, like strands agents or the open AI agents SDK or anthropic has an SDK as well. Bedrock flows within the AWS ecosystem is another way of doing orchestration as well. Step functions is kind of an older service, right? So I don't know if this is going to keep coming up in the exam forever as the preferred way of doing orchestration in AI applications. But here we are for now. However, there are some cases where step functions has some advantages that make it the right choice. So if you do need auditable state transitions in your AI application, by the way, this is an example over here on the right. Um, again there's a link here from the slides if you want to go and explore that in more depth. This is an example of a clinical application using generative AI. And diving into that is definitely worthwhile. So follow that link if you so desire or search for it. But yeah, if you do need to have audits on those state transitions and in a clinical application, you definitely need to have auditability. There's a lot of regulations to deal with. Maybe step functions is your choice just for that reason alone. It has sort of a better record of what went on. Also, if you need retry capabilities and failure isolation step functions has that baked in for you. And maybe some other frameworks don't really make that a primary concern. If you have explicit approval steps that need to happen. If you need to go off and seek human approval for something. Step functions might be an easier way of doing that. So a lot of these AI frameworks are kind of tied to the AI stuff. You know, talking to your LMS, talking to bedrock step functions can do other stuff, right. So it's more general if you need to go off and call some other service or some other approval step that goes off on some other system. Step functions might be a good fit for orchestrating that larger system, right? So just keep in mind step functions can do stuff that some of these other frameworks can't. Still state transitions that are auditable retry mechanisms, failure, isolation, and maybe branching off to approval steps that need to be done outside of the system. And finally, remember that step functions are serverless. But just a serverless requirement alone should not be enough for you to choose step functions. Bedrock serverless too, right? So a lot of this stuff is really step functions comes into place when well, either if there's no other choice for orchestration or if you have these other requirements here, one or more of them step functions can fit those as well.

---

### Lecture: Simplified Internal and User-Facing Architectures

So let's take a look at a couple of simpler examples here, and how we might use these different services to piece together an actual functional application. So let's start with an internal application. This is just going to be an internal application for some legal Q&amp;A chatbot. Okay. So because it's internal we don't need to worry about massive scale or people attacking us right. We don't need DDoS protection because, well, hopefully nobody inside our company is going to be dosing ourselves. So we're just going to hit the API gateway directly here for this particular application. Don't have to worry about CloudFront or AF for these internal applications, right? But from there on it's going to look pretty similar whether it's internal or external. So the first thing we're going to do from API gateway is call AWS Lambda to kind of orchestrate what we're doing. And because this is legal information, you know, we probably need to have some sort of a trail here, right. So we're going to have to log someone in. And to do that authentication we might use Amazon Cognito. Once we have that identity incognito we can then build sort of a user profile that contains that person's permissions and pass that along. In this case to bedrock. Now in this case Kendra would be an appropriate tool for bedrock retrieval if we had a requirement for document level access control. Again, ACLs is going to be a key feature of Kendra. And we also want to talk to SharePoint. It turned out. So imagine a scenario where you have an internal legal chatbot where you need to have ACLs at the document level and you need SharePoint integration. Kendra is probably your beast for that, right? And Kendra can talk to bedrock and use that for some additional contextual information that we then pass on to the generation phase to actually do a generate call on bedrock and produce our output. Okay. Now you could get more fancy here and have, you know, more stuff surrounding this, like, uh, you know, CloudTrail or CloudWatch and whatnot to do auditing and logging and everything else. I'm just going to smooth over that. Obviously there's going to be, you know, security aspects to it, how you actually structure the VPCs around this. Again, we talked about that a little bit earlier, but at a high level I'm just kind of sticking to this kind of what are the building blocks here and how do we put them together. All right. Again we're using Cognito in this case for authentication. And that user context will be passed through all the way through Bedrock and Kendra for authorization as well. In this case, bedrock is being used for retrieval using Kendra for documents used for grounding that retrieval stage. Okay, so as a counterexample, let's take a look at something that's customer facing. So in this case we're making a chatbot that is exposed to the external world. And in this case we're definitely going to need something like WAF and CloudFront sitting in front of that before it hits the API gateway, because we want to make sure we aren't getting hit by bots that are going to run up our AWS bill. Right. And really, in a real world scenario, you probably would still need to have some sort of authentication, like, are you really going to open up a system to the world where anybody can log in and call bedrock on your dime? Probably not. Right. You need some control on top of that. So you might actually have a Cognito step here as well, like we did in the previous architecture. But anyway, putting that aside in this more simple application, this is kind of like, you know, the general starting point for any customer facing bedrock application. WAF goes to CloudFront and then that goes through API gateway that goes to Lambda. Lambda in turn can call bedrock for retrieve and generate, which might be in turn calling Amazon OpenSearch as a either as part of a knowledge base or maybe even directly. So this is basically your starting point for any external facing chatbot. You're going to have WAF going to CloudFront from CloudFront, maybe going to API gateway. The idea here is that your API could have some things that can be cached on CloudFront still, you know, so it's a little bit of a extra insurance against controlling your prompt or your token costs. That in turn will call Lambda, which will do the stuff needed to orchestrate the response. Maybe that talks to bedrock, which just talks to a knowledge base that's pulling data from OpenSearch. Maybe it's serverless, maybe it's managed probably serverless. And that Lambda function could potentially do other stuff too, right? So maybe it's calling OpenSearch directly. Maybe it's calling other databases to get contextual information. Maybe it's calling Kendra. I don't know. Whatever it needs to do. Right. Depends on your application. And again, if there is personal or private information involved, even if there isn't, if you just want to control access to this thing because bedrock can get expensive, you might want to integrate Cognito as well. Now these requirements that you have might also involve guardrails. So I'm kind of lumping everything into that bedrock box there. But remember, bedrock is a pretty huge system that can also incorporate guardrails to make sure that people aren't making nasty requests and you're not giving back nasty responses. Also, think about the ingestion side. So how do we get that data into Amazon OpenSearch to begin with? That might also be something you need to architect, right. So one possible pipeline would be that you have data that's getting dumped into S3. Maybe EventBridge finds out when those events happen of new data being either added or modified within S3. That could then in turn talk to a lambda function that goes and talks to bedrock to create embedding vectors for that new information, which then in turn stores that in OpenSearch for use in this larger application. So there's also architecture on the ingestion side as well. All right. Again not a comprehensive look at its potential architectures. And of course there are many many many many ways of doing it. But I just want to kind of get you wrapping your head around how to put these things together into building larger systems. We'll see more of those in the practice exam at the end of the course for sure. But you know, this is just a taste of things to come.
---

*Section 19 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*