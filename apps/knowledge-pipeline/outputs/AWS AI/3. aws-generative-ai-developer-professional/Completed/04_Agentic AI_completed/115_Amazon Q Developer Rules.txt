A quick addendum to Q developer. Let's talk about rules really quick. So rules are basically instructions you can give to Q developer to enforce consistency within your coding standards or coding style or workflow preferences, whatever they might be. So if you want to make sure that you have consistency in the code generated or the designs generated by Q developer across a project, you can create what's called a rules file to make sure that happens. You just write them in plain English in markdown format, and they just need to be placed within your project's Amazon rules directory. And after that, it will automatically be applied to everything that's generated for that project. And it's just textual instructions like, um, you know, use this indentation or, you know, always use this set of libraries or always use this set of tools, whatever it might be. You just write them up, structure it as best you can. It's very much like the claimed file if you're used to using cloud code. Same idea. So if you do want to enforce that sort of consistency across a project, just slap a markdown file in Amazon rules and you can have more than one of them in there. By the way, for different purposes. And it will be automatically applied by Q developer.