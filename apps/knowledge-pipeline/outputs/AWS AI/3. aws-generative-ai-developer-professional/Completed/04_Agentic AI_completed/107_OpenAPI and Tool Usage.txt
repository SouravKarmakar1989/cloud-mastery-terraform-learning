Something else you need to know about is open API. That's not open AI. Open API, formerly known as swagger. Don't know why they changed the name, but they did. And that was originally a specification for defining interfaces between web services. And as you can imagine, that also has applications in defining the interface between foundation models and the tools they're using in an agentic system. So by having this very strict definition of what the interface is with a tool, that helps us use those tools more reliably, right. And just to make it real, here's an example on the right of what that definition might look like for a calculator tool. And I'll let you read through that if you're interested. But it's just a concrete example of what an open API definition actually looks like. So again, its purpose in the context of generative AI and agentic AI is standardizing these function definitions between your foundation model, between your agent and the tools that it's calling. So it can define the input parameters explicitly that it expects to get what the expected output is and what its format is, and any error conditions that might be expected for a tool as well. And this lets your foundation model call those tools in the correct manner and process their results in the correct manner. These may be used with bedrock action groups, which is how bedrock agents call tools. So the way it works is you can upload an open API schema to S3 and refer to it that way, or you can just paste it into the console for bedrock in the UI, in the AWS console. The action group then uses that and refers to it to guide its own tool use. So that strict definition is very important for making sure that it uses those tools in a reliable and consistent manner.