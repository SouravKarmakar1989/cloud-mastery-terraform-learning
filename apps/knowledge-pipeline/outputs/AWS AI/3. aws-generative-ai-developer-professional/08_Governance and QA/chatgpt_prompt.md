<!-- Section 8 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 8 OF 20: Governance and QA
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: Section Intro

Welcome to the section on Governance and Quality Assurance, where we shift from building generative AI systems to ensuring they are safe, reliable, well governed and thoroughly evaluated in real world deployments. Strong governance and continuous QA are just as important as model performance, and in this section you'll learn how to implement both using AWS native tools and best practices. We'll start with Bedrock Prompt Management, a centralized way to control version and audit the prompts your applications depend on. You'll also explore Bedrock agent tracing, which provides visibility into how agents reason, take actions, and interact with tools. Next, we'll dive into evaluation techniques for foundation models, including how to measure accuracy, relevance, consistency, and safety. You'll learn how to evaluate Llms using both manual and automated approaches, and how to apply bedrock model evaluations and other metrics to make data driven decisions. We'll also look at how to evaluate the quality of your Rag pipelines, ensuring your retrieval and augmentation steps deliver the right context at the right time. From there, we'll move into deployment validation systems and responsible AI. Covering how to test your AI systems for drift, hallucination risks and policy compliance before and after going live. The second half of this section focuses on observability. You'll learn how to use CloudWatch metrics, logs, and alarms to monitor model behavior, latency, token usage, and user interactions. We'll explore other AI monitoring tools, and you'll see how CloudTrail helps you track access patterns and maintain Auditability across your AI stack. We'll also cover AWS X-ray for tracing, Foundation model API calls, end to end and lake formation for governing access to the data that powers your models and vector stores. By the end of this section, you'll know how to implement comprehensive governance, monitoring, and QA strategies that keep your generative AI solutions trustworthy, compliant, and production ready. Let's get started.

---

### Lecture: Bedrock Agent Tracing

Like I said, we're going to be talking about governance in QA in this section, governance being the need to keep track of everything you do in your system for auditing and compliance reasons. But that is also useful information for QA and testing things and trying to troubleshoot what might have gone wrong. So that's why I've kind of combined these topics together in this section. It makes more sense than you might think. Let's start by talking about bedrock agent tracing. So whenever you call a bedrock agent the response, unless you've disabled it, will include a trace with detailed information about what went on under the hood. So this is important, right? Because AI agents can do a lot of weird stuff on their own. And it's kind of a black box unless you have this sort of visibility. So what tools did I call? How did I call them? Call them. What responses did I get back? What knowledge bases did I call? How did I call them? What did I get back from the knowledge bases? All that good stuff is in agent tracing. So the agent response will include a trace object. And here's an example of one on the right that it includes a specific kind of trace an orchestration trace. And in this example it's saying well I went off and called the action group to get the current time, and this is what it came back with. So if you want to debug what went wrong, like maybe your response has the wrong time in it. This would allow you to trace it back to that tool potentially and figure out, hey, why did I get the wrong. Time from that tool? So it shows us its reasoning process, if you will. In this structured JSON data, you can deconstruct what exactly went on under the hood here. So again what did it hit? What did it get back? What action groups did it hit? How did it use them? And if there were any errors the details of those errors will be here as well. There are different trace types that you can get back. So there's pre-processing trace types. That's when it's categorizing the user input and trying to figure out what to do with it. Orchestration like this one here when it's calling an action group or a knowledge base. And that's going to be, you know, usually the most useful one you get because that's where things can tend to go wrong. Also, post-processing traces, where it collects all the information back and tries to construct a final response. There are also custom orchestration types and that will determine the order of actions. So if it's trying to figure out in what order to do things, that could be a custom orchestration trace. There's also a routing classifier trace, which details exactly what it sounds like, how a classified this particular task at hand, and how it routed the request accordingly. If something failed, you'll get back a failure trace. And if you have any guardrails in your system, there will also be guardrail traces detailing what exactly those guardrails filtered or did not filter as a result of your prompt input or output.

---

### Lecture: Evaluation Techniques for Foundation Models

So you got this fancy foundation model or a model that you fine tuned. How do you know if it's any good? How do you measure it? Turns out it's a hard problem. You know, when you have a model whose job is to just answer arbitrary questions using plain language? How do you know if it's good or not? There's no numbers that you can evaluate it against. It's not really straightforward to say. Was this an accurate result or not? Um, it's a problem. You know, people have trouble measuring the quality of these llms, and often they make the mistake of measuring them based on how they respond to questions that are already baked into their training data. Remember, don't test systems on the data that you train them with. That's cheating, right? So measuring these systems is really, really hard. And getting good metrics about which models are better than others is also very, very hard. One way to cheat is to just use humans to evaluate it. You know, use subjective judgments from real human beings as opposed to even trying to use an algorithm. Right. Um, so you can ask humans just to rate these things, how does it feel to use this thing from a user experience standpoint? How do you feel the relevancy or sensitivity of this response was to what you were asking for? You know, if you did, you feel like you got a good answer to what you asked for. And that can be dangerous too, because Llms can fool you. They can sound very authoritative about what information they're giving you, but it could still be completely wrong. Right. So even these human metrics can be incorrect as well. You can measure how creative and flexible is the model. So if I ask it to do something unexpected or something complex that wasn't baked into its training data at all, how does it deal with that? Does it just make stuff up? Um, does it say, I don't know, does it actually construct a useful response? You know, you can evaluate it based on that as well. But again, you're depending on the human's ability to differentiate, um, the model lying to you, uh, from what it should have said. Right. Another thing you can do is evaluate a model against benchmark data sets. So the idea here is that you have a set of sample questions that are used as prompts and answers that are generated by subject matter experts out there, actual human beings who know what the correct answer is to those prompts. So that can be used to test the performance of a specific application or a model, or testing the performance for a specific kinds of problems, like reasoning ability. So if you have a test set of prompts that require some level of reasoning, you know, breaking a problem down into smaller problems and putting them together. You could use a benchmark data set to provide a set of test prompts that require reasoning to solve. Compare the responses against the ideal responses that actually applied reasoning to the answer, and see how close they are, and just measure how close the generated responses from your model are to the sample ideal evaluation responses that we're providing. So that's what's going on in this diagram to the right here we have a set of test prompts that are fed into our model that generates responses. We then have an evaluator that compares those against ideal responses that are provided as evaluation responses. And it can generate a set of scores as to how similar the generated responses are to the so-called ideal responses. So this can at least be useful as a way to get initial metrics for before you get human feedback, you know, a way of measuring it offline to sort of a as an initial guide as to whether you're doing better or not. Often you see these sorts of things used for foundation model leaderboards. There's a lot of people out there that publish these charts as to which models are the best, and they use metrics like this. Often things you can measure this way include Accuracy. How close was the generated result to the ideal one? Also speed and efficiency. How quickly did it get to that result? Scalability. How well is it holding up to lots of data? More users? Longer prompts? More context? How big of a prompt can it handle? And also you can measure its ability to retrieve context. We did touch on this already with retrieval augmented generation. So if your system depends on retrieving external data, how relevant is that retrieve data to the actual question at hand. So there's a whole set of ways to measure retrieval augmented generation that get into that as well. However this is only as good as your test and evaluation data is. So these metrics can be flawed. They can be incomplete. You know, you didn't think of everything. And an even more important problem here is that often those test prompts are baked into the training data of these models. So if these test prompts and evaluation responses leak their way into the training data, maybe somebody published a blog post around it and that blog got ingested by the model. Well, now all of a sudden it has an open book exam, right? So it already knows what the test prompts and ideal responses are. And it's training data. And that's no longer a valid way to measure the actual performance of this model at that point. Huge, huge problem. Like, you see a lot of coding models out there that are tested on their ability to solve coding exercises for job interviews. They call that leetcode, right? Um, if it already knows the answers to those, what are you really testing there? You're just testing whether or not that was in their training data. That's that's not a good metric. Another way of evaluating stuff is by using another model as a judge. So let's say you have a model that you do trust that you know, produces good results compared to the one that you have. Uh, you could use that larger model, that more tuned model to judge the performance of your model. So in this architecture here again, we have a set of test prompts that are being fed to your model to generate responses. And again we have a set of evaluation data that has the ideal, uh, answers to those prompts or maybe contextual information that we looked up externally that's relevant to those prompts. I can feed those both into a judge model and ask that judge model to say, okay, I think that you did a good job or not, and that can produce scores for relevancy or comprehensiveness or accuracy or whatever. So, um, the evaluator here is just an actual trusted large language model that you use for evaluation purposes. Now, this does sort of depend on the assumption that the judge model is quote unquote better than your model. So this is more appropriate in situations where you're testing a small language model, something that's more focused, right, or something where your intention was to be smaller and more efficient, and you're trying to approximate the behavior of this larger model with as few resources as you can. So that might be an appropriate use of this technique. Why not do both? You know, you can do hybrid approaches where you compare both human feedback and also benchmark based approaches to reveal the limitations of both approaches. Right. So if human beings are saying this model is great, and my actual metrics based on evaluation compared to a set of test prompts and responses says not so much. Maybe that means that my human responses aren't really all that useful. You know, like I said, humans can be fooled pretty easily. So doing both in a hybrid approach where you're using human feedback as well as this algorithmic feedback together can help you expose problems when they don't agree with each other. And you can then dig into, well, which one is wrong and what is that telling me about this model?

---

### Lecture: Evaluating LLM's with ROUGE, BLUE, and BERT scores

Let's talk about some specific metrics that you might use to measure a large language model and its responses. So one is called rouge and that is a set of metrics for text summarization or machine translation. And its job is to count the number of overlapping units between the computer generated output, the output coming out of your model, and some evaluation ground truth output. So remember, you know, one way of evaluating these models is to take its output and compare it to some sort of set of ideal responses to your prompts. So how do we do that comparison Rouge is one such metric. What do you mean by unit? That can be a word. It can be an n gram. We'll talk about that in a minute. Or sentence fragments however you want to slice it up. So how similar is that generated result to the ideal result in terms of words or n grams or fragments of sentences? One specific type of rouge is rouge n, and that's measuring the level of overlap on n grams, which is n is just how many words at a time am I comparing to each other? I'll give you an example in a second here. So root one would be based on the overlap between unigrams, which are just one word at a time. Rouge two would be based on overlap of bigrams, which are two words at a time. And then there's rouge dash L where instead of some arbitrary number, we're choosing the longest common subsequence between the generated and the reference text. So whatever that works out to be. So that particular metric is good at evaluating coherence or the order of narrative. So it actually cares about the order of the words in the response. And that's pretty good at capturing sort of the grammar or the meaning of what you're trying to say. Right. So let's look at an example. Like if you've never heard of N-grams before, let's say the evaluation response to a given prompt is certified AI practitioner. Okay. So I can break that phrase up into unigrams where a unigram is one word. So there are three possible unigrams in that phrase certified AI and practitioner. Or I could split it up into bigrams. Bigrams are just sets of two words that are together, so there are only two bigrams in that phrase certified AI and AI practitioner. So Rouge will look at the overlap between the generated output and that ideal output in my evaluation data based on either unigrams or bigrams or whatever level of n gram I want. So if my model outputs certified ML practitioner instead of certified AI practitioner rouge, one would give that a score of two out of three, because two out of those three Unigrams certified in practitioner are common between the ideal response and the generated response, ML was not. AI was not. Rouge. Two, however, would give this a score of zero because certified AI and AI practitioner does not overlap at all with certified ML or ML practitioner. So based on the Rouge one metric here, I'm doing all right. But Rouge two says no, this is terrible. And well, it kind of is terrible because, well, these are two different exams, right? So that's an example of rouge. Another metric is blue or blue. I'm not sure how to pronounce that in French. Um, that's used more for machine translation. So the idea here is to compare the machine translation to a human translation that's considered, again, the ground truth. This works kind of similarly to Rouge in that it's also using n grams to do this evaluation. The difference is that blue is more about precision as opposed to recall, and we'll get into that in more detail shortly. But at a high level, precision just means we're comparing against everything, whereas recall means you're just comparing amongst relevant results. So remember Rouge was only looking at overlapping results between these two data sets. Blue looks at everything, so it's just going to check how many words or phrases in my translation appear in my reference translation at all. You know, it's not quite that specific. This works at the sentence by sentence level, by the way. And one other fine point here is that it has a brevity penalty as well built into it. So if your translation is overly short, it will be penalized for that. So you know, can't cheat that way. Now, this does have limited use in assessing fluency and grammar since we're going for precision over relevancy. So that is a a trade off to keep in mind there with blue versus rouge. Another metric is Bert score. And the idea here is that we're relying on those embedding vectors we talked about earlier. So remember we can train a model to create these mathematical vectors associated with words or phrases that kind of embed their underlying meaning. I'm oversimplifying, but that's the general idea, right? So Bert score is just using those embedding vectors to compare the semantic similarity between your output and the ideal output. Now, Bert is just a language model that came before all the current wave of large language models. It's kind of interesting because it works kind of backwards from the way these other models work, but that's way too much detail for this exam. Anyway, the the fundamental thing you need to know, though, is that Bert also depends on embeddings. So that's where these embedding vectors are coming from. And because it's using embeddings and not actual words, it's going to be less sensitive to things like synonyms or people that are paraphrasing things. Because we're really trying to compare the underlying meaning of what's being said as opposed to these specific words or phrases. So this can be a very good way of measuring the quality of your results without depending on specific wording. So that's a more popular metric I would say. So that's three ways of measuring your LLM rouge blue and Bert score.

---

### Lecture: Amazon Bedrock Model Evaluations

We talked about bedrock model evaluations a little bit in the past. Let's go into a little bit more depth on it because it's a pretty cool feature. It's a way to have a very robust measurement and evaluation system on top of your generative AI applications. And that's not a trivial problem, because a lot of this stuff is a little bit subjective. Some stuff, though, is not subjective, and that's what automatic model evaluations are all about. So there is a large list seen here on the right of built in task types that it can measure for you automatically using built in data sets to evaluate them with and some built in metrics. So you can see that we have general text classification where it can measure accuracy, robustness and toxicity using some standard built in data sets for measuring those tasks. And you get back specific metrics associated with that the real world knowledge score, world error rate, toxicity. These are all things you could imagine on a dashboard, right? It can also measure text summarization performance again using the in this case the giga word data set to measure that capability. And it will give you back specific metrics on Bert score and toxicity. It can measure Q&amp;A response question and answer, and for that it can measure accuracy, robustness and toxicity as well. How well does it accurately answer questions given a built in data set of questions and ground truth answers? And it can also measure your model's ability for text classification, measuring both the accuracy and robustness of the resulting classifications, in this case using a built in dataset of women's e-commerce clothing reviews. But you could provide your own data sets too, if you so desire. There are also human based evaluations you can set up as well, which is interesting. So if you do have something truly subjective that only a human can evaluate, you can set up a human based evaluation system within bedrock model evaluations. So this allows you to take two models or variants of the same model and measure them against each other, pit them against each other, with humans judging which ones. The best response. You've probably seen this if you use ChatGPT once in a while, it shows you two different responses and asks you to pick the one you like the best. That's what's going on here. Same kind of system. Now, to do that under the hood, you need to start off by setting up either a work team incognito. If you want to manage your own humans to do this evaluation, or you can use the teams that are set up through SageMaker Ground Truth or Amazon augmented AI. That's also an option for you. Also, we've talked about this in the past as well, but it supports LM as a judge. Models. So instead of using humans, you can use a separate model to evaluate the other model. So you have an evaluator model whose job is just to be the judge. And then a generator model that you're judging. Right. So you need to provide a prompt data set for using this. and that will be sent off into your own system that does retrieval, augmented generation or whatever else it does. Then your evaluator model will measure how relevant the response was to the original prompt. And we've gone through this architecture before, but just for review purposes, here it is again on the right. And there are many metrics available for this. We've talked about that before too. I'm not going to repeat myself. You also have Rag evaluation jobs as well. And these can either be retrieve only evaluation jobs where we're just focusing on the quality of retrieval from our vector store or knowledge base, whatever it is. And that will come back and give you metrics regarding the relevance and coverage of the context you're retrieving for a given prompt. You can also evaluate, retrieve, and generate and measure the performance of your Rag system from end to end, and that will include correctness, completeness, helpfulness, logical coherence, and all the other metrics we talked about before. Now, to use Rag evaluation jobs you need to provide a prompt data set. It needs to go into S3 specifically in JSON format. So that's what your prompt data set actually is physically. You can also add in your own metrics if you want to as well. And the way you do that is just giving it a prompt. And the judge model will use that prompt to compute this new metric that you've described and add that to your results as well. So you can extend this as much as you want to.

---

### Lecture: Deployment Validation Systems

So if you're using some sort of continuous integration and continuous deployment system, you'll have the need to validate your deployments to make sure that you don't need to roll them back or, you know, make sure it's safe to actually roll that out at a larger scale. One tool for that is synthetic user workflows. So the ability to actually simulate how a user will use your system from end to end. Now, unit testing probably isn't good enough for generative AI applications because it's not deterministic. So it's very hard to say this is the expected output of this foundation model because it's going to change. It has a temperature. And even if you change the temperature to be very low, the model itself changes pretty often too. So it's very difficult to have a test framework where you're saying this is the input, this is the expected output. It's not that simple. With generative AI, however, you can set up these systems that simulate the end to end usage of your application and see that it's actually at least behaving correctly before and after your deployments and then in an ongoing manner. Some useful tools for setting up such a system would be CloudWatch Synthetic Monitoring or Canaries. You can also roll your own using step functions to manage the actual flow of events. EventBridge to kick off these workflows, and Lambda to execute any actual logic that happens at each one of these steps in the workflow. When you have that data collected, you got to put it somewhere and analyze it. You might use S3 paired with Athena to analyze that data and visually look at it through. QuickSight is one potential chain of services there. You also have some output validation problems that are specific to AI. So things you might want to measure as part of your deployment validation is is my hallucination rate going through the roof or not? And if you can measure your responses against some sort of ground truth data store, you can actually measure that hallucination rate and make sure it didn't go off the rails. Semantic drift, faithfulness, compliance. We all talked about these in our previous slides about evaluating foundation models, but these are all things that can be baked into your deployment validation process to bedrock model evaluations. Does all of that for you? Of course. And if you really wanted to roll your own, you could write your own logic for this, using lambda and maybe step functions to orchestrate the whole process of generating those metrics as it goes through your system. And like I said, it's non-deterministic. So it's very hard to say this is the expected output. The best you can probably do is just making sure that the style is consistent, maybe by using another judge model to figure out if that's true or not. Are the responses you're getting at least similar based on a test prompt data set? They might not be the same, but you can maybe measure the semantic similarity of those responses and make sure they're within some sort of a threshold. So there'll be some accepted range you'll have to tune to not get a whole lot of false positives, but that is one approach to validating the consistency and the deployment as a whole, given a world of non-deterministic responses.

---

### Lecture: Principles of Responsible AI

One of the areas covered by the exam is machine learning system architecture, and specifically the best practices that Amazon has defined for designing your machine learning systems. So I didn't make this up. This is straight from Amazon, so pay attention. One thing to consider when designing a system is responsible AI, and these are the core dimensions Amazon defines for the responsible development of AI applications. And you know, good on them for thinking about this. You know there's a lot of people using AI, perhaps irresponsibly. So here are some things to think about to make sure that you're using this technology for the power of good. You want to consider the fairness of the application that you're developing. You know, is it going to be biased or not? In some unintentional way? It's explainability can be important for responsible AI. Can you explain why it made its output? Why did it say something you know, can you go back and figure out why it said something wrong and fix that if it's not explainable, you can't. Privacy and security. Are you being careful not to let it be trained on private, personally identifiable information or sensitive information? Is your AI system potentially a conduit for leaking that information out to others unintentionally? You want to make sure it doesn't even get in there, and you have guardrails in place to make sure it's master removed if it does slip through. Safety, controllability and veracity and robustness. Um, safety is kind of a broad term, right? You know, it just means, well, do people feel safe when they're using this system? Um, is it going to explode in unintentional ways? Kind of a broad, uh, meta concern there. Controllability. Do you have any control over the output of this thing? Again, that ties back to explainability in terms of being able to understand what's going on. You can't really control it if you don't understand it. But do you have knobs and dials at your disposal to make sure you can control the behavior of this system if you do need to tune it or change it later on. Veracity and robustness, of course. Does it tell the truth? Um, you know, generative AI is prone to hallucinations, they call it these days. And, well, you want to be sure you can measure that and you have a handle on just how truthful your system is and whether that's within your acceptable limits for your application. Governance. Um, how do I know what this thing is doing? How do I keep tabs on it? How do I communicate what it's doing to others? How do I make sure that I'm in compliance with any relevant regulations? Different countries have different laws about AI, and you want to make sure you're in compliance with all of them. Transparency sounds related to explainability, but really it's more about just being transparent about what your system does. So are you publishing the limitations of your model, the capabilities of your model, the shortcomings that it may have? You know, you want to be transparent about what your model can and cannot do. There are some tools available to enforce these within AWS, so this is tying it back to AWS and some more technical considerations. Amazon Bedrock, which is the bedrock of Amazon's generative AI capabilities, has model evaluation tools built into it. So using bedrock, if you were to develop a LM agent or a image generation model or anything else on top of the suite of foundation models that are offered by Amazon Bedrock, they have tools for evaluating the accuracy of that, or for throwing a set of test prompts at it and evaluating if their responses match the ideal responses or not. So they do have mechanisms for evaluating even non-deterministic LM models. SageMaker clarify is also a tool in your arsenal for responsible AI. SageMaker clarify can automatically detect bias in your data sets that are being used to train it and also in the output. So we can say, okay, for this feature, is there some imbalance in that data that we need to be aware of and potentially address and fix? You also have tools within SageMaker to fix those biases if they do exist, by rebalancing the data that you have or maybe even introducing new data. It also has model evaluation capabilities. So it has the ability to evaluate your model not just as a one off, but also continuously as it's being run. And feed that information back into SageMaker maybe to tune your model continuously. Explainability. It also has the ability to look at what happens if you remove a given feature and what impact that has on the output. So it can say, okay, the output is actually being driven primarily by these features in your training data. So you can say, you know, if I if I'm feeding it information about, I don't know, age and um, what else, you know, income, where you live, things like that. And you're trying to create a model that can predict something like whether this person should be approved for a loan. You can go back and use SageMaker clarifies explainability features to determine which of those features is most contributing to that final decision, and that can also help you with your feature selection, your, uh, your feature engineering later on. SageMaker Model Monitor is another tool at your disposal that allows you to automatically get alerts for inaccurate responses so you can keep an eye on your model once it's been deployed, and get an alarm if it's doing naughty things. Amazon augmented AI is also a tool that can be used for responsible AI that allows you to insert humans in the loop. So if you want to have human beings that are double checking these results before they go out, or maybe even involving them in the training process, things like reinforcement learning from human feedback is a popular technique for training your model. On which of two responses is better, and fine tuning your model in that way. So oftentimes there's a lot more human involvement in training these models than you might think. There's not as much A and AI as you might think. Uh, however, you know, I should point out that, um, this too can be irresponsible. You know, keep in mind that if you are leveraging a team of people in a developing country, um, well, maybe that's not responsible. You might be exploiting them, right? Amazon augmented AI, however, allows you to bring your own team if you want to. So you have complete control over who's doing that work. Or you can use their own AWS managed teams as well. And there are some general tools for SageMaker for ML governance. SageMaker ML governance is not the name of a service. It's just a heading for this set of features here. One thing is SageMaker role manager. So that allows you to define roles for groups of people that are using SageMaker and define their permissions for what they can and cannot do. There's also model cards where you can define high level information about a model that you've developed using SageMaker, and publish that that gets back to transparency, right. So a screenshot of that is over here on the right, where you can start off by just documenting what the intended use of this model is, what its risks are, what the risks might be, you know, explain that. Just general information about what the limitations and risks of this model might be. And again, just publishing that in a way that's easily consumable. They also offer a model dashboard that gives you quick access to all of the models you've been using within SageMaker, and you can dig into those further from there.

---

### Lecture: CloudWatch Logs

So, now let's talk about CloudWatch Logs. So CloudWatch Logs is the perfect place to store your application logs in AWS. And to do so, you must first define log groups. They're whatever names you want, but usually they are representing one of your applications. Then within a log group, you will have multiple log streams, and they represent log instances within an application or specific log files or specific containers that you have as part of a cluster. Then you define your log expiration policy. So you can have the logs re being retained indefinitely to never expire, or you can choose to expire them from anywhere between one day to 10 years. It's also possible to send your CloudWatch Logs into various destinations. For example, to export them in batch into Amazon S3 or to stream them into Kinesis Data Streams, Kinesis Data Firehose, AWS Lambda, Amazon OpenSearch. All the logs are encrypted by default, and you can set up your own KMS-based encryption with your own keys if you wanted to. So, now what kind of logs data goes into CloudWatch Logs? Now, what types of logs can go into CloudWatch Logs? Well, we can send the logs using the SDK or the CloudWatch Logs Agent or the CloudWatch Unified Agent. Now, the CloudWatch Unified Agents send logs to CloudWatch and so the CloudWatch Log Agent is now sort of deprecated. You have Elastic Beanstalk, which is used to collect logs from the application directly into CloudWatch. ECS will send the logs directly from the containers into CloudWatch. Lambda will send logs from the functions themselves. VPC Flow Logs will send logs specific to your VPC metadata network traffic. API Gateway will send all the requests made to the API Gateway into CloudWatch Logs. CloudTrail, you can send the logs directly based on the filter. And Route53 will log all the DNS queries made to its service. So, what if you wanted to query the logs in CloudWatch Logs? For this, you can use CloudWatch Logs Insights. So, it's a querying capability within CloudWatch Logs which allows you to write your query. You specify the timeframe you want to apply your query to and then automatically you're going to get a result as a visualization. And also, you can view the specific log lines that's made this visualization. This visualization can also be exported either as a result or added to a dashboard for being able to rerun it whenever you want. So this is very handy, and this will allow you to search and analyze log data within CloudWatch Logs. So there are lots of simple queries provided as part of the console for CloudWatch Logs Insights. For example, you can find the most 25 most recent events, or you can have a look at how many events had exceptions or errors in your logs, or you can look for a specific IP and so on. So, it provides a purpose-built query language. All the fields to allow you to build your queries are automatically detected from CloudWatch Logs, and then you can filter based on conditions. You can calculate aggregate statistics, you can sort events, limit the number of events, and so on. So as I said, you can save the queries and also add them to CloudWatch Dashboards. And you have the capability to query multiple log groups at a time, even if they are in different accounts. So remember, CloudWatch Logs Insights is a query engine, not a real-time engine. And so as such, it will only query historical data when you run the query. So as mentioned, CloudWatch Logs can be exported into several destinations. The first one is Amazon S3. So this is for a batch export to send all your logs into Amazon S3, and this export can take up to 12 hours to be completed. The API call to initiate this export is called CreateExportTask. So because this is a batch export, this is not real-time or near real-time. Instead, you must use the CloudWatch Logs subscription. So these allow you to get a real-time stream of these log events, and you can do processing and analysis. So, you can send this data into multiple places such as Kinesis Data Streams, Kinesis Data Firehose, or Lambda. And you can specify a subscription filter to say which kind of log events you want to be delivered to your destination. So, the subscription filter can send data into Kinesis Data Streams. This would be a great choice if you wanted to use, for example, the integration with Kinesis Data Firehose, Kinesis Data Analytics, or Amazon EC2, or Lambda. You can also directly send it into Kinesis Data Firehose. From there, you can send it in near real-time fashion into Amazon S3, or for example, the OpenSearch Service, or you have Lambda. So you can write your own custom Lambda function, or you can use a managed Lambda function that is sending data in real-time into the OpenSearch Service. On top of it, thanks to these subscription filters, it is possible for you to aggregate data from different CloudWatch Logs into different accounts and different regions into a common destination such as the Kinesis Data Stream in one specific accounts. And then Kinesis Data Firehose. And then in near real-time into Amazon S3. So that is very possible, and that is a way for you to perform log aggregation. So the nitty gritty of how this works is that you must use what's called destinations. So, let's say you have a sender account and the recipient accounts. So you create a CloudWatch Log subscription filter, and then this gets sent into a subscription destination, which is a virtual representant of the Kinesis Data Stream in the recipient accounts. Then you attach a destination access policy to allow the first account to actually send data into this destination. Then you create an IAM role in the recipient account, which has the permission to send records into your Kinesis Data Stream, and you make sure that this role can be assumed by the first account. And when you have all these things in place, then it is possible for you to send data from CloudWatch Logs in one account into a destination in another account. So that's it for this lecture. I hope you liked it, and I will see you in the next lecture.

---

### Lecture: CloudWatch Logs - Hands On

Okay, So I am in CloudWatch logs and we can see all the log groups we have right now. So as you can see, we have eight of them and they were created by some services. For example, this one was created by Lambda. This one was created by datasync. This one was created by glue and this one was created by us when we did do an SSM runCommand and we wanted the output to be populated in this log group. So, if we take a look at this example, for example, we have six log streams and so each of them represents a different instance that we did run a specific run command on. So, this is the same runcommond ID across the six. Here, we have a different instance ID for each of the six, so two and two and then we have, stdout and stderr. So if you look at stdout, we can look at all the logs that was generated by this command and we can have a look at all the log lines and so on. So this is quite (indistinct). And the idea is that, from the log for example, you can look through the keyword http and it would show you all the log lines that contain the word htp. If you just look for the word installing, for example, it will show you just maybe two or three log lines that contain the word installing. So that's certainly (indistinct). And so we have, for stdout, stderr, so we can see really the idea behind different log streams. Now, we can create metric filters in here, and these metric filters is a way for us to find a filter pattern. For example, installing. Okay, And then we need to select for example, a custom data, for example, this log stream and then we test a pattern and it's going to give us three matches out of five in the simple logs. Now, if you went ahead with entering this filter name, as we can see, it could call it DemoFilter and DemometricFilter. And this is a new namespace, okay. And here is DemoMetric. So this is DemoMetric filter namespace, and this is a DemometricFilter. And then, the metric value, okay. When there is a filter pattern or matching occur and so, you can say one for example, to add one and to count how many times this installing lines have been filmed. And the default value and the unit if you want it to, then click on next, create and this will give you a new metrics so, if you went into CloudWatch metric right here and we're going to clear this graph and we're going to find a new metrics. So let's refresh this page. Maybe this is going to help us. Okay, so if we go to all new spaces, as soon as this metric filter would appear, it would appear right here and we could visualize it. But currently, because we don't send any log output, then we don't see it. But the idea is that, we could create an alarm on top of this metric filter So we can click on create alarm. and this would allow us to create an alarm in case, for example, a metric went over a specific value and again, this metric is calculated based on the filter from the log streams. We could also create subscription filters. So as you can see here, we can create a filter for different outcomes. So Elasticsearch, Kinesis, datastreams, Kinesis Firehose or a Lambda subscription filter if you want to send data into custom lambda functions. And, we can create up to two subscription filters per log group according to this, okay. Now, we can also edit the retention settings. So, we can see that the logs can never expire all the way up to 120 months. Okay, so 10 years. And then, we can also export the data into Amazon S3. So you can click on export data You can choose a range of data to export and then, the stream prefix, if you wanted to just get specific log streams, and then the S3 buckets and the bucket prefix, and you'd be good to go. And the finally, in here, you can create a log group (indistinct) demo-log-group. Okay, you can set up the retention settings. KMS key, if you wanted to encrypt that log group and then click on create. And so, the encryption setting would appear then here, if a KMS key ID was specified. Okay and then finally, CloudWatch Logs Insights, is allowing you to use a nice query language to query some specific log groups. So for example, we can create this one and run the query. And then, this is not going to give us any data because we're looking for data from the past hour. But if we look at data from the past 60 days and run this query, maybe we'll find something. So you can see, we found 18 records from this query. And so, this gives us a nice query language to start gaining some insights on top of our logs. And on top of it, you can export the results if you want it to. And on the right hand side, you can see that you can save your queries. Okay? So you can query and save them here. Or, you can look at some simple queries and view the use cases of losing log insights for example, view the latency statistics for a five minute interval on Lambda, or get the top 10 by transfers by source and destination IP addresses for VPC flow logs. So it gives you, for example, if you should click on these, some nice insights to how the query language works for CloudWatch logs insights. So this is CloudWatch logs. I hope you liked it and I will see you in the next lecture.

---

### Lecture: CloudWatch Alarms

Now let's discuss CloudWatch Alarms. So alarms, as we know, they're used to trigger notifications from any metric. And you can define complex alarms and on various options such as sampling or doing percentage or maximum, and so on. Alarm has three states, OK, it means that it's not triggered, INSUFFICIENT_DATA means that there's not enough data for the alarm to determine a state and ALARM, which is that your threshold has been breached and therefore a notification will be sent. The period is how long you want the alarm to evaluate for on the metric. And so it could be very, very short, very, very long. And it can apply also to high resolution customer metrics. For example, 10 seconds, 30 seconds, or multiple of 60 seconds. Now, alarms have three main targets. The first one is actions on EC2 instances such as stopping it, terminating it, rebooting it, or recovering any instance. The second one is to trigger an auto-scaling action. For example, a scale out or a scale in. And the last one is to send a notification to the SNS service, for example, and from the SNS service, we can hook it to a Lambda function and have the Lambda function do pretty much anything we want based on an alarm being breached. So now let's talk about Composite Alarms, because we know that CloudWatch Alarms are on a single metric, but then if you wanted to have multiple metrics, you would need to use Composite Alarms. So because composite alarms are actually monitoring the states of multiple other alarms, and these alarms can be each relying on one different metric. So the Composite Alarm is the action of combining all these other alarms together, and you can use AND or OR conditions to be able to be very flexible in terms of the condition you're checking for. So it's very helpful to reduce alarm noise because you can create complex Composite Alarms and saying, for example, if the CPU is high and the network is high, then don't alert me because I only wanna know when the CPU is high and the network is a low, these kind of things. So let's take an example, we have an EC2 instance, and we're going to create a composite alarm on top of it. So therefore we create a first underlying alarm called the Alarm A, which is going to monitor the CPU of the EC2 instance. Then you create Alarm B, which is going to monitor the IOPS of the EC2 instance, and then the composite alarm is defined as the junction of Alarm A and Alarm B. And therefore, if Alarm A is in alarm and Alarm B is in alarm, and this is something we have to define ourselves, then the Composite Alarm itself will be an alarm and can trigger, for example, an SNS notification. So as you can see, you can get quite creative with the Composite Alarms. So let's talk about EC2 instance recovery. So we have the status checks and we have the instance status check, which is going to check the EC2 virtual machine. And we have the system status check, which is going to check the underlying layer hardware of our EC2 instance. And finally, the attached EBS status check, which is going to check any attached EBS volumes health. And you can define a CloudWatch Alarm on these checks, okay? So you'll monitor a specific EC2 instance, and in case the alarm is being breach, then you can start an EC2 instance recovery to make sure, for example, that you move your EC2 instance from one host to another. When you do a recovery, you get the same private, public, and elastic IP, the same metadata and the same placement group for your instance. And you can also send an alert to your SNS topic to get alerted that the EC2 instance was being recovered. Now, the cloud alarm has some good stuff. To know that, first of all is that as we've seen, we can create an alarm on top of a CloudWatch logs metric filter. So remember, the CloudWatch logs are having a metric filter, which is hooked to CloudWatch Alarm. And then when we receive too many instances of a specific word, for example, the word error, then do an alert and send a message into Amazon SNS. And so if you wanted to test alarm and notifications, you can use a CLI call called set alarm states. And this is helpful when you want to trigger an alarm, even though it didn't reach a specific threshold because you wanted to see whether or not the alarm being triggered results in the correct action for your infrastructure. So that's it for alarms, I hope you liked it, and I will see you in the next lecture for some practice.

---

### Lecture: CloudWatch Alarms - Hands On

Okay, so we are in CloudWatch Alarms, so let's go and create an alarm. So first of all, I'm going to create an EC2 instance very quickly, and we're going to create an alarm on top of the CPU utilization. So we're going ahead and create an EC2 instance of t2 micro. I will go quickly to preview and launch and then say, yes, I have this. So we don't need to keep it or anything like this, we just want the instance to be launched. And the idea is that we want to create an alarm that will terminate the instance, if the CPU goes to 100%, so let's create this alarm. So we need to select a metric. And so for this, we need to choose a metric. So we need to go and find our EC2 instance. So this is our instance ID, right here, and search for it. And I'm maybe a little bit too quick, okay. So let's wait for the instance to be launched. I will go into EC2 per instance metric, and then I will wait for it to be populating. So it took about five minutes for some metrics to appear in that cloud dashboard for my instance. So now I can probably refresh this page and I will get the chance to find the metrics I'm looking for. So let me select a metric and then paste the instance in perfect, I have it, and I will go and find the CPU utilization of my instance. So this is this metric right here. Okay, well, select this metric. As you can see, we have the eight here and then we can choose a way to compute this metric, so average, sum, maximum, and so on. The period we want to evaluate this alarm on. So five minutes is good because this metric gets populated every five minutes, if we don't enable detailed monitoring. Now we get some conditions in terms of the threshold. So is it Static or is it an Anomaly detection? Is it a greater than, equal than, and so on. So I will say for example, if you're a greater than 95% for a long time, so for, and here you can say, three out of three. So that means that for 15 minutes, you're stuck at 95%, then probably something is up with this machine. And so in that case, I could choose a notification. I could choose a Auto Scaling action. I could choose an EC2 action, or in a systems manager action, but I'm going to choose an EC2 action. Okay, and it will say, hey, if you are in alarm, okay, then just terminate this instance because maybe I know that my application sometimes has a huge failure and the CPU installation will be at a 95% or a 100% for a very long time, And the only way to resolve this is to just terminate the instance. So I will choose this, and then click on next, and say terminate, EC2 on high CPU. Click on next to verify everything, and we're good to go. So now this alarm obviously does have insufficient data, so we need to wait 15 minutes for it to be okay, and it's not going to be triggered unless we make it so. So, we could go into the EC2 instance and launch a way to get the CPU very high for 15 minutes, but this would be a very, very long, or we can use the API call name, Set alarm state, to really see what would happen if this alarm went into the breach phase. So let's have a look. This is the history of the alarm, okay. And what I'm going to do is that I'm going to set the alarm to say, so I type AWS CloudWatch, set alarm states, and we'll look at the API reference. And so we need to do set alarm state the alarm name and the state value and the state reason. So we'll do, in here. So AWS CloudWatch set alarm states, and then we need to set multiple parameters. So the alarm name is going to be this one, then the alarm, so the states value is going to be alarm. And the state reason, is going to be testing. We press enter, and now this alarm, if we refer to this page is now in the alarm state, as you can see, it says in alarm. And so the action is, when in alarm terminate the instance. And so if you look at the history, it says that the alarm updated from OK, to in alarm and then an action was made and it's, successfully executed the action to terminate my EC2 instance. So if I go into my EC2 instances here, and I refresh, as we can see is shutting down and is being terminated because while there was an alarm that was triggered on top of this EC2 instance, and we did set up the alarm to do this specific action. So that's it, I hope you liked it. I hope this makes sense to you. And I will see you in the next lecture.

---

### Lecture: CloudWatch RUM

Something that isn't explicitly related to generative AI, but does come up in the exam guide, so you at least need to know what it is. Is CloudWatch Real User monitoring or Rum for short? Hmm. So this is for testing mobile application performance. So it can be either iOS or Android that's supported. So they give you a little bit of a snippet of code you can include inside of your mobile app that integrates it with CloudWatch rum. And this will allow you to visualize and measure page load times errors. Application launch times, things like that that are specific to mobile apps, and the responsiveness of those mobile apps. And this is done from real user sessions. So it's not, you know, being synthetically generated. We're actually firing up this app on end user systems on their phones and measuring the actual performance that they're seeing from the end user standpoint. This integrates with something called application signals in CloudWatch, and it allows you to view the results using x ray traces. So that is what CloudWatch rum is all about. It's only relevant to generative AI if you have a mobile application for your generative AI thing, and you want to measure its end to end performance, so that might be a tool for doing so.

---

### Lecture: CloudWatch and GenAI Monitoring

So you've learned about CloudWatch at a high general level. Let me tie this in real quickly to generative AI and how this might fit in with your AI applications. So obviously you can use this to test prop regression if you're logging your data in CloudWatch. Maybe that's the source of your data for figuring out, um, how are my responses differing over time? How are my prompts changing over time? These are all ways to measure what's actually going on, which is the first step toward testing it, right? Those CloudWatch logs contain valuable information. The prompt inputs, the model responses, and it's going to be foundational to any monitoring and troubleshooting that you might be doing in generative AI applications, or any applications for that matter. But the specific things that it would be storing that you're interested in are prompt inputs and model responses. There are also some AI specific key performance indicators you might be able to track there as well, like prompt effectiveness, response quality, latency, end to end, and error rates coming back from your system as well. These are all things you might want to put on a dashboard somewhere and a little bit unrelated, but I just want to throw it out there. There is some talk of a feature called CloudWatch, evidently as an a b testing framework that comes up from time to time in the AWS preparation materials for this exam. That is no longer a thing that's been deprecated, so I would not expect to see CloudWatch evidently presented as an a b testing framework for you. There are other tools you can use for that. For that. For example, SageMaker deployments has that feature baked in. Other things you might want to keep track of. And these may or may not be in CloudWatch, but this seems like as good a place to put this as anywhere. Foundation model interaction tracing. So if you want to store those logs that include the traces of your AI Agentic AI systems and that detailed information about how they worked under the hood, you might want to keep track of that business impact metrics. You know, what's the actual impact this system is having on your business? Is it making any money? Is it driving sales? At the end of the day, that's all you probably really care about, right? Prompt effectiveness. You know, how well are your prompts working and achieving the desired results? Hallucination rates. You know, we talked about ways to measure that against ground truth data sets and prompt data sets and keeping track of any anomalies that might be getting detected. So token bursts, you know, are people kind of, uh, giving unexpected bursts of traffic to your system that you need to dig into? Are your responses drifting over time? Are they becoming less and less similar as time goes on? You might want to dig into why that's happening. And the first step is measuring it. Also, your bedrock model invocation logs can come in handy, of course, for digging into what might be going on over time, and you want to keep track of any cost anomalies as well. So you definitely want to be tracking the cost of this system and making sure you understand when things are shooting through the roof and if so, why, and what you can do about it.

---

### Lecture: AWS CloudTrail

Now let's talk about CloudTrail. So CloudTrail is a way to get governance compliance and audit for your AWS Accounts. And CloudTrail is enabled by default. This will allow you to get a history of all the events and API Calls made within your AWS Accounts, by the console by the SDK, the CLI other services on AWS and all these logs will be appearing in CloudTrail. Now, what you can do is that you can also put these logs from CloudTrail into CloudWatch Logs or Amazon S3. And you can create a trail to be applied to all regions or a single region, if you wanted to accumulate all these history of events accumulated across all regions into one specific, for example, S3 buckets. And when we use CloudTrail, for example, we'll say someone went ahead and deleted something in AWS. For example, say that an EC2 instance was being terminated and you wanna figure out who did it? Well, the answer is to look into CloudTrail because CloudTrail will have that API Call in it and will be able to get to the bottom of it and understand who did what and when. So to summarize CloudTrail is in the middle and the actions of the SDK, the CLI, or the console or even IAM Users and IAM Roles or other services will be in the CloudTrail console. We can look in it to inspect and audit what happened. And if you want to have all the events for more than 90 days, then we can send them into CloudWatch Logs or we can send them into an S3 buckets. So let me dive a little bit deeper for CloudTrail. So we have three kinds of events that you can see in CloudTrail. The first one is called the Management Events and these represents operations that are performed on resources in your AWS Accounts. For example, whenever someone configures security, they will use the API Call called IAM AttachRolePolicy. And this will appear in CloudTrail. If you create a subnet, this will appear as well. If you set up logging, this will appear by default. Anything that modifies your resources or your iOS accounts will appear in CloudTrail. And by default, trails are configured to log Management Events no matter what. You can separate two kind of Management Events. You have the Read Events that don't modify resources. For example, someone is listing all the users in IAM or listing all the EC2 instances in EC2, these kind of things. You can separate them from Write Events that may modify resources. For example, someone deletes or tries to delete a DynamoDB table. And obviously the Write Events have probably a lot more importance because they can wreck damages into your AWS infrastructure. Whereas the Read Events is just to get information which are still very important, but maybe less destructive. Then you have Data Events. So they're separate and by default Data Events as are not logged because they're high volume operations. So what are Data Events? Well, you have Amazon S3 object level activity for example, GetObject, DeleteObject, PutObject. And as you can see, these can be happening a lot on an S3 bucket. And so this is why they're not logged by default and you have the option to separate again Read and Write Events. So a Read Event will be a GetObject whereas a Right Event would be a DeleteObject or a PutObject. Another kind of event you can have in a CloudTrail are AWS Lambda function execution activities. So whenever someone uses the Invoke API so you can get insights about how many times your Lambda functions are being evoked. And again, this could be really high volumes, if your Lambda functions are executed a lot. And the third kind of events in CloudTrail are called CloudTrail Insights Events. And so I will talk to you about CloudTrail Insights in a bit more details in the next slide. So now let's talk about CloudTrail Insights. So when we have so many Management Events across all types of services and so many APIs happening very quickly in your accounts, it can be quite difficult to understand what looks odd, what looks unusual and what doesn't. And so this is where CloudTrail Insights comes in. So with CloudTrail Insights and you have to enable it and you have to pay for it, it will analyze your events and try to detect unusual activity in your accounts. For example, in accurate resource provisioning, hitting service limits, burst of AWS IAM actions, gaps in periodic maintenance activity. So the way it works is that CloudTrail will analyze what normal management activities look like to create the baseline and then it will continuously analyze anything that is the right type of event. So whenever something is changed or try to be changed to detect unusual patterns. So very simply the Management Events are going to be continuously analyzed by CloudTrail Insights which will generate Insights Event in case something is detected. And so these anomalies, so these Insight Events will appear in the CloudTrail console. They will also be sent to Amazon industry if you want to, and an EventBridge Event. So in CloudWatch event is going to be generated in case you need to automate on top of these CloudTrail Insights, for example, to send an email. So this is the idea behind CloudTrail Insights. Finally, let's talk about CloudTrail Event Retention. So events by default are stored for 90 days in CloudTrail and then afterwards they're deleted, but sometimes you may want to have events for longer in case you want to go back to something that happened maybe a year ago for audited purposes. And so to keep events beyond this period, what you have to do is that you have to log them to S3. So send them to S3, and then you would use Athena to analyze them. So very simply all your Management Events, your Data Events and your Insights Events are going to go into CloudTrail for 90 days, retention period. And then you would log those into your S3 buckets for long-term retention. And when you're ready to analyze them, you would use the Athena service, which is a serverless service to query data in S3 to find the events Okay I hope you like this lecture, and I will see you in the next lecture.

---

### Lecture: AWS CloudTrail - Hands On

So let's have a look at CloudTrail. And CloudTrail is a service to intercept any API calls or user activity within your accounts. And so here on here on the left hand side panel, we can have a look at the event history and this is the event history for the last 90 days of management events. So you can see all the API calls that are being made over time in this account. So it doesn't have to be very interesting, okay, but all of them will be here. So what I wanna do for example, is that I want to look in my EC2 console, and I created a demo instance. And what I'm going to do is that I'm going to terminate this instance. So I do right click, terminate, and now the instance is being terminated. And what I'm going to do is I'm going to check whether or not this event happens and appears within CloudTrail. So I'm going to wait about five minutes and get back to you. And so I just refreshed my pages, and as you can see, I ran the terminate instances, API call. And we can see what's the event source. So it's EC2 from where it was done, the access key that was used, the region that was used, and so on. And we can get the whole event right here. So that's the full power of CloudTrail is that we can see all the events really happening from within CloudTrail directly in this UI. And this is a short introduction at the practitioner level, but this is enough for you to get started and to answer questions at the exam. So that's it. I hope you liked it. And I will see you in the next lecture.

---

### Lecture: CloudTrail and GenAI

Very quick note here just to tie CloudTrail back to generative AI for you, and you've seen how it's generally useful to any system you might be building from a governance standpoint, but specifically it can also track any API calls that were done to Amazon Bedrock, and this might provide an audit trail of both which prompts were used. And also it has detail that it's logging of when those prompts were called and who actually issued those prompts. And it might seem a little bit creepy, but this is actually a compliance requirement in a lot of places, including the United States right now. So if you are interacting with a chatbot somewhere, yes, everything you do is getting logged and stored somewhere for compliance reasons and law enforcement reasons. So careful what you say out there, people. But from your standpoint as a developer, you need to have a way of tracking and logging that information. CloudTrail might be the way to do it.

---

### Lecture: AWS X-Ray

So, this is to me one of the most revolutionary services that AWS has to offer and I think it is currently underutilized. It's called AWS X-Ray and the exam is really we want you to know about X-Ray. I think this is why they ask two questions because they want people to use it, and I honestly think people should use X-Ray. So, when you do debugging production and I have debugged applications in production in the past, the good old way, what I call the good old way is test locally, add log statements everywhere and redeploying production and from the logs try to figure out what is breaking, what is happening. It's really painful. It's not best practices. Obviously, there are better ways of doing things. I'm just oversimplifying things here, but you get the idea. Debugging production is not fun. And then, if you log stuff, if you have different applications, if you log from different applications to CloudWatch then you know they all have different formats and it's really hard to centralize insights. And navigating CloudWatch logs is gonna be hard. And analytics on top of it, it's going to be hard. So, if you have a monolith, just one huge application doing everything is sort of easy to debug, but if you have distributed services you're running a hundred microservices in your LS accounts, it becomes a nightmare. It's really, really hard to debug what's happening because they all talk to each other, right? So, there's no common view of your entire architecture, your entire service map, et cetera. So, here comes AWS X-Ray. So, X-ray is going to give you a visual analysis of your application. So, this is what we're going to do in the hands-on. We'll see that basically as a client doing a request to our application we will see how many of these requests fail or don't fail. And then, from the application we'll see what it does. So, it will call other IPs, it will call SNS it will call a DynamoDB Table. And so, as you can see, we'll be able to trace exactly, visually what happens when we talk to our EC2 instance. And so, you know from this graph if I ask you where do you think that little orange or yellow error comes from? Well, is it coming from this? No, is it coming from SNS? No, it is coming from my DynamoDB Table. You can see it visually. And so, that's the whole power of tracing. Obviously, you can do more, but you start getting the idea. In terms of X-ray advantages, there's many. You can troubleshoot the performance of your application and identify bottlenecks. You can understand the dependencies in your microservice architecture because you can visually see what is happening and how all your microservices interact with one another. We can pinpoint then which service is giving us issues. We can understand how each request is behaving and then find errors and exception based on the request. We can answer questions such as, are we meeting time SLA in terms of latency or time to process a request? And we can understand which service really slows down, throttles us. Finally, we can know which users are impacted by our errors if we wanted to. So, X-ray has a lot of compatibility. It's compatible with AWS Lambda, Beanstalk, ECS, ELBs, API gateway, and EC2 instances or any application server really, even something you have on premise. So, they really made X-Ray try to be as wide as possible and as applicable as to any application they can. So, X-Ray, how does it work? Well, it leverages something called tracing. And tracing is an end to a way to basically follow a request. So, when I make a request to, for example, my application server, each component that will deal with a request that could be my database, that could be my gateway, my load balancer, my application server. Each component dealing with that request will add its own trace, and so, the trace will be made of segments and segments can be made of sub segments. And the idea is that we can also add annotations to traces to provide extra information around what happened. So, when all these things are together, you have the ability to trace every request or a simple request. So, you say I only want to get a percentage of the total request or maybe five requests per minutes. In terms of security, there is IAM authorization and you can use KMS for encryption at rest. So, once you get all these traces, basically, X-Ray provides its magic and provide this nice little graph that I showed you before. Now, how do you enable X-Ray? Well, you have two ways and I think this is what the exam will ask you about. So, you need to be very careful here. Your code can be Java, Python, Go, Node.js and .Net and it must import the AWS SDK. You need very little code modification, but you still need to do some code modification. And then, the application SDK, the X-Ray SDK, will then capture the calls to the AWS services, the HTTP and HTTPS request and the database calls for MySQL, PostgreSQL and DynamoDB. It can also capture queue calls and so on. Now, the second thing we have to do once we've modified our code is to install the X-Ray daemon or to enable the X-Ray AWS integration. So, if we run on a machine, on-premise server or EC2 instance, we need to install the daemon. And the daemon is basically a little program that works as a low level UDP packet interceptor. It can be running on Linux, Windows, and Mac. And so, you have to install it on your machine. If you use AWS Lambda or other services that already have integration with X-Ray then they will run the daemon for you and you don't have to worry about it. Now, each application also must have the IAM rights to write data to X-Ray. So, a very common question is, hey, my X-Ray application works on my computer when I test locally, but doesn't work on my EC2 machine, why? Well, the answer is probably because on your machine you're running the X-Ray daemon, but when you deploy to your EC2 instance, it's not running the X-Ray daemon, and therefore, X-Ray doesn't see your calls. Now, to make it really crystal clear here is an EC2 instance and you need to have your application code on top of it. And so, your code again needs to be modified to import the AWS X-Ray SDK, and then it will send its traces to the X-Ray daemon running the machine. So, as you can see, you also need to run the X-Ray daemon and that X-Ray daemon will send a batch every one second to AWS X-Ray. So, X-Ray does some magic. And so, to update this graph, how does that work? X-Ray will collect all the data from all the different services sending traces, and then, the service map will be computed magically from all the segments and the traces. And so, that's a really cool thing. X-ray is graphical, so even non-technical people can help troubleshoot. Talking about trouble shooting, what if X-Ray is not working on EC2? As I said, you need to ensure that the IAM role has the proper permission and you need to ensure that the EC2 instance is indeed running the X-Ray Daemon. If you want to run it on Lambda, it's a little bit different. For it, you need to ensure that the Lambda has an IAM execution role with proper policy. And I know we haven't seen what Lambda is, but it's just a question you may get. So, you need to make sure that Lambda has a proper IAM role that's expected and then you need to ensure that the X-Ray code is imported. And finally, that you have activated the active tracing Lambda option on X-Ray, but we'll see this in the Lambda section as well. So, that's it for the overview of X-Ray, a little taster. In the next lecture, we're just going to run the several apps on X-Ray to get a better feeling of how it works. So, see you in the next lecture.

---

### Lecture: AWS X-Ray - Hands On

So let's practice using X-Ray. And for this I'm gonna go into the CloudWatch console. And it turns out that the X-Ray console is now in the CloudWatch console. So you can see it on the left hand side under service map. Currently we see nothing, but we'll see something soon. That's because it's better to have X-Ray with the metrics your logs and your alarms all in one place. Okay, so if we wanted to have some data in X-Ray we need to set up a demo app but it turns out that, well, the demo app as per my trials and errors, doesn't work in AWS. So therefore I've created my own demo app that is a modification of what AWS has created. And we'll deploy this on CloudFormation. So you go to CloudFormation and you create a stack. And the stack we're going to use in here is the stack that's under the X-Ray folder called EB Javas Scorekeep X-Ray simplified. This is a simplified template from AWS. So we're going to upload a template file and choose the EB Javas Scorekeep X-Ray simplified. So the stack name is going to be called Scorekeep X-Ray. And then you scroll down, you leave everything as default except at the very bottom three things. So for subnet one, you're going to choose the first subnet. For subnet ID two, you choose the second subnet. And finally, for VPCID, you choose the VPC right here. And the reason we do so is that this is we indicate to the templates where we want to deploy our resources. So click on next, then click on next and then acknowledge and submit. And this is going to create a CloudFormation template for us. And if you have a look at it, this CloudFormation template actually deploys an ECS cluster. And then we're going to have a front-end API a front-end image, sorry, a backend API all using X-Ray so that we see data in the X-Ray folder and then it's going to be running on the T2 or T3 micro. Okay, so this is good. Now let's go in here and wait for this to be fully done. Okay, so my stack is now fully deployed and if I go into resources I can see all the things that have been created as part of this confirmation templates. So we can see, for example, an ECS execution role ASG a game table, which is DynamoDB and so on. So how do we use this web application. Well, you go to outputs, refresh this and then we have the loaded balancer. You are over here that you just open in your new page. And then you should have this kind of UI. So I'm going to leave the session as games. I'm going to leave this to random and click on create. Here we have to create a game name. So I'm going to create a Sample Game and then the rules of it is going to be Tic Tac Toe. And then click on create. And now we can view the traces recession but you click on this. This is going to take you to the old X-Ray console, which is right here. And as you can see there's a new X-Ray console, experience available. That is the one that I'm showing you right here. So for now, let's just keep on playing. So let's click on this game and click on play. And here we have a classic game of Tic Tac Toe. So you just click wherever you want. So here, here, here. And then, oh, you see there's a line. So you'll see in a second that X has won, so X wins. So yes, we have done this entire game and we could go back and play more games. What's going to happen is that as we play the games this is going to send traces into X-Ray. And so if I go into X-Ray right here and look at my service map, which is the first part. So I'm going to zoom out a little bit just for this ones. So this is my service map, and this shows the dependencies of all my components in AWS and how they relate towards the API calls that have been made. And as you can see, we have an ECS container right here. This is what's representing it. And then we also have a DynamoDB table. We have another DynamoDB table. So we have two of them. We have an SNS. And you see right now it's a bit orange is because this is an error and we'll have a look at it. We also have a session table we have a state table, an SNS topic, and another table. So we have so many DynamoDB tables and so on. And this is the service map. So you can imagine as you have many microservices and all of them making API calls to each other if they're all instrumented with X-Ray then you will start seeing the dependencies between all these services. And on top of it, in case there is an error this will be highlighted right here. So if we click on this, for example we can have a look at the latency. So we see the average latency and so on over time the number of requests over time, number of faults and the response time distribution if we want it to. And as we have a look at this, if I click on here as you can see here, it says error 100% to SNS. So it looks like that SNS is getting some errors. And so what I can do is that I can analyze these traces by clicking on view traces. So now we have these, this is the traces console and this is where we can start to query some traces so we can run a query and view what's happening. But currently we don't have any data. So instead I'm going to do is just run a query with nothing. And as you can see, we have 21 different traces. So everything looks good. And what's going to happen is that you can filter them by note. So figure out which ones are going to be important to you. So maybe you just wanna look at traces related to the ECS container or to this score, keep game table and so on. So you can start to build your query. As you can see, if I only want to look at score keep game then I can add to query. And this is going to populate my trace query right here. And I can run my query and instead of 21, I have 10 traces. And then on top of it, now half this, I can look at like the response graphs in terms of latency. So it looks like most of my requests are under 200 milliseconds but there has been one request at over 500 milliseconds. So again, it really depends on what you want to get out of it but this could be some good indications and help you look into, for example this one request right here that was slow. And we can have a look and understand. So if we look at the duration, this is probably this one. So if you click on it now you have details about a specific trace. And so this trace is going to show you the trace map just for that once. So as you can see, we have a little bit less information in here because this trace was using only the ECS container and four DynamoDB tables. And if you scroll down now you see the actual breakdown of the request based on the different events that happens. And so you can see that this post right here to this URL actually triggered a lot of API calls to DynamoDB. So first get item on score, keep game then get item on score clip states and so on. And you can see the latency, how long each take and so on. So this gives you some information around your data. And of course as you click on it, then you're going to get some segment details information to look whether or not you had errors, to look the resources, the annotations, if anything, and the metadata. So you can get a lot of information around what failed and what didn't fail. And of course, this is only relevant when there is a failure for analyzing the failures but this gives you really good latency information. And if you had CloudHSM logs enabled then you could also see the corresponding logs for that specific trace which makes X-Ray really, really, really powerful. So that's it. That's what I want you to remember for X-Ray. Now in the state of things, right now X-Ray does not have all the features from the previous console so we only have service map and traces. But if you're go in here in the X-Ray console as you can see, we have configurations such as sampling, encryption, and groups which are don't appear yet in this console. So for now I may use this console as well in the future videos. Just know that X-Ray and you're good to go. And finally, just to be clear, you need to delete the stack so that it doesn't leave it running. So you can just delete it, call the score, keep X-Ray delete the stack, and you're good to go. All right, that's it for this lecture. I hope you liked it, and I will see you in the next lecture.

---

### Lecture: AWS Lake Formation

Let's talk about AWS Lake Formation, a relatively new feature. It was announced back in 2018 and really saw a lot of refinement over 2020. And we're just now starting to see it show up in the exam. Lake formation is built on top of glue, and it's really meant as a system for making it easier to set up a secure data lake. Their tagline is it makes it easy to set up a secure data lake in days, which I guess is pretty quick. The things that it manages for you is loading your data from an external source and monitoring those data flows, so it allows you to connect to your existing data sources, whether they're already in S3 or in some on premises database. It can set up partitions for you. It can help you manage encryption and manage the keys surrounding that. And it can let you set up the transformations associated with that data from the get go. Again, it's built on top of glue, so pretty much anything glue, ETL or any of the glue features can do is available to you. If spark can do it, then Lake Formation can do it too. It's just using glue under the hood to apply those transformations, and that includes transforming it into a new format, like a columnar format like parquet or Orc. That's on the table cleaning your data, you know, getting rid of missing rows, whatever you want to do. Anything that we've talked about with glue you can do with lake formation. It can also help you manage the access control to the data in your data lake. It can help with auditing, and really, its purpose in life is to kind of guide you through the process of building your data lake in a secure fashion. So let's talk about it from a more conceptual standpoint here. Basically, you have a big S3 data lake sitting there when you're done, right. So lake formation is going to sit alongside that S3 data lake, help you create it, help you maintain it going forward so it can take as a data source anything in S3 from some relational database that you might have sitting around, or even NoSQL data sources. And those can be on premises as well. It doesn't have to be in AWS yet. So again, the purpose of lake formation is to help you set up that data lake, and that data might be coming from someplace else within lake formation. You can set up all the crawlers and ETL jobs and data catalogs and security and access control, any data cleaning or transformations, including to parquet or ORC. Again, basically anything glue can do, you can set up as part of lake formation. And once you have a lake formation set up, it can then integrate with Athena or Redshift easily or redshift spectrum. So Athena and Redshift can then query that data automatically if you have it all registered in lake formation. And a relatively new feature in 2020 is EMR can also talk to lake formation directly as well. Pricing Lake formation itself doesn't cost anything, but the underlying services do. Of course, that includes glue, S3, EMR, Athena, and Redshift. Normal charges will apply for whatever you build using it. The process for building a data lake using lake formation. It's a little more complicated than you might think. It doesn't really do it all for you. This is why they say it takes days and not just minutes. Basically, you have to start by creating an IAM user for a data analyst role. You know the person that's going to be using this data lake and building it. You then need to create a glue connection to the incoming data source or sources. Create an S3 bucket for the lake itself. And then once you've done that, you can register that S3 path in lake formation and start granting permissions to it. So this is where lake formation starts to be useful. You then create a database within lake formation for the underlying data catalog and grant necessary permissions to it. And at that point, you can start using blueprints to build up workflows, for example, making database snapshots, whatever you want to do to transform that data and get it into your data lake. You then run the workflow and grant select permissions to whoever needs to access the resulting data Lake Athena or Redshift Spectrum or now it could be EMR as well. That's it conceptually, which is pretty much all you need to know for the exam. Sometimes they like to dig in on these little troubleshooting things and scenarios of things that can go wrong with it. One finer point with AWS Lake formation is the issue of cross-account permissions. So if you do need to have people across different accounts accessing your data lake and accessing lake formation, you need to make sure that the recipient is set up as a data lake administrator. And also take note that AWS Resource Access Manager Ram can be a useful tool for managing accounts external to your organization and what resources they can access within your account. Also, there are IAM permissions for cross-account access that will be relevant here as well. Keep in mind lake formation does not support manifests in queries from Athena or Redshift. So that is one thing that can cause errors if you're using a manifest in a query. One thing to keep in mind. Also remember IAM permissions on the KMS encryption keys are needed for encrypted data catalogs in lake formation, and IAM permissions are needed to create blueprints and workflows within lake formation. So these are all potential, uh, troubleshooting things. You might want to know if you're encountering an error about being able to create a blueprint or a workflow. It's probably an IAM issue. If you're having a cross-account permission issue, you probably need to do something with Ram. So things to keep in the back of your head. I do want to spend a little bit more time on data permissions in lake formation, because reportedly this does crop up in the exam in a couple of places. So let's go into a little bit of depth. Lake formation does make it easier to set permissions on your underlying data in S3. So this is often a way to enforce really fine grained control. We have the ability to restrict permissions all the way down to the column level, just using this UI that you see on the right here, it's pretty easy to use. So just by studying this UI screen, you can understand a lot of what the capabilities are. Starting at the top, you can see that you can tie your permissions to IAM users, or roles to Saml users and groups, or to external AWS accounts. Even those are all possible, and you can just build up a list of the different principles that you want to tie a specific set of permissions to. It allows you to use policy tags if you want to, and that can be tied to databases, tables or columns. And you can base your security based on those tags. That's always a good practice. Or you can just explicitly set permissions based on either a table or a specific columns within the table below. And you can see there's a very handy UI there for saying, do you want to let them do selects or inserts or deletes or anything else that you can dream up there for a specific table, or even for specific columns on a table? So if you see questions about implementing column based security on your data, Lake Lake formation might be the way to do it.
---

*Section 8 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*