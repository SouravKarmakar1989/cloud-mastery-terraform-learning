So the code guru profiler functions thanks to an agent and we can configure that agent to fine tune it. The first one is MaxStackDepth. So this is the amount of code that is going to be represented in the profile. So for example, if you have a method A and then method A calls method B, which codes method C and then method D, then we count it as a depth of four. So if you set MaxStackDepth to two then the profiler is only going to evaluate and profile method A and method B. So it's important for you to remember this because if you wanna go very deep you need to increase that parameter. Memory usage limit percent is how much memory is allowed to be used by the profiler. Minimum time for reporting in milliseconds is the minimum time between sending reports. So of course you can increase the reporting interval in milliseconds and it will be bounded by the minimum. And so the reporting interval in milliseconds tells the agent how often to report the profiling it has done. We also have the sampling interval in milliseconds. So here this is important. This is the sampling interval used to profile samples. And so if you set it to a lower value you're going to get more and more samplings and so you're going to have a higher sampling rate. And so you may catch more functions or more methods being called. So you don't have to remember all of these of course but just now from seeing the name of it if it comes up at the exam, you should be able to understand how this setting influences the code guru agent and then you can answer the question correctly at the exam. Alright, that's it for this lecture. I hope you liked it and I will see you in the next lecture.