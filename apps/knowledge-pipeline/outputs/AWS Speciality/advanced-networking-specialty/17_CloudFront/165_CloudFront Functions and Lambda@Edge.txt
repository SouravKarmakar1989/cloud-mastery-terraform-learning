So cloud front has more capability than just a CDN. This allow you to run custom code at the Edge to do some customization. So the idea is that you want to execute edge functions as close as possible to users to make sure that you're verifying some early logics some early behavior or normalizing some Cache keys. So an Edge Function is a code that you write and that you attached to a CloudFront distribution and it will run very close to users in order to minimize the latency of whatever you're trying to do. It doesn't have any cache behavior. It's just here to change the request and the response sent by the users and back to the users. Now CloudFront has two different logic for Edge Functions. It has the CloudFront functions and Lambda@Edge. We'll see them both in this lecture. So the use cases for customization at the Edge, at the Edge is to manipulate HTTP request and responses is to implement request filtering before reaching your application is to verify user authentication and authorization is to also maybe generate HTTP responses directly at the Edge. Or do A/B testing by separating users into two groups. Or for example, doing Bot mitigation at the edge. The idea is that using Edge Functions. So either Cloud Front Functions and Lambda@Edge you do not have to manage any servers. Everything is deployed globally and it is serverless. Okay. So if we look at a traditional architecture of CloudFront, we have our clients to our Edge Locations talking to our regional Edge Cache, talking to our Origin. Now Lambda@Edge Functions are going to be deployed at the Regional Edge Cache level. Whereas CloudFront Functions are going to be deployed on the Edge Locations. So as you can see two different level for deployments of these functions, and that will impact your choice between CloudFront Functions and Lambda@Edge Functions. So if we look at CloudFront Functions, they will interact with the viewer request and the viewer response. These are the request and response that happen between the clients and the cloud front. It does not modify origin request or origin response. So these are lightweight functions return in JavaScript and they're meant to do high scale latency-sensitive CDN customizations. So it is Sub-millisecond startup time. So it's very, very quick to start and it can handle millions of requests per second. So we run at Edge Locations and it's Process-based isolation. So you want to change the viewer request and responses as I said. So what is a viewer request? Well, it's after CloudFront receives a request from a viewer and your response is before CloudFront forwards the response to the viewer. So the idea is that it's a native feature of cloud front. You manage the code directly from within CloudFront. And this allows you to do some very quick time sensitive transformations in CloudFront, okay. The idea is that you cannot call external services from the CloudFront Functions. You just want to perform some transformations, some computations or some normalization in a very quick fashion. Now Lambda@Edge is a different mindset. So these were first what CloudFront was offering as a feature CloudFront Functions are new but Lambda@Edge has been existing for a long time. And the idea was this is that you have Lambda Functions that are written in either NodeJS or Python. And you can scale through thousands of requests per second. So less scale than (indistinct)functions for sure. And they run on the nearest Regional Edge Cache. They're VM-based isolation. And they're used to change both CloudFront requests and responses. So we can change the viewer request, the Origin Request which is the request that CloudFront sends to your origin. You can change the Origin Response. So the response that CloudFront receives from the Origin and as well as the viewer response, okay? Now, when you want to deploy Lambda@Edge Functions on CloudFront, you need to author your functions in one region, which is us-east-1, and then CloudFront will replicate this function to all its regional Edge Caches. Now, if we have a look at how these things can be used together. So we have Edge Locations and Regional Edge Cache. And so the view request goes all the way like this. Then it goes through the Origin, and then the Origin Response, goes all the way like this to the viewer response. Now your Cloud Front Functions are going to run on the Edge Locations at the Viewer Request and Viewer Response level. Whereas your Lambda@Edge Functions are going to run at the Origin Request and Origin Response level. This is if you want to use both CloudFront Functions and Lambda@Edge Functions together, okay? So this is one option. And we can see a clear separation of (indistinct). But if you want to just have Lambda@Edge Functions then in that case, what's going to happen is that the Lambda@Edge Functions are going to operate on both of your requests, response. And the origin request and response. But only at the original Edge Cache Level, okay. In this case we cannot use CloudFront Functions because we cannot use both Lambda@Edge and CloudFront Functions on the Viewer Request and Responses. So this is when you need capabilities of Lambda@Edge that are not available with CloudFront Functions. For example, if you need a longer execution time, network access, third party API calls, these kind of things. So if we want to compare, you know, on the table the Cloud Front Functions and Lambda@Edge while the run time is support is different. So one is JavaScript, pure JavaScript code. The other one is NodeJS and Python. Where it gets executed is on Edge Locations for CloudFront Functions. Whereas it's original Edge Cache for Lambda@Edge. The CloudFront triggers are going to be the view request and response functions and all of them for Lambda@Edge. The isolation is process based versus VM based. Now the max execution time is what I think is the most important thing. You only get one millisecond to execute your CloudFront Functions. And therefore them must be very quick and very simple. Whereas Lambda@Edge, you get five seconds of run time for viewers triggered and 30 seconds of run time for origin triggers. The memory of your CloudFront Functions is low two MegaBytes. Whereas for Lambda@Edge, you have more memory. 128 MegaBytes or 10 GigaBytes. The total package site is very small for CloudFront Functions, 10 KiloBytes whereas Lambda@Edge has more space. There's no network access no fast system access for CloudFront Functions. Whereas you have some for Lambda@Edge. You do not have access to the request body in CloudFront Functions. You have access to it in Lambda@Edge. So CloudFront Functions is really about reading headers and so on. And the pricing is that there's a free tier for CloudFront Functions and it's one sixth of the price of @Edge. So if you can move your logic from Lambda@Edge CloudFront Functions, you're better off and Lambda@Edge as no free tier. And you get charge per request and duration. So when do you use a CloudFront Function versus a Lambda@Edge function? Well, CloudFront Functions are going to be used for Cache key normalization. So you want to transform the request attributes such as the headers, cookies, the query string, the URL. To create an optimal Cache Key. You also want to manipulate your Headers. So you want to change them. You want to insert, modify, or delete HTTP headers in a request or response. Or you want to do your rewrites, or redirects or to request authentication and authorization directly at the Edge. So in which you can create and validate user generated tokens, for example JWT to allow or deny request very close to your users. Whereas Lambda@Edge is going to be giving you a much longer execution time, okay? Up to five seconds or 30 seconds. You get adjustable CPU memory. You get to have code that can depend on third party libraries. For example, you can use the SDK to access other AWS services. You can use the network access to use external services for processing, and you have file system access. If you wanted to Cache some stuff locally or you can even access the body of the HTTP request to manipulate the body itself. So hopefully the use Caches are clear and well defined. Now, if you wanted to do authorization and authentication with CloudFront functions, the idea is that at the Edge Location, you would intercept the content and you would check the token, the digital JWT token which lives in your headers. And then you can check whether or not it is correct. And then if it's not correct then the client will get an error message directly without having your request go all the way to your origin. Whereas your Lambda@Edge Function will operate on the regional edge Cache level. It could be intercepted by Lambda Functions which can do some custom logic and even issue third party API calls, to check whether or not the authorization and authentication is properly well set up. Another very common use case for Lambda@Edge is to load content based on the user agents. So we have an extra bucket an Amazon cloud front with Original Edge Cache and we have users texting it from desktop and others just texting from mobile. Now they both wanna access a cat picture. So they do a request for cat picture and the resolution is super high. So 192, 1, 9, 2, 0 and 1, 8, 0, 8. 0 is 10 ADP and they request it from CloudFront. So if the request goes well, the request is passed all the way to the S3 bucket to get that specific image. But imagine that the same request is done from our mobile device. And this time the user agent is not Chrome. It is the iPhone OS Safari. Now, what we can do is instead of sending this big image back to our mobile which may be very inefficient, we can have Lambda@Edge Function that will inspect the User-Agent GTP header and then redirect based on the device type. And so therefore, because our device is a mobile device then the image is going to be asked into our S3 Bucket is going to be cat-640-320. Which is a lower resolution for our cat picture. And this is the picture that will be returned by our S3 Bucket into our mobile device. So the Lambda@Edge Function was able to modify the request to change the target of these request. So the other way you can use Lambda@Edge is for a global application. For example, your client gets the website from the S3 Bucket. And then for dynamic API requests it sends it into CloudFront to leverage the Cache but your Lambda Function can also be used for example to query data into DynamoDB table, which gives you a fully global and serverless way to do an API in AWS, okay. So that's it at the exam level. What you need to know is understand when you need to use Lambda@Edge or CloudFront Functions and that should be good to go. But I want to give you a bit more details so you can really understand the use cases is behind it. So hope you like this lecture. And I will see you in the next lecture.