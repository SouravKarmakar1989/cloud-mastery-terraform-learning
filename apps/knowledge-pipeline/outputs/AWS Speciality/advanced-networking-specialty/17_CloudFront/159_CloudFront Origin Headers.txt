Okay. So let's talk about origin headers for CloudFronts. So you can have custom headers. And what are those? Well, if the user's passing a request to CloudFronts, for example, this one, which already has some headers, it is possible for CloudFront to add a custom header to the request before passing it on to the origin. So the idea is that you can customize it for each origin and you can set whatever header you want. These are fixed values, this supports custom and S3 origins. And the use cases for this is going to identify which requests are coming from CloudFront or from a particular distribution on to your origin. This is going to be also very helpful as we'll see, to control access to the contents. For example, to say that the origin can only respond to your request when it actually originates from CloudFront, with the correct custom header. There's other headers that can be added back out front, and these are more dynamics, but there are specific HTTP headers based on the viewer request. So we can have a look at the viewers type of device based on the user agents. So it could be CloudFront is Android, Desktop, iOS, mobile, smart TV, Tablet, Viewer. We can get information about the location headers based on the IP address, so we can get CloudFront Viewer, City, Country, Latitude, Longitude and so on. And then we can get information around the protocol and the HTTP version. For example, we can get the CloudFront Forwarded Proto and the CloudFront Viewer HTTP version. And all of these headers can be helpful if you want to, to cache based on them. Okay. So we can use them in your Cache key or at, or choose to receive them directly on your origin. So that's it for this lecture. We'll see how to use these custom headers in one of the following hands on. So I will see you in the next lecture.