So, how do you enable end-to-end encryption between CloudFront, your ALB, and your EC2 instance to use HTTPS all the way? So on the end of CloudFront you must specify the Origin protocol policy to be HTTPS only. So CloudFront communicates with the ALB using HTTPS and then your custom Origin which is a load balancer in this instance must have an SSL or TLS certificate that is valid. And the certificate domain name must be either what we set for the Origin domain field which we configure in CloudFront, or if we're using the Host header from the clients and it's forwarded to the Origin then the host editor must match the domain of the TLS certificate on your ALB. So, because the domain itself is going to be verified by CloudFront, you cannot use self-signed certificate. It must be a certificate issued, for example by AWS Certificate Manager or by a third party service. Now, the configuration on your application load balancer is that, as I said, you must use a certificate provided by the AWS Certificate Manager or third party but then imported into the AWS Certificate Manager and then your load balancer will have correct configuration. And then of course you must set up your EC2 instance to have SSL. And so, as you know, or as you may know the AWS Certificate Manager Service, ACM, is not supported on EC2. You cannot export certificates from ACM. So therefore for your EC2 instance to set up SSL or TLS you must use a third party, or you can use a third party SSL certificate for any domain name actually, because there's no verification or you can even use a self-signed certificate on your EC2 instance, because the ALB is not going to verify the certificate itself. It's just going to use the certificate to encrypt the connection. Once you have all that in place then you will have end-to-end encryption between CloudFront, your ALB, and your EC2 instance. I hope you liked it and I will see you in the next lecture.