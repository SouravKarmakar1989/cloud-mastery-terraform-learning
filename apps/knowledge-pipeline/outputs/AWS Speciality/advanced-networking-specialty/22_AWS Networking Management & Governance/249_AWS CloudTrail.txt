Hello and welcome to this lecture, AWS CloudTrail. So I know sometimes these services could be confusing and you might not able to clearly tell what service does what. But understand that AWS CloudFormation is a service which helps you create the infrastructure in automated manner. Then AWS Service Catalog, it's just a wrapper over that. And then AWS Config is the service which helps you track the changes which happens to your resources over the time. And how Config does it? It basically gets the data from lot of internal AWS Services, and AWS Cloud release one of those service. So what is CloudTrail? It provides audit for your AWS Account activities by logging in all API calls. And it is enabled by default. Now, this is advisable as well that you should have CloudTrail enabled so that you can audit your account. If something happens to your account, maybe someone terminate some machines or someone is trying to get hold of your resources, then you can go to the CloudTrail and see what's happening. Now you can get also the history of API calls made within your AWS Account. So these API calls could be made through console, or SDK, or CLI, or from another AWS Service. All these events are kept in AWS CloudTrail for 90 days by default. And then you can persist those further into the CloudWatch or S3. Now, if resource is deleted in AWS, look at the CloudTrail. That's the message, basically. That's the single source of truth for you to find out what happened, when it happened, and who did that? Now, if you followed the AWS Config lecture, it also does similar thing, but then Config is an abstraction over AWS CloudTrail and few other services. Now, AWS CloudTrail are regional by default, but then you could also enable the global-level CloudTrail event such that, you know, AWS IAM is a global service. It is not per region. But then CloudTrail also can log all the API calls made to the AWS IAM service. And if you want to collect the CloudTrail from different regions into a single S3 bucket, you can also do that. And we'll just see that shortly. So this is how it works. You would have AWS Accounts. And then inside Account, you use multiple AWS Regions. And then you can create CloudTrail Trail. Now CloudTrail trail is a sequence of all these events which can then persisted into maybe S3 or maybe in CloudWatch for, you know, long-term archival. Now, you can create a trail. You can create a trail and all that trail can go to a single S3 bucket, or it can go to an individual S3 bucket per region. Now choice is up to you, but the CloudTrail trail can be collected into a one single S3 bucket. Now, why do we do that? Basically, once you have these events in S3 bucket, then you can apply some logic or you can query that data using AWS Athena service, for example. Now, Athena is an SQL interface to S3. If your data is in CSV format, then Athena can query that data. And then you can get a deeper insights about your data. Similarly, this data can also be sent to elastic search or open search, and then you can run your searches on top of that data, or this can go to a third-party logging tool or some SIM system for audit purpose. And alternatively, if you have an event in CloudWatch, you can write a CloudWatch filter, which looks at those logs and maybe some interesting event you got, you can create an alarm on top of that. And then you can send an SMS notification via SMS or email, whatever you prefer. So ultimately you have full control because this CloudTrail logs, you get in CloudWatch or S3, and then you decide what to do of these logs. I have seen cases where maybe customer wants to collect all these logs from all accounts at one place and then provide access to the audit team to look at if they see any suspicious activity into their AWS Account. So this can be very well done with CloudTrail trails. This is a sample event. You can see I've masked some of the data. But if you see CloudTrail event, it shows you who was the user, what time this event occurred, what action was triggered, and then what resource it was triggered on. And there are a lot of other information data points as well, but at bare minimum, this is the information you would look for and then you can find out what happened at what time. So essentially that is a CloudTrail, very important service with respect to security and audit purpose, and all the actions with respect to the networking where you are creating the VPCs, or subnets, or whatever, right? All the actions are captured in AWS CloudTrail. So that's it about this lecture. I hope you understand well the difference between all the management and governance services that we talked about in this section. And we just touched upon all this services at a very high level, because for AWS Networking Specialty Certification, these are not mandatory, but just good to know. So that's it for this section. Thanks for watching.