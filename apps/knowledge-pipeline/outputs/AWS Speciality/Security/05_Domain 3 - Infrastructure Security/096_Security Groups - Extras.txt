So now let's learn something about security groups that can come up in the exam. So it turns out that when you have a security group, if you modify a rule, it will never disrupt the tracked connections. That means that any existing connections are kept until they time out. So let me give you an example so you'll understand it better. So say we have an instance and a public subnet and a VPC and we have a security group that allows people to SSH on port 22 from anywhere. That means that us, as a user from a computer, we can SSH into our EC2 instance. Now, say for example that the SSH connection is open but then we remove that rule from the security group, what do you think will happen? Well, it turns out that yes, the connection will still be working, because it will be opened and it will be allowed at the time the rule was out there. So this connection will only close when it times out. So how do we proceed for, for example, forcing the closure of that connection? Well, you can use network ACLs. So this allow you to block the connections immediately, so you define them at the subnet level and then you would add a rule, the first one, to deny any traffic on port 22. Because NACLs are stateless, they are evaluated all the time for all inbound and outbound traffic and therefor, the connection will be closed, but because security groups are stateful at the time when the connection was allowed in, the return traffic is also allowed back and the connection remains open as long as it is not timing out. And so therefore, even if you remove the rule from the security group, the connection will work out. And that is a scenario that can come up in the exam. Okay, that's it, I hope you liked it and I will see you in the next lecture.