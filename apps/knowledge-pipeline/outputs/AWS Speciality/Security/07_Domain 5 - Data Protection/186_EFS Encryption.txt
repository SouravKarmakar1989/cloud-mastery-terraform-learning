So here is a short lecture on how to encrypt an EFS file system that is unencrypted to start with. So there's no option to just turn on encryption for your EFS file system. What you must do instead is that you create a new EFS file system and then you will migrate the files and their permissions using AWS DataSync. So to summarize, we have an unencrypted EFS file system and then we create an encrypted EFS file system that at first is empty, and then we'll use AWS DataSync to migrate from one to the other. When we're ready, then our applications can switch over from the unencrypted file system to the encrypted file system, and that's the process. Okay, that's it. I hope you liked it and I will see you in the next lecture.