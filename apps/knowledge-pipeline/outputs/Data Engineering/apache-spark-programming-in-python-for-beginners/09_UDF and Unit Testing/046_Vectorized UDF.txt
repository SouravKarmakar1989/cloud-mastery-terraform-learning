Welcome back. In this lecture I'll give you an introduction to Vectorized UDF. We'll try to understand what are vectorized user defined functions and how to implement and how to use Vectorized UDFs in Apache Spark. So let's start. So I'm in my development environment. Let's go to chapter nine, Vectorized UDF. In this lecture we will learn about Vectorized UDF. In the previous lecture we already learned about the Python UDF in Apache Spark. So let's try to understand what are Vectorized UDF or some people also call them Pandas UDF in Apache Spark. One thing, two names. Some people call it Pandas UDF, some people call it Vectorized UDF. Both are referring to the same thing. So what are Pandas UDF? They are also user defined functions, right? Like Python UDF, Pandas UDF also, user defined functions. So they take input, do the processing, return the result. That's all. That's what they are. But they are different compared to the Python UDF in two respects. The first they take or return Pandas series or Pandas data frame as an input. Recall what are Python UDFs? Python UDFs take or return Python objects. It could be a string, it could be integer, double, whatever, right? But they take Python objects and as UDF take and as series or Pandas data frame, what is Pandas series? It is like an array where you have maybe 1,000 values. So they don't take single record as an input. They don't take one member ID or one value as an input. They take a big chunk of values, right, an array of values. So if suppose I want to pass the member ID to the Vectorized UDF or Pandas UDF Spark will create a array, put 1,000 IDs or maybe 10,000 IDs that is some kind of configuration in a Spark so it'll put maybe 1,000 IDs into a Pandas array or a series and call your function. And that is very powerful. That makes Vectorized UDF or Pandas UDF super fast. Compare it with the Python UDF where every function call will get one ID, then next function call will get next ID. So far processing thousand records function is called 1,000 times. But if you write a Pandas UDF for the same purpose, 1,000 records can be processed in one function call because a Spark will pass 1,000 IDs at a time in a array to the Pandas UDF and you can process all 1,000 values in a single function call. So that makes it fast. So that's the first difference. Pandas UDFs take or return Pandas series or Pandas data frame. Second difference, they operate block by block. Block by block means you take a block of IDs or block of input means a array of input, process the entire array and then return the result as a array the entire block. And this kind of operation is known as Vectorized Operation, so they perform better. Fourth thing, serialization and deserialization of input and output in Panda's UDF is by default arrow. Because arrow is fast, it is better. So why use Pickle, Pandas UDF for vectorized UDFs are designed to perform faster, hence they are by default using arrow serialization. They do not use Pickle serialization. So that's about Panda's UDF or Vectorized UDF. I hope you understood what are Pandas UDF or what are Vectorized UDF, how they are different compared to Python UDF. Recommendation is whenever you need to write a user defined function, use Pandas UDF or Vectorized UDF. Do not use Python UDF as long as you have some compelling forceful reasons to use Python UDF, otherwise, always, always use Pandas UDF or Vectorized UDF. But one note. You must know Pandas to work with the data inside the function. Why? Because input is Pandas series or Panda's data frame, output is also either Panda series or Panda's data frame. So the code that you are going to write inside the function should be working with the Panda's data types, so it should be Panda's code. So you need to know Pandas a little bit to be able to work with the Panda's UDF or create Panda's UDF. So that's about Panda's UDF. Now let's do some hands-on to understand the mechanics of implementing and using Panda's UDF. So we have first question here. Create a Panda's UDF with the following functionality. Input is member ID and output is either guest or member. So same logic, it's a business logic. We want to write a business logic function, which takes member ID, and based on the member ID code, it tells whether this member ID belongs to a guest or this member Id belongs to a regular member. So how do we define? We have to follow the similar kind of structure or approach that we did for Python UDF. So first we have to import from a Spark dot SQL dot functions import. Import what? Pandas UDF, Pandas UDF. This is the annotation that we will be using for creating Pandas UDF. Plus we need to write code inside my function, which will be Panda's code, so I may also need to import Pandas. Pandas as let's import Pandas as PD. And then I can start defining my Pandas UDF or Vectorized UDF, how to define at Pandas UDF. That's the annotation we are going to use and return type. We need to specify this F is always none. First parameter is the function itself that we will define afterwards. Second parameter is the return type. So we will simply say return type is a string because my function is going to return a string. That's all. And then I can start defining my function. Function. Give a name to the function member type PUDF, let's call it PUDF this time. And what it takes, it takes ID as input. And what is the data type for the ID? Data type should be Pandas series. So PD dot series, that's my input data type, right? And what is the output data type? Output should also be PD dot series, right? And then I can define the body, right? So in defining Panda's UDF we should make sure that input is Pandas series. Output is also Pandas series. And if in Pandas series, if there are thousand values passed in the return also there will be thousand values, right? What is this data type then? A string. My return type is a string, but here I'm telling return type will be Pandas series. So basically return type will be a Panda series of a string, right? Think of it array of a string. So we tell what is the data type of the element within this series, right? But the return type will be series. So we define the PD dot series and what is the output that we want to produce? We want to return guest or member, but we want to process the entire series. This ID is not a single value, it is a series, right? So let's put return here first. We want to return after processing all the values in this ID, all the values, right? So how do I process a Panda series? You need to know a little bit Panda. So Panda series ID dot apply, apply Panda ID dot apply means I can pass a function here. Which function will be executed for each value in this series, right? So let me directly define the function here itself. We call it lambda. And each value will be taken as X, right? Each value of the ID, right? Let's assume there are a thousand IDs, so this function will be applied for all thousand IDs and each time this ID is known as X. And then what we want to do, same logic, return guest if X equal to zero else member, that's all. Right? And that is all. That is all my Panda's UDF, right? So let's compile it. My Pandas UDF is defined. Now how do I use it? It is a user defined function, so I can use it anywhere I can use a Spark function. In any transformation that allows me to use a Spark function I can use my user defined function also at that place, right? So let's take a simple scenario. List all bookings made by guests. Not guest. Guests, right? So we want to simply do list all bookings. So let's create a bookings DF and Spark dot table and dev dot Spark DB dot bookings, right? And what do we want to do? We want to list all bookings. So I can simply say bookings dot display, but list all bookings made by guests. So what I can do, bookings DF dot where clause, I can put a where clause. Where I can use my function member type PUDF and pass the member ID. ID and check if that returns guest. Filter only those bookings where member type PUDF on member ID is returning guest, right? And once that is done, I want to display it, right? That's all. And let's run it. We got all bookings where member ID is zero everywhere because zero represents guest and we got it, but what is the benefit of using a function to check this member IDs guest or it is member? We are centralizing the logic at one place in one function, right? Otherwise in my data frame code everywhere I have to write something like case when member ID call to zero, then it is guest else it is member. So instead of writing that case statement wherever I want to check whether this ID represents a guest or a member, I can centralize the logic in one function and use everywhere that same function and if the logic changes in future I have to modify this function only and my rest of the code works as it is without any change. That's the reason we create functions for business logic, right? So that's all. You can use it like this. And in this example I'm using this UDF in the where clause. I can use this UDF anywhere I can use Spark function, right? But as we learned, UDFs are not by default automatically registered in SQL scope. So if I want to use it in SQL I have to register it in a Spark session, then only I can use it in SQL. So how to register approach is same as Spark dot UDF dot register and register it with a name. What name I can decide what is the function. This is the function that I have defined this function I want to register, right? This function and what name I want in SQL. I can register it with the same name member type PUDF. or let's call it get member type. I want this name. I don't want this name. This is the technical name. This is a SQL name, right? So I can use that also, let's run this function is registered. Now I can use it in SQL, right? So percent SQL, I can say select is star prom, dev dot Spark DV dot bookings where where get member type, get member type and pass member ID equal to guest. Guest, right? Let's run it. And I get the same result, right? So my actual function name is member type PUDF, but I register it as get member type. So I can use get member type in my SQL expressions. Now can we use it in EXPR? Yes, we can. Let's do that. Let's assume that I want to create result DF on bookings DF and apply a where condition and write a where expression itself as a string, right? We don't need to apply EXPR in the where transformation because where expects expression, right? So what I can do I can use the same thing that I've used in SQL, that kind of expression here, right here. Maybe this goes as single code because double code inside double code is a problem and then I can display it, right? So this should also work. Worked. Remember I registered the UDF with this name. So in the expressions SQL like expression, I have to use this name. I cannot use the actual name because the function was not registered with this name, it was registered with this name, right? But in typical case, we register the function name with its original name to avoid confusion. So that's all about Vectorized UDF and Vectorized UDFs are comparatively faster. Why? Because in this kind of expression where I'm saying bookings dot where and I'm saying call this function with the member ID and check for the result as guest, behind the scene Spark will vectorize this operation. Vectorize this operation means Spark will take maybe 10,000 member IDs, pack it into a array, pass it to the function. Your function will evaluate all 10,000 values in one single function call and the result will be returned as a array of the results. 10,000 results in a series and Spark will check it for those 1,000 records at once. So this thing behind the scene happens a lot of things to vectorize this operation that is taken care of by the Spark engine itself and these functions work faster because they cut down the number of function calls significantly. That's all for this lecture. See you again. Keep learning and keep growing. (upbeat music)