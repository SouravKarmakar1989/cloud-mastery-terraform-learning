Welcome back. In this lecture, I'll give you an introduction to User-Defined Functions in Apache Spark, and we will learn how to implement User-Defined Functions in Apache Spark and how to use it. So let's start. So here I am in my dev environment. Let's go to a Spark UDF and unit testing and start with the introduction to UDF. This is the notebook. In this notebook, I will help you understand what is Spark UDF, why do we need Spark UDF, and how to implement a Spark UDF. So let's start with the most basic question. What is User-Defined Function or Spark UDF? UDF stands for User-Defined Function. User-Defined Function means you can create functions and use them in Spark programs. What are functions? Simple, functions that take input, do some processing, and return the output. That's what the function is. And User-Defined Functions means we can create customized functions in Spark. Spark gives us a capability to create User-Defined Functions. Second question, why do we need UDF? Why do we need User-Defined Functions? The reason is simple. Spark already gives us a bunch of functions. It gives us mathematical functions, string manipulation functions, date manipulation functions, aggregation functions, windowing functions, statistical functions. Lot of functions are already provided by Spark, but all those functions are general-purpose functions. They are used to develop the logic or solve general problems. But what about business logic? What if we need a function that implements some business rule or business logic? Spark cannot provide that because business rules and business logic are specific to the organization. That's why Spark offers capability to create User-Defined Functions or custom functions where we can implement whatever we want, right? We can create a function which takes input, does some business logic on the input, and produces the result as per the business logic. So that's why we need User-Defined Functions. Now let's talk about what are the different types of functions we can create in Spark. We can create three types of functions: Python UDF or Scalar Python UDF, second type is Pandas Vectorized UDF, and third type is User-Defined Table Functions. These are the three main types of functions in Spark. They're slightly different. Scalar Python UDFs are the most basic ones. Pandas Vectorized UDFs are advanced high-performing functions, and User-Defined Table Functions offers us a capability to return multiple records from the function. Otherwise, these two allow us to return single record as an output. UDTF allows us to return table-like structure or multiple rows from the function. It'll make more sense when we start doing the example in this lecture. We want to learn about Scalar Python UDF, the first type. So let's try to understand what are Scalar Python UDF. These are User-Defined Functions, means we can define the function. They take or return Python object. A function input must be a Python object. Function output must be a Python object. Python object means it could be Python string, could be Python int, it could be Python double, whatever, right? So, but it must be a Python object. These Scalar Python UDFs operate one row at a time. So you pass one record as an input, process that one record in the function, implement the logic on that one record, and return one record or processed record as an output. One record at a time. It processes one record at a time. And last point that we need to remember is for Python UDF, input and output are serialized and deserialized by Pickle or Arrow. Pickle is the default serialization for Python. So your Python function or UDF input and output values are serialized by Pickle by default, or you have an option to choose Arrow as your serialization. The serialization library which performs a little better compared to the Pickle. So these are the four features of Scalar Python UDF. Now let's start learning how to define Python UDF. So we have a very simple requirement. Define a User-Defined Function or a UDF. And here is the requirement. Create a UDF with the following functionality, which takes input and produces the output. The input should be member ID, and output should be based on the member ID. We have a business logic which says if member ID is zero, this member is a guest member. If member ID is anything else, this member is a regular member. So we want to implement that business logic in a function which takes the member ID, will look at the member ID, and return guest or member. So input is like a string, and output is also a string. That's all. So how can we define a User-Defined Function with this functionality? Let's do it. For defining a User-Defined Function or a UDF, we need to import a specialized function called udf. So from pyspark.sql.functions import udf. udf is an annotation that allows us to define a User-Defined Function. So we say @udf, use this annotation @udf, and then we tell what is the return type of the UDF. So return type of my function is a string. So we simply tell a string, and optionally we can tell should I use Pickle by default for serialization and deserialization or should I use Arrow for serialization and deserialization? It is recommended to always use Arrow because Arrow is fast. So we can say useArrow=True, that's all. We don't tell what is the input type for my UDF. We simply tell only the output type. The return type of the UDF. Return type is a string, and useArrow=True. Now we can define our function. Defining function is like defining a Python function. So we can say def and give a function name, let's call it member_type_udf function name. And then define the input parameter. It should take ID as an input, and the type of the input is str, right? That's all. And start defining the body. What do we want to do in the body? Super simple. Return guest, right, guest if ID equal to zero, else return member. For understanding how to define a function, I'm building a super, super simple function here. One-line logic and simply return the result. But you can create a function with your entire business logic as complex as you want, and that should not be any problem. And that's all. This is how we define the function, right? A User-Defined Function. Let's run this. And your User-Defined Function is defined. That's all. Spark User-Defined Functions are defined for the current session, and they can be used in the current session only. They cannot be used across different sessions. It is not like that you define this User-Defined Function once and that's all, you can use it in different, different places. No, every time you want to use this, you have to make sure that it is defined in the same session. So once it is defined, I can use the UDF, but how do I use the UDF? We use UDF in the same way we use any other Spark function. So let's say I want to read members data, create a DataFrame, and add a member type column to that DataFrame. And for adding the member type, I want to use the member_type_udf. So how can we do that? Let's create a member_df first, member_df equal to. How can I create a spark.table? Simply read the dev.spark_db.members table. I will get the DataFrame. And what I want to do, I want to do some DataFrame transformation and prepare a result DF. And the transformation is super simple. member_df, we start with member_df and add a new column in the member_df, call it member type. That's all. And how can I get the member type? What is the logic for identifying the member type? We have a function member, in fact, this UDF. We have a User-Defined Function, UDF, that takes member ID as an input and returns what is the member type. That's all, right? And then once we have the result, I can display the results. So df., let's display only few records. Let's display only three records, and see if my UDF is working or not. Member ID, first name, last name. These fields were there. At the end, I added one more column, member type. And this is guest, this is member, this is member. So that's all, it works. It works like any other Spark function. I can define my own User-Defined Functions and use it in DataFrame transformations like any other Spark function, right? But most of the Spark functions are also available in SQL, right? In Spark SQL. So can I do something like this? Like in SQL, %sql select star from dev.spark_db.members, limit three, let's do this. This is a super simple SQL which will work, but can I do a select star comma this function? Can I run this function here? This is my User-Defined Function on member ID. Can I do this? No, cannot resolve routine member_type_udf. So this User-Defined Function is not known to the Spark SQL. It is only known to the Spark DataFrame. It is defined in the DataFrame session, and it can be used from the DataFrame. It cannot be used from the Spark SQL even in the same session. So these User-Defined Functions are limited to DataFrame. When I say limited to DataFrame, that means a similar kind of approach, like copied here. We know that most of the functions we can use inside the expr, right? So let's say from pyspark.sql.functions import expr. And we learned that we have two approaches to developing expressions. This is known as column expression. I can also do expr and put all this inside the double quote, use the SQL-like expression and get it done, right? This kind of expression using expr also doesn't work in Spark for User-Defined Function. Same error, cannot resolve routine member_type_udf that we got while using it in the SQL. Cannot resolve routine member_type_udf, why? Because expr evaluates this expression as a SQL expression using SQL engine. And SQL engine doesn't know what is member_type_udf, right? It is only known to the DataFrame. So we can do it like this, but what if I want to use it in SQL like this or in expr like this, what can I do? Can I use it? Yes, you can do that. All you have to do is to register your User-Defined Function in the Spark session or in the Spark SQL engine. So how to register? A spark.udf.register, that's all. Register what? Register my UDF with a name. What name I want to give? Same name. Register my UDF with the name member_type_udf. And what is the User-Defined Function that we want? Same member_type_udf. So this is the function, and this is the name in the SQL engine. It will be registered with this name, and function must be already defined here like this, right? This function is already defined. Once it is defined, I can register it. And once it is registered, I can use it in Spark SQL. It works now, and you get the output member type UDF. I can use alias as member type. That's not a problem, right? And we get the results. Since it is registered in SQL scope or in SQL engine scope, I can also use it like expr in the DataFrame. So this also works, right? And we get the member type here. So that's what we wanted to learn in this lecture. We wanted to understand what are Scalar functions or Scalar UDF in Apache Spark, how to define that, how to use it in DataFrame. And if we want to use it in the Spark SQL or inside the SQL expressions, how to register it to the SQL engine scope. And then it can be used from the Spark SQL and it can also be used in these SQL-like expressions in the DataFrame. So that's all for this lecture. See you again. Keep learning and keep growing. (upbeat music)