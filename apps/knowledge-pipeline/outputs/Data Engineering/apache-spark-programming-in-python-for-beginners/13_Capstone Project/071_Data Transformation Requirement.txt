Welcome back. I talked about the high-level project requirement and scope in the earlier lecture. Here is the diagram. So we have entity data in the hive tables. We are asked to read data from the hive tables, transform it according to the requirements and send it to Kafka. Now let's talk about the data and the transformation requirements. You are asked to read the raw data about the entities and prepare it for sending to Kafka. Let me explain the requirement for one entity. We have an account entity. The raw data for the account entity is exported into three hive tables. Here is the list of tables. So, the account entity is made up of combining data from all three tables. Let me show you some sample records from all three tables. So we have some records in the account table, as shown here. You have an active indicator, account id, account start date, legal title one and legal title two, etc. Each account is linked with one or more parties. You can see the account id and party id in the parties table. The party id is the primary key of the parties table, and the account id is a foreign key from the account table. One account may have one or more parties associated with it. Similarly, each party may have an address. Simple. Isn't it? These three tables are for one entity. We have a few hundred entities, so the complete project for moving all entities is an enormous requirement. But for our purpose, we will work for one entity, and the same approach is applied to other entities in the bank. Make sense? Great! So you now understand the input data. We have three tables, and we need to read all three tables, combine data for each account and create one entity record. In this example, we have ten unique account ids. So the end result should send ten account entity records to the Kafka topic. Each entity record combines account information, associated parties, and addresses. Now let me show you one final record. Here it is. I opened one sample output record in the visual studio code. The final output to Kafka must be a JSON record. Everything is collapsed, so you can see the top-level structure of the JSON message. We have three top-level fields in the JSON message. Event Header Keys and Payload The JSON message is a generic structure that is being followed for all the entities. We are working on the account entity. So let's see how account entity data fits into this JSON message structure. Let me explain the event header. Great! So the event header has got five items. eventIdentifier is a UUID for each record. You are asked to dynamically generate it for each entity. eventType is a fixed string. For the account entity, it will be SBDL-Contrat, as shown here. Then you have majorSchemaVersion and minorSchemaVersion, which are also fixed numbers. eventDateTime is the timestamp. This was when the event was generated for sending it to Kafka. So basically, it is a current system timestamp. Now let me collapse the header and expand the keys. The key is a JSON Array. You can see the entity symbol here. Each element of the key is a struct of two fields. key field name and keyValue We have a single key field for the account entity. The value shown here is the account id. The key field name is contractIdentifier. But that's nothing but account_id. In the banking system, you are told to represent an account as a contract. So the account_id is sent as contractIdentifier. The key is an array. In our case, we have only one primary key field, so you will see only one element in the array. But some other entities may have composite keys. And they will be sending all the composite key fields in this array. But for the account entity, it is as simple as a single key field. That's all about the keys. The last one is the payload. And that's the actual data. Let me collapse the key and expand the payload. Great! So we have eight fields in the payload. The contractIdentifier is the account_id. Let me expand it and show you. So, each field goes as a set of three values. operation oldValue and newValue In the case of the INSERT operation, the old value is null, and hence it is not shown here. For our requirement, everything is an INSERT operation. We are not implementing the UPDATE operation in this phase. But in future phases, we will implement the UPDATE operation, so the JSON message is designed like this. The point is straight. Each field goes as a set of three values. operation oldValue and newValue For the current project, the operation type is always INSERT. The oldValue is always null and should not appear in the JSON message. The newValue is the field value from the table. Make sense? Great! Let's look at the next one. The contactStartDateTime is the account_start_date field from the accounts table. And you can see the values here. I hope it makes sense now. Right? Most fields will be sent as a triplet of three values: operation, oldValue, and newValue. In our case, the operation is always INSERT, and the oldValue is NULL. Make sense? Great! But some fields are combined and have different rules. Let's look at the contractTitle. Let me collapse all other fields and show you only the contract title. Here it is. So the contract title is an array of all the legal titles from the account table. You can see it here. Let me show you the sample data. Here it is. Look at the third record. Acosta Inc is the legal title one, and David walker is the legal title two. Now come back to the JSON message. Both legal titles are combined and sent into an array of legal titles. So this is another complexity. The rest of the fields are simple, and you can easily understand the mapping from the account table to the JSON message. Now let's come to the partyRelations. The partyRelations is an array of all the parties and their address. This account has got three associated parties. So you will see three parties in the partyRelations array. The address is also embedded inside the party record. Let me show you the first party. Here it is. So we have partyIdentifier, partyRelationshipType, partyRelationStartDateTime and the partyAddress. And that's all about the data and the transformation. You have three input tables. You can download the sample data files for those three input tables. I also showed you one output JSON sample. You can download the sample JSON output. I recommend you download all these data files and look at the input and output samples to understand the mapping and transformation requirements. In the actual project also, you will be given requirements in the same way. You will get the input and the desired output. Then you are supposed to work out the transformation logic to achieve it. Great! And that's all about the requirement. In the following lecture, I will discuss an approach to how you can go about designing a solution and creating your project. See you again. Keep Learning and Keep Growing.