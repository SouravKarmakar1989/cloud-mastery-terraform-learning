In this lesson, we are going to be looking at the requirements for ingesting the qualifying file into the Data Lake. Again, we want to do pretty much what we did for the previous files. The main difference here is that, we'll be processing a set of multiple multi line JSON files as our input and then we'll write them into the parquet format in our Process container. The other requirements are to rename a few columns as highlighted here, as well as to create the ingestion date column with the current timestamp. As you can see, we've already done these kind of transformations. Also, we've done multiple CSV files and multi line JSON files separately. So I would like you to do this as an assignment, but before you start, I would like to quickly walk you through the file as well as the data type of the data items within the file. So this is where I've got all the files and as you can see, similar to the lap times for qualifying, you're seeing a folder rather than a file. So within qualifying we've got two files, so we want to load both of them together as one file into our Data Lake. Let's open up one of these. I'm going to open this in Visual Studio Code. And as you can see, this is a multi line JSON file with multiple objects within one file. And we got two of those files and the data types don't look any complicated, so we got a set of numbers as well as the last three, which are strings. So start with your assignment and good luck with that. If you get stuck. As I said, come back to the next video where I'll explain my solution.