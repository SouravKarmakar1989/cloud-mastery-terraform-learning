In this lesson, we are going to be looking at the requirements for ingesting the lap times file into the Data Lake. Again, we want to pretty much do what we did to the previous files. The main difference here is that we will be processing a multiple CSV files rather than a single CSV file or a JSON file. That's what we did in the previous lessons. The other requirements are to rename a couple of columns, race id and driver id, and also to add the current timestamp as ingestion date. As you can see, these are things we've done before. So our main focus here is to process the multiple CSV files. So let's quickly have a look at the file and the data types before we move on to writing any code. I downloaded the files here and as you can see, lap times is not a file, it's a folder. And if you open up the folder, you will see five different files. So basically, the file has been split into five different files. So let me open up one of the files here, and I'm going to do that in text editor. And let me make it bigger for you. And as you can see, it's a standard CSV file with quite a few numeric fields and then a string as well here. And one difference you may notice with the other CSV files we have had is, this doesn't have a header, but that's kind of what you normally get when the files are split. If it's split across five different files or any number of files, you won't know which ones got the header, so normally your files won't arrive with a header and you can define the schema as we've done before and use that to read our data. Let's now have a quick look at the data types. And as we saw in the file, it's quite simple and we got five numbers and one string field. So that's what we will define the schemas. So let's get started with writing the notebook to process this data.