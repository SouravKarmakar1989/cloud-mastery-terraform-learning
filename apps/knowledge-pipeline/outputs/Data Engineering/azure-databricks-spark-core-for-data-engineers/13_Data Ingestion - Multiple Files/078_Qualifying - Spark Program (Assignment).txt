Okay. Welcome back. I hope you've managed to complete the assignment successfully. If you did complete the assignment successfully, please feel free to skip this lesson, because I'm just going to walk through my solution here. So this is my solution, as we did with everything else, the first thing we're doing is to read the JSON files. So which is the list of files within the folder. And we've specified the data types here and the schema. The schema is quite simple. We got the top five, which are integers or top six, and the other three fields are strings. And this is the important one where, because our file is a multi-line file, we specified the option multi-line as True and also similar to the lap times file we processed, we got multiple files in a folder, so we specified the folder name rather than the individual file names here. And the next thing we want to do is to rename the fields as we wanted to have the underscores, as well as the ingestion date to be populated with the current timestamp. So that's what we've done here. And the last thing is very similar to everything else we did. We are write in the output of this DataFrame into our Processed container under the qualifying folder in parquet format. So let's click on Run all to execute all the commands and then we can run a Display command to read the file and see whether the data has been returned as we expected. Okay. As you can see here, all the data has been written as expected. So the main takeaways from this lesson are, in order to read a multi-line file, we need to specify the option as multi-line as True. And also in order to read multiple files from a folder, we specify the folder name, but if there are multiple files and you want to pick only specific files, you can specify the wildcard path. So that's the end of this lesson and I'll see you in the next one.