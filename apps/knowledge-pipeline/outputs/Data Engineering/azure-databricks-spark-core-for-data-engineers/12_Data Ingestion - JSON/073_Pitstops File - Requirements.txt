In this lesson, we are going to be looking at the requirements for ingesting the pitstops.json file into the Data Lake. Again, we want to do pretty much what we did in the previous files. The main difference here is that we will be processing a multi-line JSON rather than a single line JSON. The other requirements are to rename a couple of columns race id and driver id to have underscores in them and also add the new column ingestion date with a current timestamp. As you can see, these are things we've done before. So our main focus here is to process the multi-line JSON file. Before we start, I'll quickly walk you through the file and then we can start creating our notebook. Look, here is the pit_stops.json file. I open that in Visual Studio Code, like I did with the other ones. And as you can see, the main difference is actually JSON object is in multiple lines rather than one a line like the other ones. So the default JSON for Spark is a single line JSON. When you're processing a multi-line JSON, you need to explicitly tell Spark that it is a multi-line JSON. So that's what we'll show you in the notebook in a bit. But before we do that, let's have a look at the data types of these fields. So that's the Database User Guide. And as you can see, pitstops got integers and varchar which is a string and it's also got a time component. Time is a data type in My SQL, but there isn't an equivalent one in Spark. So instead we'll use a string type to get the time element. So that will be a format like this. I think we saw that in the data as well. So that's the time element, but we can bring that as string within our notebook. Let's get started with creating the notebook.