Hope you have successfully managed to complete the assignment. If you did, then please feel free to skip this video. Otherwise, I'll just quickly walk you through my solution. So the first thing we want to do is to read the JSON file. So I've defined the types we're interested in. So we've had the integer type, string type and the float type in our data. So I've defined the schema for that one with the StructTye and the StructFields as we've discussed previously. And then I've read the data using the schema. So that's what happens here. And now the next thing we wanted to do was to rename the columns and add the ingestion date. So basically this statement is important, the current timestamp because we need the current timestamp for our ingestion date and we've got all the columns we wanted to rename to include underscores rather than Camel case. So let's execute that one. And the next thing we wanted to do is to drop the column. So I've imported the function Col, because I'm using the function Col here, you could just skip that and just use the filename within the drop function, but I've just used the Col method as well. So the last thing we wanted to do is to write the data to our Data Lake using parquet, and we wanted to overwrite the data and also partition the data by race id. So let's execute that one. Okay, that's completed now. And let's look at the data to make sure that the data is written properly so we can use the display command and also we can read the data using the spark.read.parquet method. So as you can see, the data has been read properly and I don't see any problems with the data. That's brilliant. That's the end of this lesson, and I'll see you in the next one.