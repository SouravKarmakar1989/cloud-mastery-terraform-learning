In this lesson, we are going to be looking at the requirements for ingesting the results.json file into the Data Lake. This is a standard single line JSON file similar to the constructors.json file, that we processed previously. As we processed one of these types of files already, I would like you to do this as an assignment. Our transformation requirements here are to rename the list of columns as highlighted to make them snake case, i.e. with underscores. We want to drop the status id from our source data and also create the ingestion date as we did before with the current timestamp. We also want to partition the data and return to the Data Lake by race id. So let me quickly show you the file and also the schema for the file before you get started. This is the results.json file, that I opened up in Visual Studio Code. As you can see here, it's a single line JSON with no nested objects. As you can see, they're all simple objects that are no nested objects here. And it's got quite a few records, roughly about 25,000 records or 24,960 records to process. But it is quite simple, like we did before. Let me now show you the data types for the fields. So let's come over to the Ergast Developer API and under the Database User Guide, we've got the results table defined. As you can see, we've got a number of integer fields and these ones can be integer type since spark and this will be string type. And then a float can be converted into a float type as well. So as I said, it is a simple file and we've done one of these kind of files before. So I would like you to go away and create a notebook to process this data and come back to the next video if you get stuck. But good luck with your assignment. See you then.