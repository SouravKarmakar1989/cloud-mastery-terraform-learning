In this lesson, we are going to be looking at the requirements for ingesting the drivers.json file into the Data Lake in our Process container. We want to do exactly the same as what we did to the constructors file. The only difference is that the JSON has a nested object. As you can see, the forename and the surname forms the object name. I'll walk you through the file shortly to show you what I mean by that. In a meantime, our transformation requirements are to rename the columns, driver and the driver reference and drop the URL as we did before. The new requirement here is to create a new column called Name to contain the concatenated string from forename and surname in our source file. And also to create ingestion date as we did before. We'll focus most of our time in dealing with the nested objects and we'll speed up the implementation of the rest of the requirements, because we've done that before. I'll quickly walk you through the file before we start writing any code. But first, I'll quickly walk you through the file before we start writing any code. Let's open the file in Visual Studio Code and then we can look at the data. As you can see here, we've got driver id, driver reference, number, code and all of that. But we've got this column here which is the name object, but it is another nested JSON object with the surname and the forename. So I'll show you how to write the schema to be able to read this data and also concatenate these two fields together to create a column called Name in our parquet file. So let's get started.