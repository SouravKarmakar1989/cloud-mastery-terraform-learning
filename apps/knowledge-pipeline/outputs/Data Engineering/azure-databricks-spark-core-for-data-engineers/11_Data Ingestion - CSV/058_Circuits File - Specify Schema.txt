Now that our DataFrame contains the right data and the header being identified as well, let's now focus on the scheme for our data being read into the DataFrame. So in order to see the schema of our DataFrame, we can invoke a method called printSchema. So that's again part of the DataFrame API. So you come over to DataFrame API and then there is a method called printSchema, you can search up again, as I said here or this is the documentation for printSchema, it's quite simple. It tells you the columns and the data types and whether they are nullable or not. Let's call that method on our DataFrame, and that will be circuits df.printSchema. As you can see here, the columns are fine, but the data types are all identified as strings, which doesn't look right. As you can see, the circuit id is an integer and there are some doubles and integers here as well. There is another method in our DataFrame API, which gives you some information about the data within our DataFrame, so that we can identify what data type it's supposed to have, so that's called describe. So let's do that, circuits_df.describe, and then you're going to do a show on that one. And that will tell you, the min and the max values on each of the columns and the mean, counts and things like that. That kind of helps you identify whether the data types are correct. So as you can see here, circuit id is just numbers, it's starting from 1 to 9 and we've got circuit reference, that's a string field. And these are all strings and the latitude and longitude looks like they should be doubles and the altitude is again an integer. So there are a couple of things we can do. So let's start with the first one, which is the DataFrame Reader API also gives you another parameter to pass into our csv method called inferSchema. So let's have a quick look at that. So the inferSchema is by default set to false. If you turn it into true, then it will go through the data and identify the schema itself. So let's try that first. So let's go back to our options here and add another option. And that's going to be inferSchema as true. While we're on this, let's also make it a bit more readable because it's one long line. So I'm going to just break it down into separate lines so it's a bit more easily readable. So let's execute that. And if we look at the printSchema now and as you can see, that's identified the circuit id as integer and the latitude, longitude as doubles and the altitude as integer as well, which is great. But one thing you may not have noticed is actually we got two Spark jobs now, but previously we would have had only one. So I'm going to just try and execute that one more time without the option here. As you can see, there is one Spark job here and there are two Sparks jobs here. So when you specify inferSchema as true, what's happening is a Spark going through the data and reading all of the data, identify what the schema should be and then apply that schema to the DataFrame, which is not efficient because it has to read through the data. So you don't want to be doing that. And in a production environment, it's going to be an awful lot of data and it could just slow down your reads. And also if you get data which doesn't confirm to what you're expecting, you want your process to fail and tell you that there is something wrong rather than just inferring the schema and carrying on. So this is okay to use if you are endeavor test and also sometimes it's okay to do it if you are dealing with very small amount of data. But in order to work on a production level project and also massive amount of data, you want to specify your schema and then ask Spark to use that. And if the data doesn't confirm to the schema, you want it to fail. I'll now show you how to do that. In order to specify the schema, we need to understand the data types in DataFrame. So let's switch over to the documentation again. And if you go back to the API reference and then you've got the data types here. So under data types, Spark provides you a number of data types here, you can go through each one of them to understand them in a bit more detail. But the ones we are interested in here, is the integer type for circuit id. And then we got a few string types and then we've got the double type for things like latitude longitude, and then there are two special ones you need to understand as well. They are the StructField and the StructType. So let's start with the StructType. StructType is going to consist many StructFields. So the type is a wrapper around all your StructFields, this is basically representing a row. The simplest way to think about it is the StructType represents your row and your StructFields represent your fields or individual columns. So normally we would specify a type and within that, we'll specify the number of fields and each field is going to have its own data type as well. It'll all clear. It'll all become a bit more clearer when we come to define that one. And one more thing to know is actually this is part of the class PySpark.sql.types. So the first thing we want to do is we want to import these types. So I'm going to do it here. So let's start with that. So it'll be from PySpark.sql.types, and then we're going to import the data types we are interested in. Let's execute that. And that succeeded. And now the next thing we want to do is to specify a schema. So I'm going to set it to a variable called circuits_schema, and that's going to contain our schema basically. So we want to start with the StructType because as we said, that's the row or that's the wrapper around our fields. And within that we want to specify the fields so that fields equals. And then here you will specify your list of fields. So that is an array. So within that, let's start with the first field. And that is our circuit id. And the next parameter for that is the data type. So in our case, that is the integer type. So you can tab out and then you can use the autocomplete to complete it. And the next parameter we want to specify is whether that's going to be nullable or not. So in our case, this is a primary key. So let's go for a false for that one. And now we can fill in the other fields similar to what we're doing here. And let's execute that. So I made a spelling mistake here. So that should be fields, so let's correct that. And now that's been executed. So we got a schema here for our DataFrame. So the next thing is to use that schema to read the data. So in order to do that instead of inferSchema, you specify your schema itself. So that's another parameter to our DataFrame Reader API. So that's going to be our circuit schema. Let's do that. And I'm going to just execute that now. And that's executed. And let's run the display just to make sure we got the right data. And that looks okay to us. And the important thing is to print the schema. And as you can see, the data types are as we expected, and we've used our schema to do it. So let's go back to the schema that we specified and just talk about it one more time very quickly. So as I said, StructType is going to represent your rows and the StructFields are going to represent your columns and you specify the data types for each of the columns as well. You might be wondering what do you do if you have a nested structure? So if you had an nested structure, then actually you would have further StructTypes within your StructFields. We've got a driver's file which has got a nested structure and at that time we'll use that and I'll show you how to do that as well. So this is one way of doing it and there is another way of doing it as well. And that's called DDL based schema. I'll show you that in one of the other ingestions as well. But I normally prefer to do this, when I'm working on PySpark. So before we finish this lesson, let's go and tidy up because we got quite a few DataFrame reads here, we don't need this anymore, so I'm going to just delete that one. So that's gone. So we need that statement, we need that one and we have read the data and that's all we need. Let's leave that one for now. We might need it later, but we fixed our schema so we don't need that and we don't need this as well. So that's everything. And I'll see you in the next lesson.