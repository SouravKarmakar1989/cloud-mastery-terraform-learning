In this lesson, we'll take a look at adding the new column called Ingestion Date, and which will hold the current timestamp. So let's make a start. Let's start by looking at the documentation. So in order to do that, again actually if you go to DataFrame APIs, there is a API called withColumn. So we looked at the withColumnRenamed previously, but withColumn will let you add a new column. So as you can see here, it returns a new DataFrame by adding a column or replacing an existing column that has the same name. You can use the withColumn to add a new column, but in our case, we also want to populate that new column with the current timestamp. In order to do that again, if you come to the API reference and if you look under functions, you're going to find a function called current timestamp. So that function returns the current timestamp at the start of the query evaluation as a TimestampType column. It returns a TimestampType column object, which you can add to your DataFrame, and then the DataFrame is going to have the additional column. So let's do that now. I'm going to call this one as a circuits_final df, because we got mostly everything done at this point and we're going to use the circuits_renamed df as our source and we want to use the withColumn. So the withColumn method takes two parameters. The first one is the column name. So in our case we wanted to call our column name as ingestion date. And also we want to give it the value it needs to take. That's going to be current timestamp function. So that is current_timestamp. That's the function. It's not going to work. It's going to give you an error because the function has not been imported. So I'm going to import the function again. So that will be PySpark. sql.functions and import, this is going to be current_timestamp. So let's execute that first and then we execute the circuits final df and let's display the contents of this DataFrame. And as you can see, we got the new column called Ingestion Date, and that's got the current timestamp, I am recording on the 30th of April 2021 at 14:58. While we're talking about withColumn, I want to talk you through, one more thing. In this example, the current timestamp returned a column object, so we were able to add it to our DataFrame, because I see no DataFrame is rows and columns and we are adding a new column, so that has to be of column object as opposed to a literal value. So you might be wondering if you wanted to add a literal value to every record within your DataFrame. How do you do that? So there is a function to do that. For example, let's assume that in our case we want to add another column, and that's going to be our environment. So let's say the new column is env, and it's going to have a value of say, Production. And if I run this one, it's going to fail now, because that is not a column. So that call should be a column object is what it's saying. There is another function which is called lit. Let's go back to our documentation again. And if I look for lit, for lit. It's a very simple function. All it does is it creates a column object from a literal value. So you would have to, Import the function lit. So let's add that to this import. So that's done. And all you have to do is wrap this literal value with the lit function. So if I run this one, Okay, I've lost a bracket, so I'm going to add that back. And if I run this one, it's going to have the literal value production added to it. So let's have a look at the display and I see you can see every record in here is going to have the production, value added as the environment. We don't have that as a requirement. I just wanted to show you that. So I'm going to take this out and also the withColumn statement from here and rerun these commands and they all look okay still. And now let's get rid of this display statement there, we don't need it and we've done everything we wanted to do here, so that's the end of this lesson and I'll see in the next one.