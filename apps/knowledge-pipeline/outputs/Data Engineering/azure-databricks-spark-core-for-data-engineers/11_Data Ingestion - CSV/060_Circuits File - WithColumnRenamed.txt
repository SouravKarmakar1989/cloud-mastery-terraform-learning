Okay. In this lesson, let's take a look at renaming the columns as we wanted. So our requirement was to rename the circuit id, circuit reference to have underscores in them, as well as lat, lng and alt to be renamed to latitude, longitude and altitude respectively. So let's make a start. So there are a couple of ways of doing that. As we've seen, we could have used an alias, after the call function here to rename the columns. That's one way of doing that. We've already looked at that. But I want to show you something which is specific for renaming columns. So that's an API called withColumnRenamed. That's part of DataFrame API. And as you can see it is, it returns a new DataFrame by renaming an existing column so it doesn't add a new column. You rename an existing column. So it takes two parameters, the existing column name and then the new column name and the DataFrame being created would have the new column names in them. So let's switch back to our notebook and then do that for these things. I'm going to call the new DataFrame as circuits renamed df. And we want to select from the circuits selected df. And I'm going to tab out and get the DataFrames listed. And then now we want to use width column renamed. And we want to specify the columns we want to rename. So in our case, we want to rename the circuit id. And we want the new column to be secure_id. And similarly, let's do the other columns as well. So that's done. So let's execute this statement now. And the new DataFrame has been created. So let's have a look at the data in the new DataFrame. So I'm going to use the display command again to display the DataFrame. And as you can see, these two have got underscores in them. And the latitude, longitude and altitude have got the full names in the column names.