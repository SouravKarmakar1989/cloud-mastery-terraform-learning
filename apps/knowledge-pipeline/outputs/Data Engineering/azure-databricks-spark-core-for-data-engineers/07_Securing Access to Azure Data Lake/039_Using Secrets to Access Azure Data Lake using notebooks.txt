Now that we've done all the groundwork that's required to secure the secrets and also understand the utility that's required to get the secret values at runtime, let's implement that for one of our notebooks, so that it uses the Secrets Utility to get the secret to access the Data Lake. Let's switch over to the Databricks workspace and get started. Here we are in the Databricks workspace. Let's implement the solution for our first notebook, which uses the account keys to access the storage account. Let's add a new line at the top of the notebook, to get the secrets from our Secret Scope and the Azure Key Vault. As we know, we can use the dbutils.secrets.get method to get the secret values. So I'm going to use that here. And as we saw in the last lesson, it needs two parameters. One is the Secret Scope and the other one is the Key. So I'm going to feed them in as well. And if we execute this statement, it will bring back the secret value. Let's assign this value to a variable, so that we can use that variable in this statement here, instead of the hard-coded value. So I'm going to assign that to a variable called formula1dl account key. And let me execute that. And I'm going to simply use that variable here instead of the actual value itself. And let's execute the Spark configuration here. And that's successfully executed. And if we did everything right, we should be able to access the storage account now. So let's try the dbutils.fs.ls statement here, to list the files. And as you can see here, it's listed the file successfully. And let's read the file and see the contents as well. And that successfully executed as well. So as you can see, we now use the secrets from our Secret Scope and the Key Vault and use that within this notebook to successfully access our storage account and get the data from here. And we've got no hard-coded values within our notebook. That's the end of this lesson. I'll see you in the next one.