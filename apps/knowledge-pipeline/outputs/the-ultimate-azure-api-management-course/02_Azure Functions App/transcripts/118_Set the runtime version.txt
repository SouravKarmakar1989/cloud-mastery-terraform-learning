Hi, everyone. In this video we are going to talk about the runtime version for function app. And let me go to one of the function apps we have already created before. And if we go to the configuration here, we should be able to see the runtime version of our function app. At high level. There are four main major versions of function app. Four, three, two and one. And by default, every time you create a new function app, it's going to get created based on the latest runtime version available, which is version four. As of now. And then if you want to downgrade it to run on version three, for example, you can do that by updating functions, extension version and change it from four to be three. Now, you might have noticed that we only specify the major version. So what about the minor version? So first of all, when we specify the major version only. What's going to happen when Microsoft release a new or minor versions? What's going to happen to your function app is that it's going to get updated automatically to the most recent minor version because usually minor version updates don't introduce breaking changes, as Microsoft said in their documentation here. If you only specify the major version, the function app is going to be automatically upgraded to the new minor version when they become available. Now, you might not like that and you want to have full control on the version number that you are running. In this case, you can go ahead and specify a minor version for your function app. However, you need to be aware of that every now and then. Microsoft removes the older versions from the platform, and if this happened to you that you are running in a minor version that Microsoft decided to remove from the production environment, what's going to happen is your function app is going to get upgraded to the latest version instead of the one that you have been already specified. So to avoid this from happening, you need to stay on top of your versions at all time and to make sure that they are all supported. And as I said before, in the app settings here, you can change the function extension version to upgrade it or downgrade it. That's all I have for you in this video. Thanks for watching and I'll see you in the next video.