Hi, everyone. In this video we are going to talk about the runtime versions for function app. And primarily we have two main versions. Version one, and it's going to get out of support in the next few years. And version four, which is the recommended version and the default one and supports all languages. All right. From where we can find the runtime version of my function app. If you go to configuration here, we should find an app setting that has the runtime version of my function app. And as you can see here, it's set to version four. However, you should avoid doing any changes to this app setting if you want to change the runtime version for your function app. The best way to do it is to head to the function runtime Settings tab and change your runtime version. And since I already run on the latest version, which is version four, I don't have any other option. However, if you are running your function app on runtime version one, 2 or 3, you should have an option to change it to version four. All right. Now, let's get back to the documentation here. If you still running your function app on version one, 2 or 3, you should consider the migration to version four, because as I said, version four is the recommended version to run your function app. And as I mentioned before, you shouldn't change the runtime version in the app settings directly. If you want to change it, you need to change it from function runtime settings tab in the configuration area in the function app. All right. We have talked a little bit before about how to pin your function app to a specific minor version, generally speaking. When we create our function app, it gets assigned to a major version. And when this happens, any updates to the minor versions are going to apply it automatically to your function app. However, in some situations you might want to pin your function up to a specific minor version to avoid applying the latest release automatically on your function app until you have done proper testing. And if this is the case, you can pin your function up to a specific minor version. Having said that, every now and then, Azure periodically cleans and deletes old minor versions from the function's runtime. And if your function app pinned to one of these minor versions that got removed from Azure functions, then what's going to happen is your function app is going to be automatically upgraded to the latest runtime version available at the moment. Technically speaking, there is no correlation between the runtime version and binding extension version for your function app. However, starting from version four, there is a requirement to have a minimum version number for all trigger and binding extensions. And if this happened to you, you just need to update your NuGet package to have the minimum version number that supported runtime version four for the function app. And as we mentioned before, version two and three has been already retired and gone out of support, which means there is no new features, security patches or performance optimization for runtime version two and three. For function app. That's why you should consider migrating to version four as soon as you can. And if you are developing your function apps locally using Visual Studio, if you remember, we will have these two app settings in your Visual Studio and I'm going to show it to you in a minute. So I'm going to open my Visual Studio here and open one of the previous function projects we have already created before. Let's open function app 17. And then I'm going to go to my project file here. You should be able to see these two app settings, one that set the target framework dot net version six, and the other sets the Azure function version, which is version four. And these are the two appsettings been mentioned here. And starting from version two, the runtime started to use a new binding extensibility model, which has few benefits. The most important benefits is that decoupling between the runtime and the binding. And this will allow you to have a binding extension set to a specific version that's separate from the runtime version and the completely independent. And there is a support for third party binding extensions, lighter execution environment as well. And in this table here, you will be able to see the different triggers and bindings in and out for different Azure services and the supported runtime version. And we've already covered a few of them already in previous video. And you have talked about the time duration for the function app before, and it's going to be determined based on the plan you are using to host your function app. The consumption plan has a five minute default timeout and a maximum ten minutes while premium and dedicated plan has a default 30 minute timeout and unlimited maximum timeouts. This is what you need to be aware of when it comes to function app runtime version. That's all I have for you in this video. Thanks for watching and I will see you in the next video.