Hi, everyone. In this video we are going to talk about what is Azure Functions app and what its capabilities. In the most simplest terms, Function app is one of the Azure Serverless Solutions allows you to do three things write less code, maintain less infrastructure and pay less money so you don't have to worry about the infrastructure or servers that are running your functions app anymore. It's completely managed by Azure and you're going to be just focused on the code you write and host in your function app. And this could be a way to improve your productivity and focus on what really matters to your business. Azure function supports many programming languages C Sharp Java, JavaScript, PowerShell, Python, Golang and other, and you get to choose your preferred programming language that you want to use when you create a function app. Now let's talk a little bit about the capability of function app. What are the different scenarios where function app could be really helpful for us? Let's say that we have two systems. One system is generating documents or data files, and then they place it in blob storage. We can configure a function app to be triggered every time a new file is placed in the blob storage to do some data processing, then moving the data to the other system. So we could use the function app to process file uploads. Moving on to the second scenario where we have a system, maybe stock market or Iot device wearables that are producing a lot of data that are going to get ingested in Event Hub. We also can use the function app to be triggered based on the data coming through the event hub to do data processing, then moving the data on to Cosmos DB, which after that is going to be available through dashboard visualizations. So we could use function app to handle real time stream or event processing. Moving on to the other scenario where we have a machine learning algorithm that generates data classification requests and we put it in a queue. We could use functions app to be triggered based on the requests coming in the queue to do the required processing, then generating the classification results. So we could be using the function app in machine learning and AI processing. Moving on to the other scenario where we have a database that requires the cleansing or de-duplication. We could have a function up that is going to be triggered every 15 minutes or so to do data cleansing tasks and do any de-duplication required in our database to make sure that we are not sending multiple communications to the same customer. So we could be using the Functions app to run scheduled tasks. Moving on to the other example, we could be using the function app. To listen to Http endpoints. So the customers could send their requests to the Http endpoint and behind the scene is going to trigger a function app to do some processing and then generate the output. So we can use the function app to build a scalable web API architecture. Moving into the other situation where we could have multiple functions up, each of them is responsible for a specific part in the process and the execution is going to get through to the next function app only when it finishes with the first one. Let's assume that the customer wants to do cash withdrawal from a bank. Let's assume these functions app. The first one is going to do identity verification and then the second one is going to do the balance check. Then the third one could do cash withdrawal. So we could use functions app as a function chaining. Another scenario here we could have is fan out fan in assuming a customer wants to do car flight, hotel booking so we could have functions app to get the customer request and then we are going to fan out the execution to multiple functions up. One function app is going to be responsible for flight booking and the other functions app is going to be responsible for the hotel booking. And the third Functions app is going to be responsible for call booking and then we are going to find in the result from all of these booking functions up and send them in into one function up to know the result of all of these three separate functions up. We could also have functions app as an aggregator to aggregate separate events coming through different channels and we can aggregate them through having a functions app to do events correlation. Also we could use Functions app to monitor the execution of a long running process and update the customer with the results. And maybe we could have a functions app behind an API gateway. And then we are going to receive an Http request on the API gateway and then we are going to pass it through to the Functions app to do a long running process, then get the result back to the API management so we could use the function app to implement asynchronous Http APIs. Finally here we could use functions app to get human inputs. Maybe we want manage manager approval for an expense report before going ahead with the purchase. We could use the function app to implement this as well. Moving into the other scenario where we could have a functions app that is going to be triggered for changes that happen in the database and then the function app is going to send the notifications to the right customer. And finally here, let's say that we have messages coming through a message queue in Azure service bus and we can configure a function app to be triggered every time a new message placed in the queue. And these messages could be coming from your web application or maybe from a third party system. And then the Functions app is going to be triggered to process these requests and update the data in Cosmos DB. That's not all. These are just very common scenarios for using functions. App. However, Functions app is capable of doing a lot more than that as we are going to see in more details in this course. That's all I have for you in this video. Thanks for watching. And I'll see you in the next video.