In this video are going to talk about the core concepts of function app. And regardless of the language you are going to be using or the binding you are going to specify in your function app, these concepts are going to stay the same. So let's go to Function app and let's head to the very first function app we have created. And as you can see here, we don't have any functions in this function app. And this is why we cannot execute anything on this function up. Yes, we have a URL here that we could hit and it's going to tell us that the function app is up and running, but we haven't specified any code from our side on the function app to execute it. And in order to do that, we need to go ahead and create a function. So the function in function app is the cornerstone of the execution. In order to be able to execute our own code, we need to create a function in the function app. So I'm going to go ahead and create an Http function. All right. My function has been created. So let's go to the code page. And as you can see here, we have three files Readme file. Leave it aside for now. We have run the CSX and this is the C-sharp code that we have on our function. We can go ahead and test it out on get Http and we should be able to see the result in the output tab. Which is all right. If your function is simple and straightforward, you can do your changes in the portal using the inline code editor here. However, if your function is a little bit more complex, that requires a little bit more triggers and bindings to be configured based on other Azure services. Then in this case you should go for the local development tools and use either Visual Studio Code or Visual Studio or maybe any other IDE you would like based on the language you are going to be using. Now. If I'm going to go to a function, the Json, this file has both bindings and triggers configurations we have created in our function. And this file is going to be generated automatically when we are using a compiled language. However, if we are using a scripting language, then we need to define this file ourselves. And again, the structure of function, the Json is very similar where you have the name of your binding or trigger and the type of the trigger here. And in this case it's Http trigger and the direction of the trigger. Now, one last thing I want to show you here is I'm probably it's a question, are we able to create multiple functions using multiple languages in one function app? And the answer is yes and no. Previously this was allowed. Previously we were able to create one function app and have multiple functions. Each of them is running on a different language. But now this is not allowed anymore. When we create a function app, all the functions in the function app has to be in the same language. As Microsoft puts in the documentation here. This was a very quick video to show you the basics of functions and different files in the functions of the function app. Thanks for watching and I'll see you in the next video.