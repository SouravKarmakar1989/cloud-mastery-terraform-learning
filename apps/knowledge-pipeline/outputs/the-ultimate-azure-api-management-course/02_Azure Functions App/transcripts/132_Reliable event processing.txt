Hi, everyone. In this video we are going to talk about reliable event processing using Azure functions. One of the most important things you need to keep in mind when you build serverless architecture in Azure is that the imminent need to have reliable messaging processing. That avoids losing messages. And losing messages is very common scenario in distributed systems. And we are going to see this in more details in this example here. Let's assume that we have a publisher that generates events at a rate of 100 events per second. And we have multiple function app instances to process and consume these events. This definitely is going to be the happy scenario. But what if the publisher sends us a corrupted data? Or the function have faced unhandled exceptions. Or maybe the target or the downstream system goes offline. As you are implementing a distributed system in Azure, we need to think proactively how we can build our system to be reliable and resilient against these kinds of failures. And one of the things we can do here is to use additional Azure services like using Azure service bus queues to allow us to have reliable messaging framework. And when we pair it with function f trigger. So we are going to have function app instance triggered when a new message being placed in the queue. So the function app is going to place a lock on the queue to process it. And if the execution failed, then the lock is going to be released to allow other function app instances to pick up this message and try it one more time. And this is going to be the retry cycle. And the retry cycle is going to continue until the message is succeeded or the retry count is been exhausted. Then the message is going to be delivered to the Poisson queue or dead letter queue. And let's say that when a single message stuck in the retry cycle, it's not going to block other healthy messages from processing because other function instances will be able to pick other healthy messages in the queue, even though one single message here stuck in the retry cycle. Which means that the overall throughput will not be affected if we have one or more unhealthy messages come through our service bus queue. However, if you are thinking to use the storage cuz it has some limitations as it doesn't guarantee ordering and it's not optimized for high throughput required by event hub. Unlike Event Hub. Event Hub doesn't have the lock concept as we have in Azure Service bus. And in order for the event hub to allow high throughput and allow multiple function app instances to consume different events and to allow replayability. Then the Event Hub has to implement the feature and behave like a video player. What it's going to read from a single point in a stream per partition. Then you will be able to move forward or backward. So when error happened in your stream and you decide to keep the pointer where it is without progressing either forward or backward, then other messages will not be able to be processed because the pointer is stuck at this unhealthy message or event and keep retrying the execution over and over and over. And then the number of unprocessed events are going to pile up. And Azure functions avoids deadlock by all hosts. So it's going to advance the pointer regardless of success or failure. And it's going to be your responsibility to deal with the failure appropriately in your function app code. Now let's see how Azure function is going to consume event hub events. First of all, a pointer is going to be created and stored in Azure storage account for each partition of Event Hub. Then when new messages are arrived, it's going to trigger the function app executions to start processing these messages. And when the function app completes the execution, with or without exception, then the pointer is going to be moving ahead and then the new pointer location is going to be stored in the storage account. And in case if we failed to progress the pointer, then we are going to be ending up processing the same messages over and over and over. And the way Azure functions handle event hub events have some important points you need to be aware of. First of all, you need to handle exception because unhandled exceptions might cause you to lose messages and it's going to be important to set retry policy, to delay processing and hope that the second attempt will be succeeded. Second point, and it's very common in event driven architecture. You want to be prepared that each message is going to be delivered at least once. Which means there is a chance that your function app will get the same message twice and you want to be able to handle this in your code. Now coming to handling exceptions. And we have talked about it in more details in a previous video. As a general rule of thumb, you need to include try catch block at the highest level of your function app code to allow the function app to catch any exception that might happen when your function app processes the event. Now getting to the retry mechanism and policy. When we've talked about the retry policies before, if you remember, as I said before, some errors are transient. A very good example is that database deadlock. Like, yes, we have a database deadlock now, but if we tried a few seconds later, maybe we don't get the deadlock exception anymore. That's why it's going to be a good idea to retry these kind of errors, hoping that in the upcoming attempts it's going to be resolved on its own without sending the error back to the user. And as a best practice, you want to retry a certain message up to three times. And of course, you want to introduce delay in between. And if all of these trials had failed, then you need to put this message in another queue to allow the processing on other healthy messages. Then you will need to get back to these unprocessed or corrupted messages to handle them later to see what's wrong and how you can process them appropriately. Now let's see what kind of non exception errors that could happen. Maybe the function app crashed and the function didn't complete the execution. At that point, the pointer is not going to be progressed to the next section. And in this case, when the function app is up and running again, then it's again a process. The same message one more time. This is why we said you want to implement your function app in a way that expects the delivery more than once because as you can see here, this scenario, if your function app crashed in the middle of an execution, then it will be processing the same message more than once. And you need to be prepared for that. And as Azure gives you the assurance that every message is going to be processed at least once, then it's going to give the impression that some messages are going to be processed more than once. That's why we need to implement the Idempotency principles in our function app to avoid these scenarios. Now when we want to stop and restart the execution. It's absolutely normal to get small number of failures in our function app in an event driven system. However, if the number of failures became too big, then we want to do something. We want to stop our function app from being triggered every time a new message being placed in the queue. Because we know that something else is wrong and we want to stop that failure from coming until we fix the main issue. And this pattern called circuit breaker and allows you to break the circuit of failure until you fix the main issue and then you can restart the execution one more time. And I have covered the circuit breaker pattern along with other cloud design patterns in my course cloud design patterns. Go and check it out if you are interested to learn more about cloud design patterns. And when we implement the circuit breaker, we want to have two things in mind. Share this data across all instances, and we want to have a master process that can manage the circuit state. Having said that, Azure Logic apps and durable functions are the natural fit for these workflows and scenarios. You can still use other Azure services, but these are the recommended ones to implement. Circuit Breaker pattern. So how do we define the failure threshold across instances? So let's say that if we got more than 100 failures within 30s across all instances, then we are going to break the execution for the incoming messages. So all new messages are not going to trigger our function app anymore because we have exceeded our failure threshold that we have specified. And what you can do in this situation is to log the failures to storage accounts or maybe any other media. And then after logging the failure, you want to inspect the total count of failures to see if the threshold is met or not. 100 failures in 30s. And if the threshold is met, then we are going to trigger event grid to break the circuit. And we can use Azure Logic apps to hold function app as well. Following the same failure thresholds and definitions we are going to specify. Then we are going to use Azure Logic apps to trigger event grid workflow to stop Azure function from execution. If we have exceeded our failure threshold, then we are going to send a notification to someone to look into the error with a restart option in the email to allow them to restart the function app one more time. So the email recipient is going to investigate the issue and see if this is the right time to restart or re-enable the functionality one more time. And if so, they will be able to do that by clicking in a link in the email. And by using this approach, we are sure that no messages are lost and messages are processed in order. And you can break the circuit when a failure happens or when a failure exceeds the failure threshold that you are going to specify in your architecture. And these are some samples here for implementing reliable event processing systems. That's all I have for you in this video. Thanks for watching. And I'll see you in the next video.