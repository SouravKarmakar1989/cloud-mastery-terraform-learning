Hi, everyone. In this video, we are going to talk about target based scaling in function app and we have already touched on it in the previous video. If you remember when we talked about event driven scaling. Currently target based scaling supported in these four extension types. Service Bus queues and Topics. Storage Queues. Event Hub and Cosmos DB. And target based scaling is a replacement and improvement of a previous Azure function app scaling method, which was called incremental scaling model, and that was the default scaling option for these four extension types. And the main difference between incremental scaling and target scaling is the number of instances you could add at a time. So in the incremental scaling, you add or remove maximum one worker per instance while in the target based scaling, you could add or scale up to four instances at a time. And in case you are wondering how the scaling decisions are made, it's going to be made based on this equation. However, it's going to be calculated by Azure, so you don't need to worry about it. Just understand how it works. The desired number of instances are going to equal the event source length multiplied by target executions per instance, and Azure SDK has a default number for target executions per instance. So you don't need to do any changes in it. However, if you want to do some changes by reducing it or increasing it, I'm going to show you how you can do that later on. Now, there are few considerations you need to be aware of when you use target based scaling for your function app. Target based scaling is enabled by default, and if you want to disable it, you need to opt out. Function runtime version must be 4.3 or later. Target business scaling is enabled by default on function app with runtime version 4.19 or later. And in case you overrided the maximum scale out limit for your instances, as you have seen in the previous video, the target based scaling is going to respect the new limits that you have applied and it's going to be considered in the target based scaling activities. And to get the most accurate scaling decisions, you need to use only one target based the triggered function per function app. And if multiple functions in the same function are requesting scale out, then the sum across all of these functions is going to be calculated to be the desired number of instances and scale out requests is going to override scale in requests. And when you have only scale in requests, then the maximum scaling value is going to be applied. All right. We have already talked about that. The target based scaling is enabled by default. However, if you want to disable it, you need to add this app setting with zero value into your function app to disable target based scaling for your function app. And if you want to customize the target based scaling for your function app to make it more or less aggressive, you can do so by referring to this table here, which shows the default values for each of these extension types. And when you run your function app in a premium plan, you can have runtime scale monitoring, as you can see here in the function runtime settings. Under the runtime version, you would have an option to enable or disable runtime scale monitoring for your function app. Target based scaling supports dynamic concurrency. So when you are using service bus or storage queues, you can enable dynamic concurrency on your function app by adding this app settings for the concurrency. And we are going to talk about the concurrency in more details in an upcoming video. All right. That's all I have for you in target based scaling. Thanks for watching. And I'll see you in the next video.