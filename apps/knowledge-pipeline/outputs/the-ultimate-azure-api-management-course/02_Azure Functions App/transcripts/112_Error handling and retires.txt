Hi, everyone. One of the most important things you need to build in your function app is proper error handling and retry mechanism. But why do we need to build that? Let's say that you are writing super clean error free code. Do you still need to worry about error handling mechanism in your function app? And the answer is yes. Not because of the code you are writing itself, but because of other dependencies and services APIs that you are integrating with that might be prone to error and you need to make sure that you are handling this error wisely and efficiently. One of the errors that could happen when you are binding or triggering your function app with other services. There might be some errors happening on the other Azure services and you want to make sure that you are capturing this error and provide a clear and concise error message to the logs to be able to tell exactly what went wrong and how you can improve or fix this error. Or maybe in your function app, you are calling some Azure service APIs and maybe you are providing an incorrect parameter or inaccurate values. And in this case, you want something to tell you exactly what you have done wrong and what should be the right fix to it. Having said that, not everything is going to be on your side when you are interacting with Azure Service APIs. Sometimes the error might be on Azure service side, maybe because of the availability of the service or maybe when you hit a throttling limit. Also, it's going to be very common for you to integrate with other third party APIs, packages and libraries, and we want to make sure that you are handling any error that could happen in these libraries or packages wisely and efficiently in your function app codes. I believe that everyone would agree with me that error handling is mandatory in our function app and there is no questions about that. Now let's come to how we can handle different kind of errors that might happen in our function app. And the first thing we can do here is to enable application insights in our function app. To be honest, I couldn't think of any reason why do we need to disable application insight for a function app? There is absolutely no reason for doing that. As a best practice, we should or must enable application insights for our function app to help us collect error data and performance metrics of our function app. And this will help us to know what went wrong and how we can solve it and fix the issue. The second thing we could do is to use a structured error handling. And I'm sure all of you has written code in different programming languages before and try and catch a block is not an uncommon thing everyone has used. Try and catch a block to catch different kind of errors that might happen in your code and it's a really good practice and we should keep doing that. However, I'm going to add one more thing here. When we use try and catch a block in our code, don't just limit yourself to use the general catch block. That's pretty much catches any exception that could happen. However, I want you to be more specific in the exceptions you are going to catch here by providing multiple catch blocks for different exceptions that might happen in the try. Block And if you are going to ask me what kind of exception blocks I should provide here, and it's absolutely depends on the code you are writing in the try block. Let's say that you are writing some code that connects to a database to get some data. So what you need to do here, you need to make sure that you are able to authenticate with the database correctly and you need to catch an exception in case of a deadlock happen in the database side. Then you are going to have one general exception to catch any other exception that could happen in your try block. And this is not a C sharp code, so I'm not going to dive deep into it. And I'm sure you are all on top of it. And the third thing we need to do here is to plan our retry strategy. And we are going to cover it in more details in a minute. Finally here, you need to design your function app for idempotency. So your function app will be able to detect and process duplicates without any problems. And it's going to be very important when you are using Azure service bus queue and you are processing messages through your function app and we are going to see this and cover this in more details in function app best practices. Now coming to the retrace strategies. Predominantly, there are two kinds of strategies we could apply in our function app. First of all, we could use a built in retry behavior and this is going to be related to trigger extensions. And I'm going to use Azure Service bus as an example. Let's say that we have built our function app to be triggered every time we place a new message in an Azure service bus queue. And after a number of failures, we want to stop the function app from keep processing the poisoned message that maybe has incorrect data or inaccurate format. So we are going to configure our service bus queue to put the poisoned message that's been failed multiple times and exceeds the maximum number of retries we have specified. So we are going to move this poison message into a dead letter queue to stop the function app from executing and processing the poison message any more. And in this table here, you have a different information on different retry sources and where you can configure it. Then we are going to come to the second strategy, which is the retry policies. And it's available from version three onwards of the function at run time. And you can define it for Timer Kafka and Event Hub Triggers. And what the retry policy does, it's going to tell the function runtime to retry the function execution until one of two scenarios happen. Until we get a successful completion and execution of our function, which is the happy scenario that we are all after, or we are going to be exceeding the maximum number of retries we have specified. And in this case, we are not going to retry executing the function app anymore because we have exhausted the maximum number of retries we have configured in our retry policy. All right, then let's get to the retry strategy here. And we have two things we could configure. Either we could have a fixed delay or exponential backoff. What is the main difference in the fixed delay? We are specifying a specific amount of time between the retries in our function app. Let's say that we are going to configure our function app to keep retrying every 30s or a minute. So it's going to be a fixed delay time that the function app has to wait before it initiates the second retry and so on. But a question why do we want to do that? Why don't we just rush through our retries all together at once and hoping one of them is going to succeed? And the answer is, sometimes we may need to wait a little bit in order to get a successful response in the upcoming retries. And I'm going to give you an example. Let's say that we have configured our function app with a maximum retry count set to five. And our function app is trying to get some data from a database. And in our first attempt, we have got a deadlock exception. Now, what's going to happen if we have executed or processed our function app five more times straight away after we have got that deadlock exception? Pretty much we are not going to get a different result because there is a deadlock in the database and we need to give it some time so it will dissolve in its own. That's why it's going to be important for us to specify a fixed delay between our retries to give our function app a chance to be succeeded in the upcoming retries. Having said that, you need to balance the fixed delay amount so it won't be too short or too long. Now coming to the other strategy, which is the exponential backoff and this is the best practice. So we are going to start with a small waiting time. Then we are going to build on top of it as we are getting more failures. And let me open notepad to make it easier for you. So let's say that we have configured our exponential delay with one second and our first attempt is going to wait one second before it sends the second retry and the second retry. You are going to wait one second before sending the third request. And on the third request, we are going to wait two seconds before sending the fourth request. And the fourth request you are going to wait three seconds and so on. So we are increasing the wait time between our retries as the number of retries grows and this will help our function app to get the benefits of the both worlds. So we are going to start with a really small waiting time so it wouldn't disrupt or affect the end user a lot. And as you are getting more and more errors in our retry attempts, we are going to increase the waiting time. We are waiting between these attempts and this is one of the main disadvantages of the fixed delay. Because if we set our fixed delay to be 30s, then at all times we have to wait 30s before a second attempt. And in some situations we might be able to get through with a really small waiting time. That's why exponential backoff is way better and the recommended way to configure your retry strategy. Now coming to the maximum retry counts where this value is going to be stored. Actually, it's in memory value. So let's say that you have configured your maximum retry count and set it to ten. And our function app failed in the first request and we have sent five more retries on our function app, hoping to get a successful execution. After sending these five retries, our function app crashed and then we restarted our function app. So we can get back to normal and continue our function app processing. Now. Imagine what would be the current retry count on our function app. As I've mentioned, it's in memory value. So this value is going to be deleted as we are restarting our function app instance. And then we are going to start counting one more time from zero. This is important to understand for one reason, because sometimes you are going to define your maximum retry count and set it to ten. And maybe in the logs you might see 13 or 14 executions of your function app and you might be wondering why the function app kept executing even though it already exceeds the maximum number of retries I have already configured. And the reason is because of a crash might be happening in the instance and then the in-memory data has been lost. And then the function app started the retry count one more time from zero. All right, Now, finally here, let's have a quick look at a retry example. And as I said before, you are able to define it on Timer Kafka and Event Hub. We can configure it here in the function attributes. And as you can see here, we're defining exponential backoff retry on the function that we have in front of us here. Error handling and retry mechanisms are one of the most important things you need to implement in your function app. And as we are improving the error handling and error messages in our function app, this will help us to have clear logs that will tell us exactly what went wrong and how we can solve different issues related to our function app. At the same time, when we configure retry strategies, were improving the resiliency and availability of our function app, which of course is going to improve the overall availability and resiliency of your architecture. That's all I have for you in this video. Thanks for watching and I'll see you in the next video.