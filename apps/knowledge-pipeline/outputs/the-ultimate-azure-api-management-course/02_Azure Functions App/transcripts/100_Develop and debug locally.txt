Hi, everyone. In this video we are going to see how can we develop and debug our Azure functions locally here. Let's go ahead and create a new project. And we have done it before. I'm going to call it local function. And let's go to the next page. I'm going to choose Dotnet version seven based on Http trigger and authorization level is going to be anonymous. And I'm going to go ahead and create my function app. So this project is a function app. Project currently has only one function, which is function one. We could have multiple functions in the same function app. So I'm going to copy function one over and rename it to be function two. And of course I need to change the class name constructor and the tag here. Let's go ahead and build our solution. This is how we can add multiple functions in the same function app. Now let's have a quick look at the other files that are created for us when we have created a function app project. We have hosted the Json file and local settings dot Json file. Now. If we go to Microsoft documentation here, it says it's not going to be a good idea. If you are going to mix the development between your local environment on Visual Studio and inline editor in the Azure Portal. That's why if you are going to publish your function app from Visual Studio or Visual Studio code, then you want to be able to maintain or change the project code anymore in Azure portal. And I think this is a really good feature to protect us from going through shortcuts that might break our function up later on. So now let's go ahead and publish our function app. Gonna publish it on Azure based on Windows operating system, and I'm going to go ahead and create a new one. I am going to create a new resource group. I'm going to call it RG Dash Local. Consumption plan, Australia East and let's go ahead and publish my function app to Azure Portal. All right. My function app has been published to Azure. Now. Let's come back to host and local settings files. We have only one host file per function app. No matter how many functions we are going to have in our function app, always, always, always, we are going to have only one host file for our function app and it has the configurations that affects all functions in our function app. It's kind of a global setting. We have to set the function app level and it's going to get applied to all functions under this function app. Now. Going into the local settings here. This is where we can configure the required settings to run our function app locally. And as you can see here, we have already two attributes been defined. One of them for using webjobs storage. And in this case we are going to use the emulator instead of the remote blob storage. However, later on I'm going to show you how we can bind our function app to use the remote blob storage. While we are doing the local development as an option if you want to do it. And also we have the function runtime here. It's based on dot net isolated. All right. What else could we have in the local settings here? Let's go back to the documentation. And if we scroll a little bit down here, we should be able to see the different values or settings we could specify in our local settings file. We already have Azure Webjobs storage and it's been set to true. And we already have functions work a runtime and set to dotnet isolated. Let's go ahead and add function name disabled. And this is one of the interesting settings. So let's add a comma and Azure Webjobs. And in here I need to put my function name and I'm going to put function one and disabled. And the value here, I'm going to make it false. All right. Let's get back to the documentation here. We could add one more setting to make our function work at runtime version equals version seven. So let's go ahead and add that as well. And it's going to be set to version seven. Now, let's go ahead and save our changes and let's run my function app locally. All right. Because I've created two functions in this function app. As you can see here, we have two URLs we could hit separately. So I'm going to go ahead and copy the URL. Of each of these functions and hit them separately in my browser. So this is function one. And I'm going to do the same with function two. All right, Now let's get back to Visual Studio and stop the execution. Now I'm going to change Azure Webjobs function when disabled to be true. So I will not be able to run function one anymore in my local environment. And as you can see here, when we run it, we only are able to see function to URL and function. One is disabled. Now what if we go back to the browser and since you have already the endpoint for function one, what's going to happen if we try to hit it one more time? As you can see here, it doesn't get anything back because we have disabled it locally. All right. So these are the different settings we could specify in the local settings file. Having said that, if I go to the Azure portal and have a look at our function app. And let's have a look at the configuration. As you can see here, we are not able to see any of these settings. We have defined in our local settings file because simply these are just for our local development environment. All right. Maybe because I haven't published it yet, but let's go ahead and publish it. And again, we want to be able to see these settings in Azure Portal, but let's validate it anyways. All right, my function app has been published, so let's go and have a quick look at the app settings after we published our function app. And as you can see here, we don't have any of these values or settings we have created in our local settings file. Because this one is just specific for our local development environment. All right, now let's go back to population and go to hosting and open manage Azure App service settings. And in here you should be able to see all the settings we have either locally or in the Azure portal. However, you might be able to notice here, some of the settings have a local value only. That's why we didn't see it in Azure portal like Azure Webjobs function. When disabled, we have set it locally only. Unseen for the runtime version. However, for the settings that has both local and remote value. Then when we run the function app locally, then it's going to pick up the local value. Otherwise it's going to pick up the remote value. When we run it on Azure portal. And as you can see here, some settings has just removed value. There is no local values for these. All right. Now I want to talk to you about Azure. Our Microsoft or call it the Azure simulator that we could use to bind our function app locally to an emulator blob storage. And the tool here is called Azure eight and it comes with Visual Studio 2022. You could install it if you don't or install Visual Studio code extension. Now, if I come back to Visual Studio and go ahead and create a new project, I'm not going to create a new one. I'm just going to show you something. So when I create a function app here, you might notice this checkbox use Azure right for runtime storage account. So when we run our function app locally, we will be able to see the result that we get when we run our function app in Azure portal. So we will be able to see all of these logs information locally in our environments. And as I said before, I'm going to show you later on how you can change your local functional behavior to allow it to publish these event logs to a blob storage instead of relying on your own local environments as an option if you wanted to do it. I think that's all I have for you in this video. Now I'm going to go ahead and clean things up. Let's go back to Azure Portal. Let's go to resource groups and I'm going to go to org local and I'm going to go ahead and delete the resource group. Thanks for watching and I'll see you in the next video.