Hi, everyone. In this video we are going to talk about event driven scaling in Azure functions. When you host your function app in a consumption plan, the instance of the function host is going to be limited to 1.5 gigs of memory and one CPU. And this is going to be shared across all functions within the function app, which means all of these functions are going to scale in or out at the same time. However, if you have multiple functions apps hosted in the same consumption plan. Each function app is going to scale independently of other function apps in the same consumption plan. And when you are using a premium plan and if you remember, we have three tiers in premium plan EP one, two and three, based on the size of the premium plan that you are going to choose. This is going to determine the available memory and CPU for all apps in the instance. Now how the function app scales in and out based on the workload coming to our function. Function app is able to do this with the help of our little friend here scale controller, which is going to instruct the function app to add more instances if the scale controller detects more workload and requests coming to our function app. And also scale controller is going to instruct our function app to scale back in when there isn't enough number of requests coming to our function app. So the scale controller here is what determines if the function app should be scaling in or out based on the incoming requests. We have talked about the cold start at length in the premium plan video, if you remember, And one of the reasons you could run into cold start scenario when you run your function app in a consumption plan and when the function app didn't get enough traffic for a long time, then the scale controller is going to scale back in to zero instances. So in the function app starts to get new requests one more time when the scale controller is going to create a new instance and launch the app. On that instance, the time that's required for a scale controller to have a brand new instance up and running with your app. This is what we call the cold start and this is going to be critical in Http trigger function app because you don't want the customer to be waiting for your function app to be up and running in a new instance to be able to send responses to them. And as a way to avoid cold start, you could consider put your function app in a premium plan or dedicated plan. Either app service plan or through app service environment. And you need to enable and configure always on setting on your function app. Now let's dive deep into scaling behaviors to understand it better. We have one metric here, which is the maximum instances. A single instance may be able to handle multiple requests at the same time, and there is no limit on the concurrent executions. However, the function app can scale up to the maximum number of instances you specify on the plan. However, you might need to lower the maximum number to allow throttling to happen. All right. When scale controller detects an increased workload coming to our function app, then it will instruct our function app to scale out by adding new instances to our function app. However, this happens with specific rates for Http triggers. At most new instances are going to be allocated once per second. However, for non Http triggers, new instances are going to be allocated at most once every 30s. And this could be a good reason for you to consider running your function app in a premium plan, which will allow you to have a fast scaling time when you want to scale out and add new instances to your function app. Finally here we have target based scaling and this supported for service bus queues and topics, storage queues, event hubs. Cosmos DB extensions. All right. If you remember when we talked about the maximum instances that you might want to lower the maximum number of instances for your function app to limit the scale out for this function. However, by default, the consumption plan allows your function app to be able to scale up to 200 instances, and the premium plan allows you to scale your function app up to 100 instances. And if you want to bring the maximum number of instances down, you could do so by running this command here to reduce the maximum number of instances for your function app. And when the scale controller detects that the number of instances of our function app is too much compared to the number of the incoming requests and the workload, at that point, the scale controller is going to terminate some instances of our function app. However, scale controller does this in a safe manner. So before an instance is going to be terminated, no new requests are going to be sent to that terminating instance. And if the terminating instances are in the middle of some executions, it will be allowed some time to finish the processes that we are performing. And this is called the drain mode. And the drain mode is going to be different for different hosting plans. If you are running your function app in a consumption plan, you could have up to ten minutes of extended shutdown periods. However, if you are running it in a premium plan, you could extend it up to 60 minutes. That's all I have for you for event driven scaling. And we are going to cover best practices for scalable apps in upcoming video. Thanks for watching and I'll see you in the next video.