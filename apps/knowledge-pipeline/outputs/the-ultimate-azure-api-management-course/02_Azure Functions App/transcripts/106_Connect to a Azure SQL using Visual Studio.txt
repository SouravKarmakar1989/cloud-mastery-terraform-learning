Hi, everyone. In this video we are going to see how to connect a local function app to Azure SQL. And we have done something similar in the previous video when we connected our local function app to Azure storage queue. And let's go ahead and see how can we do this? Let's go to Visual Studio and create a new project. It's going to be Azure Functions project. I'm going to call it Function App. Azure SQL. And let's go to the next page. Dotnet seven http trigger. Anonymous authorization level. And let's go ahead and create our function app. All right, Now let's go ahead and publish our function app to Azure. Let's select Azure. Windows operating system and let's go ahead and create a new function. Let's stick with the default name and create a new resource group. Let's call it RG Function App SQL. And let's select consumption Plan Australia East. And let's use the existing storage account and app inside instances. And let's go ahead and publish our function app to Azure while we are waiting for this. Let's go ahead and create Azure SQL Database where we are going to be publishing our outputs, binding messages. Let's go ahead and create a new Azure SQL database and let's go ahead and create a single database. Let's put it in. Function apps, Equal Resource group and let's call the database. Function App. SQL Database. And I don't have any SQL Server, so let's go ahead and create a new one. I'm going to call my server name to be SQL Server. Function app. SQL. And I'm going to put it in Australia East region. I'm going to go ahead with the Azure Active Directory authentication. And I need to select Azure Active Directory Admin, so I'm going to specify my own account to be the admin for this server. Let's go ahead with that. And it's going to be no elastic pool development, environment, general purpose. And I'm going to go for locally redundant going to networking. I'm going to provide a public endpoint for my Azure SQL. And I'm going to disable the firewalls and all of these protections. Let's go to security. I'm not going to enable defender or anything and let's go all the way down and create our sequel. Database. Now until my Azure SQL is ready for me. Let's get back to my function app and it is still being published to Azure Portal, so let's give it a few more minutes until it's ready for us. All right. Publishing my function app takes longer than usual, so let's get back to my database. And let's go ahead to the query editor to create a database table. And let's go back to the documentation here and copy this SQL statement to create a database. But before we get into that, I need to authenticate with my database first. First of all, I need to whitelist my IP address. Let's go ahead and do that. Then let's authenticate using my Azure Active Directory credentials. Now let's put the Create table statement and let's go ahead and run it. And let's have a look here. This is the table we have just created. All right. My function F has been published. So now let's go ahead and create Azure SQL binding and install the required packages into my function app. Let's go ahead and close this one. And same as before, I need to go to hosting manage Azure App service settings. However, this time we don't have a section for SQL Connection string. As you can see here, then that's why we will need to go ahead and add a new setting for Azure SQL Connection String. And to get the right name for this, let's get back to the documentation here. And if we scroll a little bit down here, we should be able to find SQL connection string and this is what are we going to add? So let's go ahead and add it. And now I need to specify my local and remote connection strings. So let's get back to my Azure portal. Into my database and go to the connection strings. I don't need to save this anymore. Let's go to Connection Strings. And let's copy the connection string that uses Active Directory authentication. And let's put it in the local and remote connection string values for my function app. And let's go ahead and see if our changes. All right, Now, let's get back to my function code and let's go to the documentation one more time. And let's go ahead and copy this code here and put it in our function app code. So I'm going to replace function one with the code sample I've got from Microsoft documentation. As you can see here, we have one more class, so let's get back to the documentation and copy output type class and let's put it in my code as well. All right. As you can see here, we are missing an assembly reference. So let's get back to the documentation and let's go ahead and execute this command here. But before we will be able to do that, I need to install Azure SQL Extension. So I'm going to use this command here, which I'm going to leave in the resources of this video. So we can install. Azure SQL extension. All right. Now let's go ahead and. Install or add SQL extension package into my project. However, I need to do this in PowerShell. So let's go ahead and run it. As you can see here, there are no projects in this path. So probably I need to go one level down to be able to see the project files. So I'm going to go for this folder and then execute the command one more time. And as you can see here, we have been able to run it successfully. And as you can see here, we have so many errors. So let's get rid of the static classes. And as you can see here, we are missing one more class to do item. So let's get back to the documentation and copy to do item class. And let's put it in our function app code and I'm going to leave it in the same namespace. There is no reason to create a separate namespace for that and let's get rid of one extra curly brackets. All right, now we have error here for the ID. In the definition here, ID is Capital I and here it's a small I. So let's fix it up. And the ID type I'm using here is already good. So I'm going to use a new good without any need to convert it to to string. And I have one last error here for SQL output attribute. So let's see potential fixes and let's use worker dot extension dot SQL. And now we have resolved all the issues we had coming up for this solution. Now let's go ahead and run our function app. All right, let's copy the URL into my browser. All right. All good. We are getting our welcome message to Azure functions. Now, if we go back to our Azure SQL database. And select all from Todo table. We should be able to find our message being inserted into the Azure table because that's how we have configured our function app to have an output binding to Azure SQL so we can store all messages. We are going to be putting in our function app here locally in my environment, in my Visual Studio into Azure SQL Database. That's all I have for you in this video. Now I'm going to go ahead and clean things up, so I'm going to go to the Azure SQL Database and let's go to the resource group and let's clean it. And it's going to delete the SQL database, SQL Server App Service plan and function app. And let's go ahead and delete that. That's all I have for you in this video. Thanks for watching. And I'll see you in the next video.