Hi, everyone. In this video we are going to talk about GraphQL resolver policies and we have quite a few here. First one to start with is SQL resolver. And this pretty much allows you to configure your connection string and a SQL statement in your API management policy statement. As you can see here in the example here, you get to specify the SQL connection string and a typical SQL statement you can put in your policy statement and any parameters you may want to pass on to with your request. Moving on to the next one, very similar to the previous. However, this is for Cosmos DB where you get to specify connection string and you retrieve query that you want to pass on to Cosmos DB. And as you can see, these are different examples to send a query request. Read, request. Delete, request and write request to the cosmos DB using Cosmos DB Resolver. I'm moving on. We have Http data source resolver as well and same as before, you will get to specify the URL and you need to set the method and then you need to set the body with the format you would like. Finally here we have publish events to GraphQL subscription and if we scroll down to the example here, you need to specify the URL and method and body template, and then the target here is going to be GraphQL subscription ID and you need to specify your GraphQL ID. That's all you need to know about GraphQL. Of course, it will make more sense if you're already familiar with GraphQL and you've already used it before. Thanks for watching and I'll see you in the next video.