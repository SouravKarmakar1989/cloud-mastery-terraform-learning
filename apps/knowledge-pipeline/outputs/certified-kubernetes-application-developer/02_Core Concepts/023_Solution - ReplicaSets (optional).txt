Instructor: Okay, so in this video we're gonna go over the solution for the lab on ReplicaSets. So let's start with the first question. So the first question is, how many pods exist on the system? So just like before we run the kubectl get pods command and there are no pods at the moment, so select zero. The next question is how many ReplicaSets exist on the system? So we run the kubectl get replica set command and it's zero. Okay, so some changes were made and let's check the ReplicaSet now and we see that there's one created. So let's just select one. Okay, now the question is how many pods are desired in the new ReplicaSet? So we know that from the output, the desired number of pods in the ReplicaSet is number four. So let's select four. And what is the image use to create the pods in the new ReplicaSet? So we can find that out by looking at the ReplicaSet in a bit more detail. And for that we know we have to run the kubectl describe command. And we're gonna run describe ReplicaSet and then new ReplicaSet. And hit the tab key to auto complete the names of what you're looking for. Okay, so in this output we see the pod template and the image that's used. It's BusyBox 777. So that's the question. And here is the answer. So BusyBox 777. Now how many pods are ready in the new ReplicaSet? So if you look at the output here, it says the number of pods that are ready are zero. You can also see the same kind of here, the pod status zero running. So it is zero. Now why do you think the pods are not ready? So if you look at this, you have... So we'll need to look at one of these pods in a bit more detail. Of course it's obvious, but I just wanna show you if you're not sure what the error is then you've got to look at one of these pods in a bit more detail. So let's run the kubectl describe command and look at any one of the pods in a bit more detail. And you see it's basically because it's failed to pull the image BusyBox 777, right? So the repository does not exist. So there's no image by the name BusyBox 777. That's the reason so let's check that. Now the question is to delete any one of the pod, sorry let's clear from the get pods command and we've got to delete any one of these pods. Okay, so that's done. So how many pods exist now? Let's run the get pods command again. And we see that there's still four pods running, right? So even though we deleted one, the one that we deleted is not here, the one that starts with the wk, but there are still four pods. So that exercise was just to show that even if you delete a pod part of ReplicaSet it's automatically going to recreate a new one. And if you look at the age here you can see that the new one that was created was just 17 seconds ago as opposed to the other ones that have been running for three minutes or more. Okay, so why are there still four pods even after you deleted it? It's not because I didn't delete it properly. It's not because you cannot delete a pod from a ReplicaSet. It's because ReplicaSet ensures that the desired number of pods always run. Okay, so the next one is to create a ReplicaSet set using the ReplicaSet definition file located at root. So let's look at where that is. So there's one here and there's an issue with the file so we have to try and fix it. So let's first try to create it. So kubectle create and here's F/root. ReplicaSet definition.yaml. Okay, so the error is that it's unable to recognize this file. No matches for kind ReplicaSet in version v1. So there's something wrong here. Either the kind ReplicaSet is incorrect or the version v1 is incorrect. So let's look at the file and we see that the API version is v1 and kind is ReplicaSet. And as we learned from the previous video the API version is incorrect. But if you're not sure what it is, one way to find that out is using the kubectl explain command. So run the kubectl explain ReplicaSet command. Sorry. And here you can see the version. So the version is supposed to be apps/v1, right? So that's the problem. So let's go ahead and fix that. apps/v1. And let's try and delete that again. Okay, so it has now being created. Okay, so let's go to the next one. So next task is to fix the issue in the second file and create the ReplicaSet. So let's try that. Let's just first try to create it. Okay, so it says there's an invalid value selector does not match template labels. So let's take a look at the file here. And we see the API version is app/v1 which is correct. The kind is ReplicaSet. That also seems to be correct. The metadata is the name. And then you have the spec section. So you have replicas, you have selector. So there is something wrong here, which is why it didn't work. Let's find that out. So here it says invalid value selector does not match the template labels. So basically the issue here is that you have the labels specified here called tier front end, and then the label for the pod that the ReplicaSet will create is nginx. So these two should match otherwise the ReplicaSet cannot basically manage the pods that it creates. So you've got to either change this to front end or change this to nginx. But at the end both of them should match. So let's change this to nginx. Save this file and run it again. And that's now created. Okay, let's go to the next one. Now we've got to delete the new two newly created ReplicaSets. So let's first look at the status right now. You can also run just RS instead of ReplicaSet which is the short form. And now we've got to delete both of these ReplicaSets. So kubectl delete RS ReplicaSet 1 and 2. You can also give multiple in the same command. Okay, so that's done. Now fix the original ReplicaSet to use the correct BusyBox image. So we know that the first one, which is we now only have one. And we know that there are none of the pods are in a ready state. And we also know that because the image name is incorrect. So we've got to now fix that. So for this, let's run the kubectl edit command and edit the new ReplicaSet. And let's go into the container section and change the image name to just BusyBox and save that file. And let's check status of pods now in the ReplicaSet. So it's still zero. Let's just give it some time for it to delete the old pods and create new ones with the new image. Right, RS. Okay, so when you update a ReplicaSet even though the image name is updated the pods are not automatically recreated, right? So you have to either delete and recreate the entire ReplicaSet or you have to delete all the pods so that the ReplicaSet creates new pods with the new image name. So let's go back and let's look at pods. And you see there's all still in the error state. So we're just going to go through and delete them. We're gonna give the name for each one. And we'll just wait all of them to be terminated. Let's run get pods again. Okay, it's in a creating state and it's in a running state. Let's check the status of the ReplicaSet now. And you can see that they're all in a ready state. Okay, let's go to the next one. Now we've got to scale the ReplicaSet to five pods. So let's run the kubectl scale command and specify the number of replicas to five. Let's check the status and we can see that a new pod has been added. We can also do that using the edit command. So we could do a kubectl edit RS and specify the name of the ReplicaSet and then go here and just change this number to the desired number and then save the file. But we're not gonna do that right now. Okay, so that's done. And now the task is to scale the ReplicaSet down to two pods. So this time, let's use the other approach. So let's edit the ReplicaSet. Go to the number here, set it to two. Okay, let's check. Okay, so that's it for this lab. And next we'll look at deployments.