Okay, so in this lecture we are going to practice in Copilot and there's a link here in the course code under the Copilot directory that will take us into editorials page and this is the one we're going to follow. So the first thing is that on the left hand side panel, you can go to install Copilot. So let's go here, and then you do install Copilot. And so Copilot is a CLI and you can install it in many different ways so you have, for example, Mac, Linux, Linux here, or Windows installation. So please follow the one for your platform. For me, I'm just going to use the one right here, but again, it really depends on what your platform is. And so once you're done just type copilots --help, and then you will see that you have your command right here. Also, if you haven't done so, please make sure you have both the AWS CLI that is installed as well as Docker Desktop that you can just install online as well to make sure they are available on your computer. Okay, so now we are going to clone a repository which contains a Copilot compatible installation. So we have a good clone and if you want to have a look at this, this is a service directly from the AWS samples that contains code around a Docker file on how to configure an image. So we're good to go, this is your basic Copilot directory, and then we do a change directory into example, just to be in our clone directory. Then we're going to set up our spot. This is where we're going to use Copilot. So we do copilot init to initialize the Copilot CLI. And then we have to give a name to our application. So we need to enter a few names. For example, this one's going to be example-app. And then you have to say which type of deployment you want. So you have several options. You have request-driven web service on App Runner, you have load balance web service to use a public ALB by default, and then ECS on Fargate behind the scenes. You have a backend service when you have a private service and maybe your load balancer is optional. You have a worker service when you have a SQS to ECS on Fargate type of architecture. A static site or a scheduled job. So different types of workload, but here we're going to use on our end, a load balanced web service. Then you need to name our service, so this is the front -end service. And then we need to specify a Docker file to use. So we have a basic Docker file in our clone directory, so we just use this one, and then it's going to look at the proposed changes. So the position is like what is going to happen, what is going to be deployed for it to work? And then automatically a stack set is going to be created on CloudFormation, and then CloudFormation is going to deploy that application for you. So we need to wait a little bit right now. So you may be prompted to make sure that you're all set for the deployments. So if you're all set, just press y and then it's going to start deploying the stack. So now we need to run and talk about which environment we have, okay? So here we can say, for example, it's the test environment and then it said doesn't exist, so we're going to create the test environment and now we're going to have CloudFormation deploy our entire stack. So here we have a look at what goes on into CloudFormation. As you can see here, this first CloudFormation stack was created to configure the CloudFormation stack set administration role that is used to do AWS CloudFormation stack sets. And the second is this stack, and this stack is the actual application on Copilot. So you can have a look at the events and what is being created. So in terms of resources right now, we have a KMS key, an S3 bucket, and an ECR repository. So again, we just need to wait a little bit for a Copilot to do its magic. But the idea is that just from a Docker file and the Copilot's configuration files of course, we're able to create a full pipeline on AWS to deploy our application to use Docker and to make sure that everything is up and running with best in class architecture for our type of application. So let's wait a little bit to see if everything is deployed. So this is keep and running, but here it's very nice because you can see what is being created and the reason why it's being created, which I think is a quite nice thing to have from the Copilot UI. So we see why we have IAM roles, ECS services, target groups, listeners, and so on. So it's very handy. Now I'm still going to wait a little bit until this is done. So that's it, everything has been deployed. And now let's take this URL right here and we're going to open a new tab. Press enter. And yes, you see the Copilot logo on a public URL, so this is the app. It's a very simple one, it's just a logo, but it could be any kind of HTTP app, of course. So another thing that's good to look at is to have a look at CloudFormation. So you can see that we have an app here where we have an ECS cluster, a load balancer, a bunch of security groups, and it's very important for you to look at the resources and understand what has been created, it's great for your learning. We have a load balancer, we have a VPC, and so on. And then of course for the front-end, here we have more information around Lambda function, IAM roles, listen roles for your elastic load balancer, log groups, and here we have our ECS service, which is very important. So if you wanted to have a look at ECS itself, then I would definitely encourage you to go into the ECS service and then have a look at your cluster called the example-app-test-Cluster, and have a look at your services and see how everything was set up because it was set up according to best practices. Also, check out your your load balancer if you could. So that's it. Now you want to delete everything and clean up. So here, very simple. Let's go back to the example. Just run copilot app delete, and you would be good to go, and yes. And lastly, if you want to have a look at the files that have been created here, there is a Copilot directory that has been created. And so if you look within the Copilot directory, we have two things. We have environments and front-end. So let's go into environments and see what there is. Now there's a test environments and then there's a file named manifest.yml. And this file, I'm just going to open it right here, contains what has been created for you at the prompt. So we have the name, which is the the test environment, and then the type of it is environment and any kind of additional settings you would set in your manifest. And of course you can do the exact same thing with the front-end and have a look at what's in it. So you go under Copilot, front-end, and then manifest.yml. And here you have more settings that you can change if you wanted to around your front-end service. And this is where you would customize everything with having your configuration as code, and then you would push again with Copilot. So run copilot app delete is going to take a little bit of time, but it's going to clean up and make sure that you don't have any ongoing costs. So let's say for this lecture, I hope you liked it and I will see you in the next lecture.