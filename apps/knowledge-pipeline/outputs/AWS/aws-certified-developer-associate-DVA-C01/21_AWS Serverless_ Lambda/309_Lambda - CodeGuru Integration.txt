So now let's talk about how Lambda and CodeGuru work together. So by using CodeGuru Profiler, you're going to get insights into the runtime performance of your Lambda functions. For this, when you make it work, CodeGuru is going to get a profiler group for your Lambda function and that is supported for Java and Python runtimes. And to just activate the CodeGuru integration, you just activate it from the Lambda console. So out of this, we're going to get runtime performance insights for Lambda functions. And when you activate this integration, this profiling, CodeGuru Profiler layer is going to be added to your function as a Lambda layer. The environment variables related to CodeGuru will be added to your function. And finally, for this to fully work, of course, you need to have IAM permissions. And so this AmazonCodeGuruProfilerAgentAccess policy will be added to the function IAM role. So hopefully that makes sense. I hope you liked it, and I will see you in the next lecture.