<!-- Section 3 of 13 | Course: aws-certified-machine-learning-engineer-associate-mla-c01 -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 3 OF 13: Data Transformation, Integrity, and Feature Engineering
**Course:** `aws-certified-machine-learning-engineer-associate-mla-c01`

---
### Lecture: Intro  Data Transformation, Integrity, and Feature Engineering

Hey, Frank. Here. Back again. This next section will be mostly covered by myself. We're going to go into data transformation, data integrity and feature engineering here. Some of this information isn't really AWS specific. How you deal with missing data, outliers and unbalanced data are all just general problems you have to solve in machine learning. But I'll also go into depth with using elastic MapReduce to process data at scale, and also using the newer SageMaker tools such as SageMaker Data Wrangler. We'll see how SageMaker clarify can be used to detect class imbalances, and to identify the most important features in your training data. We can't talk about data transformation without covering AWS glue. It has an entire suite of its own for structuring and transforming data at scale. And as is often the case with AWS, there's more than one way to do it. But we'll cover the main ones along with some fairly in-depth hands on labs and demos as well. Now, a word of warning whenever we're dealing with big data, there's the potential for big AWS spills as well. So if you choose to follow along, take care to delete any resources you create along the way and make sure you set up a billing alarm so you know right away if something you forgot about is running up a larger bill than you want. You can learn just fine by watching these demos and activities. So if you're worried about unexpected AWS expenses, just watch. It's safer that way. But let's get started with data transformation, integrity, and feature engineering.

---

### Lecture: Elastic MapReduce (EMR) and Hadoop Overview

Next, let's dive into the world of elastic MapReduce or EMR for short. It is a managed Hadoop framework that runs on EC2 instances, and it's kind of a confusing name because MapReduce itself is kind of an obsolete part of Hadoop, and it's a lot more than just a Hadoop cluster on EMR. So yeah, it does include Hadoop, and MapReduce is in there, but it's the stuff that's built on top of Hadoop that you tend to use more often, technologies like spark or HBase or Presto or Flink or Hive and even more things come pre-installed with EMR, potentially. There's also something called an EMR notebook that's much like a Jupyter notebook that runs on your EMR cluster, and it has several integration points with AWS services as well. This is relevant for the world of machine learning, because if you have a massive data set that you need to prepare and normalize or scale or otherwise treat before it goes into your algorithms. EMR provides a way of distributing the load of processing that data across an entire cluster of computers. So for massive data sets, often you need a cluster to actually process that data and prepare it for your machine learning training jobs in parallel across an entire cluster. A cluster, an EMR is a collection of EC2 instances where every EC2 instance is called a node. Each node has a role within the cluster, which is called the node type. First is the master node that manages the cluster by running software components to coordinate the distribution of data and tasks among the other nodes for processing. It tracks the status of tasks and monitors the health of the cluster. Every cluster has a master node. You can also create a single node cluster of just a master node. If you have just enough processing that runs on a single machine. The master node is also sometimes referred to as the leader node. We also have core nodes. These are nodes with software components that run tasks and store data on the Hadoop Distributed File System, or HDFS for short. On your cluster, multi node clusters will have at least one core node. And then we have task nodes. These are nodes with software components that only run tasks and do not store data on HDFS. These are optional. You don't need them, but they are good use of spot instances because you can introduce them and take them out of the cluster as needed, because they don't really impact the storage on your cluster since they don't actually talk to HDFS. The Hadoop distributed file system, there aren't any permanent files stored on the task node that your cluster needs, so they're only used for computation. So if you have a sudden task that needs to run on a massive amount of data, and you're only going to need that once, you can introduce a task node into your cluster and then remove it when you're done. And your cluster will just continually, happily run along without it after you've removed it. There are a couple of ways to use an EMR cluster. There's a transient and a long running cluster. A transient cluster is configured to be automatically terminated once all the steps that you've defined to run on it have been completed. So the steps you might run would include loading input data processing that data storing the result of the output, and then shutting down the entire cluster. So if you have a predefined sequence of things that you want your cluster to do, you can save money by automatically terminating that cluster as soon as that task has been completed. You can also have long running clusters where you create a cluster and you want to interact with the applications on it directly, and then just manually terminate it when you're done with it. So this is more appropriate for, say, ad hoc queries or experimenting with data sets where you don't really know what you want to do upfront and you don't have some repeatable sequence that you just want to run over and over again. In that case, you would want to have a long running cluster and just terminate it by hand when you're done. Now, when you launch a cluster, you select the frameworks and applications to install for your data processing needs at that time. Once you have a cluster spun up, either way you can connect to it directly to the master node through EC2 and run jobs from the terminal there. Or you can submit ordered steps via the console as well if you can predefine those through the console in AWS. Either way works. We talked about integration points between EMR and AWS. Obviously, those are important. For example, it uses EC2 of course, for the actual underlying instances that comprise the nodes on your cluster. It can work with Amazon VPC to have your cluster within a virtual network. You can store your input data and your output data on Amazon S3 instead of HDFS if you wish. You can also use Amazon CloudWatch to monitor the performance of your cluster and configure alarms on the individual nodes in your cluster. IAM can be used to configure permissions to your cluster. CloudTrail will create an audit trail for the requests made to the services on your cluster. And finally, AWS data pipeline can be used to schedule and start your clusters that are running a series of steps that are predefined. Let's talk a little bit more about the storage on EMR. Now the default storage solution on Hadoop is HDFS. And this is a Hadoop system. So we do have HDFS available to you. It's a distributed scalable file system for Hadoop. It distributes the data that it stores across every instance in your cluster. And it stores multiple copies of data on different instances to ensure that no data is lost if an individual instance fails, every file in HDFS is stored as blocks and is distributed across the Hadoop cluster. By default, the size of a block in HDFS is 128MB. Now this is ephemeral storage. Once you terminate your cluster, that storage that was stored locally on those nodes goes with it. So that's the reason not to use HDFS. However it's going to be a lot faster. Right. Because we don't have to go across the internet to access that data. It's all local on the actual nodes that are doing the processing of your data. And Hadoop has a lot of smarts built into it so that it tries to optimize things such that where the code is running to process a bit of data is the same node where that data is stored. So HDFS is very good from a performance standpoint. But it has the downside that when you shut down your cluster that data goes away. That's not a good thing. However we have EMR, ZFS which allows you to use S3 as though it were an HDFS file system, and it turns out it's still pretty darn fast. There's also an optional consistent view in EMR, ZFS that's for S3 consistency, and you can actually use DynamoDB to track that consistency across the EMR. FS so a key thing with EMR is that you can use S3 in place of HDFS. That's the key point there. You can also of course use the local file system if you want to as well for ephemeral things. But again that's not going to be distributed. So that is only useful for example on the master node where you're trying to basically stage data and get it where it needs to be. We can also back HDFS with elastic block store. So EBS and HDFS have a relationship their. EMR charges by the hour plus any EC2 charges under the hood. It does promise that it will provision new nodes for you automatically if a core node fails. So you don't have to worry about that at least. And you can add and remove task nodes on the fly as well. Like we talked about, you can use spot instances to add additional capacity or remove capacity without impacting the underlying storage. On the HDFS file system. You can also resize the core nodes on a running cluster as well. You can get away with that for capacity planning to.

---

### Lecture: EMR Serverless

Let's talk about a newer feature in EMR. EMR serverless works pretty much the same idea as EMR. The difference is that the intermediate step of defining just how much capacity we need is removed from us, so we can let EMR just decide for itself how many nodes it needs in the cluster to actually do what we want it to do automatically. So to use EMR serverless, we just start by choosing our EMR release and runtime spark, hive or Presto, just like we would before. And then we just submit queries and scripts to our EMR serverless instance via job run requests. So rather than, you know, logging into your master node directly, it's serverless. So you know, you just want to interact with it indirectly. You'll give it a path to some S3 thing somewhere that says, this is where my script is. It'll reference data from S3, and you just feed that script or that hive query into it from S3 to your serverless server, and it will automatically, in theory, figure out how much capacity you need to actually successfully run that job. So EMR will manage the underlying capacity for you. But if you want to think about the underlying capacity, you still can. And this might be a good idea in a lot of situations. So if you know that your job is requires a few resources, you can specify that up front. You can specify the default worker sizes up front. And you can also pre-initialize your capacity. So instead of starting from nothing, you can tell EMR serverless, hey, I think the base capacity I'm going to need is this many nodes that are this size and start from there. That will help you out. Otherwise it will try to compute it for you automatically. And even if you do start off with a base setting of a pre-initialized capacity, it will analyze the resources that are actually needed for your job and adjust that accordingly, and schedule workers as you need them and reschedule them as you don't need them. As of right now, it all works within the same region across multiple availability zones, of course, so your serverless cluster will be within one region. So why is this such a big deal? Well, you no longer have to figure out how many workers you need, right? So a really big pain point in things like Apache Spark is that if you guess too small, if you don't have enough resources for a job, you're going to end up running out of memory. Your job will just fail halfway through, and it can be a very time consuming and expensive process to figure out how much capacity I really need, because it's really not a very good way of figuring that out ahead of time. So EMR serverless is solving that problem. So that's actually kind of kind of impressive if you ask me. It will automatically provision the capacity you need for your job. So you don't have to worry about it, which is really a big deal. You can focus on the scripts and what you're trying to do, instead of figuring out how much capacity you need to do it. And like I said, it's not really serverless in the way of some other serverless servers where you really don't think about the servers at all. Um, under the hood, you're still using Spark or Hive or Presto, and those technologies kind of assume that you have some knowledge of what's going on at the worker node level or the driver level, right? So if you still want to configure spark down to that level, you're still going to have to think about the worker nodes and their capacities. The difference is you don't necessarily know how many of them you have, and you don't necessarily know where they are. How do I go about using EMR serverless? Well, you start off with an IAM user that you are going to be interacting with it from, and that will work with the aws CLI. So your user will invoke the CLI to create this serverless cluster. For now only the CLI is supported. I'm sure that will change pretty soon. I can't imagine that they wouldn't have console support and SDK support sometime in the future, but for now it's command line interface only. You then need to set up a job execution role in IAM for the job itself, and that makes sure that your job has permission to access Amazon AWS, EMR, serverless, as well as any permissions that it needs to access your scripts in S3. Any data that it needs to access in S3. If you're doing something with spark SQL, you might need access to glue for glue, metadata, stuff, or any KMS keys that you might need for any security that you've set up as well. With that in hand, you can then create an EMR serverless application using spark, hive, whatever you want, and then just feed it in your job through an EMR job request there. So you give it a link to your spark script, your hive query, whatever it is, might look like this. So here's an example of a command line to invoke an EMR serverless job. We're just saying AWS EMR serverless start job run passing in our application ID. The execution role that we talked about earlier with a path to that. And then under job driver this is where it gets interesting. We're saying under spark submit the entry point is this path to the script itself. We can pass in arguments to that entry point as well. If we want a parameter to where it should put its output, for example. And we can also override the spark submit parameters as well. So if you wanted to submit parameters to spark itself on the command line of spark submit, you can specify that here too. So again, you know, even though we're calling this serverless, I'm still thinking about here how big my executor cores are. How many? What are my driver cores look like? Things like that. So you still have that level of control here? Even if you're even if you're handing off the actual capacity and the amount of nodes off to EMR. Serverless. Also, you can send configuration overrides that are specific to EMR. Serverless. In this example here, we're saying that we want to output our CloudWatch logs to this path in S3. When we're done, we get our output in our logs wherever we said to put them. And that's it. You just shut it down when you're done and it's just like EMR. Let's talk about the lifecycle of an EMR serverless application real quick here. So you start off by explicitly creating your application through a command currently from the CLI. If that succeeds then great you created your application. If not, it will automatically terminate. From that point you have a new application sitting there that you've created. You might choose to terminate it right away. That's an option. I'm not sure why you would do that, but normally you would proceed to issue a start command to start your application and that will kick it off and actually run it. Once it started, it will either succeed and then move to the stopping phase when you're done, or it will fail and move to the stopping phase automatically. Once it is successfully stopped, you can either run it again, go back to start, or go to the terminated phase and shut it down when you're done. Now I want to stress that this is not all happening automatically. I need to call commands through the API such as create application, start application, stop application and when I'm done, delete application to make this all happen. So I've had students contact me saying, hey, you know, you use EMR and a lot of your courses and you're always warning us to remember to shut down your cluster when you're done so you don't get charged for them unexpectedly. Should I just use EMR serverless so I don't have to think about it? No, that does not solve the problem. It helps because you will automatically, you know, not be using any more capacity than you need, but you still need to explicitly delete your application when you're done with it. If you forget to do that, you will still be billed for that serverless application, even if it's sitting there doing nothing. Okay, so don't think that EMR serverless is an excuse to get lazy in managing your resources. You still need to think about explicitly starting and stopping and terminating that cluster when you're done. EMR serverless just means that it's thinking about the capacity instead of you, but you still need to manage that capacity to some extent yourself. Let's talk a little bit more depth about Pre-initialized capacity on EMR serverless. So here's an example off to the right here of what that might look like from the command line. You see here we have a initial capacity block here that we didn't have before. We're saying that we want to start off with five drivers with a specific resource configuration of a CPU type, and the the memory that each one has available to it. Also, for the executor nodes, we're saying we want 50 of them to start with with four vCPUs apiece and eight gigabytes per executor as well. So, you know, this lets us say how many processes do I want to power my application. It's still EMR serverless job to figure out where those processes are running and the capacity needed to actually make it happen. But you can give it a hint like this to say, hey, I know this is a pretty demanding job. I want to start with 50 executors, go figure out how to make that happen. But just to make things go more smoothly and make sure that I have enough capacity out of the gate, I want you to start from this point. There's also a maximum capacity argument being sent here as well. I want to say that I don't want to use any more than 400 virtual CPUs and no more than one terabyte of memory, so that's a pretty good safeguard to have in place in case you do something silly that just wants to use infinite resources, that will be a little bit of a safety net for you there. One note from the documentation, and this does look like the kind of thing that I could imagine seeing on the exam at some point. Uh, remember that spark adds a 10% overhead to any memory requested for drivers and executors. So make sure your initial capacity request takes that into account. Make sure you're requesting at least 10% more memory than your job is actually requesting. Some notes on security. With EMR serverless. It's pretty much the same thing as EMR for storage. If you're using EMR, EFS under the hood, that's S3. So you know, server side or client side encryption on S3 at rest is going to be there for you for In-transit. TLS will be there for you between the EMR nodes and S3. If you're writing directly to S3, obviously all the security that comes with S3 is available to you, as well as the S3, SSD, KMS, whatever you want to use for any ephemeral storage on the local disks of your nodes. Those are encrypted as well and in transit. Any communication between the driver nodes and executors is also encrypted automatically. If you're using hive, and a communication between the glue. Metastore and EMR is also encrypted in transit using TLS. And a quick note from the documentation here I wanted to pass along. Remember that if you are using S3, you can force Https on in transit traffic to S3 using the AWS Secure Transport Policy. Another serverless approach to EMR is EMR on EKS that's Elastic Kubernetes Service. So if your world is built around Kubernetes and that is true for many people these days, this is a way to use EMR and spark in particular within EKS, within Amazon EKS. So this allows you to submit a spark job on Elastic Kubernetes Service without having to worry about provisioning the clusters for it. So this gives you all the benefits of Kubernetes as opposed to just having a standalone EMR cluster. And also it gives you the benefits of all the fully managed aspects of EMR. Right? So you don't have to deal with setting up your own spark application within EKS. This automates all of it for you. So it's a fully managed solution for elastic, MapReduce and spark in particular on top of your ECS world. The advantage of doing this is that you can share resources between spark and other applications that you might have running in Kubernetes. So this diagram to the right kind of speaks to that. We have EMR on EKS at the top here going through X, and you can see how a spark application is just one of maybe many things you're doing on that X cluster, right? So you can have other applications doing totally different stuff. Maybe an analytics application. You might even have different versions of spark running alongside everything else. And through the magic of Kubernetes, we can share those resources more efficiently. So this may be a better way of making use of the hardware that you have and ultimately saving money. And you know, we also see that it has all these integrations with other Amazon services at the bottom there as well, spread out potentially between multiple availability zones. Again, you don't have to worry about that too much. With just a few clicks in the console, you can choose the Apache Spark version that you want. Deploy an EMR workload to Amazon EKS. EMR will automatically package that workload into a container and provide these pre-built connectors for integrating with these other AWS services. EMR will then deploy the container on the EKS cluster, and it will manage all the scaling and logging and monitoring of that workload for you. Ethan.

---

### Lecture: Apache Spark on EMR

So what is Hadoop anyway? Well, it consists of a few different modules here that we'll talk about. And the rest of the stuff on your EMR cluster often builds on top of these modules as well. So when we talk about Hadoop itself, we're usually talking about the HDFS file system Yarn and MapReduce. And underlying all of these is something called Hadoop core or Hadoop common. That would be the libraries and utilities required for all of these modules to run on top of. So it provides the file system and operating level system stuff that it needs to extract itself from the OS, and all the Java archive files and scripts that are needed to start up Hadoop itself. At the lowest level above that would be HDFS. That is the Hadoop Distributed File System, a distributed, scalable file system for Hadoop. Again, it distributes the data it stores across the instances in the cluster. Multiple copies of the data are stored on different instances to ensure that no data is lost. If an individual instance fails, it is lost upon the termination of the cluster, however, but it's useful for caching intermediate results during MapReduce processing or for workloads that have significant random IO. On top of HDFS, we have Hadoop Yarn. Yarn stands for yet another resource Negotiator. It's a component introduced in Hadoop 2.0 to centrally manage cluster resources from multiple data processing frameworks. That enables us to use things other than MapReduce, as we'll see shortly. So what is MapReduce? Well, MapReduce is a software framework for easily writing applications that process vast amounts of data in parallel on large clusters of commodity hardware in a reliable, fault tolerant manner. It dates back to some ideas from Google in its early days of big data processing. MapReduce consists of mapper functions and reduce functions that you write in code. A map function maps data to sets of key value pairs, called the intermediate results, and map functions generally do things like transform your data or reformat it, or extract the data that you need. That's why it's relevant to the world of exploratory data analysis. Then we have reduced functions that combine those intermediate results and applies additional algorithms and produces the final output. So generally, the mappers are the things that are transforming your data and preparing it. The reducers are aggregating that data and distilling it down to a final answer that you want. However, these days, Apache Spark has largely taken the place of MapReduce and thanks to the existence of yarn, spark can actually sit on top of your Hadoop cluster and use the underlying resource negotiator and file system that Hadoop offers, but just offer a faster alternative to MapReduce. So spark, which can be optionally installed on your EMR cluster, is an open source distributed processing system commonly used for big data workloads. It's really the hotness right now instead of MapReduce. It utilizes in-memory caching, optimized query execution for fast analytic queries against data of any size. So it uses something called a directed acyclic graph. That's kind of its main trick for its speed. Compared to MapReduce, it can be more smart about the dependencies and processing and how to schedule those more effectively. Spark has APIs for Java, Scala, Python, and R, and it supports code reuse across multiple workloads like batch processing and interactive queries, and real time analytics and machine learning and graph processing. It has a bunch of different use cases that we'll talk about stream processing, machine learning, interactive SQL. However, spark is generally not used for OLTP or batch processing jobs. It's a more for transforming data as it comes in. How spark works under the hood. Well, spark applications are run as an independent set of processes on a cluster. They're all coordinated by the spark context object of the main program. This is known as the driver program. That's the actual code that you write to make your spark job run. The spark context connects to different cluster managers which allocate the resources across the applications. So for example, Yarn or spark has its own built in one. You can use as well if you're not on a Hadoop cluster. Upon connecting, spark will acquire executors on nodes in the cluster. The executors are processes that run computations and store data for your applications. The application code is sent to the executors, and in the final step, the spark context will send tasks to the executors to run. Ether spark itself has many different components, just like Hadoop does. So underlying everything is spark core. It acts as the foundation for the platform. It's responsible for things like memory management and fault recovery, and scheduling and distributing and monitoring your jobs and interacting with storage systems. It has APIs for Java, Scala, Python, and R, and at the lowest level it uses something called a resilient distributed data set, or an RDD that represents a logical collection of data partitioned across different compute nodes. Now, as we'll see, there's a layer above that with spark SQL that is a distributed query engine that provides low latency, interactive queries up to 100 times faster than MapReduce. It includes a cost based optimizer. Columnar storage and code generation for fast queries, and it supports various data sources coming from JDBC, ODBC, JSON, HDFS, hive, ORC, or parquet files. It also supports querying hive tables using hive QL if you want. But the really important thing about spark SQL is that it exposes something called a data frame in Python or a data set in Scala. And this is sort of taking the place of the lower level resilient distributed data sets. In spark these days. So modern spark code tends to interact with data in much the same way as you would with a data frame in pandas or a database table in a relational database, you can actually issue SQL commands to your spark cluster, and under the hood it will figure out how to actually transform that into a distributed query that executes across your entire cluster. So very useful stuff. Also, we have Spark Streaming. That's a real time solution that leverages Spark Core's fast scheduling capabilities to do streaming Analytics. So data gets ingested in mini batches and analytics on that data within the same application. Code written for batch analytics can be applied to those mini batches. So it's pretty cool because you can use the same code that you wrote for batch processing and apply that to your real time streaming. With Spark Streaming, it supports ingestion from Twitter, Kafka, flume, HDFS, and Zeromq. As as we'll see, it can also integrate with AWS Kinesis. We also have Mllib, the machine learning library for spark. Obviously that's relevant to the machine learning exam. We'll talk about that in more depth of course, and what it can do. And finally we have graphics, which is a distributed graph processing framework built on top of spark. We're not talking about charts and graphs like QuickSight here. We're talking about computer science graphs here. So, you know, for example, a graph of people in a social network. It's more of a data structures thing. It provides ETL and exploratory analysis and iterative graph computation to enable users to interactively build and transform a graph data structure at scale. So let's talk about Mllib a little bit more. It offers several different machine learning algorithms, but what's special is that they're all implemented in a way that is distributed and scalable. So not all machine learning algorithms really lend themselves well to parallel processing. A lot of them sort of need to be reimagined in order to spread that load out across an entire cluster of computers. So with spark Mllib, it has a specific set of things that it can do for classification tasks. It offers logistic regression, which is, you know, still a great alternative, and Naive Bayes. It can also do regression across a cluster, a decision trees. It has a recommender engine built in built on alternating least squares. It has a clustering K-means implementation built in for topic modeling. It has LDA. If you want to extract topics from a text information from documents in an unsupervised manner, and it has various workflow utilities that are useful for machine learning in general, such as pipelines and feature transformation and persistence. It also has distributed implementations of the SVD. PCA and statistics functions as well. We'll talk about more about what those mean later in the course, if those are new to you in the modeling section. But again, the special thing here is that this can run on a cluster. A lot of these algorithms will not run on a cluster in their default state. If you were to run them on, say, scikit learn, for example. So with spark Mllib that allows you to process massive data sets and actually train machine learning models on them across an entire cluster. And as we'll see later in the course, you can even include spark within SageMaker as well. Spark streaming deserves a little bit more discussion as well. Generally, spark applications use a data set in their code that refer to your data, which is treated a lot like a database table. With spark streaming, that table just keeps on growing as new chunks of data are received in real time. And you can query that data using windows of time. So, for example, you could look at the past hour of data in your data stream coming in and just query that like a database basically. So that's at a high level how structured streaming works in spark. IT models inbound streaming data as basically an unbounded database table that you can query whenever you want to. And Spark Streaming does integrate with Kinesis. You could have a Kinesis producer publishing data to a Kinesis data stream, and there is a way in the kql to actually implement a spark data set built on top of that data stream that you can just query like any other spark data set. Another key point in Spark is Zeppelin. That's basically a notebook for Zeppelin. It allows you to run spark code interactively within a notebook environment in a browser, so you can execute SQL queries against your spark data using spark SQL. You can also query your results and visualize them as well, and charts and graphs using things like matplotlib and seaborn. So it makes spark feel a lot more like a data science tool, and allows you to actually pre-process your data in a familiar format that data scientists are used to.

---

### Lecture: Feature Engineering and the Curse of Dimensionality

Let's dive into the world of feature engineering in the world of machine learning. What is feature engineering anyway? Well, basically it's the process of applying what you know about your data to sort of trim down the features that you're using, or maybe create new features or transform the features you have. What do I mean by features? Well, those are the attributes of your training data, the things that you're training your model with. Let's take an example. So let's say we're trying to predict how much money people make based on various attributes of the people. So your features in that case might be the age of a person, their height, their weight, their address, what kind of car they drive, any number of things. Right. Some of those things are going to be relevant to the things you're trying to predict, and some of them won't be. So the process of feature engineering is in part just selecting which features are important to what I'm trying to predict and choosing those features wisely. A lot of times you need to transform those features in some way as well. You know, maybe the raw data isn't useful for the specific model you're using. Maybe things need to be normalized or scaled in some way, or encoded in some specific way. Often you'll have things like missing data in the real world. Often you do not have complete data for every single data point, and the way that you choose to deal with that can very much influence the quality of the resulting model that you have. Also, sometimes you want to create new features from the existing ones that you have. Perhaps the numerical trends in the data that you have for a given feature are better represented by taking the log of it, or the square of it, or something like that. Or maybe you're better off taking several features and combining them mathematically into one to reduce your dimensionality. This is all what feature engineering is about. You can't just take all the data you have and throw it into this big machine learning hopper and expect good things to come out the other end. This is really the art of machine learning. This is where your expertise is applied to actually get good results out of it. It's not just a mechanical process where you follow these steps. Take all the data you have, throw it into this algorithm and see what predictions you make. That's what separates the good machine learning practitioners from the bad ones. The ones that can actually do feature engineering are the ones that are the most successful and the most valuable in the job marketplace, of course. And this isn't stuff that's generally taught, right. So this is largely a lot of stuff that is learned through experience and actually being out there in the real world and practicing machine learning, and that's why this exam actually focuses a lot on it. The machine learning certification exam is trying to weed out people who actually have experience in this field, versus people who don't. They're really trying to give people who actually know what they're doing and have real world experience a very big edge on this exam. And, well, I'm going to do my best to teach it to you here. It's not something that's generally covered, but there are some general best practices that if you know them, they'll definitely help you out in this exam. Why is feature engineering important in the first place? Well, it's about the curse of dimensionality. What do we mean by that? Well, like I said, you can't just throw every feature you have into the machine and expect good things to happen. Too many features can actually be very problematic for a few different reasons. First is that it leads to sparse data. So again, come back to the example of trying to train a model on attributes of people. There are hundreds of attributes on a person you could come up with, right? Like we said, age, height, weight. What car do you drive? How much money do you make? Where do you live? Who knows? You know, um, where did you go to college? The list goes on and on and on. And you can actually envision each person as a vector in the dimensional space of all these features. Okay. So stay with me here. Imagine, for example, that the only feature we have is a person's age. You could represent a person by a vector along a single age axis, right. Going from 0 to 100 or whatever. Now we throw in another dimension, say their height. We have another dimension, another axis that we have this vector pointing to that encodes both their age on one axis and their height on another, right. So now we have a two dimensional vector. Throw in a third dimension there. Say how much money they make. Now we have a vector in three dimensions where one dimension is their age, one dimension is their height, one dimension is how much money they make. And as we keep adding more and more dimensions, the available space that we have to work with just keeps exploding, right? Um, this is what we call the curse of dimensionality. So the more features you have, the larger the space that we can find a solution is within. And having a big space to try to find the right solution in just makes it a whole lot harder to find that optimal solution. So the more features you have the more sparse your data becomes within that solution space, and the harder it is to actually find the best solution so you're better off boiling those features down to the ones that matter the most. That will give you less sparse data and make it a lot easier to find the correct solution. Also, just from a performance standpoint, imagine trying to create a neural network that has inputs for every one of those features encoded in whatever way that it needs, right? This neural network would have to be massive, extremely wide at the bottom, probably extremely deep as well, to actually find all the relationships between these many features. And it's just going to be ridiculously hard to get that to converge on anything. So a big part of success in machine learning is not just choosing the algorithm, not just cleaning your data, but also choosing the data that you're using in the first place. That's what feature engineering is all about. Again, a lot of it comes down to domain knowledge and sort of using your common sense about what will work and what won't toward improving your model and just experimenting with different things and seeing what makes an effect and what doesn't, what helps what hurts things. So a lot of it is just going back and forth with does this feature help things? No. Okay. We won't use it. Does this feature help things? No. Okay. Try something else. Now, you don't always have to guess. To be fair, there are some more principled ways of doing dimensionality reduction. so one of them is called PCA. Principal component analysis. And we'll talk about that more in the modeling section of the course. But PCA is a way of taking all of those higher dimensions, all those different features that you have and distilling them down into a smaller number of features to a smaller number of dimensions. And it tries to do this in a way that preserves information as well as possible. So, I mean, if you have enough computational power to actually use PCA on a large set of features, that is a more principled way of distilling it down to the features that actually matter, and the features you end up with aren't actually things you can put a label on. It's just, you know, artificially created features that capture the the essence of the features that you started with. K-means clustering is another way of doing this. What's nice is that these are both unsupervised techniques, so you don't have to actually train these on anything. You can just throw the feature data you have into these algorithms, and it will boil out, if you will, a smaller set of dimensions that will work just well, hopefully closely as well. But again, more features is not better. That leads to what we call the curse of dimensionality. And that's one of the main reasons that we want to do feature engineering. And one of the main things you're going to be doing in that process.

---

### Lecture: Lab  Preparing Data for TF-IDF with Spark and EMR Studio, Part 1

So in this lab, we're going to practice preparing data at scale on Apache Spark on a cluster. And we're going to do it in the context of preparing data for a TF IDF algorithm. And you might not know what tf IDF is. So let's start by covering that. TF IDF stands for term frequency and inverse document frequency. Basically it's used in search algorithms. For the most part, it's a good way to figure out how relevant a given term is for a given document. So if you can compute the tf IDF score for every term in a corpus of documents, you can use that to figure out which documents are most relevant to that search term. It sounds fancy, but it's actually a lot simpler than it sounds. Term frequency or TF just measures how often a word occurs within a document. So that's it. Term frequency is just like what it sounds. A word that occurs very frequently within a given document is probably pretty important to that document's meaning. For example, a document that contains the word or phrase machine learning a lot is probably about machine learning Document frequency is just how often that word occurs across the entire set of documents. So, for example, maybe you're looking across all of Wikipedia or every web page that's being indexed by a search engine. The document frequency would tell you how common that word is across all documents, and that tends to tell you about common words like a and the and, and and words that are common in every document but that aren't really relevant to a specific document. They're just common words. So by taking these two things together, the term frequency, inverse document frequency, you can figure out just how important a given term is to a given document. So you can measure that relevancy by just taking the term frequency and dividing it by the document frequency. Now it turns out that division is the same thing as multiplying by the inverse mathematically. And that's why we call this TF-IDF. It's the same thing mathematically as term frequency. Inverse document frequency. We just take how often the word appears in a document and divide that by how often it appears just everywhere. That will give you a measure of just how special that word is for that. Given document. Now, in practice, there are some nuances to how we actually use this. For example, we use the actual log of the inverse document frequency instead of the raw value. That's because word frequencies in reality tend to be distributed exponentially. So by taking a log, we end up with a slightly better weighting of the words given their overall popularity. And there are some general limitations to tfidf one is that we just treat everything as a bag of words. Every document is just a collection of words, and we don't really pay attention to the relationships between the words so much. Obviously, that's not always the case, and it turns out that parsing out those words is a good part of the work, as we'll soon see also. What do you do about things like synonyms and tenses of words and abbreviations and misspellings? We'll see in our example here that we'll at least deal with capitalization by making everything lowercase. But there are more complicated things you could do to try to make this work better. Also, doing this at scale becomes challenging. Mapping all of these terms and documents together into numbers that tie together is kind of hard to do. And that's where Apache Spark is going to come in to help. We talked a little bit about the bag of words approach. One way around that limitation is to look at not just unigrams or individual words in a document, but also things like bigrams or more generally, n-grams. So look at words that occur together. For example, if I had a document that just contained the phrase I love certification exams, I could just look at the unigrams and consider the terms to be the individual words. I love certification and exams, but I could also look at bigrams and consider those terms as well. That would be every grouping of two words that appears together. So for example, the bigrams in this sentence are I love love. Certification and certification exams. That is, every unique combination of two words that appear in that document. I could go further to trigrams and those would be I love certification and love certification exams. I bring this up because one of the sample questions that AWS provides you for the certification exam gets into the guts of all this. So here's an example of what a tfidf matrix might look like. That's considering both unigrams and bigrams. Let's imagine that we have two documents in our entire corpus here. One document only contains the sentence I love certification exams and the other document just contains the sentence I love puppies. This is what that matrix might look like when we're actually computing TF-IDF for every individual term, across every individual document. The documents appear on the rows of the matrix, and each individual term appears on the columns. So since we're looking at both unigrams and bigrams, we start by taking each individual unique word that appears in this in this corpus as individual terms. So that consists of the words I love certification exams and puppies. Since the words I and love appear in both documents, those aren't counted twice. Those are deduplicated there. We only consider those once in the matrix, so a term can only appear once in a given column. And then we're looking at bigrams as well. And the unique bigrams across this entire document set are I love love. Certification love puppies and certification exams. Again, the bigram I love is shared among these two documents, so they will appear more than once. And the job of TF-IDF would be to flesh out this matrix and actually compute the TF-IDF values for each document for each individual term, and again, those terms can be unigrams or bigrams in this example. In this particular example, the dimensions of this matrix are two by nine. We have two rows corresponding to two documents, and nine columns corresponding to nine unique terms, which consists of both the unigrams and bigrams that are uniquely represented within this document set. So how do you use TF-IDF? Well, you could make a very simple search algorithm using nothing but it. So you could just go through and compute TF-IDF ahead of time for every term in a corpus of data. And then for a given search term, sort the documents by their TF-IDF score for that term, then just display the results and you're done. So let's actually try that out. We're actually going to try to apply TF-IDF on a subset of the Wikipedia data set. And we're going to build our own little search engine using TF-IDF on Wikipedia. And to make it interesting, we're going to do it on AWS using elastic MapReduce and Apache Spark. And our primary goal here is to show you how to preprocess that data ahead of time, because, well, that's what this section is really about.

---

### Lecture: Lab  Preparing Data for TF-IDF with Spark and EMR Studio, Part 2

All right. So for this activity we're going to recreate Wikipedia if you will, using a little subset of data that we took from the real Wikipedia. But instead of being all trendy and using some deep neural network to power search, we're going to do it the old fashioned way with term frequency, inverse document frequency instead. And to make it more relevant to this course, we're going to build that on elastic MapReduce using Apache Spark running on EMR workspaces, which in turn has an EMR notebook all running in an EMR studio instance. So let's get started. I've already signed in to my AWS console here. And a little note here. Do not do this under your root account if you can help it. I did run into a bug with EMR studio that seems specific to root accounts, so assuming that's still there, better to log in with a specific IAM user if you can. Instead go on to your console home and go to EMR if you haven't been there before. So let's go ahead and use EMR to spin up a little notebook instance we can use in order to play around with PySpark and actually implement Tfidf TF-IDF. Using some little sample of some real Wikipedia data. Now this used to be a lot harder. It used to be that you had to open up a tunnel to look at a notebook that was running directly on your EMR server, on a cluster somewhere. These days it's a lot easier. You can just go to EMR studio here and let's go to studios and hit Create Studio, and you can see that it has this pre-populated thing for an interactive workload. Well, that's what we want. We want to interactively work with PySpark in a notebook environment. So these default settings are going to be fine. And furthermore it's going to be using an EMR serverless application. So we don't even have to worry about setting up an EMR cluster ourselves either. It's all serverless now, so that makes life a whole lot simpler. Now, do make sure that you're logged in as an IAM user and not as your root account. When you're doing this, there's a lot of trickery it has to do under the hood to make sure that your user has the appropriate permissions in IAM to actually run that serverless application properly, and it can't do that if you're the root user. So if you are signed in as a root user. Sign out. Create an IAM user if you have to and log in using that IAM user instead. All right. So let's go ahead and create a studio and launch workspace. It's just that simple. Well let's change something here right. Just to do something make ourselves feel a little bit productive. Let's at least change the name. So the studio name by default is studio underscore one. Let's call that something else. Like, I don't know, tf IDF. Now in the real world, this is going to be some sort of a shared environment used by your organization or by your team. So in a practical sense, this is probably going to be a name that's more associated with your work group. You know, the people that you're working with, the the organization that's going to be using this studio together. Remember, EMR studio has collaboration features so multiple people can work on it at the same time. So that might not be the best name in the real world, but for what we're doing, just a little toy example using tf IDF, that makes sense. Now we're going to go ahead and let it create its own S3 bucket for storage. We're going to let it create its own service role as well. That saves us a whole lot of time. Instead of mucking about with IAM. And we're also going to set up a workspace setting here, so let's go to change that as well. We'll call that Tfidf as well, just for lack of a better idea. The workspace is basically the new name for the notebook itself. So this is going to be encompassing the environment that your actual Jupyter notebook is running on. And that is going to attach itself to the EMR serverless application. Right. So it's kind of this hierarchy going on, right. So at the bottom we have an EMR serverless application that's actually doing the work of running our code and running spark above that. That's wrapped by a workspace that contains a notebook to let us interact with that serverless application on EMR. And above that we have a Visual Studio that includes this workspace. So not much to change here. I'm just going to stick with the default application name. And you can't actually change these. So we're just going to leave them as they are. It's serverless. So it has pre-initialized capacity. But it does have the capability in theory to grow as needed and expand its capabilities. Now if you did want to have more control, there is a custom option here when you create the studio, and that will give you finer control over the exact IAM permissions you want to use, the exact S3 buckets you want to use. You can even create your own EC2 cluster from scratch if you don't want to use EMR serverless. So that's there as well. But again, that's a lot of work you don't need to deal with right now, and you don't need that for the exam for sure. It's just the concepts here. Let's go ahead and create that studio and launch a workspace. Shouldn't take too long. And so now the workspace is launching for this studio instance. And that's going to be encompassing our notebook. So we're just going to wait for that to open up in a new browser tab us. Again that shouldn't take too long. Here we go. And what we're going to do is upload the notebook that I already gave you. Now you could just hit PySpark here and go to town and start writing stuff from scratch. But I'm not going to make you do that because again, the exam does not expect you to actually write code. So let's just hit this little upload files button here and navigate to your course materials. You should find a tfidf dot ipynb file. Let's go ahead and upload that and we will overwrite the default one that came with this workspace. And now we should be able to double click that and open it up. All right. So you can see that we have the PySpark kernel selected here but no clusters attached yet. That's because it's still in the process of spinning up up that EMR serverless instance. So we're gonna have to give that a little bit of time. You can see that it's now in the status of attaching that serverless interactive app. And that went pretty quickly. Now once it's to select a kernel now we're going to be using PySpark because that's what this notebook was written in to allow us to do spark and not just Python. So let's select PySpark. And you can see that it's back in the process of still attaching that. We can't actually use this notebook until it's done. So we're just going to wait a little bit for that to finish. Again it shouldn't take too long. And it says it's attached. Very cool. And PySpark. So we should be good to go. Now one thing we have to take care of is making sure that our workspace here can actually get to our data, so we need to put that into an S3 bucket that we have permissions to access. Now, because we didn't go to the trouble of creating a custom configuration for this workspace, we didn't set up explicit permissions to a specific S3 bucket. So to make life easier, I'm just going to throw that data into the bucket that it automatically created as part of the quickstart there. So to find out where that bucket is, you need to go back to EMR studio, select the studio we just created and then click on Workplace Storage. And that is the place it has access to. If you did want to add another bucket there that you want to put stuff in or get grant access to, then you'd have to go through all the stuff of making sure that your VPC can access the external internet to hit S3, make sure that your IAM permissions have the right permissions to access that bucket, and so on and so forth. But again, we're going to cheat and make things easier by just using the bucket that we know works. So here we are in our bucket. And we just hit upload and we'll go to Add files. And if you just go to your course materials you should find the subset small dot TSV file. We'll go ahead and upload that. There it is. Let's go ahead and click on that. And from there we can copy the URI that we need to access from within our script. So let's go ahead and copy that back to our notebook. And we're just going to replace this string here with the one that you just copied. All right let's go ahead and restart this kernel just to clear out all this other stuff from when I ran it originally. To do that, I'm going to go to kernel and say restart kernel and clear all outputs and restart. And that might take a moment. You can see down at the bottom it says restarting. Give it a little bit of time to finish. This also has a nice little side effect of kind of forcing the serverless application to do what it needs to do to get spun up. Honestly, this can be a little bit, uh, a little bit finicky sometimes. I found a couple of troubleshooting steps, so if you've run into some trouble so far, if you did get some permission errors that popped up again, that's probably because you're running as the root user and not as an IAM user. It cannot automatically attach the permissions you need to your user account if you're the root user. So make sure that you're running this as an IAM user. And sometimes this just plain doesn't work. You know, I've run into a lot of instances where I start to run this code and it says it's running, but nothing at all happens. I get no feedback whatsoever. It just immediately terminates. And it seems to be that spinning up, that serverless cluster is just a little bit flaky sometimes, you know, sometimes you just have to try it a few times until you get an instance that actually works. I could, you know, editorialize a little bit about the reliability of AWS these days, but I'll spare you that, you know, sometimes you just have to try it a few times and get an instance that works. All right. It looks like this, uh, kernel finally restarted. That took kind of a surprising amount of time. Let's make sure it actually works. So we're going to start off by just reading in some raw data from Wikipedia, we have this subset small TSV file that I've already loaded into S3 for you in a publicly available bucket, to save you the trouble of uploading that data and finding where it comes from. So all we're going to do is read that in using spark, and then call show on the resulting data frame to see what's in there. So let's shift enter to run that first block. And it looks like it's working so far. That's a good sign. Execution started starting the spark application. First time you do this it might take a minute or so to spin up. So we'll just give that a little bit of time to run again. If you're not seeing that though, if there's no output whatsoever you might have got a bad serverless cluster. So go ahead and start over until you get one that does work. Unfortunately, I don't have better advice there. Anyway, this one is working, so let's just wait for that to finish. Go ahead and load up that data and it's done. Very cool. All right we're back. That didn't take too long, but pretty cool huh? So we actually loaded up that data set. And here is what it looks like. You can see that we don't have any column names. So that's the first problem we're going to have to address. Say what these are. But obviously these are some sort of identifiers and some sort of article title. So apparently this data set starts with anarchism and then autism and albedo and goes through the A's and then the date of the article and the text of the article itself. So pretty interesting data set here. Show the top 20 rows there. First thing we need to do is give those columns a name so we can refer to them more intuitively. So we're going to go ahead and load up that raw data that we just loaded up from the CSV file, and load that up into a spark data frame. And do that with the specific column names of ID, title, time and document. And go ahead and show the results of that. So again I'll shift enter to run that. And now that we've loaded up spark already, that runs much more quickly. And you can see we have the same data here but with human readable column names. All right. That's progress. So now we need to clean our data out. So first let's explore it shall we. Exploratory data analysis a bit here. We know that Tfidf cannot handle null documents. So let's first make sure that we don't have any null documents. The syntax for spark is just to call the filter command here on the operator is null on the document field, and we want to count up how many they are. So at a high level what this line is doing is saying count up how many documents have null values in the articles data frame. And the answer is go, spark, go. It's one. There's actually one null document. So it's a good thing we checked. We have to get rid of that. Now if you were to dig into that I'm not going to do that right now because I'm conscious of your time. But it is a corrupt record. So we're just going to drop it. You know, there is this whole problem of how do you deal with missing data in data analysis. But when you're talking about one row probably makes sense just to drop it. So we're going to go ahead and run a filter for just documents that are not null, and then count up how many are null, just to double check that there aren't any left after that operation and make sure that the is not null. Operator did what we wanted and only gave us back a new data frame called cleaned articles that contains not null documents. Let's kick that off. And it returns zero, indicating that our new cleaned article's data frame has no null documents. Good. That's progress. Now, the thing we need to deal with next is the TF-IDF model itself requires numbers, not words. So it needs us to tokenize all of the words in these articles into numerical representations. Basically, we need to come up with some sort of a mapping of each unique word in our data set that maps to some unique number. So we need to do that before we can actually run any fun algorithms on it. So we're going to go ahead and tokenize these articles to split them up into words. And then we'll store them into a sparse vector that is a numeric representation of the words in each article. So again, I'm not going to dig into the code too much here. But basically we're going to make use of something called a tokenizer and a hashing TF from the PySpark machine learning library to take care of that for us. So we'll first tokenize our data, which again just figures out what words are unique, what's the list of unique words. And then we'll call this hashing function to associate those unique words with numerical hashing values, and we'll show what we end up with again, just hitting shift. Enter in each column here and each cell here as we go. Go, spark. Go! All right. There we have it. So you can see here that for each document ID we have a list of words. And followed by the numerical representations of those words. So very cool. Looks like it worked. Now it also basically computed term frequencies for us by also adding up how often each hashed word occurs in each article. So without digging too much into the structure that it gave us back here, that information does include the term frequencies for each article. However, we want tf IDF scores for each term in every document, so we're only halfway there, but it's not hard to do. We also have an IDF thing that we can use from the Python, from the Spark Machine learning library that will do that part of it as well. So we're going to go ahead here and create an IDF model here and take in those raw features from above and create a new features column in the end that contains those tf IDF scores for each individual feature. Let's go ahead and kick that off. And we'll show the results. All right. So now we have a features column as well as a raw features column that contains not just the TF but also the tf IDF scores. So let's use that information to actually do sort of a search. So let's say we want to search for the term Gettysburg. Well this is a little bit harder than you would think it would be in spark. But this is one way to do it. So again we need to use numbers not words. So I can't just say search for Gettysburg. First I need to figure out what is the hash value for Gettysburg in that same structure that we had above. So I need to figure out what's the hash value from the model that I used above to compute these scores for the actual term Gettysburg. So that's what this huge block of code does. Basically, it's saying I want to create a data frame that just contains the word Gettysburg, just that one feature. And I'm going to go ahead and use that same hashing TF transform on it to transform that and then collect back the actual hash value associated with that term. So by running all this big chunk of code, we should get a number. And that number is 116,775. So apparently that's the magic number that represents Gettysburg. Again I want to stress that understanding the code of spark is not relevant for the exam. That's why I'm kind of glossing over that there. I don't want to clutter your brain with stuff you don't need. But the important thing here is that we do have a a hash value for Gettysburg. And now all we need to do is extract the TF-IDF value for Gettysburg for each document. So previously we extracted the scores for every term in every document. All we care about though is Gettysburg. So let's go ahead and extract that using this UDF function on our data frame to suck that out, basically, and create a new data frame that's only about Gettysburg TF-IDF scores. Kick that off. And now we have our new data frame. That's just the TF-IDF scores for Gettysburg. Now if you run into an error, just try it a few more times. If you're just running on a single extra large instance, that's kind of borderline enough resources for what we're doing here. So sometimes that will fail and you just have to try again. Spark can be a little bit non-deterministic that way, but we're almost there now. We have all of our documents here with the score for Gettysburg associated with it. All we need to do now is sort them by the score for Gettysburg, and we can already see Abraham Lincoln sticking out there already. Right. So let's go ahead and order that by that score for Gettysburg and print out the results. And we'll truncate that to 100 characters per column just to make sure we can read it. Here we have it. So for the search term Gettysburg, we got back articles about Abraham Lincoln, Abner Doubleday, who was a Civil War person, the American Civil War itself, the city of Austin, Texas. And then we start to get a little bit sketchy and we get the result of Assyria. But the top results definitely make sense. So TF-IDF seemed to have worked. But what's really cool is that we did this from scratch, right? So we just took a raw set of data from Wikipedia. We created our own TF-IDF model. After cleaning that data and creating a model for it, and we applied that model to actually do sort of a quasi search here for a term and get back meaningful results. All on EMR using an EMR workspace within it, running an EMR notebook, running PySpark within an EMR studio instance that is backed by an EMR cluster. So very cool. We did it. Let's clean up our mess though. So let's go ahead and close out of this and go back to EMR studio. Now there are quite a few resources here that we need to get rid of to make sure that we're not going to get any surprise charges on our bill. And, you know, honestly, AWS is getting pretty sneaky about this stuff lately. They make it a little bit too easy to leave stuff running that you're not using. And in the case of things like SageMaker studio, you could find yourself with $1,000 bill at the end of the month if you're not careful. Um, so let's be paranoid. Let's start by going to the underlying EMR serverless application we created for this workspace. And we're going to find that under Manage Applications. And there's the interactive serverless app that we were using there. Let's go ahead and delete that. So I'm just going to say stop the application. Wait for that to stop. Can't delete it till it's done. And now that it is stopped we're going to go ahead and delete that permanently. All right. Next we need to delete the workspace that was using that serverless application. There it is under the workspaces section. We'll go ahead and select that and say delete confirm. And that is deleting. And now finally we can go back to our studio itself. We got kicked into a different UI here that doesn't show that to you. So you have to go back to the main EMR screen here and go back to studios. There's that studio. Select that and delete it as well. All right. Now there's still some leftover stuff in S3 and also an IAM role you might want to get rid of just to clean things up. But that's not going to cost you really anything. So I'm not going to go into that too much. But just be sure to clean up your mess here with the studio itself and the underlying workspace and EMR serverless app, to make sure that you don't get any surprises on your bill at the end of the month. And there you have it. We've used EMR studio to go ahead and create a interactive notebook that we use to actually implement Tfidf, and did a little Wikipedia search from scratch, so very cool.

---

### Lecture: Imputing Missing Data

So a big part of feature engineering is imputation of missing data. What do you do when your data has a missing data elements in it? This is what happens in the real world. For every observation you have, there's going to be some missing data points more than likely. Well, a simple solution is just called mean replacement. The idea is that if you have a missing attribute or feature within one of the rows of your data, just replace it with the mean from the entire column. And remember we're talking about columns, not rows. Here you want to take the mean of all of the other observations of that same feature. Doesn't really make sense to take the mean of all the other features for that row, right? So mean replacement is all about taking the mean of that column and replacing all the empty values with that mean. So it's fast and it's easy. Those are some of the the positives of this approach. It also doesn't affect the mean or the sample size of your overall data set, because you're just replacing missing data with the mean. It won't affect the overall mean of the entire data set, which can be nice. Now, one nuance is that if you have a lot of outliers in your data set, which is also something you have to deal with when preparing your data, you might find that median is actually a better choice than the mean. So if you have a data set of a bunch of people and maybe one of those columns is income, and some people don't report their income because they think it's sensitive. Uh, you know, you might have your mean skewed by a bunch of millionaires or billionaires in your data set. So if you do mean imputation in that sort of a situation where you have outliers, you might end up with an overly high or overly low value that you're using for replacement. So if you do have outliers that are skewing your mean, you might want to think about using median instead. That will be less sensitive to those outliers. But generally speaking, it's not the best choice for imputation. First of all, it only works on the column level. So if there are correlations between other features in your data set, it's not going to pick up on those. So, you know, if there is a relationship between, say, age and income, that relationship is going to be totally missed. So you could end up saying that a ten year old is making, you know, $50,000 a year, because that's the mean of your data set But it really doesn't make sense, right? I mean, a 15 year old wouldn't be making that much money yet. So it's a very naive approach from that standpoint. The other issue is that you can't really use it on categorical features. You know, how do you take the mean of a categorical piece of data? That just doesn't make sense, right? Although you could use the most frequent value that appears, you know, the most commonly seen category would be a reasonable thing to do. In that case, it's sort of in the same spirit as mean replacement, but not really the same thing. Overall, though, it's not going to be a very accurate method. It's a very ham handed attempt at doing imputation. So although it's quick and easy and has some advantages in practice, if someone's asking you, say on a certification exam, what's the best way to do data imputation mean replacement probably, isn't it? It's also probably not just dropping the missing rows, although as we've seen sometimes, that's a reasonable thing to do. If you do have enough data such that dropping a few rows doesn't matter if you don't have too many rows that contain missing data, Well, it doesn't sound that unreasonable. The other thing, too, is that you want to make sure that dropping the rows that have missing data doesn't bias your data set in some some way. What if there's an actual relationship between which rows are missing data and some other attribute of those observations? For example, let's say that we're looking at income. Again. There might be a situation where people that have very high or very low incomes are more likely to not report it. So by removing or dropping all of those observations, you're actually removing a lot of people that have very high or low incomes from your model. And that might have a very bad effect on the accuracy of the model you end up with. So you want to make sure that if you are going to drop data, that it's not going to bias the data set in some way as, as a byproduct. Right? So it's a very quick and easy thing to do, probably the quickest and easiest thing to do. You can literally do this in one line of code in Python, but it's probably never going to be the best approach. So again, if an exam is asking you what's the best way to impute missing data dropping data probably is not the right answer. Almost anything is going to be better. Maybe you could just substitute a similar field, right? I mean, that would also be a simple way of doing it. For example, I might have a data set of customer reviews on movies, right? Maybe if I have a review summary and a full text review as well, it would make more sense to just take the review summary and copy that into the full text for people who left the full text blank as an example. So almost anything is better than just dropping data. But, you know, in the real world, if you're just trying to do something quick and dirty and sort of like start experimenting with some data just to start playing with it, it can be a reasonable thing to do. I just wouldn't leave that in place for production necessarily. The thing you probably really want to do in production is by using machine learning itself to impute your missing data into your machine learning training. So it's a kind of a meta thing. There are different ways of doing this. One is called KNN that stands for K Nearest Neighbors. And if you have any experience with machine learning, you probably know what that is already The general idea is to find the k where k is some number of the most similar rows to the ones that you're looking at that has missing data, and just average together the values from those most similar rows. So you can imagine having some sort of a distance metric between each row. Maybe it's just the Euclidean distance between the normalized features within each row or something like that. And if you find the, say, ten nearest rows that are most similar to the one that's missing data, you can just take the average of that feature from those ten most similar rows and impute the value from that. So that takes advantage of relationships between the other features of your data set, which is a good thing. Um, one problem with it, though, is that that idea assumes that you have numerical data that you're trying to impute and not categorical data. It's tough to take the average of a category, but there are ways of doing that with techniques like Hamming distance. But KNN is generally a better fit for numerical data, not categorical. If you have categorical data, you're probably better served by actually developing a deep learning model. Neural networks are great at doing categorization Problems. So the idea would be to actually build a machine learning model to impute the data for your machine learning model, right? It's a kind of a cycle there, and that works really well for categorical data. It really well. It's tough to beat deep learning these days. However, of course it is complicated. There's a lot of code involved and a lot of tuning as well. Um, but it's hard to beat the results if you actually have a deep learning model that tries to predict what a missing feature is based on other features in your data set, that's going to work out. A lot of work, a lot of computational effort, but it's going to give you the best results. You can also just do a multiple regression on the other features that are in your data set. That's also a totally reasonable thing to do. And three regressions. You can find linear or non-linear relationships between your missing feature and the other features that are in your data set. And there is a very advanced technique along these lines called mice, which stands for multiple imputation by chained equations. It's kind of the state of the art in this space right now for imputing missing data. So, you know, if you see a question that says what's a good way to impute missing data? And one of the answers is mice. A good chance that might be a reasonable answer for you there. To be clear, I didn't actually see a question like that on the exam, but if I were writing the exam, I might put that on there. All right. And finally, probably the best way to deal with missing data is to just get more data. So if you have a bunch of rows that have missing data, maybe you just have to try harder to get more complete data from people. And it's hard to beat just getting more real data so that you can just not have to worry about all the rows that have missing data. Again, you want to be careful that if you are dropping data that you're not biasing your data set in some way. But, uh, really, the best way to deal with not having enough data is to just get more of it. Sometimes you just have to go back and figure out where that data came from and collect more, better quality data. So the better quality data you have going into your system, the better the results you will get. And while imputation techniques are a way to cover up issues where you just don't have enough data and you can't get more of it, it's always a good idea to just get more and better data. If you can.

---

### Lecture: Dealing with Unbalanced Data

Another problem in the world of feature engineering is handling unbalanced data. What do you mean by that? Well, that's when we have a large discrepancy between our positive and negative cases in our training data. So a common example is in the world of fraud detection, actual fraud is pretty rare, right. So most of your training data is going to contain training rows that are not fraudulent. This can lead to a difficulty in actually building a model that can identify fraud, because it had so few data points to learn from compared to all of the non fraud data points. So it's very easy for a model to say, okay, well, since fraud actually only happens like 0.01% of the time, I'm just going to predict that it's not fraud all the time. And hey, my accuracy is awesome now. Right. So if you have an unbalanced data set like that, you can end up in a situation like that where you have a machine learning model that looks like it has high accuracy, but it's just guessing. No, every time. And that's not helpful, right? So there are ways of dealing with this in feature engineering. Now first of all, don't let the terminology confuse you. This is actually something that I got hung up on a lot at first. When I say positive and negative cases, I'm not talking about good and bad, so don't conflate positive and negative with a positive and negative outcome. Positive simply means, is this the thing that I'm testing for? Is that what happened? So that might be fraud, right? So if I'm if my model is trying to detect fraud, then fraud is the positive case, even though fraud is a very negative thing. Remember positive is just a thing that you're trying to detect, whatever that is. So beat that into your head. Because if you keep conflating positive and negative with like moral judgments, that's not what it's about in this. In this context, this is mainly a problem with neural networks, by the way. So, you know, it is a real issue that if you have an unbalanced data set like this, it's probably not going to learn the right thing. And we have to deal with that somehow. What's one way of dealing with it? Just oversampling is a simple solution. So just take samples from your minority class. You know, in this example of fraud, just take more of those samples that are known to be fraud and copy them over and over and over again. You know, make an army of clones, if you will, of your fraudulent test cases. And you can do that at random. You would think that that wouldn't actually help, but it does with a neural network. So that's a very simple thing you can do. Just fabricate more of your minority case by making copies of other samples from that minority case. The other way you can go is undersampling. So instead of creating more of your minority cases, remove the majority ones. So in the case of fraud, we'd be talking about just removing some of those non-fraudulent cases to balance it out a little bit more. However, throwing data away is usually not the right answer. I mean, why would you ever want to do that? You're discarding information, right? So the only time when undersampling might make sense is if you're specifically trying to avoid some scaling issue with your training, right. Maybe you just have more data than you can handle on the hardware that you're given. And if you have too much data to actually process and handle, fine. Throw away some of the the majority case. That might be a reasonable thing to do, but the better solution would be to get more computational power, right and actually scale this out on a cluster or something. So Undersampling usually not the best approach. Something that's even better than Undersampling or oversampling is something called Smote. And this is something you might see. It stands for Synthetic Minority Oversampling technique, Kind of a creative acronym. What it does is it artificially generates new samples of the minority class using nearest neighbors. So just like we talked about using KNN for imputation, same idea here. We're running k nearest neighbors on each sample of the minority class. And then we create new samples from those KNN results by taking the mean of those neighbors. So instead of just, you know, naively making copies of other test cases for the minority class, we're actually fabricating new ones based on averages from other samples, and fabricating them that way works pretty well. So it both generates new samples and under samples, the minority majority class, which is good. So this is better than just oversampling by making copies, because it's actually fabricating a new data points that have some basis in reality still. So remember, if you're dealing with unbalanced data, a Smote is a very good choice. A simpler approach too, is just adjusting the thresholds. When you're actually making inferences and actually applying your model to the data that you have. So when you're making predictions for a classification, say fraud or not fraud, you're going to have some sort of threshold of probability at which you say, okay, this is probably fraud. You know, most machine learning models don't just output a fraud or not fraud. It actually will give you some sort of probability that it's fraud or not fraud. And you have to choose the threshold of probability at which you say, okay, this is probably fraud. It deserves some investigation. So if you have too many false positives, one way to fix that is to just increase that threshold, right. So that is guaranteed to reduce your false positive rate. But it comes at the cost of more false negatives. So before you do something like this you have to think about the impact that that threshold will have. So if I raise my threshold, that means I'm going to have fewer actual things that are flagged as fraud. That might mean that I miss out some actual fraudulent transactions there, but I'm not going to be bothering my customers as much saying, hey, I fly. This is fraud. I shut down your credit card. You might actually want the opposite effect, right? Maybe I want to be even more liberal in what I'm flagging as fraud, so I would lower that threshold to actually get more fraud cases that are flagged. And fraud might be a case where you're better off guessing wrong if it's not fraud, than the other way around, right? So you need to think about the cost of a false positive versus a false negative, and choose your thresholds accordingly.

---

### Lecture: Handling Outliers

Yet another problem in the world of feature engineering is dealing with outliers in your data set. How do you handle them? Do you handle them? How do you identify them? So before we talk about outliers, we need a little bit of a mathematical background here. Don't worry it's not hard. So let's start with the concept of variance. How do you measure variance. Well we usually refer to it as sigma squared. And you'll find out why it's called that momentarily. But for now just know that variance is the average of the squared differences from the mean. So to compute the variance of a data set you first figure out the mean of it. So let's say I have some data. It could represent anything. Let's say the maximum number of people that were standing in line for a given hour or something. And the first hour I observed one person standing in line, then four, then five, then four, then eight. Okay. So the first step in computing the variance is to find the mean the average of that data. I just add them all together, divide it by the number of data points. And that comes out to 4.4. In this case, as the average number of people standing in line. Now the next step is to find the differences from the mean for each data point. So I know the mean is 4.4. So for my first data .4.4 and I have a one for that data point. So one -4.4 is -3.44, -4.4 is -0.4. And so on and so forth. So I end up with these both positive and negative numbers that represent the variance from the mean for each data point. Okay. But what I want is a single number that represents the variance of this entire data set. So the next thing I'll do is find the squared differences. So we just go through each one of these raw differences from the mean and square them. This is for a couple of different reasons. First of all I want to make sure that negative variances count just as much as positive variances. Otherwise they'd all just cancel each other out and that would be bad. I also want to give more weight to the outliers. So this amplifies the effect of things that are very different from the mean, all while still making sure that negative and positive variances are compared comparably. Because the square of a negative is a positive right you always end up with a positive value when you square something. So let's look at what happens when I do this. So -3.4 squared. That's positive 11.6. -0.4 squared is a much smaller number 0.16. Because that's much closer to the mean of 4.4.6 is also close to the mean only 0.36 there. But as we get up to the positive outlier 3.6, that ends up being 12.96. And to find the actual variance value, we just take the average of all those squared differences from the mean, add up all those squared variances, divide it by five the number of values that we have, and we end up with a total variance of 5.04. That's all variance is. Let's move on to standard deviation. Typically we talk about standard deviation more so than variance. And it turns out that standard deviation is just the square root of the variance. So I had a variance of 5.04. The standard deviation is 2.24. So you see now why we call a variance sigma squared. Because sigma itself represents the standard deviation. So if I take the square root of sigma squared I get sigma. And it ends up in this example to be 2.24. Now this is a histogram of the actual data we were looking at. We see that the number four occurred twice in our data set. And then we had 1115 and one eight. Now the standard deviation is usually used as a way to think about how to identify outliers in your data set. So if I'm within one standard deviation of the mean of 4.4, that's considered to be kind of a typical value in a normal distribution. But you can see in this example the numbers one and eight actually lie outside of that range. So if I take 4.4 plus or -2.24, we end up around there and there and 1 in 8 both fall outside of that range of standard deviation. So we can say mathematically that one and eight are outliers. We don't have to guess and eyeball it. We can put a mathematical basis to that. Now there is still sort of a judgment call as to what you consider an outlier in terms of how many standard deviations it's away from the mean by. So you can generally talk about how much of an outlier a data point is by how many standard deviations from the mean it is. So that's something you'll see standard deviation used for in the real world, and you might define your outliers as being one standard deviation away from the mean, or two standard deviations away from the mean. That's kind of the judgment call that you need to make as you're doing your feature engineering. So sometimes it's appropriate to remove those outliers once you've identified them. And sometimes it isn't. Make sure you make that decision responsibly. So for example, if I'm doing collaborative filtering and I'm trying to make movie recommendations or something like that, you might have a few power users that have watched every movie ever made and rated every movie ever made. They could end up having an inordinate influence on the recommendations for everybody else, and you usually don't want a handful of people to have that much power in your machine learning model. So that might be an example where it would be a legitimate thing to filter out an outlier and identify them by how many ratings they've actually put into the system. Or maybe an outlier would be someone who doesn't have enough ratings, or you might be looking at web log data, where an outlier could be telling you that there's something very wrong with your data to begin with. It could be malicious traffic, or it could be bots or other agents that should be discarded that don't represent actual human beings that you're trying to model. But if someone really wanted the mean average income in the United States and not the median, they specifically want the mean. And you shouldn't just throw out the billionaires in your country because you don't like them. The fact is, there are billions of dollars are going to push that mean amount up, even if it doesn't budge the median. So don't fudge your numbers by throwing out outliers, but throw out outliers if it's not consistent with what you're trying to model in the first place. A3. So how do we identify outliers? Well, remember our old friend standard deviation. We covered that a few slides ago. It's a very useful tool for detecting outliers. And it's extremely principled to find the standard deviation of a data set. And if you see a data point that's outside 1 or 2 standard deviations, then you have an outlier. Remember the box and whisker diagrams that we talked about earlier too. Now those have a built in way of detecting and visualizing outliers. And those define outliers as lying outside 1.5 the interquartile range. So what multiple do you choose. Well you kind of have to use common sense here. There's no hard and fast rule as to what is an outlier. You have to look at your data, kind of eyeball it, look at the distribution, look at the histogram, see if there's actual things that stick out to you as obvious outliers and understand what they are before you just throw them away. Also, in the context of AWS. AWS has an outlier algorithm of its own called Random Cut Forest, and it's creeping up into more and more of their services. You can find random cut forests within QuickSight, Kinesis Analytics, SageMaker, and more. So if you're talking about outliers and outlier detection, Amazon seems really, really proud of their random cut forest algorithms. So there's a good chance that that's what they're looking for in the exam. If they mention random cut forest in the context of outlier detection. Let's look at an example here of income inequality. So let's say that we're modeling income distributions. We're just looking at how much money each people makes each each year. And we're trying to like understand what that means about the data set as a whole. So in this simple example here again you're not going to be given Python code in the exam. But this is just to make it real for you guys. We've set up a normal distribution here of income centered around $27,000. And we've appended in a single billionaire and plotted the histogram of that. And as you can see, that that one billionaire really screwed up our distribution. So all of the people, all those normal people, all the non billionaires have been squished together into one little line off on the left there. And our billionaire, that one little data point you can't even see has skewed our data massively. And if we were to take the median the mean of this we'd get a really wacky number. So a very simple outlier detection function here has been written. All it does is compute the median of the entire data set, computes the standard deviation, and if anything lies outside of two standard deviations, it throws it out. And if we call this reject outliers function and then plot the histogram, we get much more meaningful data by rejecting that outlier. So and now we have a more meaningful mean as well. That's closer to the $27,000 that we started with. So that's an example of where you want to think about where your outliers are coming from. Is it really appropriate to throw away that billionaire or not? What effect does that really having on the business outcome that you're trying to achieve at the end of the day?

---

### Lecture: Binning, Transforming, Encoding, Scaling, and Shuffling

Let's quickly go through some other techniques you might use in the process of feature engineering. One is called binning. The idea here is just to take your numerical data and transform it into categorical data by binning these values together based on ranges of values. So as an example, maybe I have the ages of people in my data set. I might put everyone in their 20s into one bucket, everyone in their 30s into another bucket, and so on and so forth. That would be an example of binning, where I'm just putting everyone in a given range into a certain category. So instead of saying that I'm going to train based on the fact that you're 22 and three months old, I'm just going to bucket you into the bin of 20 year olds, right? So I've changed that number of 22 point, whatever it is, into a category of 20 somethings. So that's all binning is. Why would you want to do that? Well, there's a few reasons. One is that sometimes you're you have some uncertainty in your measurements. So maybe your measurements aren't exactly precise and you're not actually adding any information by saying this person is 22.37 years old versus 22.38 years old. Maybe some people will remember the wrong birthday or something, or, you know, you ask them on different days and you got different values as a result. So binning is a way of covering up imprecision in your measurements. That's one way. That's one reason. Another reason might be that you just really want to use a model that works on categorical data instead of numerical data. Um, you know, that's kind of a questionable thing to be doing because you're basically throwing some information away by binning, right? So if you're doing that, you should think hard about why you're doing that. The only really legitimate reason to do this is if there is uncertainty or errors in your actual underlying measurements that you're trying to get rid of. Uh, there's also something called quantile binning that you should understand. The nice thing about quantile binning is that it categorizes your data by their place in the data distribution. So it ensures that every one of your bins has an equal number of samples within them. So with quantile binning, you know, I make sure that I have my data distributed in such a way that I have the same number of samples in each resulting bin. Sometimes that's a useful thing to do. So remember, quantile binning will have even sizes in each bin. Another thing we might do is transforming our data, applying some sort of a function to our features to make it better suited for our algorithms. So for example, if you have feature data that has an exponential trend within it that might benefit from doing a logarithmic transform on it to make that data look more linear, that might help out your model and actually finding real trends in it. Sometimes models have difficulty with non-linear data coming into it. A real world example is YouTube. They published a paper on how their recommendations work, which is great reading, by the way. There's a reference to that in the slide here. They have a whole section on feature engineering there that you might find useful. And one thing they do is for any numeric feature x that they have, you know, for example, how long has it been since you watched a video? They also feed in the square of that and the square root of it. And the idea there is that they can learn super and sublinear functions in the underlying data that way. So, you know, they're not just throwing in raw values. They're also throwing in the square and the square root. Just, uh, just to be careful and see if there actually are non-linear trends there that they should be picking up on. They found that that actually improved their results. So that's an example of transforming data. It's not necessarily replacing data with a transformation. Sometimes you're actually creating a new feature from transforming an existing one. That's what's going on here. So they're feeding in both the original feature x and x squared and the square root of x. You can see in this graph here why you might want to do that. So if I'm starting off with a function of x here on the green line, you can see that by taking the ln the logarithm of that I end up with a linear relationship instead, which might be easier for models to pick up on. I could also raise that to a higher power, which would actually make things worse in this case. But but sometimes more data is better. Again, you know, we're talking about the curse of dimensionality. So there is a limit to that. But that's what feature engineering is all about, trying to find that balance between having just enough information and too much information. Another very common thing you'll do while preparing your data is encoding. And you see this a lot in the world of deep learning. So a lot of times your model will require a very specific kind of input, and you have to transform your data and encode it into the format that your model requires. A very common example is called one hot encoding. Okay, so make sure you understand how this works. The idea is that I create a bucket for every category that I have, and basically I have a one that represents that that category exists and a zero that represents that. It's not that category. Let's look at this picture as an example. Let's say that I'm building a deep learning model that tries to do handwriting recognition on people, drawing the numbers zero through nine. This is a very common example that we'll look at more later. So to one hot encode this information, I know that this thing represents the number eight. And to represent that in a one hot encoded manner. Basically I have ten different buckets for every possible digit that that might represent. 01234, five, six, seven, 8 or 9. Now I usually start counting at zero here. So you can see here that in the ninth slot there there's a one that represents the number eight. And every other slot there has a zero representing that it is not that category. That's all one hot encoding is. So again, if I had a one in that first slot that would represent the number zero. If I had a one inch the second slot, that would represent the number one and so on and so forth. We do this because in deep learning, neurons generally are either on or off. They're activated or they're not activated. So I can't just feed in the number eight or the number one into an input neuron and expect it to work. That's that's not how these things operate. Instead, I need to have this one hot encoding scheme where every single training value, that label is actually going to be fed into ten different input neurons, where only one of them represents the actual category I have. So stare at that picture a little bit. Make sure you understand it. If you're not familiar with one hot encoding, that is probably something you'll see on the exam. We can also talk about scaling and normalizing your data. Again, pretty much every model requires this as well. A lot of models prefer their feature data to be normally distributed around zero, and this is also true of most deep learning and neural networks. And at a minimum, most models will require that your feature data is at least scaled to comparable values. I mean, there are models out there that don't care so much, such as decision trees, but most of them will be sensitive to the scale of your input data. Otherwise, if you have features that have larger magnitudes, they'll end up having more weight on your model than they should. Going back to the example of people, if I'm trying to train a system based on their income, which might be some very large number like 50,000, and also their age, which is a relatively small number like, you know, 30 or 40. If I weren't normalizing that data down to comparable ranges before training on it, that income would have a much higher impact on the model than their ages. And that's going to result in a model that doesn't do a very good job. Now, it's very easy to do this, especially with scikit learn in Python. It has a pre-processor module that helps you out with this sort of thing. It has something called Minmaxscaler that will do it for you very easily. The only thing is you have to remember to scale your results back up. If what you're predicting is not just categories and actual numeric data. So sometimes if you're predicting something, you have to make sure to reapply that scaling in reverse to actually get a meaningful result out of your model at the end of the day. Finally, we will talk about shuffling. A lot of algorithms benefit from shuffling your training data. Otherwise, sometimes there's sort of a residual signal in your training data resulting from the order in which that data was collected. So you want to make sure you're eliminating any byproducts of like, how the data was actually collected by shuffling it and just randomizing the order that's fed into your model. So often that makes a difference in the quality as well. There are a lot of stories I've seen where someone got a really bad result out of their machine learning model, but by just shuffling the input, things got a lot better. So don't forget to do that as well. And that's the the world of feature engineering in a nutshell

---

### Lecture: SageMaker AI Overview

Let's talk about Amazon SageMaker for a while here. SageMaker is basically Amazon's service for machine learning, and while it can do generative AI, there are other services that are more specifically built for generative AI that we'll get into as well. But SageMaker goes back to before generative AI was really a thing. And it's more generally for machine learning algorithms that might include deep learning or generative AI. But it's it's more broad than that. It can do traditional deep learning as well, and it's built to handle the entire workflow surrounding machine learning. It's not just about the algorithms and the training. It's also about fetching, cleaning and preparing your data and then training and evaluating the model as well. And once you have a model that's trained and evaluated, you can deploy that model and evaluate its results in production. So it covers the entire machine learning lifecycle that we talked about through various features of SageMaker. Diving into the training and deployment stage here. So this is what it would look like to actually train and deploy a model under SageMaker conceptually. So outside you have a client application that wants to get predictions from some machine learning model that you've built using SageMaker that will talk to an endpoint, which could be many endpoints. Right? If you're going to be distributing this thing and scaling it out, those endpoints will then communicate with your model, which has been deployed and hosted somewhere. That model will make use of artifacts that hosted in S3, and also the actual model code for doing inference that is stored possibly in ECR in Elastic Container Registry. Now, before you have that model, I'm kind of working backwards here. Um, you have to train that model first to that tool will consume an image of code for training and also consume training data from S3 that's been prepared beforehand. The output of that model training is then the model artifact that goes to the model deployment and hosting stage. So we've kind of gone from the top down. Let's go from the bottom up here to if you're training a model, then you need training code somewhere that's hosted in ECR that needs to consume training data from S3. Once that model is trained, it will produce model artifacts that are again stored in S3. That model gets deployed using inference code that is hosted from ECR, that is fronted by endpoints for inference that your client app ultimately talks to Arthur. Now, there's a few ways of setting all this up. One way is by using code, and if you want to write Python code, then you can do so to orchestrate all of this and control all of the data processing and training and deployment right from a SageMaker notebook. This is just a old school Jupyter notebook here, but with SageMaker those are spun up on EC2 instances for you from the console. It comes with built in access to S3 for you, and it also includes some handy dandy libraries like scikit learn, spark or TensorFlow or PySpark, whatever you want to use. It also comes with a wide variety of built in models that will will dive into very shortly. And from this notebook you can write code pretty easily. That will spin up training instances using whatever instance types you need. Kick off that training, and once that model is trained and evaluated and tuned, you can deploy that model to actually make predictions at scale. So you can orchestrate the entire life cycle here from within a little SageMaker notebook using Python code and a Python notebook that includes processing your data, training your model, tuning your model, and deploying your model. You can also do that from the console, so if you don't like writing code, much of this can be done just from the UI in the SageMaker console. So it's possible to set up training jobs where you're just saying, hey, I want to use one of these many built in algorithms for my training. I'm going to point it to this source of training data and do pretty much all of it from the console itself. So you don't have to write Python code to use SageMaker and build machine learning models that can then predictions in production. It's also possible to do this through the console as well.

---

### Lecture: SageMaker AI Domains

So before you can do anything in SageMaker, I that's what it's called now by the way SageMaker I you need to create what's called a domain. And everything you do within SageMaker is within the umbrella of this domain that you create. So what is a domain. Well you can see on this diagram on the right here it consists of a bunch of stuff. One the main thing is a EFS volume that's shared throughout the entire domain. So the SageMaker domain will carve up that EFS volume among your different users and the shared resources. But under the hood, there's this one EFS volume that's associated with that domain. It also has a list of authorized users that are allowed to access that domain and the stuff within it. And it also has a bunch of configuration settings associated at the domain level, such as various policy settings and also VPC settings, which we'll talk about in more depth shortly. So under a domain you can also have user profiles. And a user profile is basically the concept of a user, a person that's using a SageMaker domain. And anything that's user centric in the UI is going to be tied to a user profile within that domain. Now, those user profiles in turn own personal applications. So as a user within a SageMaker domain, you might spin up a SageMaker studio instance to do stuff, you know, train your models, deploy your models, whatever it might be. You have your own private EFS directory as part of your user profile, so you don't necessarily have to share everything you're doing. You have some private workspace there that's carved out of that EFS volume as well within its own private directory. But you also have shared resources that you can use across other users within the domain. So you can also share your training data, share your notebooks, whatever you want to do. And on the topic of sharing, you also have shared spaces within the SageMaker domain. This gives you access to a shared EFS directory where you can share information back and forth between users. And there's also a communal IDE application associated with SageMaker as well, SageMaker studio, that can be used across all the users within your domain. One of the finer points of using SageMaker AI domain is how VPCs are associated with it. So when you create a SageMaker domain, by default it will have two VPCs associated with it. One is going to be tied to the external internet. So if you need to access data on the internet or deploy data to the internet, it will go through that VPC. There's that VPC by the way is managed by SageMaker I you don't have to set that up. It will do it for you. However, you do have to have your own VPC as well. And that's going to be used for any encrypted traffic to your own EFS volume that's managed by SageMaker. So for your private data that's going to be managed through your VPC that you need to specify. And as part of that VPC specification, you need to also specify the subnets within it. And generally you want to have all of them and also any security groups that you want associated with that VPC. Now you don't have to go with that default configuration. You can change that to only go through your VPC. Now you can also cut out that entire left side of the diagram there by selecting VPC. Only when you create your domain. By doing that, all the traffic will go through your VPC, and you can have finer level control over what that VPC is allowed to access. So that's the SageMaker AI domain in a nutshell. Basically an organizational unit of all the stuff you're doing within SageMaker. It encompasses all of your users, all of your applications, your shared workspaces, and also the network architecture underneath it.

---

### Lecture: Data Processing, Training, and Deployment with SageMaker AI

Let's go into a little bit more detail about how you will prepare your data, train your model, and deploy your model under SageMaker for data preparation. Usually your data will come from S3. FSX for lustre is also an option, depending on the scale that you're dealing with, and the ideal format of that data will vary based on the algorithm that you're using. So there's a lot of built in machine learning algorithms with SageMaker you can choose from, and each one of those will have their own recommendations for the ideal format of data to be fed into it for the best performance. Often it's something like record IO or protobuf, maybe some sort of a columnar data store that might be more efficient for processing that data. For training a model, you can also ingest data from Amazon Athena, which is one of their database products, elastic MapReduce, which is a distributed processing framework. Redshift, which is our data warehouse solution, or Amazon Key Spaces database as well. It's another possibility. Among others, you can also integrate SageMaker with Apache Spark, which is another distributed data processing system that is not specific to AWS, but it's widely used throughout the industry. So if you want to use Apache Spark for your data processing, you can that does integrate with SageMaker as well and all of the popular data science and data analytics libraries for Python are available to you for data preparation on SageMaker as well. Scikit learn, numpy, and pandas are all there at your disposal within SageMaker notebooks if you want to do it there. Conceptually, processing is pretty simple. You just copy data in from S3 that's been prepared in some manner to be amenable to what you're trying to do, and then you spin up a processing container that can either be your own code or it could be something built into SageMaker. So if you don't want to roll your own code and make your own container for the processing code, you can just use the built in algorithm as well. So there are built in processing containers out there that you can just choose from from SageMaker. Once it's done processing your data, that output will be processed and stored into another S3 bucket that you specify, which in turn will be passed on to training. When you are training, all you need to do is create a training job under SageMaker as an input that will take the URL of that S3 bucket that we just produced that contains the processed training data. You will assign various compute resources for that. Machine learning training and training can be expensive and require pretty hefty hardware. So this is where things can get a little bit pricey, but you get to choose what kind of hardware you want to use and how much of it. Different algorithms have different capabilities in terms of scalability, so some do best with a single computer, some will do best with multiple instance types, and they all have their own recommendations about what kind of instance type to use. And then you will have an S3 bucket for the output, the actual model artifacts of that training that that's where the train model gets stored. And you will also need to provide an ECR. That's that's Elastic Container Registry path to where your training code lives. So again, everything is sort of container based here we had a container for the data processing. Also a container for the training code all under ECR. Once you have that all set up, like I said, you can use any one of a number of built in training algorithms or roll your own sparks machine learning library is also available for you to if you want to stick with spark. Or you can roll your own Python, TensorFlow, PyTorch, MXNet code, whatever you want. Or if you're not doing deep learning, you can also use scikit learn or RL estimator if you want to do a reinforcement learning. Also, algorithms like XGBoost. Hugging face is an option here as well, so that's a big repository of models out there, including a lot of the latest and greatest open source generative AI models and large language models and small language models. Chainer is also something you could use there. You can always just provide your own Docker image that does anything you want as well. Whatever you can dream up, you can still use SageMaker to make that part of your larger pipeline, and there are algorithms available for purchase from the AWS marketplace as well. Once you've trained your model, you need to deploy it and actually put it into production. That model will be saved to S3 in a bucket somewhere. And there's a couple of options for deployment. There's runtime, so I can actually make a persistent endpoint for making real time predictions on demand out there, and scale that out as much as I need to. But if you don't need real time predictions, maybe you just need to make predictions in a batch manner where you have a big group of data coming in and you just need to get a big batch of predictions coming out all at once. In that case, you could use SageMaker batch transform to just get predictions all at once for an entire data set without having a runtime endpoint at all. And there's lots of options there surrounding deployment, and we'll get into some of these in a little bit more depth. Inference pipelines can be used for orchestrating more complex processing. You might need SageMaker neo can be used for deploying your trained models to edge devices. So if you need to use your model someplace that might not have reliable internet connectivity or connectivity to the cloud, it's possible to deploy that to the edge device itself. Or if maybe you can't tolerate the latency involved in going to the cloud. There's also something called elastic inference that can be used to accelerate your deployed models. You just kind of slap it on top there and it makes it faster. There are automatic scaling features in SageMaker that can automatically increase the number of endpoints as needed. So if you get an increase in traffic, it can automatically add capacity to your inference model there. There's also something called shadow testing, and there are more features around this as well that we'll get into in a bit. But there are ways to evaluate new models against your currently deployed models to try and catch errors. So again, an important part of operations is the ability to test your deployments and the ability to quickly roll them back if something unexpected happens So SageMaker does have a lot of features that allow you to deploy newly trained models in a controlled manner and roll things back if they're not performing as expected.

---

### Lecture: Amazon SageMaker Ground Truth and Label Generation

Let's finally talk about SageMaker Ground Truth service. This is a relatively new product from Amazon. And what is it? Well, it's basically a way of using humans to label your data. And I kind of struggled with where to put this in the course. In the end, I ended up putting it in feature engineering because fundamentally it comes down to dealing with missing data. And in this case, it's really more about missing labels and missing features more often than not. But you could, you know, actually apply this to missing features as well. Basically the idea is that if you have missing data that can be inferred easily by a human ground, truth let's you farm these tasks out to actual human beings to fill in that missing data for you. And the most common example of this is in the world of image classification. So for example, if I'm training a new image classification model, in order to train that, someone's going to have to go through and actually tag all these training images with what's actually in them, right? It's not going to be an easy thing for a computer to do necessarily. So for example, in this picture here we have someone classifying this picture as being of a basketball or a soccer game. Someone's got to go through and do all that work. Well, kind of a bird is this Where are the birds in this image? Things like that. So Ground truth will manage a fleet of human beings who will go through and label your data for training purposes. So if you have a huge data set, like a bunch of pictures and you need labels on them, sometimes human beings are the best way to actually get those labels ground truth is what manages that process for you. But it's more than just managing the humans that label your data. The thing that sets ground truth apart is that it creates its own model as those labels are coming in. So this model is learning as it receives more and more label data from the humans, and over time, it's only going to send out labels to the humans that it's not sure about. So as time goes on, as it gets more and more labels from the human labelers, it will create a model of its own, and only the ambiguous cases will be sent out to human labelers going forward. So this model builds up over time, gets better and better over time, and only the things that the model is not sure about actually get farmed out to human beings. This ends up reducing the cost of your labeling jobs by up to 70%, which is pretty substantial. Who are these people? Well, you have different choices of who ground truth can use. One is just using the Amazon Mechanical Turk workforce. This is a huge workforce of people around the world who will label your data and do a bunch of other, you know, simple tasks for you for a very small amount of money. You can also choose to farm it out to your own internal team. So if you're dealing with very sensitive data, that might make sense for you. And there are professional labeling companies out there as well, who do nothing but have a fleet of human beings who label training data for a living. So if you want someone who's a little bit more specialized in this sort of thing, you can spend even more money and use one of them. Now, there are other techniques than just using humans to actually generate training labels that you don't have. One other would be to use AWS's recognition service, which we'll talk about more later on in the course. Basically, it's an AWS service for image recognition. So if you do need to classify images, that could be either for creating labels or maybe even feature data, you know, maybe you just have a feature in your data set for what is in this image Recognition has a pre-trained model for most common types of objects that you can just use. So if you are just using, you know, Classifications of general objects in the world. Recognition might be able to do that for you if you're dealing with textual information as opposed to images, comprehend might be useful. Basically, that is a AWS service for doing text analysis and topic modeling. So you could automatically generate topics or sentiments for a given document that way. So that might be a way of both creating labels and maybe even additional training features. Right. So this is another example of feature engineering where you might generate additional features in your model by using something like comprehend. So if I have a document, maybe I can use comprehend to generate new features that include the topic of that document or the sentiment that that document represents. And that might be useful information for training a machine learning model. So that's why this is in this section about feature engineering. You know, you can actually apply these techniques of using existing services or even human beings to generate more information that your model can use. Really, any pre-trained model or unsupervised learning technique can be helpful in generating new training labels or even new training features in your data set. So ground truth is pretty simple, but if it's still too hard for you, there's something called ground Truth Plus where somebody just does it all for you, and it's a turnkey solution where you just basically hire someone through AWS to set it all up and manage the whole project for you. So in the documentation, they call it our team of AWS experts. I'm not really sure where they come from, but these people will manage the workflow and set it all up for you and manage your team of labelers for you. All you do is fill out this form that says who you are, what you're trying to do at a high level, and they'll contact you and try to work out the details and discuss the pricing, which they do not publicly disclose. But I imagine it's not cheap because of this sort of white glove treatment that they give you as they set things up and they start actually getting results back from their army of labelers, you can track their progress through the Ground Truth Plus project portal, where they give you some handy dandy charts, uh, displaying how the progress is going and what the results look like when they're finally done, you'll get your label data back from S3 but not a whole lot to talk about here. Basically, it's a way of handing off the whole problem of labeling your data to somebody else. Uh, ground truth. Plus, if you're willing to pay for it.

---

### Lecture: Amazon Mechanical Turk

So now let's talk about Amazon Mechanical Turk. So this is the original Mechanical Turk in the 1770 year. This was created by an inventor, and this was an actual robot that was playing chess in front of people, but it was an illusion, of course, because there were no robots in that time. And that robot was cleverly operated by someone inside that was playing chess. And with some mapping, the robot was moving. But thanks to an illusion, no one could see that there was an operator inside. So this is the thing that the service got named after. And the idea is that Amazon Mechanical Turk is a crowdsourcing marketplace to perform simple human tasks. So the idea is that you have access to a distributed virtual workforce. So you give tasks to it, and behind the scenes, humans are going to do these tasks, but these tasks can be very simple and very cheap. For example, say you have a data set of 10 million images and you want to label these images, then you're going to create a task on Mechanical Turk and actual humans, all around the world, will go ahead and tag those images, and you can set a reward per image. For example, if you set 10 cents per image, then tagging all these images will cost you a million dollars. But of course, it's up to you to set the pricing and so on. But the idea is that you have access to a very, very big workforce that is really eager to work on these kind of tasks. So the use cases for Amazon Mechanical Turk is image classification, data collection, business processing, anything that is going to be simple and easily and can easily be distributed to many, many people at a time. So why do we want a Amazon Mechanical Turk from an AI perspective? Well, of course, to label your images, to review recommendations, and so on, it can be very, very helpful. So there is deep integration between Amazon Mechanical Turk and Amazon A2I, or a SageMaker Ground Truth and so on. So here's an example of what a worker looks like when they go on Amazon Mechanical Turk. So here, as you can see, they see a lot of different jobs that they can do. The reward for each job that they have to do, for example, filling an Excel spreadsheet and so on. Then they can accept the work and work on it. So, if you set the right reward, and the job is fast enough, then you will get a lot of people working on your job very quickly. So that's it for Amazon Mechanical Turk. It's a service that allows you to access many humans at a time. I hope you liked it and I will see you in the next lecture.

---

### Lecture: SageMaker Data Wrangler

SageMaker data Wrangler is probably the most directly applicable technology in SageMaker for data engineering. It's basically an ETL pipeline that's baked into SageMaker studio. And as you might imagine, it has a lot in common with a glue data studio. It's used to prepare data primarily for machine learning, though, so it's custom tailored for machine learning pipelines. It allows you to import data from wherever it might be. Maybe it's CSV files in S3 or streaming in, who knows? It allows you to visualize that data and make sure that you're happy with the distribution of that data, and know about any outliers that might be happening before you go on to push that into your machine learning model. And it can transform your data. They have more than 300 transformations to choose from, from within Data Wrangler, or you can integrate your own transformations using pandas code or PySpark or PySpark SQL if you want to do something special. Another cool feature is that it has something called quick Model, and that allows you to actually train your model that you're trying to prepare the data for with some subset of that data, and quickly measure its results So that allows you to experiment with different transformations and different ways of preparing your data to optimize it for the model you're feeding it into, and experiment and figure out what the best transformations are to produce the best results from the model that you're going to be training this data from. Block diagram. Let's see what that looks like. So basically SageMaker Data Wrangler sits in the middle here between data sources and where you might send it to. You can see it can take in data from S3, from lake formation, from Athena, from SageMaker feature store, which we talked about earlier, or from redshift or from anything that can talk to JDBC. That would include things like software as a service, applications like Salesforce or something, or maybe external systems such as Databricks. Once you've gone through the Data Wrangler pipeline that can export code in a Jupyter notebook, that can then be used as part of a machine learning notebook to further import and transform and process your data. So it's important to understand that Data Wrangler isn't really doing the transformations of your pipeline itself. It's just generating code to do those transformations in your pipeline. So think of it more as a code generation tool. At the end of the day, that code can then have an endpoint of its own where it dumps this data to. Maybe it goes into Amazon SageMaker processing to train a model. Maybe it goes to SageMaker pipelines as part of a larger process. Or maybe it goes back into SageMaker feature store to make these transformed features available to other models. Let's just walk through an example here. So this is what it looks like. This is SageMaker studio. So if we want to import data we start from the import tab here up top. And in this example we're taking in this CSV file. So we're specifying a specific file name under a specific S3 bucket with a specific file type. And you can see it's previewing what's in that CSV file automatically just to make sure it's what you expect. So this is a data set we looked at earlier back in our SQL activities way back when. Same data set here where we have passengers on the Titanic and the dataset tells you who survived, who didn't, what their class was. You know, first class, second class, third class, their name and other information about them. You can further expand that preview to see what's going on and actually see what the data types are that were inferred for each one, and you can change that if you need to. So if it guessed wrong on the data types, you can change that there. If you want to have more human readable column names, you can also change that here as well. You can also visualize the data and make sure that the distribution is what you expect. So here we can see the age is following a pretty good distribution. They trended younger than I expected on the Titanic. Actually it's just a sanity check. So this isn't a data analytics course. So I'm not going to get into this too much, but it allows you to preview the properties of that data and make sure that it's what you expect before you turn around and feed that into a machine learning model. And then you can transform the data. So in this example here we're taking the class and one hot encoding it so that transformation is called encode categorical. Very common thing to do with machine learning. So typically with machine learning you know the inputs have to be these binary ones and zeros. You can't just feed it an arbitrary number typically. So what we're going to do is instead of passing in one feature for the class where that might be one, 2 or 3, we'll have three different features going in that basically says, am I in class one, am I in class two, or am I in class three? And one of those three features will be true, or one for each individual passenger. So that's what Encode categorical does. But in the machine learning world we call that one hot encoding. Just one example of the transformations you might do, and your choices of how you transform this data and how you present it to your model can make a big difference in the quality of that model. So things like how you normalize that data can also be extremely important as well, or just what data you're giving it and what data you're holding back right now. The really cool thing is that you can do what's called a quick model to train your model using that data, using that transformed data so you can test how well your choices for transformation actually affected the machine learning model down the pipeline here, which is a pretty powerful thing. So I can make sure that my choices for ETL here are going to be optimal for the model that I'm feeding this data into. And then when I'm done, I can export that data flow again. All this does is create code for you basically. So this is going to output a Jupyter notebook is one output. But basically it's Python code that embodies all of the ETL steps that you just set up. So that allows you to put into a Jupyter notebook, which is just an environment for running Python code, the code that is needed to extract that data, transform it the way you specify it in Data Wrangler, and apply that over and over again through that block of code as need be. So again, you know, data Wrangler isn't really sitting in your pipeline itself, but it's producing code that goes into your pipeline. A very important distinction there. If you have trouble with Data Wrangler, there are some troubleshooting tips here. One is to make sure your studio user, your SageMaker studio user has the appropriate IAM roles for data Wrangler, and you also make sure need to make sure that your data sources allow Data Wrangler to access it. So if Data Wrangler doesn't have permissions to access your data, that will cause problems. So you need to make sure that any data source you have has the Amazon SageMaker full access policy attached to it. You can also run into resource limits here. So if you see an error along the lines of the following instance type is not available, that probably doesn't really mean that we're running out of instances in AWS. It just means that you may need to request a quota increase, so you can use more instances than you might have originally. So if you do run into that error, you just need to go to service quotas. And under there go to Amazon SageMaker. Under there, go to Studio Kernel Gateway apps and just request more ML m5 dot four Xlarge instances. And there's a form for that. Someone has to go and approve it. But that's what that error message really means. You need to increase your quota. That's data Wrangler in a nutshell.

---

### Lecture: Demo  SageMaker Studio, Canvas, and Data Wrangler

So let's play around a little bit with SageMaker. And specifically SageMaker studio and SageMaker canvas are kind of tied together these days. If this is your first time to SageMaker, it's going to prompt you to set things up. And for me, I already did this process for a single user. What that does is create what's called a domain. And you need a domain in order to use studio. So this is just a way to take a look at who's authorized to do what. And for a single user it doesn't do much. It just sets up the permissions you need for yourself and opens up things to the internet that you need for getting data back and forth. It gives you the permissions you need for canvas and SageMaker studio notebooks, whatever it might be for organizations, it's going to do a little bit more. That's going to require you to define roles and responsibilities for individuals within your organization, and that's where the role manager comes into play. But I have already done this, so I can just go ahead and start playing. Now, before we start, I'm going to ask that you do not follow along with me for a couple of reasons. First of all, this costs real money. So if you mess things up, if you forget to shut something down, you could very easily end up with a very, very, very large bill at the end of the month. And I don't want that to happen to you. Also, the UI for SageMaker changes on almost a daily basis. So what's important for the purpose of the exam is that you understand the concepts here, not the specific UI, because that will change all the time. And if you're trying to follow along with me, you might get confused as things change. So just watch would be my recommendation here. So let's go to studio. That's kind of the umbrella for everything in SageMaker right. And you can see I already have a default user profile that I created as part of that domain. So I'm going to go ahead and use that user profile and hit Open Studio. And here we are I will skip the tour because I'm going to give you one. And you can see there's a bunch of stuff you can do here to kind of get overview data. So feel free to go through that if you want to. But you know, just going down the the menu here, you can see there's a bunch of things you can do. There's a set of applications you can run within studio. Again, SageMaker studio is just kind of an integrated development environment for machine learning within AWS. So we have JupyterLab. If you want to write your own Python code, or RStudio if you want to write your own r R code or canvas, which is what we're going to be using momentarily. And stop asking me for feedback. Man, that gets annoying, doesn't it? All right. So we can see here we can look at our underlying data. We can do automatic machine learning. So if we just want to like automatically create a model based on that data, we can do so easily. We can set up experiments to pip models against each other and try to find the best model for a given job. And we can also manage any training and evaluation jobs you might be doing from here as well. We can also set up pipelines for automating our ML workflows from within SageMaker studio, and view any models that we've created along the way. SageMaker Jumpstart is also available from SageMaker studio, allowing you to kick things off with pre-trained foundation models. So if you want to build a generative AI application within a notebook, you can do so from there. And we'll look at that in more detail as well. But for now, let's just do some classic machine learning. So let's go ahead and start with data. And I want to use Data Wrangler to aggregate and prepare my data. So I've gone ahead and uploaded some sample data into S3 already for an absenteeism data set. And this is a publicly accessible data set that I found somewhere, the UCI repository. I think it is, but it's a data set that has a bunch of information about employees, you know, how far do they live from work, how often do they work things like that? How much even things like how much they weigh and how old they are. And they try to correlate that with their rate of absenteeism on the job. How many days off do they call in sick each year? So let's see if we can predict absenteeism based on the attributes of employees. All right. So let's go ahead and run this in canvas. So you can see that they're really pushing you to run everything in canvas within SageMaker studio these days. So fine let's go ahead and do that. Run in canvas no code required. And as you'll see, we can actually prepare our data without doing much at all. We can just say in plain language and use generative AI how we want to manipulate that data and preview what that will do as we go. So that'll take a minute to spin up. So I'm going to go ahead and pause this recording until that's ready. All right. That did take about one minute as promised. So let's go ahead and open canvas and wrangle some data. Here we are. So it kind of walks us through the process here. So the first thing we need to do is import and prepare some data. We need to tell it where that data is. So let's go ahead and click on the friendly Import and Prepare button. Now I know this data is tabular data. It's not an image. So it's just a raw data in CSV format. And the first thing we got to do is tell it where that data lives. So there's some sample data here you can play with if you don't have any data of your own. But I actually do have some data of my own. So I'm going to go ahead and say in Amazon S3, I've got some data for you. So first we need to point to where it is. So I need to give it a URI to where my data is in S3. So I happen to have an S3 window opened up here for me. That's in my SageMaker Sundog bucket. And let's use the absentee full dot CSV file. That's kind of the actually, no, let's use absentee train because that's specifically for training. So I'm going to click on that file and copy my URI for S3. And I'm going to paste that into canvas thusly and go. All right so I've got some data. Let's go ahead and select that and hit next. And this gives me a preview of what that data looks like. So so you can see we got a bunch of columns here that are being imported from that CSV data a distance from work service time, their age, their average workload. And I'd have to go back and look at the documentation for this data set to know what some of these columns really mean. Did they receive any disciplinary action? What's their level of education? Do they drink? Do they smoke? Do they have a pet? Interesting. There might be interesting correlation to see if somebody has a sick pet If that affects her absenteeism rate. And then in the final column here we have the absenteeism, which is the number of days out of the year that they called in sick. So it looks like it imported successfully. One thing that might be problematic, though is this workload average. You can see it has commas within the numbers there, and that might throw things off into thinking that it's a string instead of a number. So let's watch out for that as we go. Now if we want to, we can do random sampling, but there aren't a whole lot of rows in here. I think it's only like 500 or so. So no point in sampling for such a small data set. But if you did have a larger data set, you could do a random sample. So you could sort of preview what's going to happen as you're wrangling this data a little bit more quickly. We need to get this data set a name though, let's call it absenteeism and we'll go ahead and import it. All right. So again it's kind of guiding us on what to do here. So it set up the state of flow. Basically we have our source data from S3. But getting imported into some sort of um. Well, I can't even see it there because it's being obscured here. But what's prompting us to do is to get data insights. So let's go ahead and do that. You can also use chat for data prep. But this will take us to the same place basically. So let's go ahead and get some insights about that data. So what this is going to do is alert me to any potential issues in my data automatically. It's going to automatically analyze this thing for things like missing data or duplicate data or outliers or whatnot. So that's kind of cool. Now again, we can use the full data set because it's small to begin with. So I'm going to have to use an ML, M54 Xlarge. Fortunately, with this small amount of data, that's not going to run for very long and cost me too much, but I am going to set that to one instance instead of two to save some money. This is a regression problem. So basically I am saying that I'm trying to predict a number. Now I could frame that as a classification problem with like a really large number of classifications, right? Where each each individual number is a class. But let's go ahead and treat it as a regression problem. And the target column. The thing I'm trying to predict is the absenteeism time in hours. All right, so let's give this analysis a name. Absenteeism data analysis. Whatever. And go ahead and create that Duke. And it says it will take about ten minutes. So again I'm going to pause this recording and come back when that's done. All right. Here we are ten minutes later. And it gave me some pretty interesting information about this data set. So this did take about ten minutes for which I'm going to be charged for time on a fairly beefy machine learning instance in EC2. Worth pointing out here that for a data set this small, I would have been better off just analyzing it on my own desktop for free using some Python code. So, you know, don't use big expensive cloud systems if you don't need to. I'm just doing this for the purpose of illustration, but some interesting findings here. So it's summarizing the data set here. No missing data. That's a good thing. It's also breaking down the feature types that it extracted from that data and how many features it found. Again, features are just the columns in my training data. The things that I'm using to make a prediction from no missing data. But there were a lot of duplicate rows, which is kind of a surprise. Uh, let's go down here to the high priority warnings, where it tells me what are the main issues that I need to watch out for here. One is duplicate rows. So that kind of makes me wonder how valid this data is. I don't think it's real. Uh, 33% of the data is duplicate. With that many features, it's very unlikely that that's just by chance. So I'm thinking a lot of the rows in this data were fabricated. And that's something that I should be aware of. Maybe I want to deal with that during pre-processing. Also, it's alerting me that there are some potential outliers. So let's dig into that in more detail and see if that's something I want to consider removing or not. Um, outliers can mess up your models, but let's let's see just how skewed it really is. Also, it's saying that a quick model built on this data didn't do very well. So the predictive power of the data I have might not be that good And that just might mean that, well, this is fake data, for one thing, apparently. So maybe that's why. Um, but it just might mean that maybe the features I have are not predictive of absenteeism at all. Maybe it really is just random life circumstances that drive people to call in sick, and it doesn't really correlate to any of this stuff. Anyway, let's take a look at some of these duplicate rows so we can see that there were ten instances out of only, uh, what was it, 699 rows? I think it was, yeah, 699 rows. Ten of them had the same exact properties, which seems unlikely to me. Like really, were there ten, 38 year olds with exactly that workload average per day with exactly that hit target with that, exactly that distance to work. I don't know, this this seems a little bit fishy. So I think I'm going to make the choice to actually remove those duplicates when I pre-process this data. And it's also alerting me to some anomalous samples here. It's just warning me that it thinks that some of these, uh, rows of data might not make sense. But if I look through it, these look like reasonable numbers to me. So I'm going to say probably not really an issue. These don't look like they're absolutely crazy out of bounds. They look like real people. So I'm not going to worry about that just now. You can also look at the distribution of the target column for the actual number of hours that were called in sick for, and we can see there was an outlier here at. I think that's around 40 hours there. Uh, again, this seems plausible. So I'm not going to take that as a sign that the data itself is invalid. I think it just means that this person had a really bad year. And if we go down further, we can see the results of the quick model here. And we can see that, um, it did not do a very good job at all in terms of accuracy on actually making predictions on data that it hasn't seen before. So the model that it came up with quickly was not a very much use, which is interesting in and of itself. We can also dig into individual features here and see how they break down. And this is interesting. So this is a summary of features ordered by prediction power. So even though our model wasn't that good, we can tell that the workload average per day was the biggest predictor of absenteeism. So it turns out the the more you work someone, the more likely they are to end up sick. And also transportation expense. So somebody needs to pay a lot of money in order to get to work. They're going to be more likely to call in sick. Apparently everything else, not so much, which is a little bit of a surprise. You know, whether you drink or smoke does not matter at all, it seems. And things like your weight and it doesn't really matter too much either age and height a little bit. But I mean, compared to transportation expense and workload average, clearly these are the two main drivers of the data. So even without building a formal model and just kind of visualizing and pre-processing the data already we've gained some insights into this data that we have here. And we can go and look at histograms for each one of those features individually. Again, we're just going to eyeball it and make sure nothing's too crazy. Um, even though they're being flagged statistically as outliers, they're not really outside the bounds of plausibility, in my opinion, so I'm not going to worry about that. All right. So that was useful. So we have our little analysis here of the data that we have. Now that we've done that we can go ahead and actually wrangle this data. So so let's go back to our data flow here. And we can see that we have our data quality and insights report. And we have our data types. So let's back up here and open this up and go ahead and let's transform this data. And hey we can actually do that with chat with Gennai. Let's try that. It's a new feature. All right. So we have this handy dandy visualization of our features. And their distributions like might come in handy. But we already know from that analysis that we have some issues to deal with. We can see the steps that we're applying to our data transformation being built up here on the right. So we're starting with a given source for the data itself from S3. And we're assigning data types to those as well. So we can see already there's an issue here where that workload average is being treated as a string instead of a number. So let's see if we can fix that. Um, now there are some you can do this by hand. You can click on add transform and select from a library of transforms. Here again there's a very large number of them that you can dig into there. But I'm going to do it the easy way and just use chat. So let's say uh transform the work load average per day feature from a string to a long type, accounting for the use of commas in the numbers. Let's see what it does. Huh. So apparently it generated some code for me. It's always a good idea to double check what generative AI does. Looks like it's just going to strip out the commas and cast it as a long, so that seems reasonable. Let's go ahead and add that to our steps. So now we've added that step just from our Genni chat session there automatically to transform that data in the way that we said. Let's also get rid of those duplicates because I don't trust those duplicate rows. They seem suspicious. I'll just say remove duplicate rows. And I could just add a transform for that, I'm sure. But it's even quicker to just type that in. Again, it generated some Python code for me to do. That should just be one line drop duplicates. Yep. Seems reasonable. Let's add that to our steps as well. All right. So now we've built up a flow here for processing our data or wrangling it as you might as the case may be. So we've got all of our steps here lined up that we're going to apply to this transformation. Let's click on our data flow and visualize that as well. If we go back here so we can see that we have our report over here. And then we forked off here to say I'm going to transform this using a couple of transforms that I defined. So at this point in our data flow, we've transformed the data the way that we want. We've dealt with that incorrectly identified string column and we've gotten rid of duplicates. And now from here I can export that data to S3 if I want to. If I want to use it outside of canvas, or I can export it to a Jupyter notebook if I want to write some code surrounding that data. Or I can just click on create model and automatically export that data and create a model all within canvas. Let's go ahead and do that. Then let's give this data set a name. Um, absenteeism processed, I don't know whatever you want to call it and export. Off it goes. All right. The export succeeded. Now I can create a model from that exported data. That model also needs a name. Let's call it absenteeism model. And we need to select the problem type. So this is a predictive analysis problem I'm basically using tabular data to try to predict the number of absentee hours based on that data. It's not image analysis, obviously. It's not text analysis. I'm not trying to do text classification, and I'm not fine tuning a foundation model either. But interesting to note that you can do this within SageMaker canvas, right? So if I did want to ingest a bunch of data to fine tune a large language model or even a small language model, I could do that here as well. But in this case I'm doing predictive analysis. So we'll click on create. Now I need to select the column that I want to predict. That is going to be that final absenteeism time in hours. And we can double check all this data. Sure enough everything is numeric again. That's good. And our target column is identified. All right. And it's actually automatically recommended the appropriate model type here. So instead of a regression model it is saying that I'm actually better off with a category prediction model. So instead of trying to predict a floating point number, it's going to try to classify absenteeism in terms of the number of hours. Okay, I'll take your word for it. Typically it makes the right choice here. So kind of AutoML in action here. And to actually build the model we have a quick build option which is well quick 2 to 15 minutes. But if I want to choose accuracy over speed and I want to spend more time and money, I could do a standard build as well. But I'm going to stick with quick build because I'm impatient. So go ahead and click the quick build button and off we go. Now the data validation has completed. It's actually building the model itself. And I'll come back when that's done. And it's done. It said it failed, but it really didn't. So I'm going to close that erroneous error message. And I'm also going to ignore this survey. But here we have our model and what actually happened. So again even after running that more detailed model it came up with the same conclusions. The column impact here is saying that the transportation expense and the workload average per day are the biggest predictors of absenteeism, which is an interesting finding. Is it not? So we have our model here. And now that we have this model, we can use it to make predictions if we want to. Let's go ahead and click predict. And you can do a batch prediction or a single prediction. Whatever it might be. We can click on manual. And from there we could select a data set to be used for predictions. So to do that I have to make a data set for the stuff I want to make predictions for. It says it must have the same number of feature columns as the training data set. Okay no problem, create data set. Let's call it absenteeism predict. And we can do that from a local file or hit S3. And going back to my S3 view let's see where I put that data. There's an absentee predictor CSV file here. Let's go ahead and select that. And get a URI for that. In you go. And so now I can select that prediction file. Preview my data set and create it. So now that that's done processing I can select it for predictions and generate predictions. So I can see my predictions are now ready. I can click on that. And I can ship that off to QuickSight if I wanted to. Or do anything under this little button here, I can download that data to do whatever I need with it. I can get a link to the place in S3 where that result was stored, or I can just preview it. Let's do that for now, and we can see that I did indeed get predictions for absenteeism based on all of this prediction data that I set in. So the data that I provided, it had a list of transportation expenses and distances and service times and ages and everything else. But the absenteeism time column was left blank. And what this has done for me is use that model that I generated using all the training data to make predictions based on the features that I gave it. So that's pretty cool. Um, it's also giving me sort of a measure of confidence there. The probability it's not terribly confident, but given the number of classes here, you know, one for every number of hours that are possible, it's not that bad, really. Um, so this is telling me, hey, given the model that I made, I predict that this person's going to be out two hours during this month. So pretty neat. And from here again, I can visualize that or download the data to do what I will with it. So that is pretty cool. We used a SageMaker canvas to use Data Wrangler to pre-process our data, to build a model after transforming our data, and also to make predictions based on that model, we could go further. We could actually deploy that model as well if we wanted to. So if we wanted to, we could create some inference endpoints and actually host this model in real time. So I could build a service that hits that on demand. That gets really expensive very quickly. So I'm not going to actually do that, but that's where you would do it from. So that's pretty neat. That's a model from end to end in SageMaker canvas. And if I go back to my models here, I can actually see that little card from my model there that I just created. So I can do stuff with it. I can view it or rename it or delete it or whatever I need to do under MLOps. That's another way to actually set up those deployments, to set up inference models. And what else can we look at ready to use? So just to go through some other capabilities of canvas here, they also have these built in things that are built on top of the higher level AI services at AWS has to offer. So for example, if I wanted to play around with object detection and images, all I can, all I have to do is use Amazon Rekognition for that, right? I don't have to go off and build a model. They already have a model. So let's click on that. And it brings up this UI where I can do a one off single prediction on an image, and it tells me what's in it. If you have your own image, you can do that. Let's see what I got. What's in my photo? What's in my pictures folder here that I might use? Oh, there's a picture of a of some dude in front of a radio telescope. Huh. Interesting. So it identified a face ahead, a person, and, uh, a hat, some sunglasses, and it says I'm a man. You're right. Uh oh. You got that one wrong, though. That's not a person. That's some sort of a hose there. But, you know, these these models aren't perfect. But there you have it. Kind of a way to play around with some of these. And if I wanted to actually use this for something more useful, I could set up a batch prediction where I give it a data set of images to do object detection on. And that's just one of many of those built in ready to use models. Right. So we can also do object detection. We can do sentiment analysis, language detection, text detection, entity detection, any of this stuff built on top of comprehend, Rekognition or Textract in these cases. And also there's a new Gen I button. And this is just, you know, a standard chatbot interface for doing stuff using AI. I mean, it's just like any other chatbot, really. You can choose what foundation model you want to be playing with. In this case, a claw two is the default. I'm sure there'll be newer ones added shortly, because claw 3.5 is currently the state of the art as of this recording. But yeah, that's the SageMaker canvas in a nutshell. Remember to log out when you're done, because this does consume resources while you're using it. And this will warn you about anything that you might have still running that's still going to be racking up charges. Very important to very important to make sure that any endpoints you set up are not still running if you don't want them, because that's going to keep on running. And also very important, just remember to log out in the first place, because again, there are instances associated with this workspace in canvas that are racking up money as we speak. So log out and we're back to the home page.

---

### Lecture: SageMaker Model Monitor and SageMaker Clarify

Let's spend a little bit of time talking about SageMaker model monitor. Unfortunately, if you deploy a machine learning system into production, you can't really guarantee that it's going to perform the same way forever. The nature of the data coming into that might be changing over time in ways that you didn't anticipate, and it might affect the performance of your model in ways that you don't want. So SageMaker Model Monitor allows you to receive alerts through CloudWatch on any quality deviations on your deployed models. It can visualize data drift, so it can allow you to keep track of how the nature of your input data is changing over time. For example, if you're predicting whether somebody is creditworthy for a loan, maybe that starts to change over time as the distribution of the attributes of the people that you're making predictions for changes, or maybe you have some sort of a problem where you're starting to miss some input features, maybe there's a data quality problem there, or missing data that didn't originally occur that would result in data drift that you might want to know about. It can automatically detect anomalies and outliers in the data as well that you might need to, uh, address. It can detect new features in your data that weren't there before. So maybe all of a sudden you're seeing new information in your data that's coming into your model. Um, you might want to know about that. Maybe that's something you want to take advantage of, right? And it doesn't require any code. You know, you can do this all visually within SageMaker studio SageMaker model. Monitor also integrates with SageMaker clarify and SageMaker clarifies job is to detect potential bias or one of its jobs anyway. For example, if you have imbalances across different groups or ages or income brackets, it can find that out. And using SageMaker Model monitor, you can monitor for that bias and be alerted to that as well. Through CloudWatch. SageMaker clarify comes into the picture by helping to explain the model's behavior. So by using clarify, it can tell you not only that you have potential bias, but also tell you which features in your data is contributing the most to those predictions and where that bias might be coming from. There are several bias metrics that Clarifai can use, and I don't think it really is worth getting into the details. Just know that there's a bunch of ways that it can measure bias. And, um, they've all been thought through quite a bit. Kullback-Leibler divergence is a very popular one that I see a lot, but all of these are supported as metrics of bias in your training data. So there's a lot you can pick from here. I'm not going to get into the details for a practitioner level exam. A little bit more about SageMaker model monitor. The data for Model monitor is stored in S3 and secured. So where it's storing all of that, monitoring information is safe. Those monitoring jobs are scheduled with what we call a monitoring schedule. So this can happen on a schedule and as often as not often as you want. Those metrics, like I said, are emitted to CloudWatch. And then from CloudWatch you can use notifications to trigger alarms. So model monitor just dumps the data into logs in CloudWatch. It's up to you to actually trigger an alarm from that. Once you get that alarm. The idea is you would take some sort of corrective action, which might be retraining the model or auditing the data to see what went wrong. SageMaker Model Monitor can also integrate with TensorBoard, QuickSight, and Tableau for visualizing that data outside of SageMaker studio. Or you can just visualize it within SageMaker studio using its built in visualizations. But if you have third party dashboards outside that you want to integrate with, it can do that too. So there are specific monitoring types within Model Monitor that can monitor for you. One is the drift in data quality. So relative to some baseline that you define In some statistical property of those features you can call quality and just measure if that statistical property is drifting over time or not. So quality is subjective. It's up to you to define what quality means. But whatever you define it as, it can detect drift and it automatically and drift in model quality, not just data quality. So what's the accuracy of your model? Conceptually, that works the same way as the data quality with a baseline that you define for the model quality. But it's measuring the accuracy of the actual model and how well it is at predicting things correctly. How do you know if it's correct or not? Well, you can integrate that with something called ground truth. So you can actually integrate model monitor with an army of humans who are out there helping you to label this data in what is considered to be correct. So you can compare the output of your model to human generated labels. And model monitor can monitor the difference between the two. How accurate is your model compared to what these humans said? It can also detect bias drift. We talked about bias detection using Clarifai as well and also feature attribution drift. So this is also a feature of Clarifai where it's actually measuring the impact of each feature on your outputs. And if that is starting to change over time, model monitor can figure that out.

---

### Lecture: Partial Dependence Plots (PDPs), Shapley values, and SHAP

The exam expects you to have a fair amount of depth on how Clarifai works. So let's go into a little bit more information here on what it gives you. One thing you can produce with SageMaker clarify is something called a partial dependence plot, or PDP for short. And the idea of a PDP is to show the dependence of predicted target response on the set of input features. What does that mean? Let's look at this example on the right here. So we have a set of feature values on the bottom here. So what we've done is bucketed these values up. So each one of these dots represents some range of values just like you would do in a histogram. Right. And on the y axis we're seeing model prediction values. So we're seeing the effect that a given feature in this case age has on the model predictions. And that will tell you just how much of an influence that particular feature might have on the model and what it's predicting and, and how that might change across different values of that feature. So in this particular example, you can state that for the age, once you get above the age of 50, for whatever reason, the model predictions remain the same, which is kind of interesting. That might imply that age is somehow very important to the predictions. This model makes. And somehow there's a tipping point at age 50. Very interesting. That might alert you to some sort of issue with your data. Actually, maybe you just don't have enough data points above the age of 50 and your training data, and that might point you to a imbalance in your training data, which is the whole point of clarifai. Right now, you can also get back data distributions for each bucket of values. So if you want the raw data there as to how that data is distributed for those values, and you want to see if you do in fact have some sort of a class imbalance, you can get that back from Clarifai as well. So in addition to these graphical plots, you can also get back the raw data from the API as well. We should also talk about Shapley values because this is how Clarifai works under the hood. It's the technique it uses for determining the impact that each one of your features has on the model's output. And this is actually an idea that came out of game theory that's been applied to machine learning. The idea is pretty simple. Basically, you just drop individual features one at a time and see what impact that has on your model and measure that effect over each feature. And well, that actually gets kind of complicated when you have a lot of features because there's a lot of combinations to test there. Right. So under the hood, SageMaker clarify uses something called Shapley additive explanations or Shap for short as an approximation technique. So instead of doing this brute force calculation across every possible combination of features. Shap is a way of approximating that and doing that more efficiently, but still giving you useful information. So again, clarify is all about telling you the impact that individual features have on your model and pointing you toward potential unintended properties there that might point to problems in your data. If you're dealing with time series data, there is something specifically called asymmetric Shapley values. So for time series data instead of Shapley or Shap, you're going to be using asymmetric Shapley values. That's just the algorithm used to determine the combination of input features at each time step toward forecasts or predictions. So in addition to measuring the impact of each feature individually, we're also measuring the impact across time. So even more complicated. But these are the terms you need to know for Amazon SageMaker. Clarify.

---

### Lecture: SageMaker Feature Store

Let's talk about SageMaker feature store. And that is a the word feature in a different sense. So in this context, a feature is a property that's used to train a machine learning model. So an example might be if you're trying to predict somebody's political party based on features, those features might be somebody's address, their income, their age, things like that. Their features are really just, um, columns or fields in a row where you're using those pieces of information to try to predict a label for that row. So the features in this case might be the properties of that person, like their address, income or age. And the label that we're trying to predict is their political party in that example. But that's what we mean by features in this context. Machine learning models require fast, secure access to that feature data to train a machine learning model. As you can imagine, it needs to suck up that data at very high quantities and very quickly. And it also needs to do it securely because some of that information could be sensitive or personal. It's also a challenge to keep that data organized and to share those features across different models. So you want to avoid a situation where you're storing these features more than once. You want to be able to share them when you're trying to do different things with machine learning, where those features come from is totally up to you. You can see SageMaker feature store kind of sits in the middle of this whole universe here of where data might come from. Maybe it's coming from SageMaker studio, maybe it's coming from a pipeline where it like step functions or SageMaker pipelines or Apache Airflow, where it's just dumping stuff in there along the way. Your features might be coming from some sort of EMR pipeline or from glue or from SageMaker processing. Maybe these features are streaming in from Kinesis or Kafka, and maybe they're being created from SageMaker, Data Wrangler, or from glue. Databrew. Right. These are all examples of where data might come from. So where it comes from can be pretty much anywhere. What feature store brings to the table is organizing that data. So it has a feature store. And within a feature store we have what we call feature groups where we organize these features together. And each feature group will contain record identifiers, feature names, and event times associated with that feature. How does it all work? So there's basically a streaming world and a batch world here with SageMaker feature store. So let's imagine that we're streaming data in from Kinesis or MSK or Apache Spark or SageMaker, Data Wrangler or any other streaming source. Basically, there's a put record API that can be used with feature store, where you can feed that streaming data into feature store and it will process it. So under the hood, feature store will both have an online repository of those features and also a batch repository that's the offline store in S3 to query features from the online store and stream them back out. There's a Getrecord API you can use for that, so your model can stream those features out using the Getrecord API. Or if you want to do batch access, that's what that offline store is for. So SageMaker Feature Store will automatically create a glue data catalog for that data stored in S3. And you can do anything you want with that S3 data. You can use tools like Athena or Data Wrangler. Anything that can hit S3 can be done with the offline feature store. You can also dump data directly into it if you want to, so you don't have to stream data into SageMaker feature store. You can just put it right into its offline store in S3 and do it that way too. Whatever works for you. The bottom line here is that SageMaker feature store works with streaming situations, and also with batch situations that are based on S3 security. Everything is encrypted at rest and in transit automatically, and if you have a KMS customer master key, you can use it with it. Fine grained access control to feature store is possible through IAM, and you can also secure it with AWS Privatelink to make sure that nobody can access it. That shouldn't.

---

### Lecture: SageMaker Canvas

Just a quick overview of SageMaker canvas. This will certainly show up on the exam once or twice. You just need to know that it's the no code machine learning environment that's geared more toward business analysts. So if you just have some data somewhere and you don't know how to write code or really do machine learning yourself, you just want to make a model without thinking about it too much. SageMaker canvas can do that for you as long as you give it some CSV data. You can say, okay, here's the column that I want to make predictions for, and it does the rest for you using AutoML techniques so it can automatically build a model for you and make predictions from it. It can also join data sets together if you need to. It can perform classification or regression as part of its predictions, and it even automatically cleans your data for you. So it will look for missing values, for outliers, for duplicate rows and deal with it for you without you even having to think about it too much. So think of it as machine learning for the non ML engineer. It also allows you to share your models in your data sets with SageMaker studio. So if you do want to give this to an ML engineer to do something more interesting, you can do that as well. So it is possible to export models and the data that you use to train those models from canvas to SageMaker studio and use those as a model within SageMaker studio. Also, it now has support for generative AI models as well. So pretty much anything that you see in bedrock as a foundation model or through Amazon SageMaker Jumpstart is fair game within SageMaker canvas as well, and you can even fine tune these within canvas. So you can think of canvas as more than just an automatic machine learning. It's also a user friendly interface for just creating generative AI applications as well, so you can create your own foundation model and extend it with your own data through fine tuning, all within SageMaker canvas, without even having to be an AI expert or a machine learning expert. Pretty cool stuff. So canvas, AI and machine learning for the masses, if you will.

---

### Lecture: AWS Glue

All right. Let's talk about AWS glue, which is a system for building your table definitions and performing ETL, extract, transform and loading of your data automatically. Now glue is a huge deal on the exam right now. So back when they removed all the machine learning content from the AWS Big Data Exam, it was called at the time into its own exam for machine learning. They kind of filled that gap mostly with more content about glue and redshift. So glue really plays a major role in the exam today. So it's very important to pay attention to this section. There's not really a whole lot you need to know about glue, but you'd need to really internalize it and understand how it connects all these different services together and how it might be used as pieces of larger applications. So what is glue all about? Well, it's a serverless system. So again, you know, there's nothing you need to maintain there that will automatically handle discovery and definition of table definitions and schema. So its main use is to serve as a central metadata repository for your data lake. You will discover those schemas out of your unstructured data, sitting in S3 or whatever, and publish table definitions for use with analysis tools such as Athena or Redshift or EMR. So again, the purpose of glue itself is to extract structure from your unstructured data. If you have data sitting in a data lake in S3, it can provide a schema for that so that you can query it using SQL or SQL like tools, including RDS and Redshift and Athena and Amazon EMR, and pretty much any other SQL database that can use a schema like that. The other thing glue does is custom ETL jobs. So once it discovers that schema, it can do some processing of it for you. And this can be trigger driven based on when that data is received or on a schedule or on demand. And it's fully managed. So again, there's no services for you to maintain, no hardware for you to maintain the ETL jobs use Apache Spark under the hood. We haven't talked about that yet, but spark is a very popular system for distributed data processing. But you don't need to manage the spark cluster, which is really nice. So you have the entire power of Apache Spark behind you with glue ETL without having to manage that spark cluster, which is a really, really nice thing. So one piece of it is the glue crawler and data catalog, which it populates. So the purpose of the Amazon glue crawler is to scan data in S3. And often it will infer that schema automatically just based on the data that it sees sitting there. But sometimes you have to give it some hints. You can schedule that to run periodically if you need to. And what that does is populate the glue data catalog. So we have data sitting in S3. Glue can periodically or on demand scan that data and construct a schema of it. Again that's just the table definition. You know, what are the columns of data in that information that you have stored there in an unstructured manner? What's their data types? Things like that. Column names, it can infer that as well, where it's stored. That's all important information as well. And it uses that information to allow things like redshift, Athena or systems running on EMR like hive to query your unstructured data in S3 as if it were structured data just like it were a traditional relational database. Now the data itself remains where it was originally, so the data stays in S3. You're not duplicating that data. We're not copying it into an actual database table somewhere else. We're just using glue to basically provide the glue between these relational database interfaces and the unstructured data lake that's sitting back here in S3. Once cataloged, that data is immediately available for analysis, and we'll build this out and go into a lot more detail into these systems later on. And then, you know, once you actually have that data sitting in something like Athena, you can use something like Amazon QuickSight to visualize that data. So glue again is the glue between your unstructured data and structured data analytics tools. So let's talk about glue and S3. Partitions really quickly here. So it's not entirely magic. You know you do need to think about how you're going to structure your unstructured data. I know that sounds backwards, but the way that you organize your data will affect how efficient glue can work and how efficiently your systems can query this unstructured data. Now, the glue crawler will extract partitions of your data based on how your S3 data is organized. So you want to think up front about how you're going to be querying your data lake in S3, and what those partitions should represent for the best performance. So, for example, let's imagine that you have devices sending sensor data into your system, into your data lake every hour, just dumping raw data into S3. So think about how are you going to use that data. Are you querying primarily by time ranges? If so, then you want to partition based on those time ranges, right? So you'd want to organize your S3 buckets as year, followed by month, followed by date, followed by device. Okay. That way, you know, we can start by winnowing things down by the year and further by the month, furthermore, by the date, and then finally by the device that generated that data that will allow the partitions to really allow us to efficiently access data for a given date without having to slog through all the other information for other dates that we don't care about. On the other hand, if you're querying primarily by device and not by time, you might want to put the device up first. That way, your primary partition would be the device ID and then followed by the year, month, and date. So if you're going to be querying for specific individual devices all the time, and that's kind of the primary thing you want to partition on, you'd want to make that the top level bucket that you're organizing your data under within S3. So this is something that you might see in the exam. What's the optimal way to organize your data that's being stored on S3 such that glue can extract that and present it as partitions that make sense for what you're going to do with it Ethan.

---

### Lecture: AWS Glue Studio

There are a few new features in AWS glue. They seem to be investing a lot in this feature right now, so not sure you're going to see this on the exam quite yet, but you probably will soon. So one of these new features is called AWS Glue Studio. And basically it's a visual interface for defining and creating ETL workflows. So it allows you to create these more complex workflows where you might have multiple things happening in parallel from the same source, or maybe different sources being processed in parallel in different ways. But it lets you set up these complex jobs in a visual manner, and often without writing any code. So it offers you this visual job editor that I'll show you in a moment, that allows you to create directed acyclic graphs for complex workflows like this. And the sources of that currently can can include S3 or Kinesis or Kafka or any JDBC source as well. You can then graphically set up transforms of that data in the Glue Studio UI. So you can do transformations, you can sample the data, you can join tables together and a few other things as well. And then you can take the output of that transform and send it to S3 or the Glue data catalog. It also supports partitioning out of the box. So if you do have partitioned data, it will automatically take advantage of that and accelerate itself accordingly. Also, it offers a visual job dashboard that lets you visualize the status of your glue ETL jobs and how they're running, what's running, how long they've been taking. So that can be useful for quick troubleshooting and making sure that your jobs are running as expected. Let's dive in and show you how it actually works. So let me give you a little bit of a quick tour here of Glue Studio. It's a little bit hard to find these days because in some places they call it Glue Studio and other places they call it visual ETL. You can get there a couple of ways. One would be from this go to ETL jobs button here, or from the little sidebar here. It's under visual ETL under ETL jobs. And you can see now we're calling Glue Studio again. Let's click on the visual ETL button just to give you a little bit of a tour here. And you can see that the list of sources has grown considerably over time. So it's not just glue, Data catalog, S3, Kinesis, and Kafka, pretty much any data source you can imagine, even if it's outside of Amazon, is fair game. So. So any external database, MySQL, Microsoft SQL, whatever you can imagine, even snowflake, Google BigQuery, you know, things from competing vendors, even Azure, uh, MongoDB, zoom, third party tools like QuickBooks or LinkedIn. I mean, I think even Salesforce PayPal is in here somewhere. The list goes on. So pretty much any data you can imagine is fair game for Glue Studio. So we start off by adding a node for a source. Let's just keep it simple and say it's something that I have in S3. Just click on that node for the data source and tell it where it is. So I'll just pick one of the data sources that we had from earlier in the course under order logs. Order logs Sundog soft. So that's where that data is coming in. And now if we look at the output schema it will tell me what that data looks like okay. So now we can do stuff to transform that data. Let's go ahead and click on that and click on plus and add a transform node. And you can see there's many many different things you can do here as well. So we can do changing the schema SQL queries, uh, filters, droppings, things. There should be even some capabilities for writing your own custom code somewhere. Yeah, there it is. Custom transform. But let's do something simple and just change the schema, which lets you do a lot of common things. So let's say I just want to drop the description field, uh, change the year to an integer. Change the date to an integer. You know, whatever you want to do. I'm just just playing around here and that would do what you expect. So click on that. And you can also look at the output schema there as well to make sure it did what you said. The description is gone year and day are now an integer. And let's also apply a filter to that. So let's do a transform filter. And we could say for example add a condition. Let's say we want to filter everything out where the year is less than 2001. It's just that simple. All right. And click on that. And you can see the output schema is the same. We just filtered out some data and now we can put that somewhere. So let's click on plus again and add a target. And let's just dump that in S3 as well. Again, you can see that there's many other places you can send this to, including snowflake, BigQuery, Azure. They don't care. So pretty amazing. But let's just pick another S3 bucket. And then I could specify what bucket I want to put that in right. And by the way it doesn't have to be linear like this. So this is actually a directed acyclic graph. So you could have things happening in parallel alongside each other. I could click back up here and do that filter in parallel instead of after that transform node if I wanted to. So you can have these more complicated structures where processing is happening in parallel to make it even more efficient. If that makes sense for what you're doing. It doesn't have to be linear like this. All right. So that's Glue Studio just to kind of show you what it is and what it can do. Let's go back to the previous page. And from here you can monitor your existing jobs and see what they're doing. Last time you changed them and everything else. And they also have some examples you can look at as well. So that's Glue Studio. You know I think they're kind of pushing people more toward data Wrangler these days. But it's still there for you. Also known as visual ETL.

---

### Lecture: AWS Glue Data Quality

In. More recent feature in Glue Studio is AWS Glue Data Quality. This is basically a step you can inject in your glue job to automatically evaluate the quality of the data coming in, and if it violates certain parameters or rules that you set up, then it can automatically fail the job or just log something in CloudWatch for you. Now you can create these rules either manually, or they can create them for you automatically. So you can actually tell glue data quality. Hey, go look at my source data. I know this is good data here. Infer some rules from it. You know what's the expected range of the data that I'm seeing here? What's the standard deviation of typical values that I'm seeing? Should I have alarms in place if I if I exceed those ranges. Right. And once you set up those data quality rules, you can just integrate them right into your glue jobs, like you can see in this diagram to the right. So just like another transformation stage there, one of your stages could be evaluating the data quality using glue data quality. Now if you are going to be creating these rules by hand, it uses something called the Data Quality Definition language or dcdl. And we'll look at some examples of that on the next slide. And again you can have this do a couple of different things. You can set it up so that if the data quality rule fails, it will fail the whole job. That can be annoying though, because if you do have sort of a a threshold that's a little bit too tight, you can get a lot of false positives there. Alternately, you can just report the results to CloudWatch where you can interpret them and act on them as you see fit. A little bit of an example here of what it looks like in the glue UI. So here they've created a rule called rule set one Rs1. And you can see down there some examples of the Dcdl language for those rules. And this was created by the automatic recommendations. So you can see that you can run glue data quality on your data set. And it will automatically create these dcdl rules for you. And you can go in there and edit them if you want to. If you don't agree with the things that I came up with. So there's some examples here. It's saying that the row count should be between these values. If I'm seeing less than the expected number of rows, something might be wrong. You can test to make sure that you have complete data in certain columns. You can test to make sure that the length of certain columns is within a certain range of values. There. Things like that. Right. Standard deviation also. That's an interesting one. So you can say, well, if the date of infraction or the ticket number is outside the expected bounds, we should also do something about it okay. And you can see after you actually run that on the right here what the results would look like. So in this example here, the Rs1 rule set that we made failed because the standard deviation on date of infraction and the column value seen in date of infraction were not as expected. Again, these are examples of where the thresholds can be a little bit too tight and lead to potentially a false positive. But maybe it's a real issue. Maybe there's some bad data in there or some outlier that needs to be accounted for. That would mess up your analysis later on. So that's AWS glue data quality, relatively recent feature. It should be fair game in the exam from say, 2024 onward though.

---

### Lecture: AWS Glue DataBrew

Let's talk about glue Databrew. This is definitely something you need to know in the field of data engineering, right? Because this is a visual data preparation tool. So it's another UI that you can use just for transforming data, doing the tea of your ETL. So very, very very relevant to this exam. It is a UI for pre-processing large data sets. Those data sets can come from S3 from a data warehouse or any database you want. The output, however, will go to S3 once it's been transformed by Databrew, and there are over 250 ready made transformations that you can visually apply to that data. So you can just kind of drag and drop these things in Databrew and have it automatically transform your data in pretty much any way you can imagine. And I'm not going to go through all 250 of them. If you do want to go through the documentation on AWS's website and at least scan through them, it could hurt. You know, it's good to know what the capabilities are, but the short version is pretty much anything you can dream up is probably in there. You can create these recipes of transformations that can then be saved as jobs within a larger project. So if you have multiple transformations you want to apply to your data, you can create a recipe of those transformations and then save that off and apply that to different places, different projects, different data sets. Those recipes are made up of recipe actions. And if you go to that documentation for the transformations, you'll see those definitions of those recipe actions. So as an example here on the right this is a recipe action called nest to map. And this is used to take selected columns and convert them to key value pairs. So instead of individual columns that might convert it into a JSON document that has these key value mappings between those columns and their values. So I don't know why you'd want to do that, but you can do that. And you can also optionally remove the source columns that you combined into that map as well as part of it. So that's what's going on here in the parameters. We're specifying what source columns we want to nest. So we're going to take the age the weight kilogram and height centimeter columns. We're going to put them into a new single column called column name, where it's just going to be stored as this JSON based map that will say, for example, age equals 53 weight, kilograms equals 100 height centimeters equals, you know, 500, whatever it is. Right? You can tell I don't really deal with the metric system because I'm sure those numbers make no sense. And then we also say remove source columns. So the original age weight and height columns will be deleted as part of that nest to map transform. You can also define data quality rules as part of Databrew as well to kind of make sure that your data makes sense as you go along. And you can also create data sets with custom SQL from Redshift and Snowflake as part of Databrew as well. From a security standpoint, Databrew does integrate with KMS using customer master keys only. It of course uses SSL in transit, and you can use IAM to restrict who can do what. As with most services and also as with most services, it integrates with CloudWatch and CloudTrail for alarming and governance and all that good stuff.

---

### Lecture: Demo  Glue DataBrew

So let's just explore glue Databrew here a little bit. Again, I'll give you a little quick tour of the UI, just to make it a little bit more intuitive as to what it's all about. So I'm at the glue Databrew console page here, and let's create a sample project just using some of the sample data they provide. Uh, let's explore popular baby names in 2020, and we'll create a new project in Databrew. For that we need to give it a IAM role for security purposes. Let's use this one that I created already. And it's going to go out and provision some computing resources for performing this interactive section of Databrew on this data set. Um, as you recall from the pricing, it's a $1 per interactive session. And then we if you want to actually run this as an ongoing job, then that's billed by the resources that you use. But for now it's going to spin up some computing resources pretty quickly. It's going to initiate my databrew session any minute now. This actually does happen pretty quick It's not like spinning up an EC2 node or anything like that. And here we go. It's preparing a data frame from that sample data, loading it in from S3. And we're ready to rock and roll here. So you can see that we have this data frame view of that sample data here where we have how many times each baby name appeared, the gender, some ID associated with it, the actual name itself, and a year field, which is 1880. I thought this was a 2020 data set, but I'm not going to get into that now. Anyway, the idea here is that you can apply these recipes and build them up, that transform this data in any way that you want to, and you can run this interactively and download the results. Or you can create a job that allows you to run it repeatedly and even run that on a schedule. So, for example, let's say that I only want to get baby names that start with the letter L for some reason. Just as a really simple example, let's create a new recipe here. So we have this. We can add a step to our recipe. And here's the menu of all those different transformations that you can perform 250 plus of them. There are column actions. You can rename columns or change their types, or move them around, or duplicate them or delete them. You can filter things, which we're going to do. You can change the formatting of text fields. There's data cleaning operations here that are built in. So if you want to remove special characters or remove numbers, whatever you want to do, if you need to prep that in in ahead of further processing, you can do that very easily with the clean filters. We can also extract information between delimiters. So if you had a field that had delimited data and you just wanted to extract data between those delimiters, that's easy to do. You could just specify what the delimiters are and where you want to put that column. Right. Very easy way of doing that. Also, you can, uh, impute missing values automatically. That's kind of a cool feature. So what do you want to do if data is missing? Do you want to just fill it with null or empty values? Do you want to delete the row entirely? Or maybe you just want to fill it with the most frequent value as a placeholder? Sometimes that's the right thing to do. Very easy to do in databrew. Uh, what else can we do? Uh, invalid values. You can deal with that. You can deal with duplicate values however you want to. You can split columns, merge them together, create new ones based on some sort of a function. Unnest pivot GroupBy join union. Now we're getting into sort of SQL style operations, right? So pretty much anything you could do from a database you can do in Databrew. We can scale numeric values. That can come in real handy if you're preparing data for deep learning, right? A lot of times you need to normalize that data before you feed it into a neural network. And these scaling operations make that really easy to do. What else we got? Aggregates text functions. These are all basically SQL functions available to you. And this kind of fleshes out a lot of those 250 options there. But you can see pretty much any transformation you might want to do is in here. So that's really neat. It allows you a very scalable and powerful engine for transforming and pre-processing data that usually doesn't involve writing any code. So pretty cool. So let's say we want to filter data to just get baby names to start the letter L. So I'll say filter by condition. And we'll select the name column and we'll say it must start with L, and I can actually preview that with the preview button here. And you can see that only the baby names that start with L have been preserved. Pretty cool. I can then apply that to the entire data set. And now we have a data set of baby names that start with the letter L. Pretty cool. You'll see there's shortcuts to a lot of these up here in this dashboard. And what can I do. Let's go to actions. So I can actually download that filter data as a CSV file, which is neat. Or I can create a new job out of it if I want to run it repeatedly. So it'll take me down to the jobs tab here. I can create a job name for it. Uh, baby names, l whatever you want to call it. And we can set the output settings here. So the file types that you can output right now are kind of limited, but it can be CSV glue, parquet, parquet, Avro, ORC, XML or JSON. Those are the current output choices into the S3 bucket of your choice and the compression of your choice. Snappy gzip, bzip2, or deflate and brotli. Those are the two new ones. Whatever delimiter you want. Comma separated. Colon separated. Tab separated. Anything you can imagine here that you might want. And yeah, once you have this you can actually run that job uh, as a on demand thing. Or you can create a schedule for it. If you go back to the Databrew console here and go to jobs, you can click on the schedules tab. And here's where you would actually create a schedule to run a job repeatedly on some sort of a cron style schedule. So you can do that too. Again, going back to the main pricing tab here. Currently those interactive sessions are just $1 per session, but if you want to make a job and run it repeatedly, it's charged at $0.48 per node hour for that processing cost. And if you want to calculate that, there's a handy dandy cost calculator. So that's databrew in a nutshell. As you can see, its whole purpose of existence is to provide you a very easy way to pre-process data at a very scalable way and a very quick way without writing any code. And it's relatively new feature and possibly an alternative to using glue ETL for you.

---

### Lecture: Handling PII in DataBrew Transformations

Something the exam guide calls out is handling personally identifiable information in your Databrew transformation. So let's have a little bit of a diversion into that world. We talked about some of these techniques earlier in the course, but just to reinforce it and how it relates to Databrew one way of dealing with PII is with substitution. So I could just replace that with some sort of random numbers. Right. And substitute that information with something that is not personally identifiable. I could shuffle it. I could take all of the PII values in my rows and shuffle them all together so that Person A's credit card number might be associated with person B instead with credit card numbers, though probably not a good idea to have raw credit card numbers floating around at all. Deterministic encryption is one way of encrypting that personally identifiable information in such a way that a given value always results in the same encrypted value. It's deterministic. That way you can also have probabilistic encryption, and that's a system where there might be more than one result from an encryption of a given field. You can also decrypt something that was encrypted using a decrypt command, and there's probably no better way of dealing with PII than not having it in the first place. If you want to null it out or just delete it. The delete transformation can be used to just drop that information entirely. You can also mask out personally identifiable information. So a lot of times you'll see only the last four digits of a credit card or a social security number being displayed where the rest of it is masked out or scrambled in some way. Masking is a way to do that. And finally, there's hashing, where you apply some sort of a hash function to the value in a cryptographic fashion. Hashing is a little bit different, though, in that multiple values can hash to the same result. So sometimes that's okay, you know, but it does provide some anonymity to what you're doing as well. So some techniques for handling personally identifiable information in databrew. Transformations. And these are the specific names of those transformations that are associated with each technique.

---

### Lecture: Intro to Amazon Athena

Let's dive into Amazon Athena next. That is a serverless interactive query engine for S3 data. So pretty cool stuff. You can take potentially a massive data lake in S3 and just put Athena on top of it and query it just like it were a SQL database. So let's dive into what it's all about. What is Athena? Technically it's description as an interactive query service for S3. So again, it's basically a SQL interface for your data in S3. There's no need to load that data into some intermediate database. The data just stays put in S3. And Athena knows how to interpret that and query it as though it were an actual database. Under the hood it uses Presto, but you don't really need to know that. It's just kind of what they started building Athena on top of. It's evolved quite a bit since then, though, so its roots may have been in Presto, but it's really more of its own thing these days. The big thing about Athena, though, is that it is fully serverless, so you don't need to think about the underlying servers or capacity or anything like that. You just go to Athena, point it at your data in S3, and start writing queries. Well, there's more you can do than that, but you could just do that. That's all there is to it. You just go to the UI, the the console for Athena and go to town. You don't have to think about setting up all the underlying capacity. It's all abstracted away for you. It supports many different data formats, so your data in S3 can be pretty much anything you can dream up, whether it's comma separated value or tab separated value. Those are both legitimate. So any human readable data, you can put Athena on top of it and it can make sense of it and query it. JSON format also supported ORC columnar format also supported. Good thing about ORC and parquet is that they are both splittable formats, and that can be useful for improving performance and segmenting your data in ways that make sense for your queries. Avro also Splittable and as far as compression goes, you can store your data in snappy zlib ae or gzip compression. Now there are some takeaways here you should keep in mind for the exam. Remember Orkin Parquet are columnar and splittable formats. Avro is only splittable. CSV, TSV and JSON are all human readable formats. Okay, so csv, tsv, JSON you can open up that file in a text editor and make sense of it. ORC parquet Avro not so much. Avro will be splittable, so you can process Avro data in parallel, because Athena can split that up and chew away on it in parallel in different chunks, right? Orc and parquet, in addition, are columnar formats, and that can accelerate queries that are only specific to certain columns. And pretty much any compression you can dream up is supported as well. So for the exam purposes, those are the things you want to remember. The data itself can be unstructured, semi-structured, or structured. Athena doesn't care. So whatever level of schema you might have, it can take advantage of it, or it can infer a new schema just by examining the data or taking one that you've provided to it. Some examples of usage here are given by Amazon. Include the ad hoc querying of web logs. So Athena is given as a better example of how to deal with querying web log data in S3, and they would rather have you use this than Elasticsearch, for example. You can also use it for querying staging data before loading it into redshift. So maybe you have a bunch of data being dumped into S3 and you want to transform it and load it into a big redshift data warehouse. But maybe you want to be able to play around with that data and investigate it and see what it looks like beforehand. Athena might be a good way of sort of getting a bigger picture of what that data looks like before you actually commit it into a data warehouse. It's also useful for looking at other logs besides web logs in S3, including CloudTrail logs, CloudFront logs, VPC logs, elastic Load balancer logs, whatever you have. If it's in S3, Athena can query it. It also offers integration with tools like Jupyter and Zeppelin and RStudio notebooks, because you can just treat it like a database. It has ODBC and JDBC interfaces, so you can treat Athena like any other relational database that you might interact with or integrate with. That also includes QuickSight. You can also integrate Amazon's QuickSight visualization tool into Athena. To actually use that as sort of the glue. Well, glue is a poor choice of word because we are actually using the glue service for this. But the thing that connects your unstructured data in S3 to a more structured visualization or analysis tool, such as QuickSight.

---

### Lecture: Athena and Glue

So speaking of glue, let's talk about how Athena integrates with AWS glue to actually impart structure on your unstructured data in S3 so that it can actually query it like a database. So again, let's do a little brief recap on how glue works. You might have a glue crawler populating the glue data catalog for your S3 data. That's looking at what's stored in S3, and trying to extract columns and table definitions out of it for you. And you can use the glue console to refine that definition as needed. So once you have a glue data catalog published for your S3 data, Athena will see it automatically and it can build a table from it automatically as well. So anytime Athena sees something in your glue data catalog in your account, it's going to make a table for that for you so you can query it just like you would any other SQL database. And it's not just Athena that can use that glue data catalog either. It will allow any other analytics tool to visualize or analyze that data as well. For example, RDS, redshift, redshift spectrum, EMR, any application that's compatible with an Apache Hive metastore as well. Because remember the glue data catalog can be used as a hive metastore too. But in this example we're using it to expose that table definition to Amazon Athena. And with Athena integrated with AWS Glue Data Catalog that allows you to create a unified metadata repository across various services. Crawl data to discover schemas, populate your catalog with new and modified table and partition definitions, and maintain schema versioning all under the hood. And Athena just sits on top of that and provides a SQL interface to that underlying glue structure. Another feature of Athena is work groups, and you definitely need to know what these are all about. Basically, it's a way. It's what it sounds like. You can organize users and teams or even applications and workloads into different work groups and group them together. And you can use this to control the query access of a given work group or to track costs by a given work group. So maybe you have a specific team of people that should only have access to specific types of queries, or limits on the queries that they can run. Right. And you want to track their costs individually to figure out what kind of bill they're running up for the organization. Work groups are integrated with IAM, CloudWatch, and SNS, so you can do things like have it notify you when limits are hit. And the way you use this is you can have your own data limits on each work group, so you can limit how much data queries can scan by work group. So if you want to make sure that somebody in a given team isn't going to construct some wildly inefficient query that has this huge bill that results, you can limit that at the work group level. Each work group also has its own individual query history. So, you know, if you want to make sure that a specific team of people isn't seeing the queries that another team makes, you can break them up into work groups to make sure that each work group only sees its own query history. Work groups, of course, can also have their own IAM policies, because that's how you're going to manage their permissions and what they can see. And you can also have your own encryption settings on each work group as well. So it's a good way of organizing many people in an organization and limiting what they can do and giving them specific access permissions by using work groups and Athena. So just remember, Athena work groups are a thing. You can use them to limit the access of a given group of people, or to measure the costs that they're doing, and to have limits on the amount of data that their queries can actually scan. The cost model is very simple because it is a serverless application. You just pay as you go for the activity that you actually consume. It charges you $5 currently per terabyte scan, which is pretty generous. Important to remember that successful or canceled queries do count toward that scanned number there. You do get charged for those, but any failed queries are not charged, so any successful or canceled queries you will be billed for. Failed queries, however, are free. There's also no charge for any DDL operations such as create, alter or drop. Those are free as well. Now a very important point is that if you want to save money using Athena, it can save you a lot of money by converting your data to a columnar format like we talked about with ORC and parquet. So not only does that give you better performance for applications that are typically querying on a small number of columns, it can also save you 30 to 90% in terms of money as well. That's because it allows Athena to selectively read only the required columns when doing a query and processing your data. So if you're only accessing certain columns from your queries by having a columnar format, you're reducing the amount of data that you actually have to scan with Athena. And remember, you are charged by terabyte scan. So by reducing that scanning you win. So remember Athena works best with column Reformats it can save you a lot of money and give you better performance as well. Examples of columnar formats include orc and parquet. All right. And in addition to Athena, of course, glue and S3 have their own charges as well. Athena just sits on top of glue to get the table definition for your S3 data, and your data is still being stored in S3. So there are separate charges for glue and S3 in addition to using Athena as well. Also, I should point out that partitioning your data can also help Athena to reduce costs as well. So if you do have your data structured in S3 in different partitions such as by date or hour or something, queries that are restricted to that given partition will also scan less data as well. So in addition to using a columnar format, partitioning your data within S3 can also save you money with Athena as well. Let's talk about security two. That's always important with Athena. It offers many different ways of securing your Athena traffic. One is through access control, who can actually get to Athena and query your your information in the first place. And you can use IAM access control lists and S3 bucket policies to restrict access to that information. The IAM policies that are relevant here are the Amazon Athena Full Access and AWS QuickSight Athena Access policies. You can also encrypt your results if you are sensitive about the results of your queries, and you can encrypt them at rest in a staging directory in S3, and that can be encrypted in several different ways. You can. Again, these are important to remember guys, so you can encrypt your S3 data results in server side encryption using an S3 managed key that is called ssh S3. You can also do server side encryption of your results using a KMS key that's called ssh kms. Or you can encrypt them on the client side with a KMS key as well. That's called csc kms. And depending on your security requirements, one or more of these may make sense. So think about how that data is flowing, where it's being stored and how you want it to be secured. You can also have cross-account access defined in S3 bucket policy. So it is possible for you to access Athena to a S3 bucket that is not owned by your account. If that other S3 bucket has a policy that grants access to your account, it's possible for an Athena console in one account to access a data lake stored in another account. You can set that up as far as In-transit security goes. You can use TLS, the transport layer security for all the traffic going between Athena and S3 as well. That can be set up to for In-transit security with Athena. Always important to remember security aspects guys. It's a huge part of the exam. Finally, let's talk about Anti-patterns. Again, these come straight out of the AWS Big Data whitepaper of things they don't want you to use. Athena for one, is for highly formatted reports or for visualization at the end of the day. Athena is just a SQL query engine. If you want to do nicely formatted stuff for visualizing things with charts and graphs, well, that's what QuickSight is for and we'll be getting to that shortly. Also, if you want to be doing ETL, extract, transform and load operations using Athena, that's generally not the best tool to use. Glue is an alternative there and using glue ETL instead. You can also do that with Apache Spark or what have you for larger scale tasks. So that's Athena in a nutshell. Let's go and play with it, shall we?

---

### Lecture: Athena and CREATE TABLE AS SELECT

Let's talk about the Create table as select statement or CTAs for short. You will probably see this in the context of Amazon Athena, although this is also a thing in other databases as well. The syntax might be a little bit different, though very similar in spirit to a materialized view in redshift. It creates a new table from query results. So you can do any Select statement you can imagine, and create a new table from the result of that query. And maybe you can use that to create a new table that's a subset of another table. So I can say hey I'm going to go do a select from this old table for some subset of what's in that table, and create a new table based on that query result. However, it's also a neat little trick for converting data into a new underlying format, and this is especially interesting remembering that Athena can sit on top of a data lake in S3, so you could use this as a way to convert data in S3 to a totally different format that might be more performant for Athena, right? So, for example, let's take a look at these two examples down below. Here, the one on the left we're saying create table new table with format equals parquet right. Compression equals snappy as select star from old table. So what's going on here. We're going to select everything from old table. And we're just going to rewrite it into a table called new Table. But along the way we're going to change it to parquet which is a columnar format that's going to be better optimized for doing queries in Athena, and also with snappy compression to further optimize it as well. So this is a little easy way to very quickly compress or change the format of your raw data in S3 and changing it to something that works much more efficiently with Athena. Another example on the right here, where same spirit. Here we're selecting everything from old table and putting it into a new table called my ORC task table. In this case, not only are we changing the format to ORC along the way, we're also specifying a new external location. So I'm saying, okay, not only do I want to change the format for this table, I'm going to explicitly store it within this S3 path. Within that my Athena results bucket. Right? So that's kind of an interesting trick because not only are we changing how it's exposed to Athena itself, we are also changing and storing the underlying raw data into this other table in S3. So that might be a quick and dirty way of doing data transformations or transforming the actual format of the underlying data, the raw data, into something else, just using Athena. And then I can go to that S3 bucket and pick it up there if I want to. So create table as select. Very handy trick for converting data into something more performant, and maybe even putting it off somewhere where you can get it later. Don't forget that.

---

### Lecture: Athena Performance

A few quick notes on optimizing Athena's performance. The exam allows to focus on topics like this. Uh, basically, it comes down to three things that we want to talk about here. One is you're usually going to get better performance by using columnar data formats like Orc or parquet. Uh, Athena will just work better if you can pre-process your data somehow to be in a columnar format. And you can do that with things like glue or through an ETL transform. Uh, you know, there's a lot of tools you can use to transform your data into a format that's more optimized for Athena's use. So if you are concerned about performance, uh, make sure that you're using a columnar data format under the hood with Athena and Orc or parquet. So if it's hitting S3 data, if it's in that format already, it will save you quite a bit of work. Also remember, a small number of large files will generally perform better than a large number of small files with Athena. So if you can have a small number of large ish columnar data files, that's probably going to be your optimal performance case right there. Also, take advantage of partitions. So if you're able to organize your data in S3 in a partition format, that lines up with how you're going to be querying that data. So for example, if you're, uh, hitting up your data most commonly by a specific date range or something like that, then partitioning your data by date would make a lot of sense right now. If you do need to add partitions after the fact, let's say that you have an existing data set in S3 that is not partitioned, and you want to improve its performance by adding partitions. You don't have to totally start from scratch. What you can do in Athena is use a command called MSK Repair Table to go back and add that metadata for the partitions into Athena. So if you do need to add partitions later on to an existing Athena database, you can use MSK repair table to accomplish that. So those are the main points about Athena performance. Just remember those three things and you should be in good shape.

---

### Lecture: Athena ACID Transactions

A new feature in Athena is Acid transaction support. That means that we have strict guarantees about transactions, so you can actually have concurrent users hitting the same row at the same time and have it all work consistently. Under the hood is powered by Apache Iceberg, and to use it, all you need to do is add table type equals iceberg. When you're creating a new table in Athena using the create table command. And this allows you, like I said, to have concurrent users safely making row level modifications or deletions, whatever it is, without worrying about them stomping on each other. This is actually compatible with elastic, MapReduce, Apache Spark, and anything that supports the Apache Iceberg table format. The good thing is that it removes any need for custom record locking, so without acid support, you would need to have some sort of a solution for locking a record and unlocking it, surrounding any modifications to it, so that if two people were hitting the same record at the same time and trying to write to it, they wouldn't be fighting with each other. Iceberg and acid transactions just does that for you automatically now. Also, a little side benefit is that it gives you what's called time travel operations. So you actually have the ability to recover data that was recently deleted just using a plain old Select statement. So some of the historical data that's keeping around for the purpose of asset transactions can be used for that purpose. Now remember when we talked about governed tables in lake formation. Well this is kind of another way of doing the same thing. So governed tables gave us acid support on S3 tables in lake formation. But this is another way of getting acid features with Athena. So you could set up a governed table in lake formation and then hit that table with Athena and you would have acid support. This is just another way of doing it. So you can just create a table explicitly from Athena directly with a table type of iceberg, and you'll get an acid support on that as well. So kind of two different ways of solving the same problem there. Now you might also remember that in lake formation with governed tables It had a feature to automatically compact those tables over time, because over time there's sort of this overhead that builds up and it can actually make your performance degrade as time goes on. Now, with Athena acid, at least for now, you do need to do that yourself periodically. So if you are using Acid transactions in Athena, you'll probably want to do a command like this once in a while. Optimize table rewrite data using bin pack where catalog equals whatever. So a command like that will go back and compact your data. That's sort of been bloated by this acid support over time and make it performant again. So that's the kind of thing I could see showing up on the exam that that kind of trivia. What do you do if you're acid? Transactions are getting slower over time? Well, the answer would be to periodically compact them using the bin pack command here that you're seeing. That's Athena acid in a nutshell, an exciting new feature in 2022, as more and more of the AWS data analytics world goes to Acid support.

---

### Lecture: Athena Fine-Grained Access

Let's talk about Athena. Fine grained access to AWS Glue data catalog. It's a not fine grained in the traditional sense. The idea is that we have IAM based database and table level security for specific operations. So this is broader than data filters. In lake formation. We're not applying cell level security or row level security or column level security here. It's more about the operations we can perform on the databases and tables within Glue data catalog. You also cannot restrict this to specific table versions. It applies to everything. Now at a minimum, you need to have an IAM policy for Athena that grants access to the database and to the Glue data catalog in each region. But we can do more. We can lock things down if we want to as well. So you might have policies set up in IAM that might restrict access to alter or create database operations, for creating new tables, for dropping databases or dropping tables for repairing tables, or even showing all the databases or showing all of the tables. So these are things you can lock down using IAM am and Athena. Fine grained access to AWS Glue Catalog. All you have to do is figure out the mapping of those operations to their underlying IAM actions, that it's not always straightforward. So here's an example on the right for controlling access to drop table. Here we're saying we're going to allow the actions required to drop a table to a specific catalog, to a specific database, and also to a specific table within that database. And it turns out that to drop a table, you need access to get the partition and partitions that that table is in. You need to get the table and the database it's in, and you need permission to delete the table and to delete the partition as well that it might be within. So you kind of have to look up what those actions are for a given operation that is documented. However, there's a little link there if you ever need to refer to it. Can't imagine that they would expect you to memorize that stuff for the exam. However, you can kind of use your common sense to figure out what actions might correspond to different database operations that you might want to lock down using. Athena. Fine grained access to AWS Glue data catalog.
---

*Section 3 of 13 uploaded. Confirm receipt and await next upload or GENERATE command.*