All right, so why is DynamoDB relevant to generative AI? It's definitely not a vector store. I mean, you could store vectors in there if you really wanted to, but it's not going to do a vector search for you, at least not today. However, it is still a useful tool for serving your larger system with near real time data, and that still has a place in your architecture for larger systems. And one specific application is using it for storing short and long term memory for your AI agents. So you need to store your chat history somewhere if you want that to persist over time, right? So when you go to ChatGPT and you start a conversation, you can come back to that conversation a month later and pick it up. That had to be stored somewhere, right? That's what we call long term memory. You got to put that someplace. Another example would be kind of like storing your longer term preferences as a user somewhere. DynamoDB could be an appropriate place for that as well. So for context awareness, which is what we call that in a chatbot, DynamoDB could be a solution for you there. And that's something that the AWS blog calls out specifically here on the right. Also, although it's not really a vector store itself, you can attach it to one through OpenSearch, which is kind of cool. There's a zero ETL that's extract, transform, and load integration with OpenSearch, which means that OpenSearch can be tied directly to your DynamoDB data and have a pipeline that just lashes them together, basically, so you can go to OpenSearch and back that by DynamoDB in real time. So that's a way of accessing data from DynamoDB, but fronting it by OpenSearch and using OpenSearch as your vector store for bedrock that ultimately can get data from DynamoDB. And that way you can incorporate your DynamoDB data into your knowledge bases.