Let's quickly talk about another tool in your tool chest for improving the quality of your retrieval augmented generation context that's retrieved from your vector stores. That's reranking models used with Amazon bedrock. So that attempts to improve the relevance of the retrieved results from your underlying underlying vector store from your knowledge base. Now, the problem is that you know, the chunks you got back might not necessarily be all that relevant to the original query. And this is a way of making sure that it is. So the idea here is that we're not just looking at the vector distances, the actual embeddings, the semantic search results from that information that we're retrieving for context. We're also going to look at the relevance of those individual chunks to the original query. And then we're going to reorder those results accordingly to choose what the best ones are. So it's not just about semantic search. In this case we're using that as a starting point. And then using the relevance of those chunks to the original query to try to rerank them again in order to get the best result at the top somehow. How do you do that? Well, there's a rerank operation in the bedrock API that you can use to do that and trigger that reranking whenever you want relative to a query to your bedrock model. And you can also specify a Reranking model explicitly when hitting your knowledge base at that level as well. Now there are built in ones you can use. All it has to do is measure that relevance somehow from the chunk to the query. Right. It's not that complicated of a problem to describe. There are two different models you can choose for that problem. There are from Amazon and currently from cohere. However they are only available in limited regions. So do double check that Reranking is available in the region where you've deployed bedrock before trying to use it.