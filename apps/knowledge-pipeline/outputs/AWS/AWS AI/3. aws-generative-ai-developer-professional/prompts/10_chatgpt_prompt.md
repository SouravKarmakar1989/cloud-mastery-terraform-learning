<!-- Section 10 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 10 OF 20: Analytics Services You Should Know
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: Intro  Other Services You Should Know

Welcome to the next part of the course, our series on other services you should know for the AWS Certified Generative AI Developer Professional exam. These upcoming sections focus on a wide range of AWS services that aren't central to generative AI itself, but are essential for designing complete, production ready AI solutions within real AWS environments. Now, while the exam guide does not group these services into specific content domains, they are explicitly listed as in scope for the exam, and they appear throughout AWS Skill Builder's recommended preparation materials. That means the exam expects you to understand what these services do, when to use them, and how they fit into the broader architecture surrounding your gen AI components. Think of this as the ecosystem around your foundation models analytics tools that process the data that you feed them. Compute and container services that run supporting applications, databases and caches that hold the information you retrieve. Machine learning services that complement bedrock and SageMaker. Developer tools that help you automate deployments, networking and delivery services that connect everything together. And storage systems that keep your data durable and accessible. These services aren't the star of the show, but they're vital supporting actors. Understanding them will help you design solutions that are well architected, cost efficient, secure, and tightly integrated with the rest of your AWS environment. This series will be presented mostly by Stefan, who will walk you through each of these categories and explain what you need to know for both the exam and real world AI system design. With that, let's hand things over to Stefan and explore the broader AWS ecosystem that surrounds your generative AI applications.

---

### Lecture: Amazon Athena

Let's dive into Amazon Athena next. That is a serverless interactive query engine for S3 data. So pretty cool stuff. You can take potentially a massive data lake in S3 and just put Athena on top of it and query it just like it were a SQL database. So let's dive into what it's all about. What is Athena? Technically it's description again interactive query service for S3. So again it's basically a SQL interface for your data in S3. There's no need to load that data into some intermediate database. The data just stays put in S3. And Athena knows how to interpret that and query it as though it were an actual database. Under the hood it uses Presto, but you don't really need to know that. It's just kind of what they started building Athena on top of. It's evolved quite a bit since then though, so its roots may have been in Presto, but it's really more of its own thing these days. The big thing about Athena, though, is that it is fully serverless, so you don't need to think about the underlying servers or capacity or anything like that. You just go to Athena, point it at your data in S3, and start writing queries. Well, there's more you can do than that, but you could just do that. That's all there is to it. You just go to the UI, the the console for Athena and go to town. You don't have to think about setting up all the underlying capacity. It's all abstracted away for you. It supports many different data formats, so your data in S3 can be pretty much anything you can dream up, whether it's comma separated value or tab separated value. Those are both legitimate. So any human readable data, you can put Athena on top of it and it can make sense of it and query it. JSON format also supported ORC columnar format also supported. Good thing about ORC and parquet is that they are both splittable formats, and that can be useful for improving performance and segmenting your data in ways that make sense for your queries. Avro also Splittable and as far as compression goes, you can store your data in snappy zlib ae or gzip compression. Now there are some takeaways here you should keep in mind for the exam. Remember Orkin Parquet are columnar and splittable formats. Avro is only Splittable. CSV, TSV and JSON are all human readable formats. Okay, so CSV, TSV, JSON you can open up that file in a text editor and make sense of it. ORC parquet. Avro not so much. Avro will be splittable, so you can process Avro data in parallel, because Athena can split that up and chew away on it in parallel in different chunks. Write orc and parquet, in addition, are columnar formats, and that can accelerate queries that are only specific to certain columns, and pretty much any compression you can dream up is supported as well. So for the exam purposes, those are the things you want to remember. The data itself can be unstructured, semi-structured or structured. Athena doesn't care. So whatever level of schema you might have, it can take advantage of it, or it can infer a new schema just by examining the data or taking what you've provided to it. Some examples of usage here given by Amazon include the ad hoc querying of web logs. So Athena is given as a better example of how to deal with querying web log data in S3. And they would rather have you use this in Elasticsearch, for example. You can also use it for querying staging data before loading it into redshift. So maybe you have a bunch of data being dumped into S3 and you want to transform it and load it into a redshift data warehouse. But maybe you want to be able to play around with that data and investigate it and see what it looks like beforehand. Athena might be a good way of sort of getting a bigger picture of what that data looks like before you actually commit it into a data warehouse. It's also useful for looking at other logs besides web logs in S3, including CloudTrail logs, CloudFront logs, VPC logs, elastic Load balancer logs, whatever you have. If it's in S3, Athena can query it. It also offers integration with tools like Jupyter and Zeppelin and RStudio notebooks, because you can just treat it like a database. It has ODBC and JDBC interfaces, so you can treat Athena like any other relational database that you might interact with or integrate with. That also includes QuickSight. You can also integrate Amazon's QuickSight visualization tool into Athena. To actually use that as sort of the glue. Well, glue's a poor choice of word because we are actually using the glue service for this. But the thing that connects your unstructured data in S3 to a more structured visualization or analysis tool, such as QuickSight.

---

### Lecture: Amazon EMR

Next, let's talk about EMR or elastic MapReduce, another very important service in the data analytics world on AWS. What is EMR? Again it stands for elastic MapReduce. And what it really is is a managed Hadoop framework that runs on EC2 instances. So basically Amazon took Hadoop and they integrated with EC2. That means that you can launch a cluster that runs Hadoop using the AWS console. This is a managed framework. But you know you do have to specify what servers you want to be running it with, how many instances you want in your cluster, what software you want installed on it, and in return, you will actually be able to log into these instances and do low level stuff with it. So, for example, you could say that you want Apache Spark installed on your EMR cluster. So instead of having this totally opaque system like you did with glue ETL where you don't see the servers, if you want to get down and dirty and actually have low level access to Apache Spark and want to be able to have explicit control over the resources that it has, EMR might be for you. You can also include applications such as Base or Presto or Flink or Hive and more, and we'll talk about those in more depth shortly. If you want to develop code that takes advantage of your cluster on EMR, it offers something called EMR notebooks, where you can develop and iterate on your scripts on your cluster just using a notebook environment in your browser. And they've integrated EMR or integrated Hadoop rather with AWS at several key points, which we'll also talk about more. So what makes up an EMR cluster? Well, an EMR cluster is fundamentally just a collection of EC2 instances that are running Hadoop. Now each one of these instances is called a node. Each node has a role within the cluster which we call the node type. So there are three different node types that an instance in an EMR cluster might be performing. One is the master node. You always need at least one of these. So the master node will manage the cluster by running software components to coordinate the distribution of data and the tasks among other nodes for processing. Assessing its job is to track the status of those tasks and monitor the health of the entire cluster, so every cluster will have a master node. You could even create a single node cluster that has nothing but a master node. So a bare bones minimal single node EMR cluster would just have a master node that does everything. The master node is also referred to as the leader node sometimes. Then we have the core node. So the core node has software components that run tasks and store data in the Hadoop distributed file system HDFS on your cluster or on EMR. It might be EMR FS, which allows you to write data into S3 instead. But the core nodes are what are doing the actual work and distributing that work across the cluster. So the trick here is that your job would be split across many core nodes, potentially each of them responsible for storing a part of your data and for running the processing on a portion of that data. So a multi node cluster will have at least one core node. If you're going to distribute your processing you need at least a core node. Now the third type for EMR is a task node. So this is kind of a newish thing. So a task node has software components that only runs tasks but does not store data in HDFS or EMR FS. They are optional. So if you need to add more processing capacity to your cluster but you don't need more storage capacity, a task node might be a good choice. And if you think about it, EMR is often using S3 for its storage under the hood, right? So you don't always need to have more storage available to you on an EMR cluster because it's all getting funneled into S3 anyway. So a task node can make a lot of sense on EMR, where you don't necessarily need a ton of storage on the EMR cluster itself. So that saves you some money, right? Like you're not paying for all the storage that you're not using. Now the other thing too, is that there's no risk of data loss when you remove a task node because it can't write data, right. So you don't have to worry about that. That means that it's a good thing to be using for spot instances, right? This is something that I can almost promise you'll see referred to on the exam somewhere. The the availability of spot instances for task nodes if you want to cut down your costs on an EMR cluster or add more capacity in a cost efficient way, a spot instance is a great way to do that because you don't really care if it goes down. You know it's just being used for extra processing capacity and the cluster can handle it if that comes in or out. As you're processing your jobs, there's no risk of data loss with a task node because they don't store data. So again, remember, a good way to expand your cluster dynamically is by using a spot instance on a task node. A few ways of using EMR. So one is by using a transient cluster, a transient cluster will automatically terminate once all the steps are complete that it's been assigned. So when you set up a transient cluster, you can say, okay, I want you to spin up this EMR cluster that has this kind of hardware behind it, and it's going to run these processing steps that I've defined ahead of time. And then when it's done, shut yourself down so you can define ahead of time steps in EMR that will load your data, process your data and store the results. And then when it's done, automatically shut down the cluster. That's what a transient cluster is all about. And obviously this saves money because you're not paying for time on this cluster beyond what you need. So if you do have these sort of one shot jobs that you need to run once in a while, a transient cluster is a good way to do that, where it will just spin up those clusters resources, run your job, and then shut down. So a good way to save money potentially. However, sometimes you want to basically build a data warehouse that's long lived. You don't want to be spinning up this thing all the time and shutting it down and losing all of your data in the process, right? So if you want to build basically a data warehouse with periodic processing on some large data set where you can access that data whenever you want or be running jobs on it continuously, a long running cluster might be more appropriate. So in this case, you would spin up a cluster, uh, you know, with whatever parameters that you specify upfront and just leave it running forever. Well, not forever, but until you manually shut it down. And if you do need to add capacity to it later on, you can always spin up more task nodes using spot instances for temporary capacity needs that you might have. Like we talked about If you want to save even more money, you can use reserved instances on these long running clusters to save even more. If you know you're going to be running this cluster for a long time, then obviously a reserved instance is a good choice for the hardware backing that cluster. So by default, termination protection will be on and auto termination will be off on a long running cluster. It will try to preserve itself as much as it possibly can. So two use cases here transient clusters. If you just want to spin up a cluster on demand, run something that you've predefined and then shut it down, or a long running cluster where you have it running all the time and can access it whenever you want to and keep that around in a more persistent manner. You specify the frameworks and applications when you launch the cluster. So we talked about, for example, running spark. That would be something that you would select when you're spinning up the cluster ahead of time. And it will automatically install those frameworks and applications that you want when it's being spun up. Once you have a cluster spun up, if it's a long lived one that's not just running in a transient manner, you can then connect directly to the master node and from that master node run your jobs directly. So if you're a command line type of person, you would basically, for example, set up a spark enabled EMR cluster, connect to the master node, kick off your Spark driver script and it would go off and do its thing using the complete power of the cluster. So that's one way of using EMR. Or you can submit steps via the AWS console to EMR. So these steps you can define strictly in a graphical manner through the console. And you can do basic things like processing data in S3 or from HDFS, which is the underlying file system of Hadoop. You can output that data to S3 or somewhere else. But once defined, you can then kick off those steps via the AWS console. Instead of going through a command line prompt by a direct connection to the master node. So two ways of using EMR. You can log into the master node and kick stuff off, you know, the old fashioned way. Or you can define your steps using the console exclusively, and kick off those steps through the console to run on your cluster.

---

### Lecture: Amazon Quicksight

Let's dive into Amazon QuickSight, which is AWS visualization tool for big data. What is QuickSight? Well, the official definition from Amazon is it's a fast, easy, cloud powered business analytics service. Let's break that down. So yeah, it's basically a service for visualizing your data. And the fast and easy cloud powered bit means that it's scalable. And it's not just meant for developers. It's really more of a web application where anybody in your organization can use this to look at dashboards or create their own dashboards, create their own charts and graphs, whatever they need to do to interpret your data and look for trends and stuff from a business standpoint. So it's made for people that are data savvy but not necessarily a developer. It allows all the employees in an organization, not just the developers, to build visualizations on top of your data. You can also build paginated reports for management. This is actually a new feature in QuickSight. Believe it or not, you'd think it would be there, but they just recently figured that out. You can use it for performing ad hoc analysis so it can be sitting on top of your data lake or data warehouse, whatever your data is. We'll talk about those sources momentarily and get real time visualization of that data as it changes. So if you want to dig into the latest data and understand how it's behaving, what the trends are, what that means for the business, you can very quickly set up new visualizations. Take a look at the data, interpret it, try to make sense of it. Try to apply that to your business. You can also get alerts on detected anomalies. So there's a feature where it can automatically monitor your data and say, hey, something's out of the ordinary here. Maybe you should do something about it person. You can also more generally just use it to get business insights from your data very quickly. That's the whole point of QuickSight. That's the quick part I guess. So it's supposed to be a relatively easy way to visualize your data. You don't necessarily have to know SQL, although it helps, but there are some features that will automatically try to create a appropriate visualization just by inspecting the data and trying to figure it out on its own as well. So knowing SQL helps. You don't have to though, and it's built for any device. So even if you're a CEO who wants to look at the latest dashboards for the business on their cell phone, QuickSight can be the front end for that. And it's fully serverless, of course. You know, we don't want your CEO having to worry about how many servers are under the hood powering QuickSight. It all scales up and down for you automatically. There are many data sources QuickSight can pull from, so where is the data that you want it to visualize? That is something you might have to worry about as a developer. The obvious choice is redshift. You might have a giant data warehouse, of course, and you just want to make some charts and graphs and reports. On top of that, data warehouse QuickSight can do that. Of course, you can sit on top of any Aurora database or RDS database. So if you're running some MySQL instance out there, or some SQL database of some sort, you can put QuickSight on top of that as well. Athena also a popular choice. So if you have a big data lake sitting in S3 or something, you can have Athena on top of that and then visualize through Athena that data in your data lake. So that's kind of a cool capability, right? Without even having a quote unquote real database. You can just have a collection of unstructured data in S3 somewhere and not only query that through Athena, but also visualize that data using QuickSight. It can also talk to OpenSearch. So a little bit more of an obscure use case there. But you could put QuickSight on top of an OpenSearch database if you wanted to. Often people will put their, you know, server data, their server log data into OpenSearch. So if you want to visualize that and look at things like server latencies or, you know, response codes over time, you can also do that with QuickSight as well. It also integrates with IoT analytics. Internet of things, although AWS is not emphasizing that as much these days. And if you have an EC2 hosted database, maybe you just put your own MySQL instance on an EC2 instance somewhere. QuickSight can talk to that too. Also, just raw files. You can just have CSV files or TSV files, tab separated value or comma separated value files, or even Excel files, or even common log formats just sitting in S3 somewhere or sitting somewhere on premises, someplace that QuickSight can get to. Somehow. And it can visualize that directly as well. Before you analyze or visualize that, you can even prepare that data if you want to, to a limited extent within QuickSight. So if you need to do things like change the names of columns in your file, you can do that. Change the field names, add calculated fields. You can use SQL queries there. You can change data types. If you need to do a little bit of massaging of the data before you visualize it, you can do that within QuickSight. Two. An important piece of QuickSight is spice, and this is its trick that it uses for accelerating its queries. Spice stands for super fast parallel in-memory calculation engine. So it's a way of copying your data from Athena into a more optimized format that leads to faster queries from QuickSight. So the idea is that it uses columnar storage in memory access machine code generation. And the idea is to accelerate your interactive queries on very large data sets. Now this does take space of course. Storage capacity. It basically needs to copy a lot of your data from Athena into this more compact and optimized representation that Spice uses. Every user gets ten gigabytes of spice by default. It is promised to be highly available and highly durable, just like S3 would be, and it can scale up to hundreds of thousands of users. So they've made it available, durable and scalable. So the idea of spice, again is just to accelerate your queries, but it is an extra layer there. So you have to think about that sometimes. So although it can accelerate very large queries that would normally time out if you're using direct query mode, direct query mode is another word for hitting Athena directly. Um, you can get into a situation where your queries are too big even for spice. So one situation you might encounter is that if it takes more than 30 minutes to import your data from Athena into spice, you're still going to time out. So there are even those spices designed to accelerate large queries. With QuickSight, there's still an upper limit there that you might hit. So something to be aware of there. But just remember spice is the super fast parallel in-memory calculation engine that exists to give you a more optimized representation of your data and accelerate your interactive queries through QuickSight, especially on large data sets. But you know, it is possible to get so large that even spice can't handle it. Here are some of the use cases that Amazon recommends for QuickSight. One is the interactive, ad hoc exploration and visualization of data. So you might recall that this is actually explicitly an anti-pattern in other services. However, QuickSight is designed from the ground up for ad hoc exploration and visualization of your data. You can also create dashboards with QuickSight, so you can make these nice to look at, dashboards to visualize data on a periodic basis, and make sure that your executives can see those and see how the business is doing. It's also useful for analyzing or visualizing data from a variety of sources, including S3 logs, any databases you might have on premise. Other AWS services that we listed earlier RDS, redshift, Athena, and S3. You can also integrate QuickSight with things like Salesforce, which might be useful to know any software as a service application can probably integrate with QuickSight and any JDBC or ODBC data source, so pretty much any data source you can imagine, QuickSight can import it and allow you to visualize it very quickly and at large scale. However, there are some things QuickSight is not made for, and although it has limited ETL capabilities, if you want to do more powerful or larger scale ETL transformations on your data, QuickSight really isn't the tool for that. You want to be using glue ETL instead, or maybe Apache Spark. But again, QuickSight does have some limited transformations you can do just to make your data look a little bit better from a security standpoint. It offers multi-factor authentication on the accounts that access QuickSight. Remember, this is more of an end user tool, so you're not going to be using lower level security features with this. It does have VPC connectivity, however, so you can add QuickSight IP address range to your database security groups. You will have to worry about how QuickSight will communicate with your databases and get through the security you have on them. It also offers row level security, or RLS, that enables QuickSight dataset owners to control access to data at row granularity based on permissions associated with the user interacting with the data, and with RLS or row level security. QuickSight users only need to manage a single set of data and apply appropriate row level data set rules to it. In late 2020, they also added column level Security or CLS. This is only present in the enterprise edition of QuickSight, but it does allow you to manage security at the column level as well as the row level. Now you also have private VPC access available. That's made possible through the Elastic Network Interface or Ini to create a secure private communication with data sources in a VPC that also uses AWS Direct Connect to create a secure and private link with your on premises resources. So if you do want to use QuickSight on top of on premises databases or files, you can use Direct Connect to do that. A little more on security and QuickSight. So how do you manage resource access? Well it's pretty simple really. Right from the QuickSight console there's a Manage QuickSight menu. And from there you can go to security and permissions. And from there you can ensure that QuickSight itself is authorized to use Athena S3 and specifically your S3 buckets. So before you get to the user level, you have to make sure that QuickSight itself is authorized to access the data that you want, whether it's Athena or S3. And again, that can be managed right from the QuickSight console. Now, if you do need to restrict what users can access specific users, you can create IAM policies to restrict what data in S3 a given QuickSight user can access. So remember you need to ensure QuickSight itself is authorized to use access your data and you also need to control what users can access what data through IAM policies. So QuickSight itself, you can just manage that through QuickSight console. But if you do need more granular permissions at the user level, IAM can be used for that. So there's also the specific issue of QuickSight and redshift together from a security standpoint. And generally speaking, QuickSight just tries to handle all this stuff under the hood for you because it's really aimed more at end users and not AWS experts. But this is one area that can trip you up. So the thing is, by default, QuickSight can only access data stored within the same region as the one that you're actually running QuickSight within. So you could have a situation where you have an existing redshift cluster running in some region like Japan or something, but you actually spun up QuickSight in Virginia. And in that case, you're going to get errors because those two regions can't talk to each other. You might think you could just create a VPC and configure that to work across regions, but that actually does not work with QuickSight. If you do have this situation, the. The way to fix it is to create a new security group with an inbound rule authorizing access from the IP range of the QuickSight servers in that region. So I'm highlighting this for a reason, guys. Those ranges are fortunately documented. You have to look them up, but that is the step you have to take. If you need to have QuickSight access a redshift cluster that's in a different region, set up a new security group with an inbound rule with the IP range of where the QuickSight servers are, and you got to look up what those IP ranges are. Now, if you have the enterprise edition of QuickSight, there are other ways of handling the cross-region problem of getting from QuickSight in one region to redshift, or even RDS data that might be in a different region. And these same techniques in some cases also work for cross-account access. So again, if you have enterprise edition, then you have the capability to create a private subnet within a VPC and then use Elastic Network Interface to put QuickSight inside of that subnet inside your VPC. Now, accessing QuickSight from within a private subnet is the part that requires Enterprise Edition to do. But once you do that, all the various techniques for connecting subnets together across regions or across accounts become available to you. So let's dive into where that goes from there. So this is step one. No matter how you're doing it, you need to put QuickSight within a private subnet in a VPC using Elastic Network Interface or Eni for short. And one way to do Cross-region access is to also put RDS or redshift or whatever your data is inside another private subnet, and then connect that with your private subnet that has QuickSight using a peering connection. Okay. So basically you put QuickSight inside a private subnet. You have RDS or redshift inside another private subnet that might be in a different account, might be in a different region, but you can still tie them together using a peering connection to gain access across regions. This is the sort of thing that the exam loves to question you about, so try to internalize this. Now, this isn't only useful for cross-region access, it can also be used in some cases for cross-account access. And if your goal is cross-account access, there's a few other ways of doing it as well. Basically, if you replace the peering connection with AWS Transit Gateway, that's another way to connect two different subnets. However, Transit Gateway itself is limited to being within the same organ region between the things you're trying to connect. However, it is possible to peer transit gateways together. So this might be a practical way of connecting things if you want to get cross-account access when those accounts are in the same org in the same region. Otherwise you may as well just use peering, right? Another approach is to use AWS Privatelink, and basically that would substitute Privatelink for AWS Transit Gateway in that top diagram there. Same idea. Another technique is using VPC sharing. And you can see that in the bottom diagram here on the right that's taking two private subnets one for QuickSight, one for your data source. And then using VPC sharing to tie them together. Again a way to provide access across two different accounts. You can see the diagram here talks about account A and account B. So that's really the goal of these approaches here. So remember these are all ways of handling Cross-region and cross-account access. If your goal is cross-region access peering is probably the way to go. But if you want Cross-account access transit gateway, Privatelink or VPC sharing might be viable depending on your requirements. Talk a bit about QuickSight user management. How do you actually manage who has access to it? Again, AWS tries to make QuickSight end user friendly so you can define users in IAM and just do it that way. That gives you more strict control over what individual users can and cannot access in terms of what data, what underlying S3 buckets, what redshift databases, what have you. Or you can actually configure it so people can just sign up over email and get approved that way. So that's a more easy way to set things up if you're comfortable with it. Also important to understand is that there is Active Directory integration available in QuickSight as well. There's something called Active Directory Connector. This is only offered in QuickSight Enterprise Edition. And again, in the spirit of making things as easy as possible with Active Directory Connector, all the keys are managed by AWS. For QuickSight, you cannot use customer provided keys with Active Directory with QuickSight. Also remember, Active Directory support is only in the Enterprise Edition. Now what you can do is set up different groups of users that correspond to Active Directory user groups and tweak their security access using IAM. If you need to. So you can still set up groups of users through Active Directory and tweak what they can and cannot access using IAM. So the takeaway here is, uh, the base starting point of security and QuickSight is IAM. You can use that to define what users can or cannot do. Email sign up is an option. And if you're using Enterprise Edition, you can use Active Directory Connector. But remember the keys are all managed by AWS. You cannot use customer provided keys and it is only supported in the Enterprise Edition.

---

### Lecture: Amazon Kinesis Data Streams

So now let's talk about Kinesis Data Streams. So it's a service used to collect and store streaming data in real time. So the keyword you have to look out for at the exam is really real time. So here is a diagram to explain better. So we have real-time data. What is real-time data? Well, it is data that is created and used on the spot. For example, whenever users click on our website, it's called a click stream, or when you have a device that's connected to the internet, for example, a connected bicycle, or when you have metrics and logs on a server, and you want to use them directly, so you wanna send those into Amazon Kinesis Data Streams. And to do so we must use what's called producers. So producers can be either applications and this is actually code you have to write to take data from your website or from your devices and send it into Kinesis Data Streams. Or for example, for metrics and logs, you can install what's called a Kinesis Agent on your servers, and it will act as a producer to Kinesis Data Streams as well. So then your data is going to be sent in real time, so as it happens, into Kinesis Data Streams. And the reason why we do so is that we want to have consumer applications consuming this data in real time and leveraging it. So we have our application, and again, you should write some code to read from Kinesis Data Streams, or we can have Lambda functions. It can read from it as well. You have something called Amazon Data Firehose that we'll see in a future lecture, and as well as, for example, services to do analytics on top of your streams, such as the Managed Service for Apache Flink. So some features of Kinesis Data Streams, so your data can be retained on the stream for up to 365 days. And because the data is persisted, you are able to reprocess, to replay data, by your consumers. That means also that once you send data into Kinesis Data Streams, you cannot delete it. You have to wait for it to expire from a time perspective for it to get deleted. Now, data up to one megabyte can be sent in two Kinesis Data Streams. But the real case, the real typical use case, is to have a lot of small real-time data. Your data is going to be in order if you send two data points with the same Partition ID. So it's a way for you to say that these two data points are related in time by sharing the same Partition ID. And you have security features, such as at-rests KMS encryption and in-flight HTTPS encryption. So if you want to write what I call an optimized producer application that is meant for high throughput, you should be using the Kinesis Producer Library, KPL, and to write an optimized consumer application, you should use the Kinesis Client Library or KCL. Okay, now your Kinesis Data Streams has two capacity mode. The first one is Provisioned mode, you which you choose the number of shards on your stream. So what is a shard? A shard is basically how big your stream is. So you can have one shard, but you can have 1,000 shards if you wanted to. The more shards you have, the more throughput you're going to have inbound. So that means you get one megabyte per second or 1,000 records per second for each shard in terms of capacity. And each shard in terms of read capacity now in terms of out traffic is two megabytes per second. So if you want to send, for example, 10,000 records per second or 10 megabytes per second, then you will need to scale to 10 shards. Now, if you wanted to adjust the number of shards over time, you can scale manually to increase or decrease the number of shards. And, of course, you need to be able to monitor your throughput to know how many shards you must have at any point of time. You're going to pay for each shard provisioned per hour. The other mode is On-demand mode. So here you don't need to provision or manage the capacity of your Kinesis Data Stream. You have a default capacity provision, which is about 4,000 records per second or four megabytes in. And then, automatically, Kinesis Data Stream is going to scale based on the observed throughput during the past 30 days. So instead, here, you're going to pay per stream per hour, and you're going to be billed per the amount of data that goes in and out of your Kinesis Data Stream. All right, that's it. I hope you liked it, and I will see you in the next lecture.

---

### Lecture: Amazon Kinesis Data Streams - Hands On

So let's go ahead and get some practice for Kinesis Data Streams. So I'm going to open the Kinesis service, and create our first Kinesis Data Streams. As we can see, we have three options here. We can use Data Streams, Data Firehose, or Data Analytics, but we only know about Data Streams so far, so let's go ahead. We get some information around the pricing, which is at per shard, we pay $0.05 per hour. Okay. And then there's a cost for making PUTs or for sending data into Kinesis Data Streams. So we'll create a Data Stream, let's name our stream, DemoStream, and then we have to define the data stream capacity. So as you can see, we have two modes, we have the On-demand mode and the Provision mode. And in the On-demand mode, you don't have to think about the capacity, it's automatically going to scale for you, so there's a maximum throughput of 200 megabytes per second, and 200,000 records per second, and the maximum read capacity of 400 megabytes per second, per consumer, if you're using the enhanced Fan-Out option. So the on demand as a pay-per throughput pricing model, but there is no free tier. Okay. And the Provision mode also doesn't have a free tier. And in the provision mode, you need to provision shards. And so there's a Shard estimator tool, if you wanted to understand how many shards you needed, based on like how many records you send per second, and what is the size of your record, and how many consumers you have. Okay. But in a example, we're going to just set up one shard. One shard gives us one megabyte per second for write and two megabytes per second for read. And obviously, if you put 10 shards, then everything is multiplied by 10. Okay, And so one shard is enough for us to do the demo. It's also the cheapest option we can get. If you do not want to pay any money in this course, then do not do this hands on, because you will pay some money for your shard, although we will deal with it, and then delete it fast enough. So when you're ready, you just click on Create data stream, and you wait for the data stream to be created. And our stream is now successfully created. So in terms of applications, we can see, we have producers, and we are recommended three options. The Kinesis Agents, the SDK, or the Kinesis Producer Library. So they're all available on GitHub, for you to check it out. This is a way to stream data from application servers into Kinesis Data Streams. The SDK is for you to develop producers at a very low level. And the KPL is for you to develop producers at a very high level, with a better API. And in terms of consumers, we get Kinesis Data Analytics, Kinesis Data Firehose, or the Kinesis Client Library, or Lambda, as well, that is not shown as an option right here. Okay. We get some monitoring information about our Kinesis Data Streams. So how many records we are sending into it? We could look at the configuration. So if you wanted to scale the stream, we can say how many shards we want. We can go from one, to say, five, and scale our Kinesis Data Stream. We could add some tags, and then we could use enhanced Fan-Out, and configure it, if we wanted to have some consumer applications leveraging the enhanced Fan-Out capability. But for now, let's keep it simple. We just want to write and read from our stream, and so therefore, we're going to use the SDK for producing and for consuming. So to do so, we want to open a CLI, and let's use CloudShell because it's fun. So I'm going to click on CloudShell right here, which is the icon, next to the bell icon. And this is going to open, for me,. a command line interface in AWS. As an alternative, you could be using your own terminal or CLI if it's preconfigured, but I like to switch things up, and this one, I really like, because there's less configuration involved at least. Creating the environment, the first time, can take a bit of time, and the CloudShell is free on AWS, so no worries here. In the meantime, on Kinesis, open the kinesis-data-streams.sh file, and we're going to use that overall. So there are two types of commands to write to Kinesis Data Stream based on your CLI version for the AWS CLI. So usually, you have version two installed but it is possible for you to somehow, have version one installed. And so you get the version, you just type aws --version, and paste, and then, you will get some information around the version of AWS CLI. So in CloudShell, the CLI version that's going to be installed is version two, as you can see here, it was CLI version 2.1.16, so we're going to use the CLI commands version two. But if you wanted to have the version one, then you would use these comments. Okay. So now we're good to go. The first we want to do is send a record into our Kinesis Data Stream. And to do so, there is an API called put-record. And put-record, we need to specify a stream name. So in this example, I didn't name my stream test, I named it DemoStream, so we'll have to change this in the CLI command, but you get the idea. Then you specify a partition key for the data settings. So user one, and remember the data that shares the same partition key will go to the same shard, but we only have one shard, so that doesn't matter in this case. Then the data itself, so users signup. And finally, because we are writing some text data, we need to say this option cli-binary-format raw-in-base64-out. Okay. So let it paste this command, so copy and paste, but let me just edit the stream name, to make sure that it is DemoStream. And the cloud channel is automatically configured with your own IM credentials, so it will inherit whatever you have for IM credentials, and also, we'll use the region by default, in which it was launched. So us-east-1. I press Enter. And now, we get a successful message. So the message was sent shardId-0000000000000. So our first shard, and the sequence number of the message is here. If I do it again, then I'm going to get a second message with a successful, so we can do user signup. We can mix up the message, then user login, and then, maybe user logout. So we're just, we're just setting a few messages into our Kinesis Data Stream. Perfect. So I'm going to clear this, and if you waited a little bit, and went into monitoring, and look at the stream metrics, I will have it on one hour, you would see a put record right here, but it takes a bit of time for CloudWatch metrics to be updated, but you would see it here. Okay. So next we want to be able to consume from Kinesis Data Stream. So to do so, we're going to first describe the stream, to get some information around what this stream is made of, because we need to be able to consume from a specific shard. So it's DemoStream, I'll press Enter, and as you can see here, we have this StreamDescription. We have one shard called shardId-0000000000000, and so we need to keep this in our mind. Okay. To be able to read from this stream. And so when you use the CLI, the SDK at a very, very low level, you need to specify from which shard you are reading from. But if you are using Kinesis Client Library, all of this is handled for you by the library itself. But we are using the CLI, so we have to specify the shard ID. So I press Q to quit this, and I'm going to consume some data. So I'm going to run this command, right here, and let me clear this. And there's two things to note, so number one, I need to change the name of the stream I'm consuming from. So DemoStream is a stream, and then the shard-iterator-type is TRIM-HORIZON. And this means that you're going to read from the very beginning of the stream, so it will read all the records that were sent for from the beginning. The other option, just make sure to only receive the records from that very moment onwards when from a new launched to CLI command. Anyway, so I'm going to press Enter. And this is going to give me a ShardIterator. And this ShardIterator can be reused to consume records, so the next API comment is kinesis get-records --shard-iterator. And then, we just specify this entire string, right here. So this consumption mode I'm doing right now, by using the low level API, describing the stream, getting a ShardIterator, and getting records is using the shared consumption mode. This is not using enhanced Fan-Out, which in my opinion, should be using the Kinesis Client Library, Consumer Library for you to really leverage, and have a nice API to do so. But this is low level. So let's click, and let's press Enter on the Kinesis get-records, and we get a batch of records out of it. So we used to have record one right here, which is PartitionKey user1. And we have some data right here, but it's base64-encoded. We have, again, another data, base64-encoded. We get some time stamp information, another data, base64-encoded. And then if I press Enter, it's going to go a little bit down. Some more data that is base64-encoded. So to just make sure we can read the data, I can go to websites, base 64 decode online, and I'm going to paste this data right here, into base 64 decode, and click on DECODE, and this gives us user signup. And if I paste the second type of data, this one, copy and paste it in here, is going to give us user login. So we which is that we sent. So that's perfect. Everything is working. And then, as you can see, there's a NextShardIterator argument right here. So the next time we consume, we need to specify this NextShardIterator argument to consume from where we stop consuming from. So this is something you have to iterate from in your code. But at a low level, we've produced data to Kinesis Data Stream, and consume data from Kinesis Data Stream, which is awesome. And we've also used CloudShell in the meantime, which I think is very handy. So that's it for this demo, just keep this stream open as we will be using it for Kinesis Data Firehose in a second. And I will see you in the next lecture.

---

### Lecture: Amazon MSK

Another analytics service you will see is Amazon Managed Streaming for Apache Kafka, also called Amazon MSK. And what is Kafka? Well, Kafka is an alternative to Amazon Kinesis. Kafka and Kinesis both allow you to stream data. So, MSK is the ability to get a fully-managed Kafka cluster on AWS. And it allows you to create, update and delete clusters on the fly. And MSK is going to create and manage Kafka broker nodes and Zookeeper broker nodes in your cluster for you and you deploy the cluster in your VPC, across multiple AZ, up to three for high availability. You also have automatic recovery from common Kafka failures and the data is stored on EBS volumes for as long as you want. So, from personal experience, I know it's very difficult to set up Apache Kafka and the fact you can just do one click and then deploy Kafka on AWS is great and this is the Amazon MSK service. So on top of it, you have the option to use MSK Serverless. And this is that you run Apache Kafka on MSK, but this time you don't provision servers, you don't manage capacity, automatically MSK will provision resources and scale, compute and storage for you. So what is Apache Kafka then? Apache Kafka is a way for you to stream data and a Kafka cluster is made of multiple brokers and then you will have producers that will produce data and so they will have to ingest data from places, such as Kinesis, IoT RDS, et cetera, et cetera, and they will send the data directly into a Kafka topic that is going to be fully replicated into other brokers. Now, this Kafka topic is having real-time streaming of data and consumers will pull from the topic to consume the data itself and then your consumer can do whatever he wants, process it or send it to various destinations, such as EMR, S3, SageMaker, Kinesis and RDS. So the idea is that Kafka is quite similar to Kinesis, but there are differences to look out for. So what are the differences between Kinesis Data Streams and Amazon MSK? Well, in Kinesis Data Streams, you have one megabyte message limit, which is the default in Amazon MSK, but you can configure it for a higher message retention. For example, 10 megabytes. You can have Data Streams with Shards in Kinesis Data Streams or in MSK, it's called Kafka Topics with Partitions, but the concept are sort of similar. To scale Kinesis Data Stream, you need to do Shard Splitting and to scale it down Merging. But in Amazon MSK to scale a topic, you can only add partitions. You cannot remove partitions. You have in-flight encryption for Kinesis data streams and then you have either plain text or TLS in-flight encryption for MSK. You get at-risk encryption for both of these clusters and, in the exam level, this is enough. Just so you know, a few differences. And also for Amazon MSK, you can keep data for as long as you want, you can go over one year, as long as you pay for the underlying EBS storage, you're good to go. So to produce to MSK you need to create a Kafka Producer and then to consume from MSK, you have multiple options. The first one is to use Kinesis Data Analytics for Apache Flink. So you want a Flink Application and you make it read it directly from the MSK cluster. You can use Glue as well to do streaming ETL jobs and they're powered by, the time, Apache Spark Streaming. You can use Lambda functions to directly have Amazon MSK as an event source or you can write your own Kafka consumer and you can make it run on whatever platform you want, for example, your Amazon EC2 instances, or an ECS cluster or an EKS cluster. And once you know this, you know pretty much everything there is to know for Amazon MSK at the exam. So I hope you liked it and I will see you in the next lecture.
---

*Section 10 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*