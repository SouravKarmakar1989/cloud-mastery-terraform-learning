<!-- Section 12 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 12 OF 20: Database Services You Should Know
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: Amazon DocumentDB

Now let's talk about DocumentDB. So the same way we had Aurora as the way for AWS to implement a sort of big cloud native version of PostgreSQL and MySQL, we have DocumentDB, which is an Aurora version for MongoDB. So MongoDB, if you don't know, that's the logo on the top right corner of your screen, it is another no SQL database and you need to remember this for the exam. So DocumentDB is a no SQL database and it's based on top of the MongoDB technology. So it's compatible with MongoDB. So MongoDB is used to store query and index JSON data, and you have the same similar deployment concept as Aurora with DocumentDB. So that means it's a fully managed database, it's highly available. Data is replicated across three availability zones and DocumentDB storage automatically will grow in increments of 10 gigabytes, and DocumentDB has been engineered so it can scale to workloads with millions of requests per second. So at the exam, if you see anything related to MongoDB, think DocumentDB, or if you see anything related to no SQL databases, think DocumentDB and also DynamoDB. That's it for this lecture. I hope you liked it, and I will see you in the next lecture.

---

### Lecture: Amazon ElastiCache

Let's talk about Amazon ElastiCache. So the same way you get RDS to have managed relational databases, ElastiCache is going to help you get managed Redis or Memcached, which are cache technologies. So what are caches? Well, they are in-memory databases with really high performance and low latency. And what the cache will help you with is to help reduce the load off of databases for read-intensive workloads. The idea is that the common queries are going to be cached, and therefore, your database will not be queried every time. Just your cache can be used to retrieve the results of these queries. What this also helps you do is make your application stateless by putting the state of your application into Amazon ElastiCache. And because we have the same benefits for RDS, AWS will take the same maintenance of the operating system, the patching, the optimization, the setup, configuration, monitoring, failure recovery, and backups. Now, if you use Amazon ElastiCache, just so you know that it will require you to do some heavy application code changes for your application. This is not something you just enable, and off you go, you have a cache. You need to change your application to query the cache before or after querying the database and we'll see the strategies in a minute. So now let's talk about the architecture for using ElastiCache. And there can be many, but I'm just giving you an example. So, let's say we have Amazon ElastiCache and RDS database and your application. The application will query ElastiCache to see if the query are already being made. And if this has already been made and it is stored in ElastiCache, then it's called a cache hit, and then it just gets the answer straight from Amazon ElastiCache and we're saving a trip to the database to do the query. Now, in case of a cache miss, then we need to fetch the data from the database. We're going to read it from the database. And then for other application or other instances where the same query will be made, we can write the data back into the cache so that such as the same query next time will result in a cache hit. The idea is that it will help to relieve load from your RDS database. And because you're storing data in the cache, there must be a cache invalidation strategy to make sure that only the most current data is used in there. And this is the whole difficulty around using caching technologies. Another architecture is around storing the user session to make your application stateless. So the idea is that your user will do login into any kind of your applications, and then the application will write the session data into Amazon ElastiCache. Now, if your user is redirected to another instance of your application, then your application can retrieve the session case, the session directly from the Amazon ElastiCache, and therefore your user is still logged in and it doesn't need to log in one more time. And so the idea is that now you made your application stateless by writing the session data of your user into Amazon ElastiCache. So if we compare Redis and Memcached, so for Redis we have multi-availability zone with auto-failover as a feature, and you can create read replicas to scale the reads and have high availability. You also get data durability using AOF persistence, and you have a backup and restore features on the open source version of Redis. It also supports sets and sorted sets, which are a very nice way to create leaderboards and provide you with some very nice features in a cache. So the way I like to think of Redis is a Redis node being replicated into another one to give you an idea of how it works. Of course, Redis is evolving fast and this may not be general, but this is a simplification. And for memcached, the idea is that you have a multiple nodes set up and they're partitioning your data. That means it's called charting so you have multiple nodes together. And there's no high availability, there's no replication. Again, not everything may be true, for example, for the serverless version of of memcached, but I'm just trying to draw a simple image here. It's not personal data. So if you have an issue with memcached, you may lose your entire cache and you have backup and restore features only for the serverless version of memcached, not the self-managed version of memcached on ElastiCache. You also have a multi-thread architecture, which may be good for performance. So the way I like to think of memcached is they're sharding so you have multiple nodes next to each other, which are partitioning, which are sharing the data, okay? So the exam may not ask you many questions or any question about choosing Redis versus Memcached but it's good for you to know as a reference point based on the use case if that appears at the exam. Alright, that's it, I hope you liked it and I will see you in the next lecture.

---

### Lecture: Amazon ElastiCache - Hands On

Okay, so let's practice using Amazon ElastiCache. So we're going to get started. Here we have several engine recommendations. We have Valkey, which is a replacement for Redis, Memcached or Redis OSS. So you can use Valkey. It's the recommended way. We can use Redis, we're gonna use Redis. It's the exact same options. And then for deployment option, we have the serverless option and we have the node-based cluster, but for us to understand exactly how things work, we're going to select the node-based cluster. So we can restore from a backup, or we have easy create to use recommended best practices configuration. So for example, we have production type of configuration or dev test or demo. And it depends on your needs, of course, or if you wanted to, you can configure everything by having the cluster cache mode. So here we're going to want to configure everything just to view all the options. So the cluster mode is going to be disabled, where we have only a single shard with one primary node and up to five read replica. But if you want to have multiple shards across multiple servers, then you would use cluster mode. All right, so we'll use it at disabled. Then this one is going to be called DemoCluster. The location is going to be on AWS Cloud, but you have the option to also run ElastiCache on premises using something called AWS Outpost. Now, do we want multi-AZ or not? This is very helpful for high availability and failover in case of a primary node failover, but for now we disable it because this will just incur more cost if we did. Do we want auto-failover, yes or no? We'll leave it as enabled. Now for cluster setting, you can specify the engine version, the port, the parameter groups, and for node type, we're going to use a micro type of instance. And so we have t2 micro, t3 micro, and t4g micro in this example, and I think t2 and t3 are in the free tier so use one of these. All right, so we have a t2 micro, and then we can have replicas, which is very helpful when you have this kind of scaling for, as we said, the cluster mode. So we are going to just have it as zero right now just for cost purposes, and this is of course, when you have multi-AZ, you should have more replicas. So this makes sense. So let's keep it as zero just for cost purposes. Now I'm going to create a subnet group, so my-first-subnet-group, and this is going to indicate in ElastiCache, which subnets we can run the cache in. So we can choose the VPC, and then automatically subnets are auto selected, but we can specify them as well if you wanted to. Now for AZ placements, we can specify which replicas can go to each AZ, but again, we're not running into multi-AZ mode right now, so it doesn't really matter. Now for next, do we want encryption at rest, yes or no? In case you want encryption at rest, you need to specify a key. And do you want encryption in transit to encrypt the data between the client and the server? If you do enable encryption in transit, then you get access control feature. So to choose who can access your cache. So we have Redis AUTH in which we specify a password, an AUTH token to connect to our Redis cluster, or we can use an user group access control list. And then you can create a user group right here from the ElastiCache console. So I'm going to disable encryption in transit. And then you can also have security groups to manage which applications have access to your cluster from a network perspective. So in terms of backup, do we want backup, yes or no? And then for maintenance, do we want maintenance windows to actually perform minor versions upgrades? Do we want to have logs such as slow logs or engine logs that we can send into CloudWatch logs? And finally, do we want to have tags? So a lot of different options. This is similar to RDS, that's why I'm going a bit quicker over this. But we've seen the different options to create our cache. So then we can review everything and then click on create to actually create our cache. My ElastiCache database is now created and I can click on it. And if I had an application that I wrote with some code, I could actually use the primary endpoints or if it was a read replica, a reader endpoint, to allow to read from my cache, okay? Now, there is no easy way for me to show you how to connect to an Redis cache because it's not that easy and you need to actually write some code and it would be too complicated. But from an AWS perspective, from this console, you can see all the details as well as nodes, metrics, logs, network security, and so on. And it looks just like RDS because yes, it is very similar service to RDS, but it's intended for Redis and Memcached. So the last thing you have to do is take this Redis cluster, action and then delete. And then do you wanna back up? No. You just type the name of the cluster, delete, and you're done with this hands on. Okay, that's it. I hope you liked it and I will see you in the next lecture.

---

### Lecture: Valkey with ElastiCache and MemoryDB

One thing we should talk about is Val Kilmer. And Val Key is basically an alternative to Reedus. Open source. And it itself is open source as well. But it's actually a fork of Reedus. It turns out, and there's a few ways you can use Valky. One is through Elastic Cache and Elastic Cache for Valky also supports vector search. That's why Valky is important. So obviously if you're doing generative AI or retrieval, augmented generation or semantic caching, this is yet another option for you for having a vector store to make that work. It is scalable. It supports billions of high dimensional embedding vectors with microsecond latency and a recall rate of greater than 99%. So if you do have high performance and scalability requirements, Elastic Cache with Valky might be an appropriate choice. You can also use Amazon Memorydb as a vector store, also using Valky, and that can also use Redisai. Vector search under the hood as well. You have a choice there with Memorydb. Memorydb, just like it says on the tin is all in memory. That means that it's incredibly fast. It's latency is really, really low, and its throughput is really, really high. So it's all distributed in memory. Can't get much faster than that. It's also distributed across multiple availability zones. Should you need that. And one little detail you might want to know, you can choose between two different vector index algorithms with Memorydb and Valky. One is flat, which is just brute force search. So that will always give you the closest vectors no matter what. But obviously it's very slow because it has to search through every single vector to try to find the closest ones. Or just like OpenSearch does by default, there's an algorithm called hash, and that will just give you an approximation that is usually pretty good, but a whole lot faster. So that's usually what you want to do. HMS is optimized for performance okay. And if you go back to the OpenSearch slides, there's a little bit more depth on how HMS works and how that compares to other algorithms. But when it comes to Memorydb, that's pretty much the only show in town. You have HMS, or you can go flat, which you probably don't want to do if you care at all about performance, right?

---

### Lecture: Amazon Neptune

Now, let's talk about Amazon Neptune. Neptune is a fully-managed graph database. So an example of what a graph dataset would be, would be, for example, something we all know which is a social network. So, if we look at a social network, people are friends, they like, they connect, they read, they comment and so on. So users have friends, posts will have comments, comments have likes from users, users shares and like posts and so, all these things are interconnected and so, they create a graph. And so, this is why Neptune is a great choice of database when it comes to graph datasets. So, Neptune has replication across 3 AZ, up to 15 read replicas. It's used to build and run applications that are gonna be with highly connected datasets, so like a social network, and because Neptune is optimized to run queries that are complex and hard on top of these graph datasets. You can store up to billions of relations on the database and query the graph with milliseconds latency. It's highly available with application across multiple Availability Zones. And it's also great for storing knowledge graphs. For example, the Wikipedia database is a knowledge graph because all the Wikipedia articles are interconnected with each other, fraud detection, recommendations engine and social networking. So, coming from an exam perspective, anytime you see anything related to graph databases, think no more than Neptune. That's it. I hope you liked it and I will see you in the next lecture.

---

### Lecture: Amazon Neptune Analytics and Vector Search

So why is Neptune relevant to generative AI? Well, it turns out there's a system called Neptune Analytics as well as the Neptune database. And this is built on top of it to provide an analytics engine that you can use to interact with your data in Neptune. And one feature of that is basically treating Neptune as a vector database. So if the information you want to use as context in your AI application lends itself to a graph data structure, maybe Neptune is a good place to put it, and you're still going to have the problem of finding semantically similar stuff within that graph database in Neptune. So that's why Neptune Analytics offers this. Vectors top k by embedding call you can do. And that is basically a vector query. So you pass it in an explicit embedding vector with vector values that you want to find the top vectors that are close to it. Right. So just like any other vector store, it will allow you to filter those results by vertex attributes as well. So kind of a way to do a bit of a hybrid search potentially if you have tags on those vertices that you might want to use for filtering purposes during that search, the top results that come back k is whatever value you want. We'll return the top nodes as well as your scores. So if you do want to use a graph database as your vector store and as your context for a knowledge base or retrieval, augmented generation Neptune Analytics could fit the bill paired with its vectors top k by embedding call.
---

*Section 12 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*