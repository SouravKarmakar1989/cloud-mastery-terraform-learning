<!-- Section 6 of 20 | Course: ultimate-aws-certified-generative-ai-developer-professional -->
<!-- Paste this after the MASTER_PROMPT and any previously uploaded sections. -->

## SECTION 6 OF 20: Managing Models with SageMaker AI
**Course:** `ultimate-aws-certified-generative-ai-developer-professional`

---
### Lecture: Section Intro

Welcome to the section on Managing Models with SageMaker AI. Your complete guide to training, deploying, monitoring and governing foundation models at scale using AWS most powerful machine learning platform. In this section, we'll focus on how to build production grade AI systems. With all these safeguards and observability and automation that real enterprise deployments require. We'll start with the full data lifecycle of data processing, training and deployment using SageMaker. Then we'll explore built in safeguards that help you deploy models safely and reliably across environments. From there, we'll dive deep into optimizing FM deployments, covering performance tuning, scalable architectures, and special considerations for large language models. You'll learn about SageMaker ground truth for high quality labeling and how to use, model, monitor, and clarify to detect drift bias and anomalies in your model outputs. We'll walk through the SageMaker model, registry versioning strategies, and lineage tracking so you can maintain full traceability and governance over your AI workflows. We'll also explore deploying and optimizing models at the edge using SageMaker, Neo and Greengrass. And we'll tour SageMaker Unified Studio, your centralized workspace for building and managing generative AI applications. Finally, we'll look at SageMaker pipelines for orchestrating automated ML workflows, and you'll get hands on experience with SageMaker Jumpstart to quickly deploy an experiment with a pre-built foundation model. By the end of this section, you'll know how to manage the full lifecycle of a foundation model using SageMaker. From ingestion and training to deployment, monitoring, and continuous improvement. Let's get started.

---

### Lecture: Data Processing, Training, and Deployment with SageMaker

Now, bedrock is not the only way to manage and deploy your models. Also, before bedrock, there was SageMaker, which has kind of been rebranded as SageMaker AI lately, and it is also a way to handle a broader machine learning workflow. So it's not just about generative AI. It's not just about AI, it's about machine learning in general. Basically, you know, models that do stuff and predict things, right? So it's kind of built around this cycle here. The idea is, you know, it can help you with the entire thing. So it has features that let you fetch, clean and prepare your data for training a model. It can then let you do the actual training of the model itself and spin up any resources it needs for that training, and then evaluate the performance of that model after you've trained it. Once you're happy with it, it can also help you with deploying that model. And then after it's deployed, it can evaluate its results, monitor it for drift, you know, make sure that it's doing the right thing. And maybe the deployment of that model results in more data that you can then turn around and feed back into the training again. So everything in this entire machine learning life cycle, uh, has accompanying tools in SageMaker to help you do it. Now, as a reminder, this particular exam does not get into the training part of it that is out of scope, so we're not going to focus on that. However, just to see how it all fits together, a SageMaker kind of has this model of how it's all stacked together. So you do have to start with a model training. And maybe in the case of generative AI, this is a pre-trained model you got off of hugging face. Maybe you fine tuned it. Whatever. Right? Wherever you came from, you had to train it somehow. But if you are training it or fine tuning it from scratch, then this is one way to do it. In SageMaker you can put your training data in S3. Uh, SageMaker can help you do that training and orchestrate it from a notebook or whatever you want. And then you can have basically a code image that gets deployed out through ECR. And then you can spin up these training instances to actually perform that training using your training code and the training data. Once you have a model that's trained and ready to go, the artifacts from that model, which might be a set of weights or who knows, could be anything that gets pushed out to S3 as well. Same idea here. Now, instead of a training code image, we have an inference code image. So that's the code for actually querying your model and making predictions. Or in the case of generative AI, uh, saying something back to you, as the case may be. And SageMaker gives you tools for deploying that and hosting it, and it would be hosted on an endpoint which SageMaker also manages for you. So that endpoint for inference is ultimately what your client application would be talking to. Now, SageMaker notebooks can direct this whole process. So if you want to do it all from Python code, you can do that with a familiar Jupyter Lab environment. And that works fine. You can spin up a notebook instance on EC2 from the SageMaker console, and that can give you access to data in S3. All the usual suspects for processing data that you might be using in Python, as well as a wide variety of built in models, so you can just start from as well. You can also spin up your training instances and deploy these models and, uh, put them out there in the real world, all from your code in your notebook if you want to do it that way. Or you can do it from the console as well. So SageMaker AI has consoles for pretty much everything you want to do. So whether you want to do this stuff through code or visually, it's totally up to you. But SageMaker AI does it all. So it gives you much lower level access if you want it, and much more flexibility than you might see in bedrock.

---

### Lecture: SageMaker Deployment Safeguards

A little more detail on the deployment side here. So there's various safeguards that SageMaker provides for you to allow you to deploy new models or newly trained models in a controlled manner. One is called deployment guardrails. And this is different from guardrails in the context of generative AI. In that context, the guardrails usually some sort of a content filter in the context of a deployment guardrail, it's just safety for the deployment itself and the ability to deploy things in a controlled manner and roll them back if things go wrong. So this is specific for asynchronous or real time inference endpoints. This isn't really a concern for batch processing, but if you have a inference endpoint that some application is hitting in real time to make predictions using SageMaker model, these deployment guardrails can come in handy. One technique is called blue green deployments, and I'm not sure why they chose those colors arbitrarily, but the idea is that you have two different versions of your model that you're trying to deploy. We'll call one blue, we'll call one green. And this is a method for controlling the traffic to those new models over time. As you're deploying the new one. Now, you can do this all at once. So let's say that the old model is the blue one. The new one is called green. So you could shift everything all over to the green model all at once. Monitor it, and once you're convinced that everything looks okay, then choose to terminate the Blue Fleet. If you're sure that you don't need to roll back to it right away. If you want to be a little bit more cautious, you can do what's called a canary deployment. This allows you to shift just a small portion of the traffic to the new green deployment and monitor it over time. So you have both models running together in tandem with just increasingly large amounts of traffic being shifted to the new deployment until you're happy with it. So in this way, if something is horribly, horribly wrong with that new model, it's only going to impact a portion of your customers instead of all of them, while you figure out how to roll it back and address the issue, right. There's also linear deployment, which allows you to sort of automate that shifting in traffic. So in linearly spaced steps you can say, I want to start off with 10% on the new model, then 20%, then 30%, until finally I've shifted all the way over to the new green model. So that's bluegreen deployments. So you can do it all at once. You can do a canary deployment or a linear deployment. Those are all your options there. And it also has auto rollbacks. So if you do detect some sort of a horrible problem that might be able to be detected automatically and automatically roll back to your blue deployment, uh, without any human intervention to make that happen even quicker. Another technique is called shadow tests. So in this in this approach, you have both models running together. And every inference query that comes to your inference endpoint is actually run twice, one by the old model and once by the new model together in parallel. And that way you can compare the performance of both the shadow variant that's being called in the background, but not actually issuing results to production to what the production instance the older version is actually doing. And if they diverge in ways you didn't expect, you might decide, well, I'm not going to roll out this new version of the model, the shadow model, at all, so you can monitor a shadow test in SageMaker console, and then you can decide when to promote it based on its performance compared to what's actually vending results in production right now. backing up a little bit here under the SageMaker studio umbrella. So like I said, SageMaker studio sort of encompasses all these features we've talked about. From SageMaker studio. You can access SageMaker autopilot or SageMaker model monitor under SageMaker autopilot. That might be automating things that include SageMaker notebooks, SageMaker experiments, SageMaker debugger, and automatic model tuning. However, you can use all those features independently as well. They don't have to be used with autopilot, but autopilot can manage all that for you as well. Some additional SageMaker features that you might want to know about SageMaker Jumpstart. We'll look at an example of this not too far from now. This gives you a one click ability to launch a new model or a new algorithm from what we call a model zoo or a repository of models. Something like hugging face, right? And right now, well, it's probably a lot more than 150 of them out there. But there are models in natural language processing, object detection, image classification, and also through hugging face all the large language models and small language models for generative AI or image generation as well. That's out there too. So with jump start, you just select a model that you want, click a button, and it just fires off a notebook that you can start from sort of some sample code to get you started there automatically. Hence the word jumpstart. SageMaker Data Wrangler. We're going to go into this in more detail as well. Its purpose is to import, transform, analyze, and export data, all within SageMaker studio. So the data processing, the data analysis of your training data. You don't have to leave SageMaker studio to do that. SageMaker Data Wrangler lets you do all of that. You could also do this through something like a AWS glue or Glue studio, but with SageMaker, you also have the option of doing it within SageMaker studio using SageMaker. Data Wrangler SageMaker feature stores. Another feature and oh, I didn't mean to say it that way, but we are going to talk about that more as well. Uh, that allows you to find, discover and share features within SageMaker studio as well. Again, a feature is basically just a column of your training data, and it has both online low latency and offline for training or batch inference modes available for you as well. So there's an online feature store and an offline feature store. We saw that way back when we talked about the diagram of the machine learning lifecycle. And those features can be organized into something we call feature groups. So if you want to have a little bit of a taxonomy there of what your data looks like, SageMaker feature Store can track that for you as well. The idea here is that by having a consistent picture of what your feature data is at various stages in the pipeline, you can prevent errors that might result from treating your data differently at different points in the pipeline. There's also SageMaker Edge Manager. This is a software agent for edge devices, so if you are deploying your models out to the edge using SageMaker neo, that can be used to collect and sample data for monitoring, label and retraining by keeping track of what's happening on the edge and then reporting back to you regarding that performance. And finally, there's asynchronous inference endpoints. So you don't necessarily have to wait for a response from an inference query. You could say, hey, make this prediction and then asynchronously get the answer back later on. That's also an option with SageMaker when you're using that for deployment.

---

### Lecture: Optimizing Foundation Model Deployments

So we've seen how SageMaker deployments work in general. And that is important information, those blue green deployments and all that stuff. But let's get into some specific advice for deploying foundation model deployments on SageMaker. So one thing to remember is that SageMaker AI offers both single and multi-model endpoints. So your endpoints can actually serve more than one model at a time. And that's one way of maximizing your resource utilization and making sure that you're not wasting any capacity or deploying more capacity than you actually need. So if you can repurpose the same fleet for multiple models, maybe that's a way to save money, right? More generally, you can have multi-container endpoints. So those containers don't necessarily have to be foundation models. They could be anything. But the point is an endpoint doesn't have to just have one model in it. Endpoints can do more than one thing, and that's fine. And each individual endpoint will support their own deployment guardrails, their own VPC, and their own network isolation. Also, keep in mind you can train and tune a model in SageMaker AI. So if you want to do something a little bit fancier, like, uh, starting with a model from hugging face or something and fine tuning it. You can actually deploy that through bedrock as well, if you don't want to deal with all the SageMaker complexity. So it is possible to use bedrock custom model import for that. So if you can create a model and tune it in SageMaker, you can say bedrock. Go grab this model from SageMaker and host that for me through a custom model import. And now your inference is totally serverless. You don't have to think about it. So isn't that beautiful? So that's a good way that bedrock interoperates with SageMaker AI that can save you some trouble. And serverless can also be a better usage of your resources too. Also, if you hear the term SageMaker AI inference components, that's a mouthful. Uh, what that means is each model can get its own scaling policy, so you can actually scale each model independently for different workloads, different use cases through SageMaker AI inference components. On the inference side with SageMaker. Also there are cross-region inference profiles. So if your endpoints are on bedrock, we're going to talk about cross region a little bit more in a bit. But the basic idea here is that with cross region inference, if one specific region goes down or gets overloaded, bedrock can automatically redistribute that traffic to other regions. And you can you can restrict that traffic to a specific geography or global. And global is actually going to give you the highest throughput. But there are built in features for doing that. There are some finer points. Get into that later. Also, keep in mind EC2 auto scaling groups can be used in front of SageMaker AI endpoints that can give you load balancing and also auto scaling, just like it says on the tin, as a way of better managing your resources for your endpoint deployments. And there are many ways to go about deploying a model on SageMaker AI, so you can choose from one of many different model servers to actually host your models with. Uh, among them are Torchserve. If it's based on PyTorch DJ serving. I didn't actually never heard of this until I actually started studying for this exam myself. But Dgl stands for Deep Java Library and apparently this is an Amazon thing, which is why you're more likely to see it on the exam. So it's basically a Java based deep learning library because, you know, most things are Python these days. For whatever reason, Java is more efficient. So, you know, not a bad motivation. Anyway, with the deep Java library, you create deep learning containers that you can then deploy using JL serving on SageMaker AI. And again, it's an Amazon thing, but you just need to know that it's a model server that can be used with SageMaker. That serves up Java code that does deep learning. Also, it supports the Triton inference server as well. And a few more quick points here. So uh, another tip is to use asynchronous inference if latency is not important. So SageMaker AI has async endpoints you can choose from if you want to. So if you don't need an immediate response, that's a way to actually kick off a request to your inference and then get back the result at some later point. You know, it'll just call you back and say, hey, I got an answer for you. Finally. So if you don't need, uh, if latency is not critical, that's the way to save a little bit of money. You could also implement yourself, um, using SNS or SQS. Just kind of building up your own queue of, uh, hits to your entrance point there and kind of like, let them back up and control the rate that they come at. Also, there are model compression techniques you can use to further save your, uh, your inference capacity with. So smaller models take less space to deploy and that can save you money. So some ways of doing that is, uh, quantization. So one trick I think deep Seek did this was taking an existing model and just quantizing the bit depth of its weights down. And sometimes that works surprisingly well. So, you know, maybe you don't need 32 bit precision for your weights. Maybe 16 is enough, maybe eight is enough for all that matter. And that's a really great way to save some space without necessarily losing a lot of performance pruning, you know, just cutting off layers in the model that you don't need. I mean, a lot of it's unused, quite honestly. Another trick is called knowledge distillation, and the idea here is that we train a smaller model from a larger model. So the idea there is that we distill the useful knowledge from that larger model and force it to go into a smaller model somehow. And, you know, it turns out that there's a lot of wasted space in some of these larger models that you don't really need. And that's a way to kind of, well, distill it down to what you do need. So that's a way to make a large model smaller, which means it takes less space to deploy. And we'll also be a little bit quicker. And general advice do not optimize prematurely. So it's important to always be measuring your actual performance, your actual costs, and your actual resource utilization before you try optimizing things prematurely. Don't solve problems that don't exist, so make sure you have monitoring frameworks set up, and you're checking CloudWatch and keeping track of what's really going on and what your real problems are, and working backwards from those problems instead of making up ones that don't actually exist. All right. That's our general advice for optimizing foundation model deployments on SageMaker AI.

---

### Lecture: SageMaker Ground Truth

Let's finally talk about SageMaker Ground Truth service. This is a relatively new product from Amazon. And what is it? Well, it's basically a way of using humans to label your data. And I kind of struggled with where to put this in the course. In the end, I ended up putting it in feature engineering because fundamentally it comes down to dealing with missing data. And in this case, it's really more about missing labels and missing features more often than not. But you could, you know, actually apply this to missing features as well. Basically the idea is that if you have missing data that can be inferred easily by a human ground, truth lets you farm these tasks out to actual human beings to fill in that missing data for you. And the most common example of this is in the world of image classification. So for example, if I'm training a new image classification model, in order to train that, someone's going to have to go through and actually tag all these training images with what's actually in them, right? It's not going to be an easy thing for a computer to do necessarily. So for example in this picture here we have someone classifying this picture as being of a basketball or a soccer game. Uh, someone's got to go through and do all that work. Well, kind of a bird is this where are the birds in this image? Things like that. So Ground truth will manage a fleet of human beings who will go through and label your data for training purposes. So if you have a huge data set, like a bunch of pictures and you need labels on them, sometimes human beings are the best way to actually get those labels. Ground truth is what manages that process for you. But it's more than just managing the humans that label your data. The thing that sets ground truth apart is that it creates its own model as those labels are coming in. So this model is learning as it receives more and more label data from the humans, and over time, it's only going to send out labels to the humans that it's not sure about. So as time goes on, as it gets more and more labels from the human labelers, it will create a model of its own, and only the ambiguous cases will be sent out to human labelers going forward. So this model builds up over time, gets better and better over time, and only the things that the model is not sure about actually get farmed out to human beings. This ends up reducing the cost of your labeling jobs by up to 70%, which is pretty substantial. Who are these people? Well, you have different choices of who ground truth can use. One is just using the Amazon Mechanical Turk workforce. This is a huge workforce of people around the world who will label your data and do a bunch of other simple tasks for you for a very small amount of money. You can also choose to farm it out to your own internal team. So if you're dealing with very sensitive data, that might make sense for you. And there are professional labeling companies out there as well who do nothing but, uh, have a fleet of human beings who label training data for a living. So if you want someone who's a little bit more specialized in this sort of thing, you can spend even more money and use one of them. Now, there are other techniques. And just using humans to actually generate training labels that you don't have. One other would be to use AWS Rekognition service, which we'll talk about more later on in the course. Basically, it's an AWS service for image recognition. So if you do need to classify images, that could be either for creating labels or maybe even feature data, you know, maybe you just have a feature in your data set for what is in this image. Recognition has a pre-trained model for most common types of objects that you can just use. So if you are just using, you know, Classifications of general objects in the world. Recognition might be able to do that for you if you're dealing with textual information as opposed to images, comprehend might be useful. Basically, that is a AWS service. You're doing text analysis and topic modeling, so you could automatically generate topics or sentiments for a given document that way. So that might be a way of both creating labels and maybe even additional training features. Right. So this is another example of feature engineering where you might generate additional features in your model by using something like comprehend. So if I have a document, maybe I can use comprehend to generate new features that include the topic of that document or the sentiment that that document represents, and that might be useful information for training a machine learning model. So that's why this is in this section about feature engineering. You know, you can actually apply these techniques of using existing services or even human beings to generate more information that your model can use. Really, any pre-trained model or unsupervised learning technique can be helpful in generating new training labels or even new training features in your data set. So ground truth is pretty simple, but if it's still too hard for you, there's something called ground Truth Plus where somebody just does it all for you, and it's a turnkey solution where you just basically hire someone through AWS to set it all up and manage the whole project for you. So in the documentation, they call it our team of AWS experts. I'm not really sure where they come from, but these people will manage the workflow and set it all up for you and manage your team of Labelers for you. All you do is fill out this form that says who you are, what you're trying to do at a high level, and they'll contact you and try to work out the details and discuss the pricing, which they do not publicly disclose. But I imagine it's not cheap because of this sort of a white glove treatment that they give you as they set things up and they start actually getting results back from their army of labelers, you can track their progress through the Ground Truth Plus project portal, where they give you some handy dandy charts, uh, displaying how the progress is going and what the results look like when they're finally done, you'll get your label data back from S3, but not a whole lot to talk about here. Uh, basically, it's a way of handing off the whole problem of labeling your data to somebody else. Uh, ground truth. Plus, if you're willing to pay for it.

---

### Lecture: SageMaker Model Monitor and Clarify

Let's spend a little bit of time talking about SageMaker model monitor. Unfortunately, if you deploy a machine learning system into production, you can't really guarantee that it's going to perform the same way forever. The nature of the data coming into that might be changing over time in ways that you did not anticipate, and it might affect the performance of your model in ways that you don't want. So SageMaker Model Monitor allows you to receive alerts through CloudWatch on any quality deviations on your deployed models. It can visualize data drift, so it can allow you to keep track of how the nature of your input data is changing over time. For example, if you're predicting whether somebody is creditworthy for a loan, maybe that starts to change over time as the distribution of the attributes of the people that you're making predictions for changes, or maybe you have some sort of a problem where you're starting to miss some input features, maybe there's a data quality problem there, or missing data that didn't originally occur that would result in data drift that you might want to know about. It can automatically detect anomalies and outliers in the data as well that you might need to, uh, address. It can detect new features in your data that weren't there before. So maybe all of a sudden you're seeing new information in your data that's coming into your model. Um, you might want to know about that. Maybe that's something you want to take advantage of, right? And it doesn't require any code. You know, you can do this all visually within SageMaker studio. SageMaker model monitor also integrates with SageMaker clarify and SageMaker clarifies job is to detect potential bias or one of its jobs anyway. For example, if you have imbalances across different groups or ages or income brackets, it can find that out. And using SageMaker Model monitor, you can monitor for that bias and be alerted to that as well. Through CloudWatch. SageMaker clarify comes into the picture by helping to explain the model's behavior. So by using clarify, it can tell you not only that you have potential bias, but also tell you which features in your data is contributing the most to those predictions and where that bias might be coming from. There are several bias metrics that clarify can use, and I don't think it really is worth getting into the details. Just know that there's a bunch of ways that it can measure bias. And, um, they've all been thought through quite a bit. Kullback-Leibler divergence is a very popular one that I see a lot, but all of these are supported as metrics of bias in your training data. So there's a lot you can pick from here a little bit more about SageMaker model monitor. The data for Model monitor is stored in S3 and secured. So where it's storing all of that monitoring information is safe. Those monitoring jobs are scheduled with what we call a monitoring schedule. So this can happen on a schedule and as often as not often as you want. Those metrics, like I said, are emitted to CloudWatch. And then from CloudWatch you can use notifications to trigger alarms. So model monitor just dumps the data into logs in CloudWatch. It's up to you to actually trigger an alarm from that. Once you get that alarm. The idea is you would take some sort of corrective action, which might be retraining the model or auditing the data to see what went wrong. SageMaker Model Monitor can also integrate with TensorBoard, QuickSight, and Tableau for visualizing that data outside of SageMaker studio. Or you can just visualize it within SageMaker studio using its built in visualizations. But if you have third party dashboards outside that you want to integrate with, they can do that too. So there are specific monitoring types within Model Monitor that can monitor for you. One is the drift in data quality. So relative to some baseline that you define some statistical property of those features you can call quality and just measure if that statistical property is drifting over time or not. So quality is subjective. It's up to you to define what quality means, but whatever you define it as, it can detect drift and it automatically and drift in model quality, not just data quality. So what's the accuracy of your model? Conceptually, that works the same way as the data quality with a baseline that you define for the model quality, but it's measuring the accuracy of the actual model and how well it is at predicting things correctly. How do you know if it's correct or not? Well, you can integrate that with something called ground truth. So you can actually integrate model monitor with an army of humans who are out there helping you to label this data in what is considered to be correct. So you can compare the output of your model to human generated labels and model monitor can monitor the difference between the two. How accurate is your model compared to what these humans said? It can also detect bias drift. We talked about bias detection using Clarifai as well and also feature attribution drift. So this is also a feature of Clarifai where it's actually measuring the impact of each feature on your outputs. And if that is starting to change over time, model monitor can figure that out.

---

### Lecture: SageMaker Model Registry

Let's go into a little bit more depth on SageMaker model registry here. It's something you definitely need to know about. It's just a catalog for your models and a way to manage information about those models, like the version, whether they're approved for use or not. Kind of like just a fancy database, right? So it allows you to associate metadata with your models, whatever you want. Again, it's kind of just like a database lookup. At the end of the day, it gives you a means of managing approval for a model. So if you want to make sure that you're kicking off some model for manual approval before it's deploy to production, well, it can keep track of that for you. And it can also be a component of actually deploying your models to production. So maybe your pipeline for machine learning depends on model registry to check. Hey, is this thing actually approved and ready to go or not? It can also be a part of your larger continuous integration and continuous deployment system, and it can allow you to also share models among different applications. That's really the the heart of it here. So by centralizing your models into one place, into this model registry, you can kind of build up this library of models within your organization and share those models across your organization for different applications. It also has an integration with SageMaker model cards. I don't think we've touched on that in this course, but model cards are just a little, um, this little website, basically a little web page that says, here's the properties of my model. Here are its limitations. Here's what it's trained on. So just sort of higher level governance information about a model. So you can point in the model registry where the model card is for this model. Again it's just associating metadata with your models and sort of a very simple database that can be part of your machine learning pipeline. To make it a little bit more real, let's take a look at this example from the AWS blog, where they have an example of a larger machine learning pipeline that involves model monitor. So step one is to create your model. So you have this little training pipeline there where we're pre-processing and training our model. And then we create our model. At that point we register that model at the number one there. That goes into the model registry at the top. At that point, we can capture the event that a new model was registered through EventBridge. And maybe that kicks off a Lambda function that says, hey, someone needs to approve this new model that goes off to the request to approve the model there at number four. That goes down to a human approver, which then will either approve or reject it, which then it can go back through the API gateway and through Lambda again, and go back to Model Registry to update that field in the model registry metadata for that model saying, yes, this is approved, it's ready to go. At the same time, it might update a parameter store that kicks things off to actually kick off the inference pipelines to deploy this model in production. So that can actually say, hey, let's go get those inference details and deploy it through SageMaker inference pipelines and notify somebody that it's ready to go for both the dev test and production stages as well. So just an example of how model registry might be a component to manage things like model approval and to keep track of basically what's where with your models. So that's all model registry is. Uh, again, think of it as a fancy database table where you have information about your models that might include things like the model car for that model, whether it's been approved, and more importantly, it's a central registry for all the models in your organization that might be shared by others.

---

### Lecture: SageMaker Lineage Tracking

Let's also spend a little bit of time talking about SageMaker ML lineage tracking feature. This is important in the field of governance for your machine learning models. It creates and stores snapshots of your machine learning workflows. So it kind of visualizes your machine learning operations pipeline and allows you to keep track of that over time and how it's changing. So it keeps a running history of your models and how they're all put together. This tracking can be useful for auditing and compliance reasons, and it can automatically or manually create tracking entities that you want to keep track of. So you can tell it exactly what do I want to track? What do I want to audit and what do I want to be able to keep track of over time through lineage tracking? This integrates with AWS Resource Access Manager for Cross-account lineage tracking. And let's take a look at what this looks like just to make it more real. So here's an example of a lineage tracking output here. The lineage metadata for a typical machine learning job. So you can see on the left we have our data sets coming in. We have data set artifacts that are coming in, flow input artifacts that might be streamed in. There is any processing job trial component. And that produces data set artifacts of that process data That process data is then fed into the training job trial component. That training job itself produces artifacts, model artifacts after it's been trained and tuned, and then that model gets packaged up and deployed through an endpoint that's also captured here as well. So that's what we mean by lineage tracking. It's basically allowing you to track the lineage of your data as it flows through the processing, the training, uh, building a model and deploying that model from end to end and keeping track of how that lineage is changing over time. The actual things you can track. Um, I wouldn't bother memorizing this for the exam, but just so you understand conceptually what it how it works, uh, one entity is called a trial component that would include things like processing jobs or training jobs or transformation jobs. And then a trial is composed of many trial components. And then we have a group of trials for a given use case that would compose an experiment. You can also group entities together into what we call a context. Another piece of vocabulary for lineage tracking is the action. That would be a workflow step. Or deploying a model. Some sort of action like that can be tracked as well. Artifacts would be things like objects or data that are stored like an S3 bucket or an ECR image that's generated as part of the lineage. And things can be connected. So these entities can be connected together through various associations. And there are various association types that can be recorded between those entities as well. So these are the sort of things that lineage tracking can track for you for actually querying that. You can see it visually like we saw, but there's also an API from Python that you can query as well, part of the Amazon SageMaker SDK for Python that lets you query that lineage tracking information, and that lets you do things like find all the models or endpoints that use a given artifact. So if you have an artifact and you want to know who's using it before you go messing with it, or you want to know who you need to tell about changes to it, that might be a useful way of using lineage tracking just to keep track of who's using your stuff, right. You can also use it to produce visualizations. If you have an external visualizer helper class to actually visualize that lineage from a Python notebook as well. And that's what we saw earlier. So again, lineage tracking, just a way to keep track of how your various models and artifacts and data are being used and connected together, and how those connections are changing over time.

---

### Lecture: Cross-Account Lineage Tracking

Just a very quick addition to our lineage tracking coverage there. So something that's called out explicitly in the exam guide is Cross-account lineage tracking in SageMaker. So if you do need to trace your data lineage across organizational boundaries, turns out that's possible as well. The trick is simple just use the SageMaker AD Association API for that, in addition to the required IAM roles and permissions to make that cross-account access possible. But just remember, uh cross-account lineage tracking is possible with the AD Association API plus the appropriate permissions.

---

### Lecture: SageMaker on the Edge (Neo)

Next, let's talk about living on the edge with SageMaker on the edge and how that works. By the edge, we mean actually deploying your SageMaker models, your trained models out to edge devices. So maybe you have an embedded computer within your self-driving car that you want to deploy things to. Maybe you have a smart camera, like an AWS deep Lens that you want to deploy your trained model to things like that. Neo is a way to compile your inference code to edge devices and make them optimized for different devices that might be embedded somewhere, so you can train once and run anywhere. As the promise of SageMaker neo, it supports multiple architectures for edge devices, including ARM, Intel, and Nvidia processors. They're all in the space of building little specialized, deep learning enabled edge devices. And again, you can embed that in whatever device you can imagine, like your car. So you don't necessarily have to be running this inference in the cloud and going over the internet. You could actually deploy this stuff to run locally within the machine that it is. So in applications where you care a lot about latency, like for example, a self-driving car, you don't want to be waiting, you know, several hundred milliseconds to get a response back from the internet somewhere to decide whether or not to slam on the brakes. Right? That needs to happen immediately. So you want that logic of that trained model sitting within the car itself. And neo is a way to compile that code into a format that can actually be run within that embedded device. That's what neo is all about. Um, how do you remember this? Well, again, I'm not sure if this is a cultural reference that holds up around the world, but neo was a character in the movie The Matrix, and The matrix is built around this premise that the world is a simulation. It's all just a computer simulation, and code is surrounding you everywhere. So everything is code. Um, so the metaphor there is that SageMaker neo is deploying the code of your trained machine learning model to the edge devices that are surrounding you in day to day life. Okay. Anyway, it can take any code that you've written in TensorFlow, MXNet, PyTorch, Onnx, or XGBoost and optimize that to the specific devices. And that opens up a whole lot of possibilities, right? It's actually pretty amazing that you can just take arbitrary TensorFlow or MXNet code and recompile that toward these different architectures, and it just works. And XGBoost, if you don't want to be dealing with neural networks necessarily, that's, you know, probably the hottest algorithm out there right now for doing machine learning outside of the world of deep learning. So a very good variety of technologies available for you there. Neo consists of both a compiler and a runtime library. So the compiler is what recompiles that code into the bytecode expected by those edge processors. And obviously there's a runtime component as well that runs on those edge edge devices to consume that neo generated code. Neo pairs well with AWS IoT Greengrass. So remember, the exam is really all about putting these components together in interesting ways, right? So Neo and Greengrass like peas in a pod, basically. So you can take a neo compiled model and you could just deploy that to an Https endpoint. So I could host a neo compiled model on a C5, M5, M4, P3, or P2 instance, but it has to be the same instance type that I use to compile that. I mean, obviously, if I'm using neo to compile my model to a specific device, I need to make sure that I'm hosting on that same kind of device. Right. But that kind of defeats the purpose of neo, right? If I just want to host it on an endpoint on some sort of a web server or some service somewhere. Why not just, you know, do that in whatever language I started with? So it gets interesting when you pair neo with IoT Greengrass. Greengrass is the mechanism whereby you actually deploy code to an edge device. So that's how I'm actually going to get my trained model to an actual edge device using inference at the edge, using local data, using a model that was trained in the cloud. So I trained my model in the cloud on SageMaker using training instances, but then I compile it with neo and deploy it to my actual edge devices using IoT. Greengrass. Greengrass uses Lambda functions extensively for inference applications. So again, you might see neo, IoT, Greengrass, and Lambda used together in a whole entire system. So that's how it all fits together. Basically. Again, neo compiles your model, your trained model into specific architectures that might be deployed to the edge. IoT Greengrass is what gets it there.

---

### Lecture: SageMaker Unified Studio

Want to introduce the concept of SageMaker Unified Studio. In case you see that term, you'll know what it is. So SageMaker has a very long history, and it's grown extensively over time and has a lot of different features. So for a while it was a little bit fragmented. You know, there was SageMaker studio over here where you could run notebooks. Everything else was in another UI, and SageMaker AI was this different thing. So SageMaker Unified Studio is what it says. It's a unified studio for all the stuff that SageMaker does all across the entire machine learning or AI lifecycle. So whether you want to do data processing and analytics, whether you want to do AI stuff or ML stuff or deploying or whatever it is, whether it's bedrock stuff or Q stuff or QuickSight integration, even you can use that for, you know, querying your data and analyzing your data all in one place. And it also has all the old SageMaker studio stuff built in as well, like just running Python notebooks and all the stuff that was in the SageMaker studio, if you remember that stuff from earlier days. So it is basically a single interface for a building, deploying, executing, and monitoring your machine learning stuff. It's explicitly built for teams. So administrators will manage the users and groups and what they can access within the SageMaker Unified Studio. And that will consist of domains that will connect assets, users and projects together. And also it has a connectivity with Visual Studio Code. So if you want to just develop with Visual Studio Code and write Python code for AI and ML locally, you can connect to that securely over the web using Visual Studio Code integration with Unified Studio. Here's what it looks like to make it real. So you can see here on the left hand panel, there's a whole bunch of stuff that SageMaker Unified Studio can do. And what I've done here is spun up a notebook to do some customer usage analysis. It's one of the sample notebooks they provide, but you can see you have this built in Jupyter notebook environment here where you can do SageMaker stuff or data analytics stuff, or machine learning stuff or AI stuff, whatever it is. And you can use that to actually, you know, develop your SageMaker resources and train models, deploy models, measure those models, whatever it is. And separately, there are a bunch of stuff you can do from that left hand menu pane there too. In addition to notebooks, you can also set up workflows. You can do query editors and visual ETL for actually processing your data and analyzing your data, and set up data processing jobs. So, you know, that's kind of where SageMaker Data Wrangler might live these days. You can also keep track of your AI and ML models that you've got set up. You can integrate with MLflow to create workflows around your machine learning stuff using MLflow if you prefer. That has tight integration into SageMaker as well. You can look at your training jobs that you've spun up, which can, you know, be pretty intensive. So you want to keep an eye on those for sure. And you can also keep an eye on the inference endpoints that you spun up as well for accessing your SageMaker models. And you can see it has a built in IDE support for both JupyterLab and for VSCode like we talked about. So that's just the beginning of it all. But that's what SageMaker Unified Studio is. It is what it says a unified studio for SageMaker stuff.

---

### Lecture: SageMaker Pipelines

Let's talk about SageMaker pipelines, which is a way of orchestrating your machine learning or AI workflows. So whether you're training models from your data and checking how it's going and deploying them, well, it'll make more sense with this example. But at a high level, it's what we call a directed acyclic graph or a Dag for machine learning workflows. And a directed acyclic graph is just like this thing you see on the right here. A bunch of processes that are connected together in complicated ways is what it really means. And you can define this visually in the pipeline designer like you're seeing in this screenshot here. Or if you really want to do it the hard way, you can do it through JSON as well. This is integrated with SageMaker Unified Studio and SageMaker AI, but it makes more sense to just walk through this example to understand what a pipeline is. So in this particular example here, let's start at the top where it says process data. That purple node at the very top. So we've got some training data that came in. And we want to train our model with it. So if I follow that branch to the left there we can see that I'm doing a train model block there in my pipeline. Once that node finishes, let's follow that to the right. And we can see that that goes to a process data where we're evaluating the output of that model. And then under that we have that orange condition block where we're deciding whether or not that evaluation passes our expectations or not. If it's not good enough, then we go to the right and we fail. We're done. We give up, right? But if it is good enough, then we pass that check over to the left there. We're going to register that model in the model registry. And then we're going to create the model at the same time. And then after the model is created, we're going to go ahead and deploy that model with batch inference enabled. So this is an example of a machine learning workflow. From training a model to evaluating the model to registering the model and creating and deploying the model all in one SageMaker pipeline. So this allows you to automate this entire process from beginning to end and kick it off with whatever training data you have. And once you do so, you'll either end up with a new model deployed or a failure if it didn't pass. So that's SageMaker pipelines with a real example.

---

### Lecture: Hands On  SageMaker JumpStart

All right, let's play around with SageMaker Foundation models just to get your feet wet or your hands dirty. Whatever metaphor you want to use for getting familiar with SageMaker and using AI within it. Now I've gone to SageMaker AI, Amazon SageMaker AI from the console here. That's where we're going to start from. And under Jumpstart, you should find a foundation models, computer vision models, and natural language processing models. Foundation models are where the hotness is, so let's click on that and see what they got. Now before we dive in, I do want to warn you that what we're about to do costs real money. We're going to be spinning up some rather expensive instances to play around with this. And if we're only going to run it for like an hour or so, it's probably not too bad. But there is a very real possibility that you will forget to shut something down or not shut things down completely, and you could end up with a very nasty surprise at the end of the month. If that's the case, you could find yourself running up a bill of thousands of dollars if you don't follow along. Exactly. So you have been warned. My recommendation would be just to watch me here and do not follow along hands on because you are taking on a very real risk. Some people get upset with me for saying that all the time, but believe me, you'd be a lot more upset if you had a bill for thousands of dollars that you weren't expecting just for following along in this course. That said, let's take a look at what they have to offer here. Obviously, this list is going to change over time as Amazon adds more and more models, but you can see they've got a pretty good selection here for image generation. They have stable diffusion. They have Meta's latest models here which are quite good. They have Jurassic two uh, something I haven't heard of before. Like light on mini instruct. Interesting. And, uh, some more llama models from meta. Uh, AI 21. Also some stuff from them. Jurassic two. That's an oldie but goodie. Uh, cohere. And what else? I'm going to skip past Falcon because we're going to come back to that. But you're seeing here a lot of models also from hugging face. And that's kind of the de facto repository for other more academic models or research models. So that is also cool to see more meta models here. And you can see there's a pretty long list, a pretty good selection here to choose from, right? So I'm going to stick with a hugging face model just because they're cheap. And let's go ahead and select Falcon 40 B. Instruct BF 16. That's a text generation model. It's not a bad 140 billion parameters. That's a lot. It is a decoder only model just like GPT. And it is freely available under the Apache 2.0 license. So let's try that. Now. Obviously you could also run Falcon 40 B or any hugging face model from within Google Colab or something, and it would probably be faster and a lot cheaper. However, you are paying for the flexibility and control that AWS gives you and the ability to to run this within your AWS resources. To get started, let's click on View model. And from here we can get some more information about the model itself. That's basically what it said on the homepage there. It also has a link to the model card, which gives you additional information from the Hugging Face website. And there's that. So you can see here just some more information about what it's good for. Um, may not be ideal for further fine tuning, so it is possible to fine tune this model, but however, this isn't probably your best choice for it. Good to know. Also, some advice here on smaller, less expensive models. Uh, Falcon seven B instruct. So good stuff here. And also some little sample code here for getting started. So good resources there. Close out of that. And let's go ahead and click on Open Model in Studio and see if we can play around with it. However we don't have a SageMaker domain yet so that's going to be the first step. Let's go ahead and click on create a SageMaker domain. If you don't have one already. A SageMaker domain again is just sort of this, uh, meta environment, if you will, for all of your SageMaker stuff within your account. So it's basically storage space that's shared amongst your users and also a repository for high level configuration information and user level information. Now you can go with a quick setup here for a single user. That's all I'm going to do because we are just playing around that meets my needs. But if you did need to do something a little bit more fancy. You could instead say, set up for organizations and give yourself more control and more options that way. So if you need specific security settings, specific access to specific tools within SageMaker studio, advanced network security, data encryption, things like that, you can do that. But I'm just playing around. So I'm going to stick with setup for single user, and I can expand that out to additional options as well there if I want to, but I don't. So I'm just going to hit set up here and let's see what it guides us through. All right. So this is a lot easier than it would normally be. So you know usually when you set up a SageMaker domain you do need to set up those user profiles explicitly and whatnot. So you can see that it does take some time to set up that default domain. So I'm going to give it that time and I will come back when it is done. All right. So after a five minutes or so that finished and we now have a default domain with some default settings. It did prompt me earlier to choose a username, so we'll take care of that at a minimum. So anyway, let's walk through what's underneath this domain because it might be informative. So you can see that it gave us a default name of quick setup domain, whatever it is. Today's date. Um we have some default permissions set up. Um, some default roles and some default, uh, default VPC and subnets as well that were created for us. So that saves us a whole lot of grief. You can also see that it's set up a default user profile as well. While we're waiting for that to finish setting up, let's take a tour through the other domain details here. Space Management. So you can also set up a shared space here if you want to be able to share resources among other users in your domain. So by default we set up this one default user. But we could have more than one user in our account if we wanted to do that. So each user would have their own individual space. But you can also share space between them as well. And that's a place where you can put notebooks and other resources that they can all work on together within a communal IDE, and all using a shared EFS directory as well. So that could come in handy if you're working on a larger team, but again, this is a quick setup just intended for a single user, so we're not doing that right now. Um, there's also various app configurations. Again, like I said, there's various configuration data associated with the domain level. Uh, for example, for Q developer for Docker settings SageMaker studio at a high level. So these are all settings that have been set to a reasonable default for us. Also for SageMaker, canvas, Jupyter and the code editor, all this other stuff. So these are all the settings that are associated at the domain level. We also have some environment settings here that you can set up if you want to. We don't need any for what we're doing. And finally, um, any specific resources associated with our domain. So always important to keep an eye on this. So you know what is running under SageMaker. Because typically the resources under SageMaker are on some pretty expensive hardware instance types. So you definitely want to check in here and make sure nothing's running that you don't expect. Right now there is nothing running, which is good because I haven't actually spun up my notebook yet. Let's go back to user profiles still spinning. Let's refresh this page. That's better. It's not really still spinning. Now we can click on that and get more details. I said I could change the name, but I actually can't. If you were to hit edit on this, the name is not something you can change after the fact, so that's okay. I'll live with it. I'll, uh, I'll just call myself default 2025 0407 T14 2663 from now on. Cool. So let's go back to the domain here. And what I'm going to do is go back to that user profile and click on launch. And I'm going to launch a SageMaker studio instance. Uh, no, no tour yet. But if you wanted to, you could, of course. All right. So I have SageMaker studio up and running. Let's go back to that model and, uh, get back to where we started from here. So again, I'm under SageMaker AI Foundation models and we were trying to launch Falcon 40 B instruct Bf 16. Let's click on view Model again to get back to that. And again try to open it in studio. So we're going to select our default user profile and open studio. All right that brings us to this page here. And let's go to notebooks. And here is a sample notebook that we can play around with. Now what we have here is just a read only preview. If I want to actually run this I can open it in JupyterLab. So let's do that. All right. So let's create a new space because we don't have one already. And give it a name. I don't know, whatever you want. Sundog playground. How about that? Create space and open notebook. And that will take time. So I'll come back when that's ready. All right. As promised, it took about five minutes. So let's go ahead and open JupyterLab. Now, if I wanted to actually run this, I could. Now. However, I actually can't. And the reason why is because once you get down to actually deploying this model, it's going to try to deploy a machine learning extra large 48 instance type, which it costs a ridiculous amount of money. And that is the default for this model just for inference, which is kind of crazy, and it actually won't allow me to do that. I've tried this earlier and it gave me a resource limit error, because in order to use that sort of hardware, you actually need to reach out to AWS and ask permission for it because it is that expensive. So I'm just going to walk through kind of what this would do if I had infinite money, which I don't. So basically, all we need to do to use this model is to import Jumpstart model. From SageMaker Jumpstart model, we select the model ID, and if we want a specific version of that model, we can specify it. We're just going to use a wild card though, which says, you know, use the latest one, whatever it is. The idea for this specific model is hugging face. LM Falcon 40 B instruct BF 16. It could be any other jumpstart foundation model that was listed there as well, and that would also work. I changed except ULA to true here, which is just used, you know, to accept all the legalese stuff there. And then we deploy the model. And to do that we just create a new Jumpstart model instance there with the model ID and version that I specified above. And then we call deploy. That's the point where I'm going to run out of resources. So again that's where the real money starts adding up. All right. So then to use it all we need to do is call model dot retrieve all examples. It turns out there's a list of example prompts you can give to this model just to see what it can do. You know, answering questions sentiment analysis, things like that. And it will iterate through all of those example payloads and print out the input and output from that large language model. And there are some notes here on some things you can specify, for example, a random seed if you want to get more consistent results. The temperature also controls the randomness in the output. Top k. Top E also used for controlling just how random those results are. And also we can specify some parameters like the stop token that's used, and that's very useful for making sure that it doesn't just keep on going and talking forever. And when we're done, of course we need to call predictor dot delete predictor. Because again this is an incredibly expensive hardware. And you definitely would not want to leave that running. But hey, you don't have to worry about it because odds are your account isn't enabled for it anyway. On the off chance that you're doing this from a corporate account that actually does have access to that kind of hardware, please do not actually run this, or you're going to get in a lot of trouble with your boss. All right, so now that I'm done, I'm going to go to file and say log out. What why do I need to reload the site for that? Okay, whatever. Um, so we are not done cleaning up, though, you know, we did shut down that specific notebook, but there's a ton of resources associated with this domain. And if we leave it running, uh, we're going to be racking up a bill for the whole time, and it's kind of hard to find some of these resources. So our ultimate goal here is to delete this entire domain so we can be sure. Let's start with the resources tab and see what's going on here. And you had to give that a minute to populate. So you can see that we do still have a Sundog Playground JupyterLab application running here using up an Mllt3 medium instance type. And if I didn't know about that, I would have just left that running and got a big bill at the end of the month. So let's go ahead and shut that down. First of all, I'll select that and say stop. And let's go ahead. And it says it's stopping right now. So I'm going to trust that that's going to do what it needs to do. But we're just getting started here folks. We also created that shared space for the notebook as well. If you as you recall. So again these shared spaces are so you can actually share that notebook across multiple users in your domain if you had them. And we did create one in the process of spinning up that notebook. However that is also using resources. So we need to delete that too. If we want to make sure we're not getting any unexpected bills. Go ahead and delete that. I have to say yes, I'm sure. Obviously, you need to be careful about this in the real world, because you could be blowing away a lot of people's work by doing this. And that is deleting. And finally, I need to delete my user profile before I can delete the domain. Let's go ahead and click on that default name. And from here I can say delete. Yes I'm sure. All right. And even though it says it's spinning up here, it says it did delete successfully. Finally I need to delete the domain itself. So under domain settings they kind of hid this. You got to scroll down a little bit and say delete domain. Yes. And delete. And let's make sure that actually happens. So this says it's going to take a few minutes. So I am going to hang out for a few minutes and make sure it really does that. This will not update on its own by the way, so you do need to refresh this page once in a while to make sure that it really deleted. And again, you want to make sure you're not running up any resources that you're not aware of, or you're going to get a very nasty bill at the end of the month. So the moral of the story is SageMaker can be really expensive. And we saw that it wouldn't even let us try to run that specific inference on that model from hugging face. It's not even a big model, you know. But even though I didn't actually spin up that massive instance type that it wanted for that foundation model, it also spun up a lot of resources under the hood that you might not be aware of. So again, you just have to be super careful with SageMaker because it is notorious for running up large bills. However, we did learn how to peruse the foundation models that are available under SageMaker Jumpstart, how to actually launch SageMaker domain, and how to launch a notebook within that domain, and all the resources that go along with it. So I hope that was useful. And kind of seeing how SageMaker domains and foundation models and Jumpstart actually work.
---

*Section 6 of 20 uploaded. Confirm receipt and await next upload or GENERATE command.*