So now let's talk about GuardDuty Findings. So these findings are going to be pulled, as we said, directly from data sources. For example, CloudTrail logs, VPC Flow Logs, or EKS logs. In each findings, based on the severity, will have a value between 0.1 or 8+, it could be high, medium, or low. So the finding as a naming convention, which looks like this, you don't need to know it, but it's good to see it once. The ThreatPurpose is the primary purpose of the threat. For example, it's Backdoor or CryptoCurrency. The ResourceAffected is which resource is to target. For example, EC2 or Amazon S3. The ThreatFamilyName, for example, NetworkPortUnusual. The DetectionMechanism, so how it was found, for example, Tcp, Udp and the Artifact, for example, a resource that is used in the malicious activity, for example, the DNS. So you can generate sample findings in GuardDuty if you wanted to test your automations. So here is an example for EC2 Finding Types. We have UnauthorizedAccess, EC2/SSHBruteForce, which indicates that someone is trying to access your EC2 instances by using BruteForce on the SSH Port or CryptoCurrency, EC2/BitcoinTool, which tells you that Bitcoin is being mined on your EC2 instance for example. Now for IAM, you have, for example, an IAMUser that is Stealth and is trying to disable CloudTrailLogging, or you have an IAMUser, which is trying to use the RootCredentials. These kind of things. For EKS, you also have, for example, MaliciousIPCaller. For Malware Protection Finding Types, you have, for example, a SuspiciousFile on an EC2 instance that's being executed or on ECS. For RDS, you're going to have, for example, AnomalousBehavior when you're trying, someone is trying to log into your RDS database. And for Amazons S3, someone is trying to disable AccountBlockPublicAccess setting or is trying to do penetration testing on Amazon S3 using KaliLinux. So all these things are finding types. Obviously, you don't need to know them, but it's good to see the kind of things that GuardDuty may uncover. So once you have uncovered a finding in GuardDuty, you go into the GuardDuty console, and in there you're going to be able to look at which API was invoked to generate this finding. So directly within GuardDuty, you can see, for example, we have the RootCredentialUsage for IAMUser, and we can see which IP address, we can see which API call, for example here, where the SendMessage API call was used to generate this finding. So you don't need to know anywhere else, and the exam may ask you this. So directly the API call can be found in GuardDuty. Now what if you wanted to reply automatically to these findings and not manually? Well, you can generate automated responses. So what's going to happen is that all the GuardDuty Findings are sent automatically into EventBridge, and from there you can build whatever automation you want. For example, send data into an SQS, trigger Lambda function, or invoke an SNS topic, which is in turn going to be able to, for example, send data into Slack or into emails and so on. So these events will be published within your accounts where they originated, but also if you have enabled multi account in GuardDuty, the events are going to be sent into the administrator accounts. For example, what kind of automation could you build? For example, you wanted to build a automation where you want to be notified for every high severity notification. Then for example, in EventBridge, you would create a rule, a filter which says, okay, I want high and critical severity. And these events are going to be sent into an SNS topic, which is going to notify by email and administrator. So that's it for Findings and Automations in GuardDuty. I hope you liked it, and I will see you in the next lecture.