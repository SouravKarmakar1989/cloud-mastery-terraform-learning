So let's do a deep dive on Amazon S3 Glacier vault policies and Vault Lock. So each vault will have one vault access policy and one Vault Lock policy. So they're difference and both these policies are written in JSON. So a vault access policy is like a bucket policy, like an S3 bucket policy. This is used to restrict user and account permissions, whereas a Vault Lock policy is a policy you lock for regulatory and compliance requirements when you set up a lock. This policy is immutable. That means it can never be changed. That's why it's called a lock policy. So for example, you can use this to forbid deleting an archive if less than one year old or to implement write once read many policy, WORM, policy. So here is our vault. We have a Vault Lock policy and so the users can or cannot do things based on the Vault Lock policy but we can also combine it with a vault access policy for more permissions. And this vault access policy can change over time. So now let's talk about the Vault Lock process. So in case you want to lock our vault. So first, we attach a Vault Lock policy and then the vault goes into in-progress regarding its lock. During this time, we're going to get back a lock ID and this lock ID has 24 hours before it expired. And so that's 24 hours for us to test the Vault Lock policy and make sure it is appropriate. Once we know that everything is working as it should be working, then we can complete the lock process and transition the Vault Lock policy from in-progress into the completed state from which the Vault Lock policy can never ever be changed and you have successfully made sure that your vault is going to be locked. So that's it. You need to remember this process going into the exam. I hope you liked it and I will see you in the next lecture.