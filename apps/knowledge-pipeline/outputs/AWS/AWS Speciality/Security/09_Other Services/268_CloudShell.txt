So just a short lecture on CloudShell, so you know one caveat for the exam. So CloudShell Shell is a way for you to get a terminal from within the AWS console, which is very handy. So CloudShell is a browser-based shell to manage your AWS resources, and you can run CLI commands. It's pre-configured, and you can run scripts without leaving your browser. It comes pre-installed with common libraries and common CLI tools, such as the AWS CLI, the ECS CLI, the SAM CLI, and SDKs for Python and Node.js. It has Bash, Powershell, Zsh, et cetera, et cetera. It comes with pre-configured AWS Credentials, which are inherited from the logged in users. So as soon as you go into CloudShell, you'll have the same permissions, and you have persistent storage up to one gigabyte per region. And it runs on Amazon Linux 2. And now you have the possibility to launch CloudShell in a VPC. So you can access your EC2 instances, your RDS instances within your VPC by defining what's called a VPC Environment. So that's it for CloudShell. I hope you liked it, and I will see you in the next lecture.